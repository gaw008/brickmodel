Initial solve_ivp BDF run interrupted by the operator after more than five minutes before the first 0-2s segment returned. It was CPU-active; no successful solver-step statistics were available. This is an interrupted computation, not a demonstrated solver failure. Partial input and initial state retained. The driver now records accepted steps incrementally using the same SciPy BDF class and tolerances.
