# Actual Figure3 TG extraction, first review candidate

Completed 44 preregistered targets (50–1100°C every50°C for both panels): 35 readable TG values, 9 explicit unknown. CSV/JSON are actual digitization output, not a future plan. No kinetics fitting, alpha conversion or material admission occurred. First extraction exit0; no failed extraction run exists to preserve. Earlier SOURCE_CHECK printed arithmetic remains unchanged.

Before extraction, existing cached digitization assets were checked: only Figure4/page9 was previously prepared, not Fig3. `pdfimages -f 8 -l 8 -list` identified nine native image strips (inventory saved); actual native extraction fig3-000..008.png is retained. For this bounded manual read the already pinned 990×1400 page8 render was used as the coordinate plane, rather than guessing strip assembly. This resolution limitation is explicit. Original source and render SHA are in tg-observations.json. No old cached assets or registry files were changed. Original figure and the red-circle overlay were both actually visually inspected after extraction; all 35 selected markers follow TG, not DTA. Sparse points are not an interpolated complete curve.

Each panel uses independent temperature200/1000°C endpoint ticks and right-hand TG axis100/84% (A),100/86% (B). Center coordinates and all interior calibration tick checks are saved in JSON. Selected curve coordinates are explicit in digitize_tg.py and each row. Temperature values derive from actual rounded x columns, not forced target temperature. Point uncertainty is ±2 pixels each axis, endpoint tick centers ±1 pixel. Independent Fraction evaluation of all8 coordinate/calibration endpoint combinations per axis, plus maximum internal-tick linear residual, gives conservative conditional readout intervals rounded outward to0.01°C or0.01percentage point. These are NOT experimental errors or full-curve uncertainty; shared-axis calibration errors are correlated.

Unknown targets: A50/250/400/600/1100°C and B200/400/600/1100°C. Reasons are inseparable overlaps with annotations/curve or final vertical axis. Some conservative unknowns might become readable from correctly assembled higher-resolution native imagery later, but this extraction neither interpolates nor substitutes printed labels. The existing printed mass-loss discrepancies (MIA3 diagram12.618 versus text12.616%) were not used to move any point or calibrate the axis. DTA is microvolts and supplies no reaction heat here.

Protocol/source matching to existing Figure4: A maps to MIA1; B to MIA3; both in source areias-maciel-holanda2025-minerals15-879. Existing Fig4 path is data/sandbox/research/areias2025/dilatometry_digitization/points.json (same paper, Figure4, original PDFpage9). This matches named formulation, not confirmed same physical specimen/aliquot. MIA1 mass formulation is70%clay/15%quartz/15%limestone/0%preprocessed sludge; MIA3 is70/15/5/10. Thus contrasts confound sludge and limestone changes. TG:25–1100°C,10°C/min, atmosphere not explicitly established. Dilatometry:25–1200°C,10°C/min, air. Do not transfer air to TG. This is processed/lime-treated feedstock, not proof of untreated original sludge or an Arlabosse material match.

Reproduction: python3 /private/tmp/brick-source-study-record-v1/material/digitize_tg.py (Pillow installed). This first candidate needs the separate reviewer requested by parent before source registry adoption. Source/graph coordinates, output tables and overlay are reviewable; no claimed independent review has yet occurred.
