# 实际点选记录

固定校准及每图13个原目标，共39项，35项picked、4项unknown。所有x由Fraction反映射后最近整数列决定，实际像素对应温度另存exact字符串。绿色足迹用固定列内G>R且G>B的全部像素，包含极浅抗锯齿边缘；候选不是自动真值，已实际看原图、关键放大局部、三张完整叠图及三张逐点联系图核读。

未知项：Fig38 300°C绿色足迹165–170、400°C为257–263，Fig39 300°C为157–162，完整足迹超出原±2像素能包范围。Fig39 400°C与品红线遮挡/混色，保留unknown。均未换列、减阈值、插值或放宽包络。

picked的y为该列完整可辨绿色足迹上下端中点；未知项pixel_y=null，footprint保留实际看到的行号/RGB与仅供复读的候选中心，不能将其送入数值转换。每项另存同列上下相邻颜色，便于独立检查足迹边界。

叠图黑圈为picked，红圈为unknown候选位置，序号对应13个目标温度；红圈不表示有观测值。联系图每行显示原生局部最近邻放大，底部红短线指向选定x列中心，不涂改曲线。所有图只保留临时目录。独立复读尚待主线程；此次不是独立第二评审。

严格使用原点±2、刻度±1像素设计条件；不能把横±2整个陡坡的y跨度当同列线宽，也不能将独立横纵转换区间冒称整个矩形温区内的曲线包络。实验误差、物理准入及训练资格没有新增。无EOS、拟合、正式文件修改。
