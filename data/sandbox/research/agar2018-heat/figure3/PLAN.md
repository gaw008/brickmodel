# Figure 3 vector extraction registration

This registration follows inspection of source page5 and SVG structure, but precedes conversion of marker coordinates to data values. No claim of blind discovery. Source DOI10.1007/s11356-018-1463-y; current source.pdf unchanged.

Extract all six run series, 30 markers each, preserving original SVG path IDs and color, marker shape and legend identity. Four filled series have fill+outline pairs; two cross/star series have one path per marker. Each includes one final legend symbol, which must lie in the displayed legend region and be excluded explicitly. No temperature-dependent point rejection. Reject unexpected counts or transforms. Dashed TGA program is excluded because it is a prescribed temperature history. Runs15/16/17/20 are feedstock,18/19 wall; not same-run pairs.

Coordinates are page points, y increasing down. Calibrate x0 using vertical grid band[207.058594,207.695312],x30[539.296875,539.933594]; y100 horizontal band[689.742188,690.378906],y900[493.871094,494.5625]. These bands are original vector line extents, not exact data locations. Check internal grid lines independently for consistency with this mapping.

Each marker receives the union bounding box of all its path coordinates, including Bezier control points (conservative convex hull bound, not only visible pixels). Evaluate affine calibration over marker min/max and all endpoint-band corners using exact Fraction; export rational bounds and outward decimal bounds. Midpoint is only a representative plot position, not original measured value. Whole marker extent plus calibration bands is a conditional digitization envelope, not instrument error/experimental confidence interval. Preserve nominal integer minute only if uniquely enclosed; never snap coordinate to it. No smoothing/interpolation/model fitting. Occluded vector objects represent embedded plot geometry, not a claim that every pixel is visible.

Validate actual180 nonempty records,30 per run, legend map, monotone x, expectedinteger minute containment1..30, no extrapolation beyond plot without flagging, source/SVG/script hashes and exact replay. Independent reviewer inspect map/calibration and selected points. Do not use derived alpha/Bi to independently validate Cp, or mix these with a different sludge. No runtime admission, training, EOS or external-model validation in this extraction phase.
