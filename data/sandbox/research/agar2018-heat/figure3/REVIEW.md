# 内部独立核查

program_knot_review目视图例并独立解析SVG，确认六颜色/形状、填充和轮廓配对、180点及轴端标定；内部网格与标定相符。其提醒已体现：path仅为文档顺序索引；六个30min包络越域要flag；不同实验墙/料温不能同步配对。

liu_audit_review以独立解析及Fraction表达式核算180记录全部360坐标区间、六位向外小数、中点、唯一整数分钟、单调且不重叠时间包络、路径覆盖/图例排除及21条内部网格；同时单独重放extract检查字节对应内容和哈希。脚本与结果保存在independent_verify.py和independent_verification.json；不是用提取函数产生独立真值。

根代理从原PDF重新执行pdftocairo26.07.0导出，SVG字节完全相同，证据svg_reexport.txt。这是当前版本转换重现性，不是温度测量精度证明。没有EOS、参数拟合、仪器误差估计或真实模型预测检验。本审查为内部代理审查，不是外部科学认证。
