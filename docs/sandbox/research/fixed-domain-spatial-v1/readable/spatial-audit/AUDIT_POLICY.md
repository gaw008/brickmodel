# Fixed-domain four-cell ledger audit policy

Frozen before any new native callback or trajectory. Adapt prior approved two-cell exact Fraction ledger arithmetic to four cells/five faces; all internal faces cancel exactly. Every local extensive tolerance is halved with the reference cell volume: species 5e-13 mol, energy 5e-8 J, A+B 5e-11 mol, analytic A 5e-10 mol. Global water/carrier 1e-12 mol and energy 2e-7 J remain unchanged. All interfaces must stay wet for this short experiment. Named work/residual accounting and failure evidence remain unchanged. These are arithmetic verification gates, not model uncertainty or a mesh convergence claim. Completion, expected horizon, source identity and per-mode coupling gates are checked separately alongside this prefix audit.

The no-EOS scaling_check.py compares actual installed skeleton API outputs for two/ four cells across zero and nonzero B inventories and multiple stretches/rates. Extensive energy/power sums and intensive stresses/composition derivatives must agree inside the existing reported numerical enclosures, with exact Fraction comparisons and no added atol. An independent exact interface-energy formula is checked. Deliberately unscaled w_B must fail the same bound for nonzero B. Phase/source scaling statements in this script are analytic dimensional checks only; native API qualification requires the subsequent callback.

Preflight review found the original intermediate fluid template pore volume exceeded the refined finite-volume cell bulk volume. The candidate is preserved in experiment/preflight-original. Fix scales only the construction template available pore volume by 2/cells; actual evolving pore volume continues to be recomputed from current bulk minus current solid inventory. This finding occurred before any new EOS execution. It is a code-construction defect, not a failed material simulation.
