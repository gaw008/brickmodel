# HEOS 湿态短前缀：资源失败与实际瓶颈

本轮生产源码为 dcabd8d，未修改。目标沿用原 wet-prefix-refinement 的四步 0..1/64 秒规定压缩，仅改测试 host 为显式 HEOS loader；独立 entropy_reference.py 原字节保留，仍使用独立 Python 水物性参照。不是活动相变或完整10%变形。

PLAN.json 在执行前登记全部原精度/资源门槛。初态采用相同库存、300K、几何，由各自后端计算能量；不覆盖能量或伪造旧身份。规范化新旧实现身份不同，当前初态身份等于其 operator，库存精确相同；初能量差1.7462298274040222e-10J小于原1e-6J。原审查发现 tuple/list 直接比较会形成空门禁，执行前已经修复；REVIEW.md保留此发现。

attempt01 在测试 helper 导入 pytest 时失败，未进 EOS。按仓库锁的 pytest8.4.2 离线补入现有 HEOS 环境，实际新增依赖版本见 dependency-repair.json。run02.py只更换唯一尝试目录，未改物理脚本/门槛。

attempt02 实际 child exit1，外部28.7344255秒。积分返回 resource_limit/wall_time_limit，elapsed26.8533748秒，10次评估/1接受步/0拒绝。25秒是协作检查上限，不能中断进行中的单次求值，所以返回时间略超。没有到达四步终点、独立前缀重构或熵参照，不能判断末端准确性通过；不是材料不可行或科学门槛失败。监督器30秒外限未触发，清理已完成。

随后按 PROFILE_PLAN.json 对相同原初态做一次实际 op.evaluate 的 cProfile 诊断。profile-attempt01 exit0/4.56048054秒；被 profile 的求值累计约2.720秒，154430函数调用。205次HEOS state_tp；事务生成器820次进入/退出累计2.648秒。JSON encoder自身1.077秒、decoder自身.610秒；其中嵌套累计不可相加。原生数值快照仅累计.018秒。证据支持当前瓶颈在每次完整fluid读取/JSON规范化的重复事务检查，而不是宣称整体HEOS已加速。Profiler本身有开销，此单次诊断不是无profile基准或轨迹验证。

下一实施：在隔离候选中保留构造时原始与canonical双核验，研究每次前后用原始流体字符串SHA替代重复解析/规范化（格式变化将更严格拒绝），或受锁的嵌套事务合并。必须保留配置/流体/警告/引用与数值拒绝路径，重新核查实现身份/manifest和故障测试，再回到本原四步计划；不能直接放宽25秒或精度门槛。当前未实施优化。

全部测试是制造材料数值资格，不证明原污泥材料或全砖全过程。全 Goal 的材料、烧结/冷却、公开对照、CLI/UI/搜索要求保持未完成。所有本轮工具句柄已终态，无需重启/重复轮询。
