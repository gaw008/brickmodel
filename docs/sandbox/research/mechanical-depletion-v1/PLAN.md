# Preregistered actual free-cell wet to dry numerical verification

Baseline 0cf4855 plus exact reviewed mechanical depletion candidate 9d569f8696.
Use unchanged free_transfer real water / ideal water vapor / chemical potential bridge and manufactured skeleton, solid caloric and transfer coefficient. Initial liquid 1e-8 mol, inert .01 mol, vapor 1e-5 mol, fixed solid 2 mol, T300 K, stretches1/1, external101325 Pa. This deliberately small inventory is a numerical event fixture, not a brick drying validation or real material package.

Integrate 0 to1e-4 s, affine_midpoint, terminal window1e-6 s, nested ordinary cap1e-4 with reuse, common horizon1e-4, original .25 safe inventory fraction. Integration rel1e-7, amount_abs1e-12 mol, energy_abs1e-6 J, stretch_abs1e-9 and scale1; initial/maxstep1e-4, minimum1e-14. Event comparisons time1e-8s, amounts1e-11mol, energy2e-6J, temperature1e-5K, pressure.1Pa; mechanics reuses1e-9 original absolute budget. 14refinements/2000panels/100rejections/120s integration resource; whole child hard150s.

Before success: actual exactly-one event, final liquid exactly0 and dry mode, end1e-4, mechanical fields retained, positive stretches, terminal mechanical ledger present, six-component event comparison metadata. At every committed prefix independently sum exact Fraction mechanical increments and signed quadrature corrections; represented and exact error and cumulative abs quadrature <=1e-9. Water total residual <=1e-11mol; fixed inert/solid unchanged. External work independently from V0*n*t^2: |E-E0+pe*(V-V0)|<=2e-6J at EVERY committed prefix. Recompute total energy ledger <=1e-6 J. Decode actual final T/P, no preselected final numeric answer. Save real failures and full encoded steps/states/events/refinements before success assertions. No claim of independent full physical trajectory oracle or convergence solely from event refinement. Tests for exact analytic mechanical trajectories remain separate.

Attempt01 failed before integration: copied manufactured oracle roundoff molar mass .018015268 is not bitwise equal to actual source reference .018015267999999997. Attempt02 binds roundoff molar mass to actual op.chemical.reference, preserving all numerical gates/physics; old test/source hashes and failure retained.

Attempt02 returned numerical_failure unresolvable_stage_time after106.8979s/192evaluations, 23trialpanels; saved12committed wetsteps. Level1 terminal comparison passed all6 gates, level2 dry continuation failed before independent halved-controls validation. No complete event was committed. Diagnostic is at+(tc-at) rounding to the preceding float and leaving an unresolvable1ULP tail; isolated reproduction/fix required before retry.

Preregistered attempt03: same physics and EVERY scientific/numerical accuracy gate; integration resource wall120→180s and outer150→210s, because192evaluations already cost107s and independent comparison still remains. This is a bounded resource adjustment based on measured cost, not a tolerance adjustment or claim that attempt02 hit a wall limit. Do not run until actual clock fix independently reviewed and applied.
