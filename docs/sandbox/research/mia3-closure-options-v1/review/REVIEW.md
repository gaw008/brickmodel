# MIA3 冷态线收缩经验基线的独立核查

结论：**所存研究计算的来源抄录、算术、执行结果与限定性表述通过本次核查。** 没有发现阻止保存这次计算证据的科学或运行正确性问题；这不是对全材料、自由烧结本构或全周期物理模型的批准。

本次仅核查上级目录的 `BASELINE_PREREGISTRATION.md`、`baseline.py`、`BASELINE_RESULT.json`、`REPORT.md` 及冻结的 `AREIAS2025_MIA3_TABLE6.json`。先读取当前 Git Python diff，随后审查未跟踪的本次独立计算脚本；没有重审或修改并行开发的源码。原脚本、登记、输入和结果均未改写。

## 原来源与物理量

- 已实际查看私有缓存的 Areias2025 原 Table 6 / PDF 第 10 页完整渲染图。MIA3 列的四个收缩打印值依次为 1150°C：1.65±0.06%，1160°C：2.05±0.15%，1170°C：3.06±0.15%，1180°C：3.61±0.12%；与输入逐项一致。
- 实际核读原 PDF 第 3–5 页方法。原文先测干燥试件、后测烧成试件长度；本次按干燥后长度为参照的烧后冷态线收缩解释，与原方法一致。它不是升温过程中的瞬时自由烧结应变。
- 原方法支持所列配方、预处理身份、30°C/min 升温、5 min 保温、关炉后热惯性冷却。没有由此补出实际冷却曲线、芯温、气氛或逐温度独立试件数，也没有解释原 ± 的统计含义。
- 原 PDF SHA-256 `9c59141a717cb3b062aed6f28b48ed6005c5564aa3ce5a86de1c083bb6eae8a6` 与渲染图 SHA-256 `f776b8bb3e82ba6169737a1f1d60100df388083e97785b59f33945574f721698` 实测匹配输入。原 PDF、图片和全文没有复制到本审查目录。

## 冻结、分组与泄漏边界

登记、输入、脚本的实测 SHA-256 均匹配原结果；详见 `CHECK_RESULT.json`。本地文件时间为登记 08:30:36 UTC、脚本 08:31:54 UTC、结果 08:31:59 UTC，与结果时间相符。协调者另说明会话中先报告了登记和输入摘要、明确尚未运行，再执行计算；本审查没有取得独立签名的事前时间戳。因此这里只确认文件内容一致及所见本地顺序，不能把摘要说成登记先后顺序的独立证明。

系数实际只依赖 1150°C 与 1170°C 两个打印值。1160°C、1180°C 的观测只参与预测误差计算，未进入系数、模型选择、权重或门槛；其他三种表内物理量也未进入拟合。读取整个已冻结 JSON 不是独立盲测，登记和报告已明确全部数据此前看过。本次是同研究、同配方、同报告程序的保留条件检查，未声称独立材料批次。1150/1170 为训练、1160/1180 为检查的使用记录位于本次结果；上游冻结输入仍是上一阶段候选记录，不应将其历史 `used_for_fit=false` 当作这些记录此后从未用于任何拟合的全局断言。

## 独立算术与实际执行

`independent_check.py` 从本次目视核读的四个十进制值出发，用两节点 Lagrange 形式独立重算，再核对原结果中的精确分数与误差；没有导入待审脚本函数、应用模型或 EOS。

| 检查温度 | 预测收缩 | 原观测 | 有符号误差 | 绝对相对误差 |
|---|---:|---:|---:|---:|
| 1160°C | 2.355% | 2.05% | +0.305 百分点 | 610/41 % |
| 1180°C | 3.765% | 3.61% | +0.155 百分点 | 1550/361 % |

独立结果为 `a=33/20 %`、`b=141/2000 百分点/°C`、MAE `23/100 百分点`、MSE `2341/40000 平方百分点`、RMSE `0.24191940806805889669539863163976711041264267778373 百分点`。与原结果全部一致。1160°C 是训练区间内插值，1180°C 是超过训练上界 10°C 的外推，原报告区分正确。

实际使用隔离的 Python 3.12.13，以 `-I` 运行原脚本，结果写入 `/private/tmp/mia3-baseline-review/rerun_result.json`。除本次执行时间外，所有结果字段与原结果相同。再次指定同一输出路径时，程序以 `FileExistsError` 拒绝，原输出摘要未变；没有覆盖研究结果。完整返回码、标准输出与错误输出见 `CHECK_RESULT.json`。

## Python 运行与维护边界

AST 解析和内存编译通过。当前隔离环境以及 PATH 中未发现 ruff、mypy、pylint、black、bandit；没有把这些检查报告为已运行，也没有为本次小型审查安装依赖。`STATIC_AND_TIMES.json` 保留实际可用性、导入和时间信息。

脚本只有标准库导入，固定输入和登记均检查内容摘要；没有动态代码执行、反序列化对象、网络或 shell 命令。结果使用排他创建和显式上下文管理器写入；异常会导致非零退出，现存输出不会被抹除。可由调用者显式指定的本地输出路径属于该 CLI 的接口，不是未受信任服务中的路径拼接。

维护建议：当前四个脚本函数无类型注解，`main` 为 61 行。作为保留原运行版本的单次研究计算，本次不改其内容来追求库代码规范。若以后把它提升为复用库或应用接口，应在新版本中补充明确类型、拆分 CLI 与纯计算，并重新记录版本来源；不能覆盖本次历史脚本后继续沿用旧执行摘要。

原 ± 未作为权重、容差或预测区间；本次没有科学通过门槛，因此算术和程序检查通过不等于科学误差已经可接受。输出的 `blind_test=false`、`sintering_mechanism_validated=false`、`full_cycle_material_qualified=false` 与报告相符。没有将这条温度经验直线批准为 FreeSolidSlab 驱动力、真实物理参数或任意炉程预测器。

## 审查交付

`CHECK_RESULT.json`、`STATIC_AND_TIMES.json`、`independent_check.py` 及本次临时重算结果保留独立审查证据。`MANIFEST.json` 固定这些文件；它只标识本次审查内容，不新增物理验证资格。
