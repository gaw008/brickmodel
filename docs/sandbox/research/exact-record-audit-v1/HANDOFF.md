# Original-prefix audit candidate — explicitly partial

API `audit_exact_run(record, actual_original_operator, *, original_initial, start, end, integration_policy, event_policy, case_sha256, runtime_identity)` returns immutable `PartialExactAudit`. It obtains a fresh `validate_binding` return and ignores replaceable caller record fields, binds external original state/exact interval/full policies/energy and water-mass identity, then rechecks live binding after arithmetic.

Numerical coverage: every committed prefix and per-step N/E ledger with signed shared faces and sources; selected liquid/vapor corrections exactly once; full cumulative signed/absolute storage and phase corrections through original DepletionRoundoffTotals; original element/mass/local/fraction limits through pure exact_depletion_writeback; original component schema and absolute cumulative represented sum residual; local and original-initial represented/exact mechanical increments and absolute quadrature rounding budgets. There is no tolerance or totals reset.

Selected-terminal coverage: rebuilds exact affine samples/evidence with original policies, binds source label to saved mode-specific operator identity (already externally verified), start/mid observation times, signed liquid face/source rates, gross evaporation samples, actual selected start inventory and terminal ledger terms/time. Calls the existing pure writeback machinery and compares the entire correction, corrected state, and input/candidate cumulative totals. This reuses audited arithmetic primitives; it is not an independently implemented theorem prover. All source-dependent paths remain no-EOS.

Explicit unimplemented gates in every result: all-candidate root order and full-panel positivity; reconstruction of actual RK/terminal quadrature from all samples; original all-event/common six comparisons and pressure certificates; consecutive passes and independent approach; discarded-attempt resource reconstruction and continuation budget. `scope=partial_audit_original_prefix_and_selected_writeback`, `resume_authorized=false` unconditionally. No simulator/CLI continuation API or admission is provided. A source-matched parsed record plus this partial audit is insufficient to resume.

Evidence: baseline-red.log preserves actual structural-only validator accepting a tampered final energy (one expected-refusal failure). Initial six tests passed; constant-sink nonzero-correction assertion failed because all actual corrections were exactly zero (tests03.log and test-before-nonlinear.py retained). Replacing that test's explicitly manufactured dynamics with liquid sink 3+100*N exercises actual nonzero corrections without changing original gates; tests04 passed7 in11.69s. It checks full correction recomputation then rejects a changed cumulative phase total that passes structural decoding. Additional tests cover final N/E/mechanical corruption, changed external tolerance, cancelled prefixes and replacement of caller record fields. Permanent test imports the production package name; conftest is temp overlay only. No repository modification, installation or native EOS performed.
