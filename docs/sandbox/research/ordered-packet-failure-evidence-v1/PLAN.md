# Same-case failure evidence experiment

Prior authoritative capture: brick-four-cell-ordered-packet-v1/attempt01, core failed correction_exceeds_evaporation_fraction, 13 accepted steps, 132 evaluations, 15 attempts, zero committed events. New implementation may retain already-computed failure inputs only. No extra EOS/callback, policy or physical changes. Original runner is copied byte-for-byte; actual installed identity must be refreshed after reviewed source installation. Keep exact prior case and initial state, original 512-step/800 s policy and an external 840 s subprocess watchdog.

Run once only after diagnostic tests, code/numerical review and installed identity pass. Compare accepted states, panels, events, error status, phase costs and callback counts against prior result, excluding actual wall times and expected changed module identity. Preserve failed proposal diagnostics separately from committed state. An unchanged failure is expected, not counted as event acceptance. Independently recompute raw writeback, positive evaporation denominator, clock residual and original limits from saved fields to identify actual cause. No gate weakening or production case admission.
