# Ordered affine writeback failure evidence review

Decision: APPROVE this bounded diagnostic change. No blocking numerical or transactional defect found in frozen candidate a2cdd548419c1f1129dbfa33c10051c230daed0313f39d3c574c1a44dc0dbb31. Test source c17f744de33b489d230ae051f24c0d2d576a8d03f686531c02365bf9100f945e. This is not approval of a successful four-cell event trajectory.

Reviewed diff against before.py (the prior 3866 core), actual affine panel assembly, depletion_writeback gates, all four tests and green03.log (4 passed in 0.50s). Reviewer did not run tests, EOS, installation or modify production.

The catch surrounds only the existing affine writeback call. On failure assignment of raw/record/totals has not completed. Only ordered mode captures data; the same exception is reraised. Both failed-refinement sites attach the detached record, including independent approach failures. No success/coarse branch references the exception. No numerical value, tolerance, provider observation, source guard, accepted panel or commit behavior is changed.

Evidence contains the actual start/raw/predictor states, start and midpoint observations, signed selected-cell terms, all integrated N/E fields, mechanical increments and exact quadrature roundoff, clock fields, root-order coefficients/intervals, exact gross positive evaporation and its downward represented value, original roundoff policy and pre-writeback totals. It also retains previous frames with an explicit uncommitted qualification. This permits independent arithmetic reconstruction of the failed panel and fraction threshold; the gross diagnostic is not replaced with net depletion. Fraction numerator/denominator records, tuples and mapping proxies recursively detach arrays and dataclasses. Actual objects reachable in the capture are data records, not current-host/provider graphs.

The genuine constant-sink close-event test uses the original writeback, not an injected failure. It checks the original rejection, identical callback sequence/evaluation count/times, no packet or event commit, identical roundoff totals, exact signed affine term rounding, all N/E/mechanical updates, positive gross downward rounding, constant-rate exact root and clock budget. Success compares full non-wall outputs and callbacks against old implementation. Default refusal preserves empty diagnostics. Deliberately failing the diagnostic serializer separately confirms explicit unavailable evidence while retaining the original fraction rejection.

Limits: process_local_source_binding contains object IDs and is deliberately not a portable source authentication proof. The actual native runner must retain case/runtime/source bindings separately. Captured root intervals certify numerical affine surrogates, not true RHS localization. Prior uncommitted event frame defaults must not be interpreted as accepted refinement comparisons. If capture reports available=false, the subsequent arithmetic audit is unavailable, not passed.

## Saved native evidence audit strategy

1. Verify runner/case/runtime hashes, original numerical policies and core failure reason; retain all artifacts before any assertion.
2. Require writeback_failure schema and complete evidence, original fraction policy and matching selected cell/root-order entry. Verify all prior frames remain absent from committed events/packets.
3. Use Fraction on saved binary64 numbers. Reconstruct each affine signed component using represented midpoint duration, then each rounded integral and raw full N/E/mechanical state.
4. Recompute positive part of the affine evaporation rate (split at its rational zero if necessary); verify exact gross and greatest downward float. Compute delta=Fraction(raw selected liquid), fraction allowance=Fraction(evap)*Fraction(policy fraction), ratio and excess without changing the threshold.
5. Recompute clock/root polynomial, nearest downward endpoint, next-float enclosure and clock residual. Recheck strict surrogate ordering from every saved coefficient/interval. Report any absent or inconsistent item as an audit failure.
6. Compare original failing attempt only for unchanged physical settings and gates; different source version is expected. Report numerical failure diagnosis, not event/spatial convergence success.
