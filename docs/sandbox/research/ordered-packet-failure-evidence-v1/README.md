# Actual near-event writeback failure: retained numerical evidence

Baseline `599efe0`. The ordered affine core now retains immutable, already
computed inputs when `depletion_writeback` refuses a speculative terminal panel.
The original exception, policy gates, provider calls and commit behavior remain
unchanged. Evidence lives under the existing failed refinement's
`comparison_details.writeback_failure`; no result schema or legacy service
admission is added. A diagnostic serialization failure is explicitly unavailable
and does not replace the original refusal.

The snapshot contains complete initial/raw/predictor states, two observations,
clock coefficients, signed liquid integrals, all integrated N/E fields,
mechanical increments, exact positive evaporation and its downward float,
policy/totals, root-order proof and previous **uncommitted** frames. Process-local
object identifiers are diagnostic references, not portable source authenticity.
The native capture separately retains actual installed runtime and case hashes.

## Implementation validation

Four permanent tests exercise a genuine short-panel failure (no mocked
writeback), full arithmetic reconstruction and immutable data; successful
packet, unchanged default refusal and diagnostic-error preservation controls
are included. Archived candidate/old comparisons also check callback sequence
and complete non-wall-time result parity.

Source related regression: **114 passed in 37.09 s**. After offline noneditable
installation, **65 passed in 3.13 s**, from `/private/tmp` without `PYTHONPATH`.
All 67 imported installed modules match source. The historical initial patch's
success-branch exception-scope mistake and NumPy scalar snapshot failure are
retained alongside their fixes; they were resolved before native execution.
See `CODE_REVIEW.md`, `NUMERICAL_REVIEW.md` and archived logs.

## Same-case actual native diagnosis

The original runner is byte-identical. Case, complete initial state, original
physical/numerical policies, final state and final interfaces match the prior
four-cell attempt byte-for-byte. The original 512 attempted-step / 800 s policy
and external 840 s supervisor are unchanged.

The process terminated exit 0 after **39.039 s**, indicating successful capture.
Core status remains **failed / correction_exceeds_evaporation_fraction**:
13 accepted steps, 132 evaluations, 15 attempted panels, zero committed events
and packets. After excluding wall times and the newly retained diagnostics,
the entire numerical result equals the prior failed result exactly. No event
comparison or independent finer approach was reached.

The failed writeback is cell **2**, following speculative cell **3** (zero-based
indices). Its terminal duration is 8.452095690003603e-10 s. Actual retained raw
liquid is 5.673486681192076e-20 mol; downward represented positive evaporation
is 1.5641070214850036e-12 mol. Their ratio is **3.6273008197389976e-8**, above the
unchanged **1e-8** fractional gate, despite being below the separate 1e-15 mol
absolute limit. Neither limit substitutes for the other.

The reviewed independent prefix auditor was copied to a new evidence directory
and actually rerun on this capture: all 13 accepted prefixes again satisfy the
original local/cumulative N/E/stretch/component and global pressure-work gates.
This does not validate the rejected speculative packet.

## Remaining work

The independent saved-field audit in `native-audit/` reconstructs the failed
panel's complete raw N/E/mechanical state, signed quadrature, gross positive
evaporation, downward float and root enclosure. The affine inventory remainder
at the downward absolute clock is 5.673486686333683e-20 mol; the raw remainder
differs by only −5.141607489530308e-29 mol. Thus the actual refusal is dominated
by the represented event-time remainder, not component summation roundoff.
Previous cell 3 proof rates and active-cell coverage can be checked, but its
nonselected initial inventories are not independently bound to a saved complete
start state; that earlier order proof has this explicitly retained evidence
limit. No stored surrogate proof establishes the true nonlinear event time.

Use this diagnosis to design a consistent event-time representation. Any
local/exact clock remedy
must propagate through quadrature, stage-time queries, ordering, program nodes,
event comparisons and ledgers; simply attaching a rounded absolute timestamp to
a different integrated duration would be inconsistent. Do not borrow earlier
evaporation, widen the fraction limit or clip an overshoot to obtain acceptance.

The full Goal remains active. Native packet acceptance, full spatial
convergence, the source-complete original-sludge material, three public
mechanism comparisons and heldout predictions, full wet-to-fired/cooled
simulation and complete application acceptance remain required.
