The completed measurement is valid as a single installed passive parent-read profile. It reports 13.151049542 s including profiler overhead and 51,187,339 function calls. Runtime fingerprints match before/after, the saved parent study is 78aec3da..., and the patched guard count is zero. This reviewer read the entire pstats table without rerunning the reader. No recorded calls reached source/HEOS evaluation, state_tp_response, saturation_pair or ArlabosseDryCaloric._source. Passive WaterState properties/type checks are expected and are not EOS calls.

The script profiles read_run_with_source_record after imports and patch setup; it excludes initial runtime fingerprinting and import time. Its elapsed timer includes patch setup/teardown, so elapsed and profiler total time are close but need not be identical. The observed cumulative cost lies primarily in audit_study (12.777 s), _sample_record (63 calls, 11.859 s), decode_source_sample (126 calls, 11.070 s), and encode_source_sample (63 calls, 6.335 s). These are nested and must not be added. Counts represent invocations, not unique physical observations. The measurement does not prove that all repeated work is safely removable.

The guard is narrower than a universal physics firewall: HEOSWaterProperties.state_tp_response and saturation_pair directly call the kernel and do not route through its patched state_tp method; ideal-vapor methods are also not patched. Full call statistics independently show those paths were not used in this actual run, so the completed result need not be repeated. For the next profile, patch the response/saturation/ideal evaluation entries and native-kernel entries as well if the intended assertion is universal no-physics admission. The current zero counter alone should not be presented as that broader guarantee. On a failed measurement the script preserves pstats in finally, but writes RESULT.json only on success; absence of RESULT is not a zero-cost/success result.

This does not identify the whole 185.5 s ordinary-session bottleneck. It neither invokes Arlabosse source I/O nor profiles nested temperature/pressure inversion. Treat source caching as a separate hypothesis, with its original source-integrity contract preserved. No source, tests, installation, EOS or full record decoder was run by this reviewer.

Verdict: approve the actual completed measurement and scoped interpretation; retain the explicit guard-coverage limitation for future experiments.
