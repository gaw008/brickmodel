# Managed source RHS final code review

APPROVE for the one preregistered dedicated-worker diagnostic. No unresolved finding in the reviewed final scope, kernel, adapter, worker, runtime selection and existing sample-record extraction. This report is outside the execution input set and changes no production byte.

The three first worker findings are closed by the same independent pure control probes: noncanonical time rejects; profile publication failure saves only a failed terminal record while preserving 4+1 work; an original evaluation error remains the original exception and obtains a durable failure record with the secondary audit error. The normal control succeeds. The original RED source/test and outcomes remain in worker-first-red/.

The managed scope uses an explicit isolated -m entry, owner PID/parent/thread/task checks, a fixed graph of actual collaborator classes, actual registered kernels and getter/function identities, and full config/fluid verification at entry and exit. It keeps per-operation kernel locking, warnings and native numerical bodies. All 9 original HEOSCandidate methods other than _transaction are independently AST-identical to 86d9733. Outside an active managed lease, the original before/after config/fluid checks remain in their original order.

ExactSourceColumn now constructs the evaluation inside the scope but publishes rhs_returned only after successful scope exit. A late verification failure leaves the produced evaluation available through rhs_failed; the primary exception and secondary exit diagnostics remain. Owned audit copies cannot mutate the lease record. The default path continues through a no-op scope and retains the original evaluate body.

Independent additional structural probe: three distinct SourceWetStorage/volume shells, actual imported IAPWS95 class without initialization, actual configured N3 faces/inverse policies/liquid relations/connections, and all four water paths traverse _closed_graph. A nested liquid relation callback override is rejected. This checks real class/field compatibility; it is explicitly not a physical construction or a numeric graph-validation test. Author tests provide the broader 36-case scope guard coverage.

The operational contract remains bounded: an instance RLock does not exclude arbitrary CoolProp global setters, and an aggregate RHS boundary check does not reproduce every possible intermediate mutation detection of per-call checks. Approval relies on the dedicated closed worker, no caller callbacks inside the physics region, and its explicit new implementation identity. It is not process attestation against hostile in-process code and does not authorize enabling this scope in arbitrary applications.

The legacy kernel file is byte-identical to the original 86d9733 kernel and the old manifest is untouched. The new manifest differs only in adapter_sha256, execution_sources and execution_contract; all native constants, external library/file identities, configuration and fluid dictionaries remain exact. Its execution hashes match the frozen files. The new case changes only its profile and the one corresponding manifest asset row; builder admission binds both config identity and the exact profile-specific asset list. The wrapper/storage/operator/energy/config-derived identity differences remain explicit, never globally stripped.

The planned request matches all 12 N and 3 U binary64 hex values, the canonical exact time and full three-cell mode vector from rhs01 event 000014 (rhs_started). All 17 private asset snapshots match their sizes and SHA values. The launcher executes the installed interpreter with -I -m, with the existing 120 s supervisor and 1 s cleanup, preserving source/installed/input before-and-after hashes. The corrected plan separates the supervisor timer from the worker entry-based timer and its cooperative checks. Full old/new returned physical and diagnostic equality remains a post-run acceptance condition; this review does not predeclare it.

Evidence: worker-fixed-green/RESULT.json, SCOPE_GRAPH_PROBE.json, MANIFEST_VERSION_REVIEW.json, MANAGED_REQUEST_REVIEW.json, and MANAGED_CODE_REVIEW_FINAL.json. Final source/test/plan hashes and installed identity counts are recorded there. Reviewer ran no EOS, native/provider initialization, installation or old numerical suite.

## Review Summary

| Severity | Count | Status |
|----------|-------|--------|
| CRITICAL | 0 | pass |
| HIGH | 0 | pass |
| MEDIUM | 0 | pass |
| LOW | 0 | pass |

Verdict: APPROVE — one preregistered diagnostic only; no retry, continuation-completion or material qualification claim.
