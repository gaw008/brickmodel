# One installed source-RHS performance sample

Baseline 86d9733; all 146 production modules and original V2 case remain unchanged.
The completed passive parent-read profile took 13.151 s including cProfile overhead,
with zero live physical calls verified by its complete call graph. It does not identify
the full ordinary-segment bottleneck.

Next perform one separately recorded measurement: use the existing public
open_source_trajectory on the saved native02/parent; do not re-run its event, initial-U
or probe. Charge the actual four reconstruction constructors and all admission time.
Declare a diagnostic end at exact 1/16 s (strictly after the saved start), but do not
integrate or accept any step. Evaluate exactly one RHS at the original saved segment
initial state and exact start time, under the original observer/guards and cProfile.
Compare its full raw evaluation payload with the prior continuous branch's first
actual RHS return. This is output equivalence for the same physical query, not a
trajectory, pause/resume or material validation. Close the diagnostic session afterward.

Additional workload: four real provider constructors and one actual RHS only. Original
parent debit, 97 RHS/16 wet/510 s source caps and every physical/numerical input remain.
The diagnostic process has a separate 120 s supervisor and 1 s cleanup grace; an
in-flight native call is only cooperatively interruptible. No full old run is repeated.
Inspect cProfile self/cumulative costs and actual call counts before choosing an
optimization. Timings include instrumentation overhead and are not uninstrumented
production benchmarks. No third sample or material/source cache is pre-authorized here.
