# Next executable step

The complete raw-sludge brick Goal remains active. This phase changes execution
cost; it does not admit a new material or finish the ordinary trajectory gate.

1. Extend the dedicated data worker to a complete source-study/ordinary-segment
   operation using the existing service, with an explicit new execution record.
   Admit a lease only after its actual builder returns, close it before leaving
   that operation, and re-admit the actual reconstructed ordinary adapter. Never
   reuse an old lease for new water objects or deserialize a lease. Initial-U,
   wet-pair collection, observers, pause and output remain outside the RHS scope.
   Keep constructor, callback and wall-time charges from the actual recorder.
2. Freeze that new runtime and run a new parent study under the original V2
   physical settings and tightened numerical policies. Current historical
   parent records stay passive: their old runtime does not authorize live
   continuation after code changes. Do not repeat the original failed runtime.
3. Use the original nonstationary target: three accepted ordinary steps and the
   continuous versus pause/continue comparison to the same original target
   horizon. Preserve the 97-RHS/16-wet/510-s common caps and the original 180-s
   ordinary policy. The worker's 120-s single-query diagnostic budget is not a
   new full-study solver limit and is not a reason to enlarge the original caps.
   Pre-register the complete new operation before running it.
4. Check all accepted local/global N/U/element budgets, actual nonzero conduction
   and temperature changes against saved inverse bounds, and both paths' full
   state/ledger equality. A partial prefix or one improved RHS is insufficient.
   Then complete persistent source-session recovery and the shared CLI/UI path.

In parallel, the raw-sludge reaction network still needs matching product
composition, stoichiometry and thermal-effect evidence. The retort basis audit
adds constraints but does not supply those missing parameters. Full firing,
sintering/cooling, the three mechanism holdouts, full-cycle convergence and
multi-generation material search remain requirements of the original contract.
Do not fill their gaps using manufactured transport or altered qualification
labels from this numerical/source-provider test.
