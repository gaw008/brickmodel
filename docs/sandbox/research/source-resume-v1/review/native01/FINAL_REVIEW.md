# Independent passive audit of native01

**PASS within the preregistered ordinary-source continuation experiment. No new blocking discrepancy was found.** This audit independently reads saved JSON graphs and recomputes ledger arithmetic with the Python standard library. It neither imports the application nor runs the original verifier, integrator or EOS.

The final audit took **0.460413 s**, with **67,806 total assertions including graph-decoding/shape assertions**. This count is not a count of independent physical validation tests. The final script SHA-256 is `f1f5957ce28314b4c4d71d493ab0a5c5d4638a02bf91b7d59f3ff6d195421500`; details are in `AUDIT.json` and `audit_saved.py`.

Verified from original saved evidence:

- The parent completed its numerical transition. Its actual returned candidate 1 reference and corrected state match the saved source record. The ordinary interval starts at that candidate's exact endpoint and spans exactly 3/64 s under the original policy.
- Continuous and resumed paths have identical full numerical results except measured elapsed time, identical 22 numerical observations and complete actual source-return fields, three accepted steps and no rejected trials. The entire saved numerical checkpoint matches the issued pause checkpoint. All 32 original paused event files remain byte-identical in the packet and restored journal; the first eight observations were retained.
- The 249 distinct events account for **76 RHS, 16 HEOS constructions, 3 initial-energy evaluations and 8 wet-pressure requests**. Resume adds exactly four constructions and 14 RHS calls, without repeating initial energy or wet-pressure requests. Declared other-branch counters match the independently counted continuous-branch increment.
- Four distinct worker PIDs have complete closed leases with 32/22/8/14 RHS calls. Every saved RHS lease has four verified provider entry checks and four exit checks against the pinned fluid and configuration. Scoped native operation counts are 93,251/70,796/23,245/47,551. These counts exclude native work outside those RHS scopes.
- All seven historical balance rows were independently recomputed from the original wet-state inventories and energies, saved wet ledger, terminal-prefix rounded ledger and exact saved integrals, explicit water writeback, dry ledger and ordinary three-step ledgers. All 21 per-cell rows match species, energy, full-integral residuals, water correction, H/O/N and fluid-mass bookkeeping exactly. Local and global original tolerances pass; the largest per-cell energy residual is approximately 3.324e-11 J. This recomputes conservation arithmetic from saved flux/integral evidence; it does not regenerate the quadrature samples or validate their physical model error.
- The three ordinary global energy changes are 8.6402e-12, -8.1855e-12 and -1.0459e-11 J, within the original 0.001 J tolerance. Inventories remain unchanged in the ordinary segment. Actual nonzero internal conduction is present. Cell temperature changes exceed the paired saved inverse numerical bounds, with the same values reported in the original acceptance result.
- All 323 packet manifest files match their bytes/hashes; all 288 original parent files match their packet copies. No unfinished-finalization or failure marker is present, and the local claim contains one successful start/restore pair. The parent source-record hash is explicitly bound to the packet metadata and paused result.
- All **329 supervised input files** match before/after/current hashes and sizes. Raw case and canonical configuration hashes are correctly distinguished. Original shared 510 s/97 RHS/16 wet limits and 570 s supervision are retained. The supervisor completed in 124.649336 s, reports its leader reaped and has no cleanup signal errors.

Time samples preserve saved S = **18.444216625 s**, including the explicit 1 s finalization allowance. After passive restore/reconstruction and before new RHS work, charged time is **23.000890041 s**; the final ordinary result carries **42.785750625 s**, so new active charge to that result is **24.341534000 s**. The whole resume worker reports **24.570495417 s**. The difference includes entry/final publication outside the result's sampling interval and is also within the comparison's wall budget. Saved samples establish retention of S and bounded new active A. They do not independently expose every intermediate monotonic timestamp or the completed live session's offline-history object. The 0.322112 s wall-anchor-to-claim interval includes the save-finalization tail and is not a precise measurement of offline waiting after process exit; this audit does not claim such precision or a separate native offline-delay experiment.

Three initial passive-checker mistakes were retained with their logs and pre-fix scripts: using compact rather than default-spaced JSON for the native configuration hash; excluding nested parent `manifest.json` while enumerating only the top-level packet manifest; and comparing the supervisor's before `{sha256, bytes}` mapping directly with its after hash mapping. Corrections changed only this independent reader. No native run was repeated. An earlier exploratory print also assumed the source-study root was a record rather than a mapping; it did not execute a solver or change evidence.

This result verifies a narrowly defined numerical and persistence lifecycle with manufactured material settings and conditional numerical envelopes. It does **not** establish source authenticity, physical material qualification, a complete firing cycle, mechanism holdouts, spatial convergence, training eligibility or production control. Cleanup evidence covers the original process group; the supervisor explicitly does not claim containment of escaped sessions.
