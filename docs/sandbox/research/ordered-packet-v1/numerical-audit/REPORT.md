# Actual four-cell ordered attempt: failed-capture numerical audit

Root confirmed process terminalexit0 at39.018s. Exit0 means the research runner saved its result successfully; **core failed** with correction_exceeds_evaporation_fraction. This audit reads files only and verifies unchanged runtime/source3866f15b. Input hashes, command implementation and machine results are retained in audit.py / audit-result.json. No native repeats or gate changes.

Saved outcome:13accepted panels,15attempts,132evaluations, last accepted t=.500262030378867; zero committed events, corrections or packets. Final modes all existing_liquid, consistent with atomic rollback. Independent prefix conservation audit belongs to Tesla and is not duplicated here.

Only one refinement record exists: level0, cap6.613035248666162e-6s, common time.5002772891679295. It failed before any comparison: comparison evaluations0 and endpoint attempts0; differences=null and event_time_s=null. Thus there are no two-pass/independent-approach results to certify, and no packet-wide maxima to interpret as actual passed data.

## Why the fraction gate failed — supported scope

The raised reason identifies the original per-cell condition: represented liquid writeback delta exceeded correction_fraction_evaporated times that cell's supplied positive affine evaporation integral. It does not identify a violated global absolute inventory budget, a pressure comparison, or a failed root-order condition. The raw failed delta/gross pair and selected cell are not serialized, so a numerical ratio or specific cell cannot be independently recovered.

Phase costs of the failed proposal record10approach evaluations/1approach panel,3terminal evaluations/1terminal panel,1dry evaluation and no comparisons. In the reviewed implementation terminal panels are counted after their writeback succeeds, then the mixed-mode followup is observed. This pattern is consistent with one speculative event finishing and a later member's terminal writeback failing. It is an inference from code/counters, not preserved first-event/root proof. It must not be reported as a committed first event or exact certified sequence.

A plausible mechanism is that after the first near event the next terminal starts with extremely little remaining liquid: a sub-ULP absolute-clock localization residual can exceed the very small permitted fraction of that short panel's gross evaporation. But neither the failed start inventory, clock residual, gross evaporation nor fractional bound is saved, so this remains a hypothesis. Do not claim the case proves that mechanism, and do not widen1e-8 fraction on this evidence.

## Missing diagnostic evidence and next bounded step

Discarded path.packet_frames / root_order and failed correction inputs are absent from core-result. Therefore auditing every numerical polynomial root and strict order is impossible for this failed capture. No amount of JSON arithmetic can reconstruct missing actual midpoint rates without new observations.

Before another native run, add narrowly scoped failure evidence at the existing writeback failure boundary: cell index, actual panel start/end, initial/raw liquid, exact signed integrated terms, clock coefficients/root interval/rounding residual, positive affine evaporation integral and represented value, policy fractional/absolute/ULP bounds, and already-created earlier packet frames/root proofs. Save it without accepting the branch or changing physics. Alternatively a reviewed exception diagnostic payload can carry these exact existing values to the runner. No extra EOS is needed to capture already calculated data.

The current proposal exception propagates the level0 DepletionRoundoffError directly; it does not automatically try a finer terminal level after that failure. A retry policy would be a separate explicit numerical control-flow change requiring bounded counters and original gates, not a retrospective reinterpretation of this attempt. First identify whether clock representability, short-panel evaporation denominator or an actual accounting defect dominates using the missing evidence.

Conclusion: capture and atomic rollback are consistent; actual4cell ordered acceptance is not established. Original spatial failure remains, and no full-state/event-order acceptance claim follows from this attempt.
