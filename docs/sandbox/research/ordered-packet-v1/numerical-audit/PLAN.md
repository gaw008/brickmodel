Saved-data audit only after root terminal notification. No production imports/EOS. Verify runtime/source identity and input hashes, every committed numerical affine root's coefficient/sample binding and strict first-root order, event sequence and per-packet frames, complete successive and independent comparison gate maxima. Distinguish conservative packet-wide event fields from per-frame raw differences. Do not duplicate independent conservation ledger audit owned by Tesla. Missing data/failed core status are retained, never converted to successful event evidence.
