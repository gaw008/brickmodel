# Ordered numerical event packets

Baseline cd89a46. Implement the preserved INTEGRATION_NEXT ordered-root path:
ordinary positive-stage RK may advance before a tangent tie, but terminal order
must come from complete shared affine numerical root evidence. Time tolerance
remains the original maximum localization/comparison error; it is not an
unconditional minimum gap between physical events. Actual mixed RHS must be
reevaluated after every switch. Coincident unresolved roots must not be sorted
by cell index as though that proves physical order.

One speculative packet carries every terminal frame through the common endpoint.
Compare each event's full N/E/T/P/stretch and the common state, preserve matching
event sequence and modes, two successive accepted refinements and an independent
finer approach. Commit accepted panels and corrections once, atomically. All
original local/global budgets, source checks, resource/cancellation bounds and
recorded qualification remain. No true-ODE root certificate is claimed.

Tests use explicit manufactured rate laws with independent closed-form roots
and piecewise mixed-mode states: closer-than-timegate events, changed later
rates after first switch, reversal of tangent ordering, rollback/resource or
cancelled packets, source changes and complete prefix conservation. Default
single-event regression stays enabled. No new material parameter or relaxed
scientific acceptance threshold is part of this implementation.

Packet core returns a separate result type. Until the versioned packet audit,
record/resume and case/service flow is implemented, legacy v1/v2 must reject it,
including raw restore and zero-event packet-policy prefixes. This is an explicit
remaining integration requirement, not final delivery. Preserve boundary RED
and GREEN evidence; obtain independent code and numerical review before commit.
