# Failed native attempt: accepted-prefix arithmetic review

Native core outcome remains FAILED: correction_exceeds_evaporation_fraction. Capture process exit0 means successful evidence capture, not successful integration. Actual core has13 accepted steps to .500262030378867,132 evaluations,15 attempted panels and zero committed packets/events. Full requested end .50032 was not reached.

After root terminal notification, ran only the independent standard-library saved-data audit (no EOS). report.json records input SHA256 bindings and exact per-prefix residuals. All13 saved accepted prefixes pass original local/cumulative amount, energy, stretch and component budgets. Maximum global ΔE+peΔV−boundary−body residual is6.166889742995567e-11 J; cumulative absolute cross-cell constraint sum is1.8549938826807922e-20 J, below original2e-8 J. Initial/final state files and before-after runtime agree.

The first refinement at level0 records the same correction failure,14 evaluations and no completed comparison differences. Its phase costs include approach10,terminal3,dry1 evaluations. This is consistent with failure after some speculative terminal/mixed work, but retained records contain no failed correction's numerical numerator/denominator or uncommitted frame. Therefore these files do not establish which writeback amount or local gross evaporation triggered the fractional bound, and no quantitative root-cause conclusion should be inferred from the reason string alone. The safe next diagnosis is preserving actual failing writeback inputs/proof as structured diagnostic evidence; do not alter the gate.

No partial packet was committed. Passing ordinary-prefix arithmetic is separate from native packet admission. The script's packet/correction branches have not been exercised by this zero-packet result and are not claimed independently validated. Root should review audit.py before broader reuse. No real-material, group-mode completion or spatial-convergence claim follows.
