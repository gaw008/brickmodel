# Saved packet-core prefix audit scope

Independent standard-library JSON/Fraction script, no production model imports. Read actual saved core wrapper only after root terminal notification. Check raw initial/final binding, before-after runtime, packet frame/event/panel binding, local and every accepted-prefix N/E/stretch ledgers, signed writeback increments, component residuals and whole-domain external-pressure work. Use only original four-cell absolute budgets; global target equals four energy absolute budgets,2e-8 J. Reconstruct represented reference volume and exact diagonal volume products. Failure prefixes may pass arithmetic without passing the experiment.

This script is not a full packet certificate auditor: it does not recompute native EOS, affine root isolation, correction gross-evaporation/clock proof, refinement gates or full parameter-box identity. For a result with committed packets, those require separate proof review before any packet admission claim. Current terminal result has zero packets and zero committed events, so only the ordinary accepted-prefix branch was actually exercised. Do not claim successful validation of unexecuted packet audit branches.
