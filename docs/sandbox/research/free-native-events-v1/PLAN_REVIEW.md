# Independent preregistered plan review

The actual PLAN.md, case.json, run.py and supervise.py were read. The case differs from the packaged physical case only in schema and numerics; all other top-level physical settings, sources, initial inventories and temperatures are unchanged. Numerics differ only in end_s, integration controls and the explicit depletion policy. Start remains 0.5, fixed end is 0.50032; cap/initial step and nested cap are 2^-14, terminal window 2^-17, common horizon 2^-16. Attempted-panel/rejection/integration-wall limits are 512/100/800 s, with one 840 s subprocess watchdog.

The plan correctly treats the initial N/r estimate as rough, requires at least one event rather than two, and excludes convergence/material/full-cycle claims. Exact-zero events may have no correction. Successful completion must reach the unchanged registered end and include subsequent integration after an actual event. The control increase is a new experiment, not a pass of the original convergence matrix. A timeout/resource/unsupported outcome remains unpassed evidence; no duplicate process, automatic retry, physical input change or gate relaxation is authorized by this review.

The owned run script preserves service artifacts and runtime identities, while the supervisor records actual process termination/timeout. The 40 s difference between kernel and subprocess caps must also cover constructor, snapshots, auditing and process overhead; it is not guaranteed sufficient. Exit 0 alone is not success: run.py does not assert completion, so the separate saved-data acceptance audit is required.

## Independent audit registration before result inspection

audit.py is a standalone standard-library script, with no production model/audit imports and no native calls. It will be invoked only after the parent reports terminal process state. It checks manifest membership/hashes, launch runtime, selected event catalog, original policies, declared initial inventory/mechanics, full per-step and original-initial N/E/component/mechanical budgets using Fraction arithmetic, full terminal raw/writeback endpoints, exact correction accounting and original local/cumulative correction limits, event terminal/mode/zero binding, six event gates, consecutive passes and distinct halved approach controls. It preserves failure reports atomically. Saved T/P and omitted speculative trajectories remain historical evidence, not independent thermodynamic evaluation.

Parent's pre-result addendum retains the previous free-application global verification targets: absolute total-energy plus external-pressure volume change minus boundary/body work <= sum of original per-cell absolute E budgets (2e-8 J for this two-cell case), and cumulative absolute cross-cell constraint work cancellation <= the same target. The script now enforces both. The nonlinear p*deltaV versus quadrature difference includes truncation; this target is an explicitly registered verification criterion, not a theorem derived from local ledger tolerances. Exceedance means not passed and requires diagnosis; it must not be dropped after seeing results.

No live result/progress was inspected, no native process was polled or relaunched, and no EOS was run by this reviewer during preparation. Only audit.py and this requested plan report were written in the temporary experiment folder. An independent reviewer is inspecting the new script before use.
