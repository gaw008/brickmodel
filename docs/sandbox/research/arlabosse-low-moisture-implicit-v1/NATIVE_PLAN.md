# Local-BE split: same-source short step refinement

Base commit 8d104d9. Explicit new split method, original midpoint function unchanged. Reuse exact native_case.py from the saved fast inverse phase, including actual water/source bindings, initial Nc0/.06, gas/U/T and unchanged 325–338K/90–110kPa domain. Frozen coefficients use actual decoded join activity/P/Vg and the common vapor reference; only local phase/selective outlet acts in the first stage, only internal k/D and heat bath in the second. Source linearization readout residuals are saved, not treated as whole-step error bounds.

Run exactly one supervised driver constructing the case once and integrating three independent fixed-step trajectories to the same .05s from the same initial state, steps 2/4/8. Expected actual RHS calls 5/9/17 (one initial plus two per step), 31 total. Original inverse U1e-5J, T1e-6K and candidate100 stay; original arithmetic U1e-8J/N1e-12mol per trajectory remain unchanged. Each integrator retains60s, common driver acceptance80s, supervisor100s/cleanup5s. Preserve any failure; no old native rerun or hidden extension.

Every local and full endpoint must pass actual source/domain/fullU checks, all inventories remain nonnegative, and each substage/cumulative water and energy account closes including actual used projections. Local intermediate stages are neither midpoints nor extra physical time. Original source scopes/material=false remain. Same initial points/inventories/source data must equal the saved fast phase.

Temporal comparison: report T,P,Nc,Nv, cumulative E,H,Q and exact arithmetic budgets at the common endpoint. The primary resolved refinement observables are outer vapor Nv and cumulative E; for both require successive absolute difference ratio in [1.5,2.5] and nonzero differences (expected first-order asymptote). No threshold is adjusted after seeing results. For T/P or other unresolved fields, report their differences and inverse numerical bounds separately; do not extrapolate those two primary passes into all-field, spatial, material or full-cycle convergence. Comparison with saved midpoint is diagnostic, not an exact reference solution. Long drying and spatial refinement follow only after actual cost/domain results.
