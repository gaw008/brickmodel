# Independent source-column liquid transport review

Read six files in REVIEWED_HASHES.json and compared changed production code against saved HEAD baselines. Reviewer changed only this temp directory.

Decoded-state helper checks exact mechanical type, water source assets/IDs, model and native gas constants, planar pressure convention, finite binary64 state, liquid/gas volume consistency with one-ulp subtraction allowance and full pressure interval before TP callback. Dry state supplies no fabricated liquid volume/enthalpy and makes no liquid TP query. Existing SolidFluidHeat now calls it with original decoded available volume and pressure error; original complete host observation golden passed. Backend exceptions propagate.

Source column option defaults None and preserves previous identity tuple; new rate/integral subclasses avoid changing legacy output layout. Actual config type/cell counts/manufactured mobility and connection restrictions are checked and bound. Adapted liquid states receive full SourceWetPoint.pressure_error_pa including volume-induced error, not just fluid bare bound. No second temperature inverse. Each internal face uses the original Darcy/harmonic resistance and donor liquid molar volume/enthalpy. Gas/heat and liquid enthalpy combine once; J and E remain separately represented outputs of the existing liquid kernel. Their enthalpy consistency projection is explicit and enters the arithmetic budget once, separate from face component summation error. Liquid balance aggregates old plus left minus right minus phase before one float projection. No latent source is added.

Programmed wrapper preserves internal liquid subclasses and supplies a gas/heat-only exterior face; both liquid boundaries remain zero. Open global water changes only by vapor exterior transport plus signed projection residual; internal liquid cancels. Nominal direction and fixed-decoded-temperature pressure scope remain explicit, with no full inverse/property/saturation uncertainty certification. Disabled connection is deliberate zero; unknown/disconnected active mobility is domain exit; zero mobility is explicit zero even with unknown connection per existing kernel.

Actual independent execution: first 33 passed in17.08s (five reviewer probes,20 helper tests,initial8 source-column tests). Two later-added tests separately passed, see tests02-added.log: nonzero available-volume error propagates into liquid pressure/direction interval and legacy complete programmed run golden stays unchanged. Five reviewer probes independently cover reverse donor/negative reference enthalpy sign, unknown connection at exact zero mobility, active dry refusal retaining initial prefix, exact accumulated energy-budget reconstruction including both enthalpy and summation projection, forged config refusal before inverse. All use explicit artificial liquid seams, not native EOS.

Six files parse; helper manifest matches reviewed three-file hashes. Ruff/mypy unavailable; no lint-clean claim. No unresolved critical/high functional defect found in scoped implementation. Approval applies to reviewed bytes and numerical behavior, not material or full firing validation. Root final freeze confirmation remains to be appended.

Final six-file manifest checked against author FREEZE.json files and current bytes: exact match. Added two tests actually2passed1.33s; no repeat of author26-test group. Config __post_init__ repeats its original exact leaf/count/manufactured guards and converts accepted relation/connection lists to tuples. For legitimately constructed config these fields are already tuples and tuple(tuple) preserves them; reapplication does not alter numerical values or bypass content binding. A manually assigned changed list is normalized then still participates in digest, so changed values are rejected against saved identity. No actual correctness risk identified that warrants a refactor in this scoped change. Final approval confirmed.
