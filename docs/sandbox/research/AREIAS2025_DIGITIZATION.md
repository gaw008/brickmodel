# Areias 2025 Fig.4 数字化事前合同

仅 Figure 4（PDF/印刷第9页）A=MIA1、B=MIA3的**实线** dL/L0（左轴，%）对温度°C；右轴虚线dL/(L0 dt) %/min不提取。沿用已核原文25–1200°C、10°C/min、空气膨胀仪方法与预处理污泥配方；不是烧制收缩最终产品测量，也不是原仪器数组。只给人工核选有限点，不拟合曲线/动力学，不自动补平滑数组。

检查官方PDF后，pdfimages列出11片分块栅格，Fig4两图分别5/6块，约500ppi；pdftocairo SVG也将图嵌为image而非可靠曲线矢量，因此不宣称取得曲线原始path。保留原PDF hash、原page图、分块信息和转换证据。可在原native分块拼接图上点选；末块纵缩放与前块轻微不同，按SVG给定比例校正到统一native坐标，或明确计入像素误差。

点选合同：先用带数字主刻度作线性轴校准（至少x两点/y两点，另主刻度核残差），保留每刻度/每曲线点的像素坐标、温度和长度百分比。只选实线清晰可分离位置；实虚线重叠/标记覆盖则跳过或提高范围，禁止凭形状填点。温度点不必整数，保留实际点选坐标，输出值合理舍入，不冒小数位精度。读图误差用所选像素包络/校准点误差/刻度残差有界传播；这是读图分辨率/操作不确定度，非实验误差或95%统计置信区间，不推断仪器精度。原文未给原始arrays/完整实验误差时保持unknown。

所有数据标为digitized_published_figure/external_validation_candidate，material_qualified=false/training_eligible=false；对两配方仅此论文条件可比较，不准入泥料含量、收缩本构或普适材料参数。任何以后拟合必须另登记校准/留出，不能同一曲线既标定又称独立验证。CC BY4.0位于题名页，归属Areias/Maciel/Holanda 2025 DOI10.3390/min15080879；本项注明改动为裁图、像素读点和坐标换算，无声称读过原引用实验论文。

完成记录：实际提取A/B各13个点，共26点，约100–1180°C；读点红圈图已分别实际view核对，所选点沿实线而非右轴虚线。以1180°C附近为例，A名义−1.681%、读图纵区间[−1.723,−1.638]%，B名义−3.086%、纵区间[−3.160,−3.013]%；两者横坐标也有约±5–6°C读图范围，不能只看纵区间推断固定温度下实验精度。中间主刻度校准残差和每点像素保留于数据。未从这些点拟合模型。

重现验证：首次`--check`真实暴露csv writer默认CRLF与read_text归一化差异，后明确LF行终止符再生成，仓库内与cwd=/private/tmp下绝对路径调用均通过26点精确重算。原PDF哈希强制匹配。原图块最后276行纵比例差对应2.338pixel，新增3pixel读图纵包络覆盖，没有隐藏校正或宣称原图无几何误差。完整原/派生文件hash和归属记录在新目录provenance.json；原source.json/facts.json未改。
