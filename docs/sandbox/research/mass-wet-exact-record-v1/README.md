# 混合单位运行记录与部分离线审计

新增mass_wet_exact_record独立schema，固体kg与流体mol保持分离，binary64用hex、Fraction/精确时间无损保存。原始case/runtime/binding在求解前保存；来源和模式身份、每个实际接受或失败路径的状态/账本/投影/成本均进入记录。读取不动态导入类型、不恢复live provider、不授予resume。

独审实际发现两个HIGH：失败全局累计修正上下文可被替换、普通步状态可换energy model identity；原版本和RED保留。修后完整原初7字段及每格储能模型身份绑定，源码8b59ac0b/test7c53a7a1应用字节一致；18源码2.70秒、18实际安装2.72秒通过，独立21项4.08秒通过。

实际安装95模块与源码及运行前后文件集合一致。唯一完整制造解析液体轨迹实际29.812730秒，18面板、2耗尽事件、396完成回调；36普通请求、8终端请求，116是保守面板工作计费而非完成面板数。case、外部binding、全部原始结果和记录在审计前保存。

首次研究包装器在审计函数已返回后，用只接受记录白名单的pack直接编码MixedReplayAudit报告而失败；总42.242097秒，错误与原输出保留。随后从原记录/原binding离线续审，用asdict(audit)输出报告，9.590764秒通过，没有重跑controller/EOS。40763391字节记录与导出完全一致，SHA256为52bef9f423a93ffa4917966ab9fd67fa14f51ba49670392ce050d1174d1797a8。

partial_audit重算所有记录路径的kg/mol/U原始前缀、分项账本、投影及累计修正，检查原上下文/模式lineage、记录的比较门槛和资源计费一致性。明确保留5项unknown：物理provider/外部来源认证、六差值完整重算、全部候选根及面板证明、独立native验证、resume准入。不能把canonical bytes或本次往返一致升级为完整数值/物理认证。

这是研究Python接口，尚未接通mixed CLI/service生命周期或完整原污泥案例。旧失败诊断repr不是可重放输入；packed-result是审计前未验证数值投影；record.json与单独捕获的外部binding才是本次离线一致性检查对象。

保存结果另经独立标准库核验：18接受步、4条细化路径、80个被检查路径步和8次投影；record、export、binding、packed结果关联正确。95模块及测试/数据资产的原前后SHA和文件集合均核实。归档包含原环境文件快照、全部原失败、候选/应用/复查报告；逐成员解包重读SHA核对通过。
