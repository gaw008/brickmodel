# Proposed programmed wet free slab: preregistered, not executed

Use unchanged exact-volume manufactured fixture slab_transfer(ingredients): actual water/ideal vapor bridge, two free compatible cells, liquid1/.5, inert.01/.008, vapor1e-5/.001, solid2 each, initialT300/301, n0=n1=t1. Put ProgrammedSolidFluidHeat around the base FreeSolidSlab, then retain WaterPhaseTransfer as outer source wrapper. Existing WaterPhaseTransfer already explicitly permits ProgrammedSolidFluidHeat and unwraps FreeSolidSlab. Only the program wrapper requires new exact FreeSolidSlab admission; no new water admission is proposed absent concrete evidence. Program gasT=(305,310,308,304)K and radiationT=(307,312,309,306)K at t=(0,3e-5,7e-5,1e-4)s. These are explicit virtual process choices, not measured kiln data. Gas reservoir pressure101325Pa and molefraction(inert1,vapor0); mechanical external pressure independently remains101325Pa. Zero original gas diffusion/Darcy and all liquidface flux means this case probes programmed heat plus local phase source, not reservoir mass exchange.

Manufactured film20W/m2/K, emissivity.5, original Stefan constant5.670374419e-8; nonzero radiation/film and shared interior conduction. Current outer area and half-cell width must determine the surface balance. Independent direct surface-identity residual check at final decode uses emitted temperatures and current n/t geometry, not an extra EOS call.

Horizon1e-4; explicit wrapper breakpoints passed to integrate; require each internal knot to occur exactly and no accepted panel cross either knot. Caps1e-4 and2.5e-5 enforce distinct actual grids. Relative1e-7,Nabs1e-11,Eabs1e-6,stretchabs1e-9 andscale1, min1e-10,max12steps/8reject,90s per run. Predict coarse3panels22eval and fine>=5panels36eval, ~93s total using prior1.6s/eval plus surface/initial/final overhead; 90s each and210s outer bounded watchdog proposed. No execution until root serial-EOS schedule and current wrapper code review are ready. Full encoded outputs written before success assertions; caught initialization/solver/assertion exceptions saved, hard-kill unreturned state cannot be reconstructed.

Every accepted prefix: eachcell totalwater error<1e-11mol, inert/solid unchanged, positive full3 mechanical state, represented/exact mechanical increment error and cumulativeabsolutequadrature<=1e-9; complete external_traction/mechanical_constraint/body ledger, percell energy usingface flux+work<1e-6J; global ΔE+peΔV−integratedboundaryheat<4e-6J; cumulative abs constraint cancellation represented/exact<=1e-6J. Boundaries use signed heat input face0−faceN. Require nonzero incoming heat. Phase directions retained evaporationcell0/condensationcell1. Endpoint coarsefine N<1e-11mol,E<2e-6J,stretch<2e-7,T<2e-6K. Original physical/error gates unchanged; failure preserved before any revision.

Sparse coupled numerical verification only. No depletion event, liquidface transport, pressureprogram mechanics, fulltrajectory independent oracle, spatial convergence or actual sludge material claim. Using same water EOS validates assembly/refinement, not water EOS itself. Installed/native run not yet performed.

Snapshot correction before native execution: preserve first-candidate.py. Serialize only selected boundary/surface/heat/rates, geometry/free, storage inverses/gas states/source IDs; no current_host/current_storage/live providers. Save final selected snapshot before surface assertions. Dry candidate-wrapper preflight required.
