# Explicit free-model application seam

Read-only design against present sources and /private/tmp/brick-free-case-integration-design/PLAN.md. No EOS/install/repo changes. Another worker owns case parsing/build/snapshot; do not duplicate it.

1. Add a fixed model-ID-to-packaged-catalog allowlist in run_provenance.py. Existing catalog_bytes() defaults to legacy wet-slab-equations-v1.json for old callers; explicit model argument selects the free catalog, unknown model fails. Do not convert caller model strings into paths. Free catalog has its own model_id/catalog_id and exact current AST anchors: CurrentSolidStorage forward/inverse, ManufacturedReactingSkeletonEnergy.evaluate, solve_free_slab_rates, FreeSolidSlab.evaluate, reaction/phase/face-energy laws and existing integration stages. Document shared-tangent resultant equilibrium, q(N)η, local constraint work, total-energy scope and source-asset versus scientific-validation distinction. Require complete parameter pointer metadata. No prescribed-motion anchors or fake lambda(t) for free model. Register actual geometry/normal/common-t outputs as result roots. Keep legacy catalog byte-identical.

2. run_service._execute currently calls catalog_bytes() unconditionally (line148). Read verified case first, choose catalog by its model_id, save those exact bytes to equation_catalog.json, copy only that catalog's evidence via existing safe path mechanism, and build_graph (already checks model equality). Keep full runtime_identity module/catalog map and all sealing/failure artifacts. A free case must not silently pass under legacy catalog. Unknown model/case failure before build must still preserve structured failure evidence.

3. resume_run currently gets current['catalogs']['wet-slab-equations-v1.json'] before read_case (line328). Move validated read_case/identity check before catalog selection; select expected packaged digest via same allowlist used by execution. Preserve all existing before/after runtime exact equality, module binding, caseSHA, policy-normalization, original initial-energy identity and checkpoint prefix audits. Do not replace strict runtime identity with model-only matching. Runtime catalog additions change identity and can invalidate old resumes; retain truthful explicit refusal rather than silently grandfathering mismatched runtimes. Byte-compatible default parser behavior alone does not make old runtime checkpoint resumable after software update.

4. Replay already re-enters shared execution using saved case/evidence. Ensure it selects by verified saved case model, not user's current dropdown/default. Verify case/catalog/model equality and preserved evidence before reconstruction. Rebuild provenance from replay's actual installed implementation and chosen catalog while retaining existing parent bindings. No claim of exact replay when numerical/runtime identity changes. Inspect experiment and sensitivity case eligibility separately: model-specific parameter pointers cannot be assumed shared merely because run_service accepts both.

5. Builder-owned snapshot must contain actually saved full stretches (n_0..n_m-1,t), current geometry from evaluate, and free residual/constraint diagnostics without recursively serializing current_host or live providers. Existing service trace accesses final_snapshot[quantity] (line271), so root names must exactly match snapshot keys and catalog result_roots. A missing unavailable field should be explicit, never fabricated as prescribed motion. Geometry can be computed algebraically from saved stretches/reference dimensions but label derived vs stored; no inferred missing T/P. For completed initial/final snapshots retain the raw thermal inverse uncertainty fields.

6. assets/app.js seriesFor hardcodes vapor column2, assumes every returned vector is one scalar per cell, and only special-cases temperature/pressure snapshots (lines82-92). Add explicit model/layout-backed vapor index, or assert supported case layout before indexing. Add normal_stretch using integration.states.mechanical_stretches.slice(0,-1) with cell labels; tangent_stretch uses only final vector element, one global series, never label it cell m+1 or plot it as spatial cell. Reject inconsistent vector length vs actual N-cell count. Expose geometry volume/width from saved initial/final snapshot as sparse points unless full prefix geometry is explicitly derived and labelled. Preserve no-interpolated-temperature note and lossless raw-text export. HTML selector labels must distinguish actual free stretches from prescribed motion; legacy case should not display unavailable free-mechanics traces.

7. local_app currently obtains/builds app case via existing case path; add explicit model-aware case choice/config only after free case artifact is source-bound. Do not silently switch existing default case or pretend old prescribed outputs became predictions. Shared run/replay/resume routes remain one service. Keep token/Host/Origin/body/path protections, job ownership and cancellation behavior unchanged. Mechanical pressure parameter must be distinct from furnace gas reservoir pressure.

Acceptance gates (no native execution until coordinated): pure mocked service tests prove both catalogs, wrong/unknown model refusal, resealed substituted-catalog refusal, correct replay and cancelled mechanical-prefix resume dispatch. Actual saved free run provenance graph has every current anchor/source and selected bytes; no legacy motion root. Frontend synthetic tests cover normals m + global tangent, unavailable legacy fields, nonfinite/malformed arrays, exact full raw JSON export, and derived/sparse geometry labels. Then a bounded free run, replay, cancel/resume and browser trace/export verify service-to-UI delivery; kernel manufactured verification is not material admission. A 2/4/8-cell physical refinement is a separate numerical study with extensive inventory/energy and q-density scaling, not demonstrated by UI support.
