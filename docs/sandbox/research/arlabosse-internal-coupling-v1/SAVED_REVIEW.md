# probe01 与 dynamic01 保存结果独立复核

**PASS。** 有限被动审计完成，`SAVED_REVIEW02.json` 中 **1,366 项检查全部通过，耗时 0.02856 s**。检查数包括文件身份比较，不是物理运行或试验次数。只使用 Python 标准库读取保存文件与算术；没有导入应用、调用 EOS、积分、安装或修改生产/原结果。

复核脚本为 `audit_saved02.py`，原始输出在 `AUDIT02.log`。第一轮 `audit_saved01.py`、`AUDIT01.log`、`SAVED_REVIEW01.json` 保留：其两项失败来自审查器把“probe 与 dynamic 合并最大值”对比“仅 dynamic 摘要最大值”的口径错误。第二轮分开统计后通过，未更改原数据或任何阈值。

## 实际保存证据

| 项目 | 已保存且复核的结果 |
|---|---|
| probe01 | 34 原验收项通过；构造 0.441828 s，单算子 3.428046 s，driver 3.872640 s，监督 9.106187 s |
| dynamic01 | 117 原验收项通过；0.05 的 binary64 表示时长，2 个接受步、6 次 attempted/completed RHS、3 个状态、2 个接受观察 |
| 动态资源 | 积分器 19.983667 s，driver 20.633554 s，监督 25.872084 s；原 60/80/100 s 与 5 s 清理门保持 |
| 进程终态 | 两次监督均 complete/返回码 0、leader_reaped=true、无 signal_errors；仍仅声明原进程组的清理范围 |
| 输入身份 | 两次监督各 389 个输入的 before/after/current SHA 相同；173 个源码/安装包文件与原冻结相同；安装测试前后身份相同 |
| 初态与前缀 | probe 与 dynamic 初始状态一致；逐格构造前缀与对应 INITIAL 一致；动态初态和原模型身份保留 |
| 导出 | `DERIVED_SUMMARY.json` 动态口径数值一致；`TRACE.csv` 6 行、54 个数值单元与原嵌套状态一致 |

温度/压力均从实际保存路径 `point.fluid.mechanical.temperature_k/pressure_pa` 读取，没有依赖不会序列化的顶层属性。

## 守恒与热力学交叉核对

逐格、每个接受步及每个累计前缀，独立用 Fraction 重算凝聚水、三种气体与 U；扣除保存的已接受投影残差后，局部及全局水、U、O2、N2 的缺陷均**精确为 0**。共享面传导/气体携带焓及分解残差重新组成其能量积分；封闭边界、内部活动性、固定干质量和正库存满足原门。预测步的物理转移没有混入已接受守恒账本。

原 8 个活度节点、6 个可读 q 节点及 .5/.6 原 q 空值保持。用原节点独立分段积分，结合旧 wet 阶段已保存的同 Python 水 95°C、100 kPa 锚点，复核全部原初点、probe 点和动态接受点的过量 U/S/F、μ、活度、偏水焓及原干 Cp 显热；均与当前保存值一致。旧锚点给出 `L95=2,281,135.2775707953 J/kg_water`，23 个相关水/模型运行源码及资产与旧监督记录和当前字节对应。

这是一项有来源身份的跨记录锚点核对，不是本次另做独立 EOS 测量或验证。旧锚点原始路径和 SHA 已记录在审计 JSON 中。

当前孔压进入纯液水查询，平衡压为同源活度乘纯水平衡压；水蒸气分压、实际局部相变速率、化学驱动、正熵产生与脱附偏焓均重新计算。由保存的纯液水/理想蒸气化学势独立恢复修正后平衡关系，最大残差 `3.1491e−11 J/mol`，小于原 `1e−7 J/mol` 门。

## 数值范围与真实结论

| 指标 | 动态实际最大值 | 原门 |
|---|---:|---:|
| 完整 U 残差绝对值加数值误差 | 7.24472e−6 J | 1e−5 J |
| 逆解温度误差界 | 4.51380e−7 K | 1e−6 K |
| 累计能量算术成本 | 1.57225e−11 J | 1e−8 J |
| 累计库存算术成本 | 6.00717e−17 mol | 1e−12 mol |

probe 自身的最大 U/温度逆解指标分别为 `9.64070e−6 J`、`6.00649e−7 K`，同样通过原门；没有与动态口径混为一项。

两格最终 T 为 `330.00006633996964 / 332.99978333711624 K`，变化为 `+6.63399696e−5 / −2.16662884e−4 K`，均超过最终逆解界加原 `1e−6 K` 初态允许量。最终孔压为 `99543.75805658806 / 100835.57469036386 Pa`；凝聚水分别减少 `5.45485976e−8 / 6.68809808e−8 mol`。这些记录证明本次两步条件耦合发生了可分辨温度变化和库存/压力变化。

**科学边界保持：** 零过量体积仍为显式假设，几何/输运仍为制造参数，温度/压力/跨材料推广误差仍 unknown；Darcy 液流、变形、反应和烧结未接入。所有材料/训练资格为 false。本次没有证明时间/空间收敛、真实泥料预测精度、完整干燥或烧砖周期。

## Review Summary

| Severity | Count | Status |
|----------|-------|--------|
| CRITICAL | 0 | pass |
| HIGH | 0 | pass |
| MEDIUM | 0 | pass |
| LOW | 0 | pass |

Verdict: APPROVE — 有限保存结果与原登记目标一致，可归档本阶段；不升级材料资格。
