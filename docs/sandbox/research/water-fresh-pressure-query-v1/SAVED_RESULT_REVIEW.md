# Independent fresh native-query saved-result review

Decision: PASS the saved **cell 0 standalone conditional query** result. No HIGH/CRITICAL finding. Reviewer executed only saved-data decoding, hashing and exact Fraction algebra; no native, proof, seed, controller or trajectory calculation was repeated.

Actual audit outputs: `review_saved_fresh01.json`/log and `review_saved_fresh.py`. Summary SHA is `826123e56023390b4ffc0548d2f5e1a425563859778c691a88c2e7b2a8764a27`; evidence SHA is `d598eddb47cf9e030b54eadb09a0121fef2d046f6787389c4c9f943ffa34600b`. The exact-byte snapshot hash equals its request identity and its decoded contents equal both live-result states, actual rate cell 0 and selected index 0. All four operation evidence blobs are present and immutable encoded records, not borrowed saved PASS flags.

## Actual host and source evidence

The fresh design temperatures are 300.125 and 305.25 K. Each saved forward result has its own exact requested temperature; its returned total U becomes the respective initial state's U, with original solid/liquid/gas inventory fields unchanged. The actual completed pair result supplies the selected inverse and equilibrium liquid snapshot. Original target U, storage identity, per-species gas inventory, fixed native T/P/rho, public/native mass scale and literal host volume were cross-checked against those actual saved results.

Source checks cover all **247 frozen files**, initial/final/current SHA equality, all **101 installed Python members**, and complete asset-directory membership. No unexpected current member was substituted or omitted. The supervisor's recorded runner/freeze/self hashes match actual files and returncode is 0 with no timeout. All 247 archived original copies were additionally checked against EXECUTION_FREEZE via FROZEN_INPUT_COPIES; `review_saved_copies01.json` records that independent PASS.

Actual host costs are two initial-forward attempts/completions and one pair attempt/completion, with host elapsed 6.480168000009144 s. Construction work is included in the query's original cumulative budget, and native internal calls remain explicitly unknown. Seed proposals record five completed mathematical suggestions, bounded by 12. The provider records four completed proof operations, with 31 tube evaluations/16 accepted leaves and four fixed-query mathematical evaluations. These categories must not be summed and called primitive native calls.

Provider elapsed is 1.3445890840084758 s within the actual original maximum 20 s. The runner completed within 60 s; supervisor elapsed 8.450848292006413 s is within its actually frozen **70 s** watchdog. These are the executed runner's budgets, not the earlier unimplemented 30/40 s helper sketch. Actual host query also remains within 30 s.

## Independent exact algebra

- Reconstructed inverse energy residual plus original energy error divided by positive original minimum Cp equals the saved required epsilon and does not exceed the unchanged returned epsilon. Original energy/T policy bounds, original final bracket, and full T±epsilon are retained. This entire T interval equals the coexistence center/domain, mechanical parameters and tube interval; no nominal-temperature substitution occurs.
- Reconstructed complete Krawczyk image and weighted norm from saved residual/Jacobian/center/preconditioner are enclosed by their saved directed results. Every strict inclusion margin remains positive and contraction is below one. Both phase derivative bounds are positive; saturation pressure is the exact saved phase intersection. Seeds match the final saved proposal iteration and remain candidate generation only.
- Original kg-solid occupied volume and bulk/Nl-volume uncertainty, gas amounts/R and distinct public/native scale reconstruct the mechanical input bounds. Exact corner evaluation verifies the reported mechanical gas-pressure enclosure; strict residual signs and full-box positive derivatives are present.
- The complete density interval is the exact union hull of **all four boxes**: coexistence liquid, mechanical root, observed fixed-query root and native density. No endpoint-only union proof is inferred. Exact rational midpoint BFS replay yields the 16 saved positive leaves with complete adjacency and no pending boxes. The native observed point and fixed-query root box lie inside this same reproved tube.
- Fixed-query pressure residual endpoints, strict query root faces and the positive whole query-box slope support its mean-value density bound. Exact public reciprocal scaling and signed host `(Nl*Mpublic)/rho_mass` roundoff are retained. Combined volume discrepancy is at most `6.36840369470945441269122540823873684265807308925191606994193e-20` m³/mol, below the original binary64 `1e-16` declaration.
- The returned pressure radius equals the exact maximum nominal-to-full-pressure-endpoint displacement **plus the entire original fixed-T error**, with no rounding underbound or cancellation. Its value is `0.0032806839757006973` Pa (displayed approximation); the exact Fraction is preserved in the audit JSON. Full pressure bounds including fixed-T error satisfy original/model pressure domains; the original saturation ambiguity is strictly respected.

## Limits of this review

This verifies the saved computation's associations, mathematical implications and source/cost records. It does not independently re-evaluate EOS functions, authenticate against a malicious interpreter, prove all possible phases, or replace original U/Cp/material-error assumptions. Only cell 0 receives this pressure proof; constructing two initial cells and evaluating their pair is not a second-cell proof or a trajectory. `consumer_admission=false` remains correct: no existing stage/controller gate or production policy was enabled by this successful standalone query.
