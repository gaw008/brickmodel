# V2：修复原生时间节点后重新验证

V1方案与native01失败保持不变。首次监督实际退出1并回收进程，耗时0.710509334秒；粗档在0.3秒以numerical_failure/unresolvable_stage_time停止，61个状态、60个接受步、421次RHS、0拒步，细档未启动。严格匹配原参考时刻的只有0、0.1、0.2秒三个点。局部/全局能量及储能解码的已保存前缀检查通过，不表示全时段或时间收敛通过。

## 缺陷与修复边界

固定原参考的下个时刻是0.30000000000000004。累积名义二进制步长的候选端点舍入到相邻的0.3，留下没有可表示中点的尾段。原端点舍入守卫保持原样；V2仅对仍未接受的候选进行调度修复：若候选到当前knot的剩余尾段无法构造原SSPRK2的中点及正半权重，将整个剩余区间中点选为更早候选，条件是新候选严格更早且两段都满足原stage可表示条件。调度时钟重锚到新候选的精确二进制值。

全部实际RHS时间、积分权重、热量与功仍按新实际端点重新计算。修复不搬动已经接受的状态或时刻，不补热、不补物质，不更改输入比较时刻，不跨当前knot，不放大名义最大步长，不重新解释能量。后续仍受原误差、拒步、步数和墙钟限制。通常多一个接受panel，但若实际域或误差使后续再细分，则照常计数；不保证无条件固定步数。

若两个显式forcing knot本身相邻到无法积分，仍失败，不合并边界。大绝对时刻的反例也禁止用向前吸收尾段把0.5秒步扩成0.75秒。原core没有接受步回调，本修复也不增加；外部强杀时尚未返回/落盘的前缀仍unknown。

## 新执行登记

这是修复已证实core缺陷后的新版本验证，不是自动重试原代码。只改变integration.py调度实现及对应回归测试；storage、host、六个物理系数/边界、参考几何、初态库存、能量定义、驱动算法、原101项时刻和所有数值/验收参数保持V1。V1的“本阶段不改core”描述作为原方案保留，V2明确增加此必要缺陷修复。

重新安装并冻结V2包，冻结文件同时包含V1方案、本文件、原native01身份与新安装身份。确认原其余164个包文件保持不变。源码回归183项已通过2.13秒；安装回归与独审在正式运行前完成。新增回归为线性随时间的外热/库存/机械功的解析积分，以及相邻真实forcing knot的失败，不是额外B轨迹。首轮测试的非法功分量fixture失败与随后真正的两项clock RED均原样保存。

唯一新正式调用仍使用同一supervise.py，一次130秒覆盖启动、粗细计算、分析、落盘；每档原生60秒，粗/细初始最大步长0.005/0.0025秒，最小1e−8秒、rtol1e−9、E_atol1e−11J、N_atol1e−12mol、最多12000接受步/1000拒步全部不变。每档完整返回后立即保存；失败时停止第二档并保留受控接受前缀，不能加时或就地覆盖失败。

原对照门保持：各档对两套旧参考温度最大差≤4e−6K，粗细差≤4e−7K，所有接受前缀绝对局部及全局热账残差≤8e−7J。每次解码仍为1e−10J/1e−9K/200次/160位。stress和实际native逐格功差仍为描述性数值，不新增事后门。V1原参考、旧DOP853和空间轨迹不重跑。

V2成功也只证明限定制造干态热弹半板的原生总E与局部功账接入；同原污泥材料、高温反应、相变、湿坯至冷却全流程资格继续未完成。
