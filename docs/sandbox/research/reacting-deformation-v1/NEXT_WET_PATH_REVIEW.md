# Read-only next path: reacting solids, wet phase transfer and depletion

No tests, EOS, installs or production edits were made while installed suite session 99225 is live. This assessment uses the actual current wrapper/depletion code and the preserved successful fixed-solid wet/depletion fixture.

## Concrete interface assessment

There is no newly identified type or identity guard that requires bypassing to exercise the new reacting host through the existing water/depletion path:

- `water_phase_transfer.py:90` already admits the concrete DeformingSolidHeat type; the new reacting behavior is an explicit regime of this same type.
- Lines 145–153 unwrap DeformingSolidHeat to its underlying thermal SolidFluidHeat. Species order, gas H2O, inventory columns and bound source-qualified liquid/vapor chemical bridge remain checked. A proposed fixture MUST rebuild complete inventory layout, actual providers and reaction bindings; merely appending a B column to the old state is invalid.
- `evaluate`, line 217, starts from a copy of the base reaction sources and adds water transfer only to liquid/H2O columns. Lines 258–259 preserve the total mechanical power and complete component mapping. The new reacting labels therefore propagate without a wrapper schema change.
- `depletion_integration.py:173` admits WaterPhaseTransfer; observations at lines 218–223 consume the same state/inverse properties exported by DeformingSolidHeatEvaluation. The current total inverse passes its mechanical uncertainty into the thermal target before inversion, so the shared thermal inverse error is not a discarded mechanical bound.
- Energy identity checks at lines 211 and 246 and the generic component schema check preserve the new total-energy binding across ordinary segments, event preview, correction and dry continuation.

The immediate missing item is **executed combined validation**, not a demonstrated need for a new wrapper implementation. The old wet evidence fixes solid inventory; the new dry evidence omits liquid transfer and its event. Start by building and running the real joined path, preserving any actual failure instead of pre-emptively relaxing guards.

Existing unsupported paths remain explicit: `water_phase_transfer.py:272` rejects liquid reappearance after depletion; strict policy at line 289 rejects condensation requiring unsupported nucleation; `depletion_integration.py:283` rejects non-evaporative liquid depletion, and nearby lines reject unresolved simultaneous events. A/B chemistry that does not consume or generate water avoids those missing contracts without pretending to implement them. Chemical water generation/consumption, renucleation and simultaneous multi-cell events need additional explicit work later.

## Smallest meaningful preregisterable joined experiment

Use one closed cell from the already verified HEOS wet-depletion fixture, its approved native/source hashes, the matched IdealWaterVapor/WaterChemicalPotential bridge, existing manufactured isotropic smoothstep prescribed motion, explicit interface coefficient 1e-6 mol/(s Pa), T0=300 K, H2Ov0=1e-8 mol, H2Ol0=1e-6 mol and inert carrier 0.001 mol. Keep the original 0.5 to 0.5+1/128 s interval and original wet reference geometry (bulk reference 1e-4 m³), not the different dry fixture volume 1.4e-4 m³.

Replace the old fixed solid with the already documented manufactured A/B phase pair: NA0=2 mol, NB0=0, molar masses 0.012 kg/mol, Cp=5 J/(mol K), vA=2e-5 and vB=1e-5 m³/mol and the existing declared formation-reference difference. Use first-order A→B with k=0.1 s^-1 and zero activation energy over the declared valid temperature domain. Bind actual providers and current storages using SolidReactionConfig; independently check NA(t)=2 exp[-0.1(t-0.5)]. Explicitly preserve A/B non-water stoichiometry.

Use ManufacturedReactingSkeletonEnergy with q0=1, wA=0, wB=10/mol, and explicitly choose the already tested reacting interface reference energy 0.3 J (gamma=100 J/m², A0=0.003 m²). This deliberately differs from the old fixed wet interface energy and must get a new fixture/model identity and freshly computed initial total E; do not assert the old initial energy remains identical. Retain the prior wet moduli, viscosity and motion if those are the declared choice. Current composition modifies mechanical storage while active evaporation changes liquid storage; this is the intended new coupling.

First run one bounded callback before integration: verify simultaneous nonzero A/B source and evaporation, no source in inert carrier, exact current reaction storage object binding, current pore geometry, total-energy inverse tolerances and unchanged mechanical power/component dictionary between host and wrapper. Its success is not an integrated result. Use a 30 s callback supervisor. Then perform one standalone integrated run with the previous 120 s internal / 150 s external wet resource limits, 500 panels, original inverse 1e-6 J / 1e-6 K and unchanged event gates time 1e-7 s, amounts 1e-10 mol, E 1e-6 J, T 1e-5 K, P 1 Pa. Declare these values and all altered fixture choices before running. The previous fixed-solid trajectory took about 57 s; added chemistry may change cost and event time, so no completion promise follows from that cost.

## Required evidence and conserved quantities

For every accepted prefix, retain and audit full state, step ledger, total-energy identity, five reacting component keys and exact Fraction component residuals. Check A+B conservation to 1e-10 mol and declared solid mass accordingly; A analytic error to 1e-9 mol. In this no-water chemistry, H2Ol+H2Ov remains conserved to the original 1e-12 mol boundary with every event correction and signed/absolute/numerical-phase roundoff explicitly accounted. Carrier remains exactly constant. Closed faces remain exactly zero. H and O atom inventories of the water subsystem follow from the water-mole invariant; do not invent elemental identities for the manufactured inert carrier.

Total E changes only by accepted bulk-pressure, fixed-N elastic/interface deformation and viscous work (plus any explicitly declared body/face work, zero here). Retain the old wet prefix E gate 1e-7 J. Check that changing mechanical composition is reflected in storage and is not added as a sixth power or hidden body heating. No extra reaction enthalpy or latent heat may be added. Recompute occupied-solid volume from actual A/B and pore volume from current bulk minus solid; compare liquid/gas split to the existing source-qualified mechanical closure.

Require an actual liquid exhaustion event followed by continued A→B reaction with exact zero liquid and a retained nonzero interface coefficient in depleted_no_nucleation mode. If the altered physics instead prevents exhaustion or enters a declared domain exit, preserve that result; do not force an event by resetting liquid or changing coefficients mid-run. Independently audit common-time event-refinement N/E/T/P differences and all correction ledgers. A second preregistered step-cap run must compare endpoint amounts/E/T/P and event time at the same unchanged gates before claiming convergence; whole-trajectory/event accuracy is not proved by one path or the prior dry scalar oracle. An independent wet temperature oracle is a separate task, because the dry constant-C expression cannot be reused when liquid/vapor caloric storage evolves.

This advances required reacting wet single-brick dynamics. It does not replace the outstanding raw-sludge source-qualified chemistry/transport, free-sintering and cooling closure, spatial transport comparisons or full-cycle application requirements.
