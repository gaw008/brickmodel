# Saved asset-mapping compatibility follow-up

APPROVE the local mapping fix. Final production SHA-256: `5695976e4f655bca189c7eadd87e62a321c048349d1ddcbf79a74b12f2d52e36` (16,681 bytes). Final author-test SHA-256: `0f5d3636f532ba0c8e2df1eb68cc47b16923e5246e3748ef538ac9811d85821b` (9,428 bytes). The three previously reviewed shared arithmetic modules are unchanged.

The failed installed saved-data run was read, not rerun: it retained status failed / source_pressure_model_binding, two completed backend constructors and reference-anchor checks, zero new endpoint evaluations, and wall time 1.0984587920247577 s. Its saved JSON/log hashes and counters are retained in mapping-failure-evidence.json. That real failure exposed a representation issue: the fixed passive decoder restores a dict, while actual water providers expose MappingProxyType. The global _same rejects different container classes before comparing their contents.

An independent cheap regression reproduced exactly that mismatch on the existing actual SourceWetStorage fixture with explicitly manufactured liquid properties. The original WaterProperties asset map is MappingProxyType; replacing only the saved mechanical map with an equal dict raised source_pressure_model_binding. The unchanged proxy baseline passed. `mapping-red.xml` records **1 failure / 6 passes in 0.503 s**, on production `f0c12306...`; mapping-before-source_inverse_pressure.py preserves those actual bytes. No new HEOS construction or native EOS was used.

The reviewed change is local: require both asset containers to implement Mapping, require every key to be exact str, then compare materialized dicts through the unchanged strict _same. Values therefore retain their original types and exact contents. Missing/extra keys, changed hashes, bytes replacing a string hash, and a list of pairs are still rejected. Global _same and the passive decoder are unchanged, as are all numerical conditions, reported gates and qualifications.

Final actual verification reran the original 22 cases together with the seven new mapping cases: **29 passed, zero failures/errors/skips, JUnit time 0.678 s**. Evidence: mapping-final.log/xml. Both dict/proxy positive cases recheck their returned records, and verify that global _same still rejects unlike mapping container types. Six physics entry points remain blocked after manufactured fixture construction. AST parsing and git diff --check pass; hashes were checked again after the run.

FINAL.json now records the current complete five-file scope and 29-test result. pre-mapping-REVIEW.md and pre-mapping-FINAL.json preserve the prior approval; the two earlier identity/metadata RED batches remain unchanged. This follow-up resolves the newly observed mapping regression only and does not claim the installed native bridge has now succeeded; the parent owns that subsequent run.
