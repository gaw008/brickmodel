# 条件压力传播：执行前登记

范围是既有 `SourceWetStorage` 的条件温度反解压力误差，不授予事件、EOS 全域或材料资格。
先通过源码测试、物理与代码独审，再安装冻结版本，执行一次保存端点核算。

输入固定为 source-prefix-trial-v1 的原生 JSON 7c0907… 与原审计；使用已审被动解码器。
真实 storage 由原 source-wet-storage-v1 的 make_case 重建，三个格点使用相同模型身份。
该构造有两个 HEOS 后端实例，各含一次参考锚点 native update 及理想导数/熵核对。
预期没有新的端点 EOS、温度反解或时间积分；这些入口在运行时显式禁止。
计数区分后端构造/参考锚点与端点求值，不能写成“完全无 EOS 计算”。

旧构造实测约 1.48 s。本次软时间预算 20 s、外层 30 s，一次安装版执行；失败保存，
只有查明新原因才决定是否复验。源/安装测试不是六个端点的重新求解。

六端点需通过实际 storage/state/inverse 绑定、原压力残差/分辨率、热容/能量误差重核。
全域围栏通过后才收紧 Lipschitz 界；温度与压力延拓必须保留完整原域。
每个全域/收紧斜率与半径、三个成对压力界按 Fraction 精确比较前阶段独立结果
`source-endpoint-comparison-v1/pressure-endpoints.json`（711d77…）。无容差调整。
原始端点没有声明事件政策，条件 gate 为 null；本次不得据此准入事件。

独立解析验证另用稳定液体 v=v0+aT-bP 的正二次根和明确误差扰动，
不使用当前传播函数计算参考真值，不使用有限网格充当真实水全域证书。

## 首次失败后的限定复验登记

首次安装执行已终态失败：1.098459 s，两个后端构造/参考锚点成功，零新端点求值。
实际模型身份一致；保存 JSON 的来源资产表为 dict，真实 provider 返回 MappingProxyType，
过严的容器类型比较触发 source_pressure_model_binding。独立制造 fixture 以相同资产键值
复现这一 RED，缺键/多键/改值/错误值类型/非 Mapping 仍必须拒绝。

修复仅让这个资产字段接受等值 Mapping 表示，严格核查字符串键及全部键值类型/内容，
不改变全局比较器、解码器、物理值、围栏或任何门槛。保留初次 JSON/log、旧源码/安装测试；
通过受影响的 31 项源码/安装测试、独审及安装文件匹配后，允许原输入/原运行器/原预算
再执行一次。第二次使用新输出文件，不覆盖失败。两次累计构造及锚点成本分别报告。
