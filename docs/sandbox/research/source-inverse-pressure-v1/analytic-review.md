# Independent analytic continuation validation

Passed1,030 checks over492 independently calculated positive quadratic roots, elapsed0.01136 s excluding imports. Actual output ANALYTIC02.log and ANALYTIC_RESULT.json; source_inverse_pressure.py SHA7cc846444bdb4314c0aafa324030cc112afb07d8f3fa964022f13f5c05bf1745 was unchanged during execution. No HEOS or source-host wrapper invocation occurred.

Reference EOS is v=v0+aT-bP with b>0, positive volume throughout the declared rectangle, and exact uP=-aT+bP. Three independent thermal-expansion cases use a positive, negative and zero. The whole-box |uP| maximum is independently obtained from affine corner values. Positive quadratic roots use Decimal precision100 and a cancellation-resistant formula, not the tested derivative or pressure solver. For each case,41 temperatures across the nonzero T error interval and four combinations of constant volume/response-volume error signs give164 roots. Every reference root lies inside both global and bootstrapped intervals. At nominal T, all perturbed roots also lie inside the independently constructed initial residual/compliance interval.

Nonzero temperature error, nonzero initial pressure residual and nonzero volume uncertainty are exercised simultaneously. The bootstrap shrinks radius from about32.3–32.6 Pa to3.0087–3.0138 Pa for these manufactured constants. Strict pressure-domain contact under nonzero temperature uncertainty is refused. Temperature domain exit, incompatible initial fence, zero Ng, negative uP bound or pressure error and forged derived radius are refused. All returned successful records remain conditional/source_certified=false.

First execution's boundary negative control increased temperature radius so far that it crossed the temperature domain before reaching pressure-contact validation. The auditor incorrectly expected the pressure reason. check_analytic01.py and ANALYTIC01.log preserve that failure. The corrected control holds the admissible T interval fixed and moves the declared lower pressure boundary to the exact global lower fence, testing the intended strict-pressure-contact condition. No model code, bounds or tolerances changed; the original T-domain rejection remains separately covered.

Scope: these are finite independent root evaluations under a manufactured EOS whose branch stability and whole-box uP bound are analytically known. The root grid is not itself a universal EOS proof. This validates the pure helper formula, domain gates and bootstrap, not source-water interval certification. The production source wrapper's saved identity/residual/caloric reconstruction is statically consistent with the previously supplied INITIAL_ROOT_CONTRACT.md; its production behavior/forgery tests are outside this standalone oracle's assertions.
