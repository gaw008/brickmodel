# Independent numerical-association review — changes requested

Read-only candidate source review plus two actual pure-data attacks. No EOS or author source edits.

[HIGH] Mechanical presence is not checked for the final accepted state. Changing only final states[-1].mechanical_stretches to None in the actual completed fixture is accepted by read_exact_run. Ledger presence is compared only to its preceding state, so an ordinary final endpoint slips through. Require complete mechanical schema across all states under this exact free-slab core contract.

[HIGH] Six-gate diagnostics require length only. Replacing packet_maximum_differences with six booleans is accepted. Require exact clock Fraction and finite nonnegative binary64 other differences, consistently for committed/refinement diagnostic schemas. This is a schema guarantee, not a demand to rerun full numerical comparisons.

Actual negative controls: test_review_associations.py; review-association-red.log reports two failures (both hostile records accepted). The second test's fixture has exact-zero corrections and therefore exercised the bool-diagnostic fallback, not the correction-cell attack.

Static followup: nonNone correction cell/species identity should bind to its selected root and panel/raw/corrected state. Currently only correction count is associated with totals, so the advertised committed-association boundary is incomplete. Nonzero correction attack not yet demonstrated in this fixture.

Policy reconstruction and accepted typed ledgers/derived component residual checks are useful; final mode lineage/unique cell/ledger equality are checked. These must remain explicitly distinct from full root/point-state/prefix conservation audit and resume authority. The current validation_scope correctly refuses those broader claims. Final approval pending concrete fixes and frozen hash.

## Final bounded association review — APPROVE

Source SHA256 f03abafa6251f3ad57a73b3622a2fd852c7246c376bd4cc43fbaebec06db4408. Original findings/history above preserved. Full-prefix mechanical presence is now checked; committed six-gate diagnostics require exact Fraction clock plus nonnegative finite float values. Correction cell/liquid/vapor indices and raw/corrected selected values bind to original species layout and selected event. No-correction events require actual raw zero liquid. Initial/final modes bind to actual liquid inventory.

Reviewer reran only independent owned attacks: review-association-green.log reports2passed1.71s, compared with preserved2-failure RED. These close both actual reported HIGHs. Static correction-index gap is also closed. No EOS or source changes. This approves strict schema and committed associations only; not full numerical root/ledger proof or resume authority. Existing explicit validation_scope remains essential.

## Numeric-annotation compatibility delta — APPROVE

Final reviewed source SHA256 `18fc962896b26a786620880e6190b882358af001f215fa602a50eea96b85118f`. `_matches` now allows exact Python int or float for a float annotation, reflecting existing physical/numerical constructors, while explicitly excluding bool. Integer-annotated fields, Fraction/exact times and explicit committed six-gate diagnostics remain strictly typed. No numeric coercion or temperature/energy gate change is introduced. Policy reconstruction still invokes original constructors and checks exact packed roundtrip.

The new actual-case fixture regression preserves integer energy_scale_j=1 through reconstruction and rejects a boolean substitute. Inspected tests10.log reports19passed12.03s; reviewer did not rerun tests/EOS for this annotation-only delta. Previous fixes and approval remain valid within the same schema/association scope, without full numerical-audit or resume claims.
