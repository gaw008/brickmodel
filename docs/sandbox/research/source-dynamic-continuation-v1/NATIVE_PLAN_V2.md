# 第二次数值精度预登记

第一份 NATIVE_PLAN.md、V1 案例、run_native.py 和 native01 的失败原样保留。
本登记在第二次运行前建立；它只调整数值求解精度，不改变所计算的物理问题或验收标准。

原生 native01 的父研究完成了 32 次 RHS、4 次 HEOS 构造、3 次初始能量和 8 次额外湿查询，
但湿 cell2 在事件端点/共同端点的选用联合压力界分别为
4.649847818827615e-4 / 4.647547864762948e-4 Pa，超过原 1e-4 Pa 门槛。
时间及 N/U/T 通过；两个普通续算分支未启动。原始记录及独立逐项推导见
failure-diagnostic/ALLOCATION.md、DIAGNOSIS.json 与 NATIVE01_parent_result.json。

两端压力求根实际误差贡献合计 3.5498859841684416e-4 Pa，温度反解传播合计
约 1.0616953613374042e-4 Pa。任意一项单独消除都不足以通过。
原误差主要来自可收紧的数值残差；保留的体积/表示误差和能量误差项不需减小。

新案例 source-nonstationary-heos-v2.json 仅相对 V1 修改：

| 数值设置 | V1 | V2 |
|---|---:|---:|
| pressure_policy.pressure_tolerance_pa | 1e-5 Pa | 1e-7 Pa |
| inverse_policy.energy_tolerance_j | 1e-5 J | 1e-6 J |

分类均为 numerical_policy，依据为原失败字段的误差预算分解，不是材料参数拟合。
保留温度停止 1e-6 K、迭代上限 100、eV=1e-12 m³、液水摩尔体积误差 1e-16 m³/mol、
全部其他误差包络、来源资产、325/327/329 K 初值、导热、几何、输运及原验收门槛。
新精度下仍须重新检查实际残差、支持域和完整原门槛，不能把预算估计当成通过证明。

在原记录尺度下，每端 actual-fluid 预算 2e-5 Pa 对应允许体积残差
1.7899822025443035e-16 m³。1e-7 Pa 压力停止是针对该尺度的数值选择，
实现同时要求括号宽度和实际压力残差满足停止，不只判断体积容差。
逆解能量 1e-6 J 对应记录条件下约 1.00798e-5 Pa 的每端温度传播上界。
新端点可能改变这些尺度，须按新记录实际判断，不移用旧界作证。

只执行一个新的 supervised-native02，使用 run_native_v2.py 和新输出目录 native02；
初次案例与程序不覆盖。原安装 146 模块保持，两个精度变化由配置表达，无需重装或重复不变测试。
运行前实际通过已安装配置入口检查 V1→V2 恰好两处差异，并对脚本进行独立审查。
人工液体两路径测试仍仅证明它原有的数值配置，不宣称测试过这个新真实 HEOS 配置。

完整验收沿用 NATIVE_PLAN.md：实际父事件接受、两个真实重建分支、三步/22 RHS、
全部状态/时间/账本一致、非零导热和可分辨逆温变化、纯数值检查点实际写读、全部资格边界。
共同预算仍为 97 RHS / 16 wet / 510 s，单段 4 步/4拒步/180 s，进程监督 570 s。
V1 父研究已耗时 206.958639 s；更严格精度和两个普通段可能触发原时间上限。
若出现资源拒绝，保留它及实际部分结果，不能增加预算或借新子进程重置成本来完成本次比较。

任何新失败先保存和诊断；本登记不预授权无变化重试。完整材料资格、全烧成周期及源会话
跨进程恢复仍为 false。真实 HEOS 物性与制造几何/输运的组合不是原泥实测验证。
