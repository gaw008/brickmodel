# Event-local nested wet approach: execution preregistration

Motivation: archived v1 wet failure and actual v2 same-case result. Local pressure enclosure removed the conservative pressure gate floor; level3 event/common inventory metric remains6.1412e-10mol above1e-10 and the original120s resource limit ends before acceptance. Do not change physical/error inputs or remove event-state comparison. Current source and installed tests are frozen until full-suite36717 is terminal.

Chosen next implementation: explicitly versioned optional nested-approach strategy in integrate_depletion, separating ordinary RK approach cap from Euler terminal threshold and reusing a per-event append-only ordinary wet spine. Keep original Euler terminal, exact nearest-downward clock evidence, all correction limits, positive evaporation, all five event/common-time comparisons, two consecutive passes and commit isolation. The quadratic higher-order terminal alternative in EVENT_NUMERICAL_DESIGN.md is deferred, not partly substituted into the linear evidence contract.

Cache scope: one fixed event-search initial state, operator/model/source binding, interface modes, inventory layout, ordinary integration policy/cap and common-time horizon. Only already evaluated ordinary wet states, observations and conservative step ledgers may be shared. Never reuse terminal panels, depleted operators, dry continuations, event corrections or mutated totals. Each speculative branch clones list containers and preserves immutable state ownership; only the selected complete branch commits once. Changed horizon, approach cap, initial state, source/model identity or event candidate invalidates the spine. Actual computation counters charge work once; cached reuse is separately counted and never reported as fresh evaluations. Source/domain failures and cancellation cannot be converted to cache hits. Arbitrary stateful ManufacturedDepletionAdapter callbacks must not receive caching by default: explicit deterministic contract or rejection/uncached behavior is required.

Accuracy: terminal-threshold agreement with shared approach is insufficient. Require independent halved-approach-cap comparison under original event/common N/E/T/P/time gates, including evidence that approach discretization differs. If maximum cap is inactive because safe-inventory fraction dominates, refine that actual approach control or report comparison uninformative; do not call two identical trajectories independent convergence. Same physical fixture and original120/150s/500-panel resource limits remain the recovery experiment. One failure remains a failure; no budget inflation.

Before native execution, no-EOS manufactured checks must establish (1) cache-on/off byte-equivalent states, full ledgers, correction totals and event results at identical chosen caps, with fewer actual callback evaluations; (2) constant and linear/time-varying liquid sinks with independent integral/root, positive inventories and nonzero spectator A->B and external work; (3) independent actual approach refinement rejects a deliberately biased coarse approach; (4) event/horizon changes, successive/simultaneous events, source identity mismatch, stateful callbacks, cancellation and resource exhaustion cannot leak speculative state or double-commit a prefix; (5) old linear-clock behavior and all correction budgets remain unchanged. Use existing manufactured-adapter semantics only after inspecting their actual contract. Register concrete fixture values/analytic error thresholds before executing these new tests.

Preserve decomposed already-evaluated event/common-state inventory differences and nominal/error T/P components, terminal start/time/panel details and approach/terminal/dry cost counts without extra EOS calls for logging. This is required to diagnose residual failures honestly and show whether event time or approach integration determines the amount metric.

This work is necessary numerical progress toward the full coupled model. It does not qualify raw-sludge parameters, implement free sintering/cooling, provide public experimental validation or satisfy the CLI/UI/full-cycle requirements by itself.
