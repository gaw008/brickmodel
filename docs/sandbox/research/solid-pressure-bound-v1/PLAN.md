# Preregistration: certified local pressure bound for solid-volume uncertainty

Baseline runtime 69a14f4 (current source unchanged after source/docs commits). Motivating actual failed joined wet run is /private/tmp/brick-reacting-wet-v1/depletion0-attempt01 and depletion-result-0.json; it remains a failed 120 s run, not an event-converged trajectory. No physics parameter, declared volume/energy uncertainty, source identity, pressure bracket, event tolerance or resource cap will be relaxed.

Existing SolidFluidStorage uses ngRT/phi_envelope² as a lower magnitude bound for dV/dp, yielding d0=error_v*phi_envelope²/(ngRT). With declared A+B volume uncertainty approximately2e-12 m³ this produces ~0.8 Pa per path and ~1.6 Pa in the two-path certified comparison even when nominal differences shrink.

Proposed bounded tightening: retain original global error calculation and its complete pressure-bracket acceptance check first. This supplies a certified enclosing interval for the nominal and volume-perturbed roots. Its upper endpoint U=min(phi_envelope,p_nom+fluid_pressure_error+d0), computed with exact represented rational arithmetic and outward rounding, bounds every pressure along the intervening root segment. Positive stable liquid compressibility plus ideal gas implies |dV/dp|>=ngRT/U² on that segment. Thus d1=error_v*U²/(ngRT)<=d0 is an additional certified volume-uncertainty pressure bound. Add the original fluid pressure error, retain the same final bracket check and use the tightened additional bound consistently in the existing liquid energy-error propagation. One bootstrap only; no iterative tolerance seeking, new EOS calls, source error reduction or domain expansion.

Must independently review assumptions against actual fluid/mechanical source contracts before implementation. Verify zero-volume-error behavior, positive gas guard, unchanged original domain rejection, directed rounding, exact analytic ideal-gas perturbed roots (including asymmetric root shifts and nonzero fluid root error), wet/stable compressibility support, near bracket boundaries and no increased bound. Register individual analytic values before testing; exact rational/Decimal references must not call production helper. Add focused old/new storage regression, review code, reinstall and run original callback then same failed joint trajectory under identical limits. Do not assert it will pass: inventory refinement and resource costs may remain limiting even if the pressure-bound floor is fixed. Full installed regression required after source edit; freeze runtime inputs during each active test/probe.
