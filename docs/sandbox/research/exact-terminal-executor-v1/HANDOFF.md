# Single speculative exact autonomous terminal executor

Frozen source e3246434b9ee2d971b58e61e3afeba974e9273432fb26b987bdbcc194d8496e2.
Frozen tests a7713739eaf035c7e2a26eddece481750ad2ebad6a3cf44eee63b8f5f82f0e1e.
Actual tests04: 12 passed in 0.15 seconds. Command: `PYTHONPATH=src:tests/sandbox /private/tmp/brick-water-backend-probe/venv/bin/python -m pytest /private/tmp/brick-exact-terminal-executor-v1/test_exact_terminal_executor.py -q`. Earlier seven/nine/eleven test logs retained; no failing run omitted. Only temporary files changed, no EOS/install/repository edits.

API: execute_exact_terminal(state, operator, *, start, common_endpoint, integration_policy, event_policy, totals, cancel=None). Only exact ConservedState/ExactFreeWaterTransfer, exact times, original IntegrationPolicy and affine DepletionPolicy, and matching original DepletionRoundoffTotals are admitted. The original physical water molar-mass equality gate is enforced before observation.

It observes the actual autonomous wrapper at the initial state, computes the smallest positive liquid N/-net rate solely as a midpoint hint, constructs a constant-rate Euler predictor at exact tau/2, and observes the actual midpoint state. It then invokes complete shared root ordering, selects the strictly first numerical affine root, constructs the complete exact terminal panel, checks/writes the selected liquid with original exact accounting, proposes the dry mode, and observes the actual corrected endpoint under the changed mode. Upper domain is min(common_endpoint,start+2*tau); selected endpoint must remain after midpoint and strictly before common_endpoint, as in the old affine terminal. No float query-time conversion occurs.

Source/model/original mode content is checked before and after all evaluations. The proposed operator may differ only in the selected interface mode: full dataclass content except interface_modes and all water-provider bindings must match, and its energy model identity must stay equal. The caller's original operator/state/totals are not mutated by the executor. Malicious provider mutation inside a callback is detected, not magically undone.

ExactTerminalAttempt.status is speculative_completed, failed, cancelled, or resource_limit. Even speculative_completed is NOT an accepted packet. Actual observations retain role initial/midpoint/endpoint, exact time, state, and typed evaluation. Predictor/root-order/panel/raw/corrected state/correction/candidate totals/candidate operator are retained as available if a later stage fails. Input totals remain distinct from candidate totals. Failure root diagnostics are preserved. ValueError-family declared numerical/domain errors plus malformed callback TypeError/AttributeError/OverflowError yield failed with the original reason string; cancellation/wall limits have their own statuses. Unexpected exceptions are not swallowed. No automatic retry or mode commit is performed.

Costs separately report evaluation API attempts/completions, predictor and terminal panel successful builds, predictor/terminal/root-order/writeback attempts, mode-transition attempts, and elapsed wall time. If the exact wrapper raises on its own post-source check after its native body returned, the API evaluation is not counted completed; it is still charged as an attempt. Pure numerical stages are separately counted even on failure. The outer packet caller must supply remaining wall allowance conservatively and a global guard through cancel, and aggregate all attempts once; this one-terminal function does not reset or administer the whole packet's accepted-step/rejection/resource budgets. terminal_window_s/caps belong to the outer ordinary approach scheduler, not this tau/2 terminal helper.

Tests execute the real root-order/full-panel/writeback code and the real exact host identity wrapper. They use supported typed provider shells and instrumented WaterPhaseTransfer evaluate_autonomous/mode-constructor seams; they are not native host/constructor validation. Tests cover complete three-observation success, actual state-dependent midpoint choosing a different root than the tangent hint, endpoint failure, cancellation, original source mutation, attempted mode coefficient mutation, equal roots, original mass gate, invalid initial energy/time, predictor positivity failure, and wall expiration both before calls and after the midpoint. Prefix/operator/totals and failure costs are checked. Root owns the actual saved-native-state probe after review.

Returned WaterTransferEvaluation may contain live provider-bearing base evaluation objects. Evidence runners should snapshot selected provider-free data, rather than blindly recursively encode the whole attempt/operator graph. New record/codec/resume support is NOT provided here.

The next layer still needs actual mixed continuation, matching exact common endpoints, every event frame comparison, two consecutive passes, independent finer approach and distinct grids, original global N/E/component/stretch prefix audits, and one atomic packet commit. This helper does not close those requirements or full material/trajectory validation. Tesla final code review requested. Do not copy temporary conftest.py into production tests.

## Domain-status-only correction

Superseding executor SHA256 d004b1b750375df9ec79996929f70ccfc249440bfc7bbd39eb02e5edc9ea42fa; tests 09c07ff097e66f3c44c10baded4ad81e9f0ad8490c8e3d4a9474f9281e29ebf5. DomainExit now returns domain_exit, while an IntegrationError with the same reason text remains failed. Observation/cost/prefix semantics and success calculations are unchanged. Original source/tests retained as before-domain-status.py and test-before-domain-status.py. Actual RED one failure/twelve passes; GREEN thirteen passes in .15s. Tesla re-review requested and packet driver author informed. The already saved native success is evidence for the prior e324 source; no rerun of the success branch is claimed after this identity change.
