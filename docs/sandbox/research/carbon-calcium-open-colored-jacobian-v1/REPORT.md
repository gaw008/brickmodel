# 开放列局部导数：静态装配通过，固定差分性能失败保留

2026-09-25 UTC。问题来自累计账目没有反馈到状态率，但SciPy全矩阵差分仍为这些结构零列反复放大扰动；八格精细参考的stderr保存overflow/invalid警告。把物理体和账目导数拆开，不改变原C/O/N/T或外界账目方程。改动前两种八格模型六时刻共12个RHS已独立保存，重构后所有字段差值严格为零。

最初固定中心差分取库存相对1e-5、T步0.001K，按最近邻三色×四坐标装配。24组八格矩阵与逐坐标同一步长差分比较，最大量纲缩放差4.121605e-11，在1e-9预算内。账目五列为零，两种体RHS不变。不过完整两格运行在相变附近步长严重收缩：直接两档11845/12757步仅到110.8547/111.0034s，表面两档12016/12796步仅到454.9734/456.0234s。四次试算主动中断，未完成30000s；不以静态通过代替动态资格。其无summary前缀逐字节无损归档，压缩大小20.55/22.59/27.67/29.72MB。

随后冻结库存相对1e-8、T步1e-6K，未修改原预算。直接对已舍入总熵产生作差的参考在14/24矩阵中超出预算，最大4.799293e-8，全部出现在总熵产生行。重新以80位精度对独立逐坐标扰动后的全部面贡献作正负求和，避免两个舍入总量相消：24矩阵最大缩放差1.905825e-16通过。旧失败输出及舍入总量误差字段保留；这是提高参考求差精度，没有改变生产熵率或放宽判据。

固定小步长表面两格基准完成8628步、55.734s，完整收支尚未审查；直接两格仍相变附近低效，主动停止并保留前缀。因此没有把固定中心差分发布成通用改进。

下一实现保留已采用的SciPy `num_jac`自适应物理体差分，只对体的最近邻列分组，五个账目输入零列显式插入；账目行取已通过80位参考的各面小中心差分。12个保存状态的体矩阵、扰动自适应因子与逐坐标同后端计算完全一致，RHS不变。此比较验证分组装配，不是独立化学势导数证明。两格直接/表面各两档30000s完整来源与收支资格正在进行，未宣称已通过。仍为研究模型，无实砖和训练资格。
