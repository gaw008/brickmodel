# 开放列的局部差分Jacobian与零反馈账目

2026-09-25 UTC，结果前冻结。八格精细直接接触记录已完整通过，但SciPy稀疏差分对全矩阵包含的五个恒零账目输入列不断增大扰动，产生overflow/invalid警告。源码中`max_diff=0`的结构零列仍参与`factor`放大；完整模型的状态率不依赖累计账目。这是明确的数值结构问题，不需要截断物理状态或放宽预算。

采用中心差分，并利用最近邻关系：同一坐标、每隔三格同时扰动，12组各正负两次；每格的四个体方程只取相邻三格对应行。总熵产生导数由该格相邻内面及末格外界面的差分求和；其余外界账目仅依赖末格，五个累计账目输入列严格为零。熵分面展开不改变原先fsum总率。所有重要步长在根参数中：库存相对1e-5、温度绝对0.001K，另以0.1倍作差分收紧比较。

先保存修改前的直接/表面八格六时刻RHS；重构后逐项必须零差。再和逐坐标独立中心差分完整矩阵比较、核对账目零列和指定量纲缩放误差≤1e-9。只证明相同步长下稀疏装配等价，不把跨相边界的差分称数学可微。随后独立小格完整轨迹和原有来源/元素/U/S/熵/时间审查通过，才能用于更细格。保留旧求解器结果和警告，当前正在运行的任务不更改。
