# Prospective same-runtime runner review

Read run.py, supervise.py, PLAN.md and the exact codec capture contract; no native execution. The declared two-phase scope, original case/policy checks, same-new-runtime manifest, authoritative parent hash link, independent directories, complete core/strict output before success assertions, prefix-history comparisons and conservative original cumulative budgets are appropriate. The supervisor preserves bounded failed attempts; external deadlines do not enlarge the original policy.

[HIGH] Audit result dataclasses cannot be passed directly to the strict run codec.
File: run.py, `audits[name] = pack(invoke())`.
The fixed `exact_record.CLASSES` contains no audit result classes. The first prefix audit may compute successfully but this expression then raises `unsupported_capture_type:PartialExactAudit`, preventing the planned four-audit/restore checks. Preserve all audit fields using an explicit dataclass-fields mapping in this research runner; do not broaden the production codec whitelist for this reporting need. Reported directly to author/root; awaiting a frozen correction and pure encoding regression.

## Review Summary

| Severity | Count | Status |
|----------|-------|--------|
| CRITICAL | 0 | pass |
| HIGH | 1 | warn |
| MEDIUM | 0 | pass |
| LOW | 0 | pass |

Verdict: WARNING — resolve the concrete reporting failure before running native phases. Core continuation approval is separate.

## Final corrected snapshot

Final runner SHA256 `d494f343b593ebb6a4fd4ed085cbfc39ff7aa85a330761807ab0303212e6cdbe`; supervisor `bf2ac2dbc70d73fe12a7dabb90a0d2f59b4886e40f190b8c803243b6d7af11fb`, independently hashed. The reporting HIGH is resolved: `audit_output` iterates every dataclass field and uses the existing verification encoder for values, without modifying the strict record registry. Read the four concrete report-object regression cases and actual `report-green.log` (4 passed in 0.13 s); original direct-pack failures and runner bytes are retained.

Profiling is removed; calls.json now explicitly says cumulative core telemetry, not an independent EOS journal. No numerical policies, parent linkage, success gates or output-before-assertion ordering changed. PLAN's final telemetry addendum states the narrower instrumentation scope. Manifest remains unfilled and must be pinned to the final installed runtime and all required inputs before execution; this review does not itself launch or fill it.

## Final Review Summary

| Severity | Open Count | Status |
|----------|------------|--------|
| CRITICAL | 0 | pass |
| HIGH | 0 | pass (1 fixed) |
| MEDIUM | 0 | pass |
| LOW | 0 | pass |

Verdict: APPROVE — bounded prospective two-phase runner at the hashes above. Native outcome remains untested here.
