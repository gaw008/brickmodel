# Bounded HEOS candidate stage 3

Implement a standalone research-only HEOS Water adapter with immutable returned snapshots and an explicit implementation descriptor, separate flash and derivative-check AbstractState objects, per-instance RLock, no shared native state, no silent fallback, no production registration or altered reference/source IDs. Constructor verifies a frozen installed-file manifest, fluid canonical JSON, version/revision and original source-gated water reference. This is pinned local provenance, not certification of a release artifact's build.

Initial actual case budget: 300 K, two previously tested liquid pressures 304469.31354 and 567435.65536 Pa, and both saturated branches. Compare with frozen Python source and reconstruct Table 3 from derivatives. Keep pressure max(.01,2e-8*p), caloric .002 J/kg, h-u-p/rho 1e-6 J/kg, cp/cv 1e-5 J/kg/K, Gibbs .001 J/kg, temperature1e-9 K, molar cp-cv1e-7 J/mol/K gates. Density comparison uses declared numerical relative 2e-8 (cross-implementation regression only). No statistical/material uncertainty is inferred. Domain293..500 K/positiveP<=100MPa remains a guard, not validation of its whole interior. Wrong phase and saturation ambiguity refuse. Numerical failures preserve diagnostics, never weaken gates.

One externally supervised initial process, maximum30s plus cleanup; no benchmark/host trajectory. Tests may use synthetic corrupted native results for rejection checks; these are test fixtures only. The two-point candidate evidence cannot admit a new provider to coupled hosts. Full domain/fault/concurrency/source mutation and consumer descriptor work remains mandatory.
