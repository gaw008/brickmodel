Remove gratuitous forced Newton update if DmassT seeds already meet the SAME coexistence targets; do not claim numerical Newton convergence when no update needed. Add post-transaction fluid digest check. Four physics cases,6domain,3immutability and5fault rejection cases rerun only because code changed, combined external30s. Original gates unchanged. Both physics andfault JSON must pass andprocessmustexit0, since physicsresult is written beforefaultsuite. No production admission/no performanceclaim.
