# Source ordinary continuation: pre-run acceptance contract

Baseline: 9d7beff. This slice preserves the full Goal contract. It implements a
new ordinary segment after a selected source wet/dry comparison, and accepted
boundary continuation of that same live segment. It does not resume the old
study controller or provide a persistent checkpoint codec/CLI.

After code review and a noneditable install, execute one new same-version
source study using the unchanged source-multicell-heos-v1.json (SHA256
323cdc6ebbd866c81e87ac1227155357253f0b05f2384c55fadead85218c2dc5).
Use the 17 verified assets from the previous private local bundle. No publisher
PDF/HTML/GIF is copied into public evidence. No monkeypatch or manufactured
liquid backend is permitted in this native run.

If the original transition is numerically accepted, choose candidate 1 and
declare the new end as its actual exact end plus three original probe-derived
horizons. Retain the original integration/event policies. Pause after one
new accepted step, then continue the same session. The exact end may require
an additional minimum-tail step; do not alter the endpoint or maximum steps
to obtain completion.

Acceptance checks:

- The old source record is verified and remains passive/material=false.
- Actual new HEOS constructors restore the same source/energy/operator content
  identities and the saved existing/dry/existing modes, using the new live cell
  storages. No new initial U evaluation or wet event replay occurs on opening
  the segment.
- Its initial state and exact time equal the selected actual dry endpoint.
- The pause preserves original step controller, all ordinary callbacks and
  rejected attempts, original policy, and cumulative ledgers/costs. Continuing
  skips a second initial domain probe. Only clean committed boundaries qualify.
- Every committed point passes the original wet initial → approach reference →
  terminal prefix/writeback → dry reference → new ordinary reference N/U audit,
  per cell and globally, with no new tolerance or grid-count factor.
- Original study starts plus all new RHS starts remain within 97; additional
  shared-pressure requests remain within 16. The original 510s source lifetime
  includes the old study, reading, rebuilding, checks and the live session's
  pauses. Its new ordinary segment retains the original 180s/4-step policy;
  these were per-reference policies in the original study, not one fictional
  180s limit across all its candidate branches.
- The full declared new endpoint is reached under those original bounds.
  Inspect exact observations/history, not only a zero process exit status.
- No result gains material/full-cycle/archive-resume qualification. This is
  still a short fixed-geometry numerical trajectory, not complete drying.

The owned research child has a 570s process wait limit and 1s cleanup grace.
Keep all first failures; do not retry the native acceptance without diagnosing
a concrete cause and recording a revised numerical/software plan. Source tests
use a separately declared artificial liquid seam. Its tighter pressure-root
solve (1e-7Pa versus 1e-5Pa) addresses the diagnosed global-compliance
amplification of actual fluid pressure residuals. Tightening temperature inverse
tolerance alone did not address that error; both original failed attempts are
retained. This does not change the real HEOS case or any acceptance gate.
