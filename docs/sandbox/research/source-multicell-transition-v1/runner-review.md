# N3 prepared runner review

APPROVE for one execution after the remaining final package identity gate. Reviewed runner SHA256 1250733f5eddb41f4c4bbdb1bdbad235d553ecb79c98c12a7c848362774c556c and preregistration e659edd1f35583e9da44889d591d0b7d4a4ebe74c450a5d2d438ba1c248f2cf0. No production or author runner bytes were edited; no run(), actual model construction or EOS was invoked.

The narrow diff from the approved N1 runner creates three separate storage and volume objects from one original source template, retains the four real backend/reference constructor counts, and records three actual initial-energy attempts separately. Each callback requires every corresponding original live storage/volume, the complete 3×4 state and three U values, the original source/energy identity, and either original all-wet or selected-cell-1 mixed modes. The shared declaration is specific to cell 1 and never correlates different spatial cells.

The declared initial values, widths, zero gas/heat transport, manufactured liquid table and fixed disabled/connected face configuration match the intended N3 case. H uses the actual exact represented net selected-cell liquid loss E+J_right−J_left; transport is not relabeled as gross evaporation. Both actual coarse and shifted first-root labels must identify cell 1. The complete transition and all per-cell pressure evidence/gates are saved; neither selected dry P success nor numerical event acceptance is forced.

Verified the predecessor and both original helper files against their pinned hashes. AST checks show saved_record, save, require, load_helper, counted_init, retained_candidate and the top exception handler are unchanged. The complete IntegrationPolicy, DepletionPolicy and DepletionRoundoffPolicy construction calls are also AST-identical to the predecessor. Original scientific tolerances, 180 s per path, 16/24 callback caps and 97 total remain. The explicit new N3 outer budget is 510 s, registered in advance, with actual historical cost references of 448.601522 s / 47 evaluations and 23.676868 s / three evaluations verified in their reports. This is not a promise of completion or a hard native interrupt, and no automatic retry is added.

Three pure control probes execute only the new initial-energy-loop AST and timeout helper with sentinel control data: full success saves 3 attempted/3 completed; failure in the second item saves 2 attempted/1 completed with the first point retained; own timeout in the second item preserves the same partial counts plus outer_timeout_requested. These probes are not physical simulations, do not import/execute the runner module or run(), and invoke zero model/EOS calls. The old failure-chain/atomic-save path is unchanged, so previously reviewed failure probes were not repeated. Detailed control output is runner-controls.json.

The source and installed logs now report 52 passed in 78.58/78.33 s; both XML files were read and show 52 tests with zero errors, failures or skips. This is author execution evidence, not tests rerun by the reviewer. Package-byte verification remains the caller's explicit final prerequisite.

## Review Summary

| Severity | Count | Status |
|----------|-------|--------|
| CRITICAL | 0 | pass |
| HIGH | 0 | pass |
| MEDIUM | 0 | pass |
| LOW | 0 | pass |

Verdict: APPROVE — these prepared bytes may run once under the preregistered bounds after final package identity verification.
