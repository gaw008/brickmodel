# 额度停止线内的分阶段开发

用户于2026-09-24本地日期授权继续开发，到账户本周剩余额度5%停止。首次实时查询：周窗口10080分钟，已用40%，剩余60%。该额度是全账户共享统计，并非本项目单独消耗；不兑换重置信用，不新建定时任务。工作从分支 `codex/physics-sandbox-v1`、提交 `cab79b1` 和干净工作区开始。

## 执行顺序

1. **补完已实现数值路径的高网格证据。** 使用现有明确选择的物理反馈Jacobian运行128格原干燥过程及10倍紧容差过程，保留完整stderr；与原128格及两档时间精度比较，再完成来源/局部质量能量/整段熵审查。原有预算不因失败放宽。
2. **分析高含水与自由水衔接的可实现范围。** 先核对现有源势函数及自由水共存所需关系。只有有出处的关系或明确声明且物理相容的条件模型才进入实现；不能把W=.8端点活动度直接截成1，不能把该条件装置称为实际砖。
3. **沿剩余依赖推进可闭合开发。** 按每次发现选择最小下一阶段，分开源关系、材料近似、数值误差和实验对照。缺材料输入的反应、烧结、冷却任务写清最小缺项，保留失败记录。
4. **阶段交付。** 完成的代码、根参数、来源/计算证据与报告分阶段提交并推送；不添加或运行软件测试，不生成或比对SHA，不启动子代理。

## 停止与验证

每个阶段间查询本周额度，接近停止线时缩小工作批次。余额达到5%即停止新增工作，使用现有接受状态/检查点结束正在计算的任务并交付。百分比为服务端提供的离散全账户值，无法保证恰好停在5.000%。不得为了消耗额度重复无新意义的计算；若有价值的自主工作全部受资料依赖阻塞，按实报告。

原生M1仍为原真实黏土任务；本窗口不将其改为完整成功，不用条件算例替代真实材料资格。新结果仍为research-only，`material_qualified=false`、`training_eligible=false`。
