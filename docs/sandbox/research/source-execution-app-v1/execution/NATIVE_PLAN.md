# Managed source application: one actual three-process journey

Pre-registration before new physical work. Baseline4a8504d; application adapter changes must finish review, tests and noneditable install first. No native work has begun in this phase. One frozen installed package throughout; never rewrite historical runtime IDs or source manifests.

Use the existing source-nonstationary-heos-resume-v5.json, raw SHA f416547debfc42e45e26894a81bad00ac8d5c0c5b2dd006423d9fc1761741028, canonical SHA 063384f38fae4620404fa7b56fb84bd68b872f7567406b8de3099a2f4b06d371. Assets root /private/tmp/brick-source-resume-v1/assets already passed passive validation; registered CoolProp8.0.0 has been reinstalled in the current isolated environment and all28 declared files match, without new EOS calls. Its configuration/9 execution-source checks still belong to actual worker admission. Old temporary environment was incomplete; do not repair or execute it.

Execute the public source-execute CLI through its normal main function in a dedicated installed driver process, with each source operation using the fixed isolated worker. Driver total510s. Outer research supervisor570s, cleanup grace10s; each application supervision cap180s, grace5s, clamped to remaining overall driver time. Driver receives SIGTERM by raising a caught BaseException so the application supervisor retains its live child handle and cooperatively cancels/kills/reaps it. The outer supervisor only contains its original process group, not escaped worker sessions; record that limitation rather than claiming machine-wide containment. No deliberate OS-kill experiment is needed beyond existing short process fixtures.

The only physical operations are source-run, source-advance paused after1 new step, source-resume with no further pause. Shared budget is null because this is one lineage, with source510s/ordinary180s/97RHS/16wet original constraints unchanged. End equals the fresh verified parent's candidate1 exact endpoint +3/64 seconds; initial and maximum step1/64. Never derive this from a displayed float. No continuous fourth branch, no parameter adjustment, no extra budget on resume, no automatic retry.

Acceptance before completion claim:
- All three source workers have observed zero exit and are reaped, and adapter result verification passes its explicitly stated operation scope. Parent original numerical transition is accepted, yet all material/training/full-cycle/historical-study-restore flags stay false.
- Pause is independently packet-readable and unclaimed before resume. Resume claim belongs to the actual new trajectory. Exact first accepted step and eight saved observations/journal prefix retained; final3accepted steps/22observations, exact duration3/64. No rejection or changed endpoint silently accepted.
- Original cumulative counters equal54RHS,12HEOS constructions,3initial-energy evaluations,8wet requests. Resumed suffix adds14RHS. Full-chain ledger, policy and original baseline match existing passive validators. SavedS+newA charged; offlineD only diagnostic, no budget reset.
- A second resume request for the same consumed input must fail before any fourth worker launch. Do not clear/copy/rename the claim or change the packet. This is an application authority test, not a new physical trial.
- Input source, installed package, explicit assets and protocol/driver files unchanged before/after; logs and request/outcome/failure/claim evidence retained. Failure or unverified results stay failed at the same thresholds.

This checks the new application adapter over the existing limited manufactured geometry/transport plus sourced water/dry-Cp study. It does not prove an actual sludge-brick material domain, complete firing, three-mechanism prediction accuracy, or a new continuous-versus-resumed numerical comparison.
