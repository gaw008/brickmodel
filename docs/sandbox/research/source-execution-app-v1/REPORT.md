# 来源执行应用接入

本阶段基线为4a8504d。新增应用入口将既有来源运行、普通段暂停与跨进程恢复接入固定子进程监督器；CLI与Python共用实现。它仍只运行当前受支持的来源试算，材料、训练数据和完整烧成资格均不因此改变。

## 实际完成的实现

`source-execute`保存原始请求、来源绑定、进程日志、运行结果及独立读取结果。准备、实际子进程和事后读取的耗时分开记录；取消/超时使用原有进程归属和回收路径。请求不能指定任意执行代码、改变恢复终点或重置计费。已使用的检查点可以查看，不能再次启动恢复。

`source-execution-inspect`及`source-checkpoint-inspect`区分记录有效、进程实际观察、运行结果、数值资格、材料资格及剩余恢复权限。完成轨迹按原保存的完成记录核验，未冒充暂停检查点，也未重新计算物理过程。共享图展开在构造和摘要前限制工作量，避免短小输入触发巨大重复展开。

## 审查与安装验证

两项独立代码审查通过。第二项Python审查针对最终terminal文件0a39f7a4…302ee9b，覆盖随后修正的共享图展开问题；它更新了第一项报告对该文件较早版本的审查结论。原始RED、错误测试假设及后续修正均保留。

- 首项代码审查143项通过12.22s；最终Python针对性47项通过8.29s。
- 最终非editable安装的196项相关测试通过10.79s，0失败/错误/跳过。首轮使用importlib测试导入方式导致既有测试间导入失败；改用append后完成，同一生产源码未变。
- 169包文件在源码、冻结清单及实际安装中逐字一致。测试工作目录为/private/tmp，无PYTHONPATH。

这些是软件与安装证据，不替代来源计算或科学预测准确性验证。

## 第一次实际运行：缺依赖失败

native01的worker9269在构造水来源时因隔离环境缺少iapws失败；原异常链保存ModuleNotFoundError。计数为1次HEOS构造开始、0次kernel/HEOS返回、0次初始能量、0次RHS及0次湿查询。worker退出1并被回收，driver1.740900375s，外层监督含清理12.010731209s。362个冻结输入未改变。暂停与恢复未启动，不能声称三进程完成。

已从原来源资产中的iapws1.5.5固定wheel离线安装缺失依赖，原水来源检查通过所有指定资产和运行时wheel字节核验，未构造EOS。模型169包文件未改变。新的native02在独立编号目录中按原数值门槛、参数和预算执行。

## 第二次实际运行：三进程完成

native02已终态0，外层监督135.341294084s，driver125.067110958s，517项输入前后未变。原510s总限额、180s普通段限额、97RHS/16wet等政策均保留。外层时间含原监督器清理宽限，不能全部解释为物理计算时间。

| 操作 | 实际worker PID | 状态 | 累计RHS / HEOS构造 / 初始能量 / 湿查询 |
|---|---|---|---|
| 父来源运行 | 10132 | completed，退出0、回收 | 32 / 4 / 3 / 8 |
| 普通段一接受步后暂停 | 10269 | paused，退出0、回收 | 40 / 8 / 3 / 8 |
| 新进程恢复至末端 | 10345 | completed，退出0、回收 | 54 / 12 / 3 / 8 |

父数值事件接受；暂停检查点在恢复前有效且可用。恢复完成3个接受步、22个观测、0拒步，精确时长为3/64秒，每步1/64秒；第一步、前八个观测及事件前缀与保存的暂停内容一致。累计账本及S+A计费沿用原链，未重置预算。驱动中的被动核验与后续独立保存审计应分别看待。

同一检查点再次恢复被明确拒绝：`source_execution_service_restore_already_claimed`，没有第四个worker PID、没有新的execution目录。这次拒绝的任务外壳状态为`supervisor_failed`，原值保留；检查点记录有效与恢复权限已消耗分别报告。

本次是应用入口的一条真实运行链，不是新的连续/恢复双路径比较，更不是全周期烧制。三个worker的material_qualified、training_eligible、full_firing_cycle及historical_study_resume_authorized均为false。

## 保存结果独审与归档

[保存结果审查](saved-review/SAVED_REVIEW.md)实际使用安装版被动读取器和完整私有来源资产，并以独立Fraction算术核对原精确时钟及账本。112项检查通过23.441s，0新EOS/物理积分/worker。32个原事件在76事件恢复日志中逐字保留；9个逐格步账、7行完整链余额符合原门槛，最大完整链能量残差3.32378e−11J、库存残差3.11063e−17mol。169源码/安装文件和517个运行输入一致；native01原203文件未改变。

原方案、两次执行、缺依赖修正、测试RED/GREEN及两项代码审查收录于[原字节证据包](EXECUTION_EVIDENCE.tar.gz)，成员长度和SHA见[归档清单](RAW_MANIFEST.json)。每个归档成员均已重开逐字核验；出版方原assets、原图和缓存不随包再分发。公开归档因缺这些原件，不应被报告成完整可恢复的私有运行目录。

## 适用边界

具体使用见[入口说明](IMPLEMENTATION.md)。当前完整运行需已登记的科学运行环境和原始来源资产；公开证据包排除出版方私有原件。冻结摘要证明输入一致，不证明物理正确或其他平台可恢复。

外层研究监督器仅包含其原进程组，不能宣称包含逃逸会话；应用监督器通过实际子进程句柄执行取消和回收。CPU/RSS无完整子进程报告时保持unknown。

原污泥完整参数、同材料反应热与产物、高温输运/烧结/冷却关系、三个机制现实预测资格、全周期及多代实验仍未完成。Goal第11节条件不变。

下一实际物理增量见[有限材料阻塞审计](material-next/BLOCKER_AUDIT.md)：作者湿Cp近似允许在模型误差unknown下研究；应推进同源湿态可积热力学与能量反馈，原始缺项不补写为观测，不能扩大解释成完整材料资格。
