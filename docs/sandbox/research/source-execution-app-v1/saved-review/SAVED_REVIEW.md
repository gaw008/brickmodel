# Native02 saved-result audit

**PASS.** The saved three-process application journey meets its original registered acceptance gates. This audit read the actual complete local results with the installed passive validators and independently checked the saved exact clocks, event counts, prefixes and conservation arithmetic. No new physical worker, EOS construction or integration was run.

Evidence: `audit_saved.py`, actual `AUDIT.log`, and `SAVED_REVIEW.json` (SHA-256 `0fb53f8491f0c619ee4f3f60851c697a6573d87c5f838cbfbf13960696f9a868`). The isolated installed Python audit exited 0 after **23.441 s** with **112 checks passed**. Live model constructors, evaluation methods, trajectory creation/restoration, integration and supervisor entry points were guarded against accidental execution; none was entered. The recorded source/package/native01/native02 evidence bytes were unchanged by the audit.

## Actual saved execution and bindings

| Phase | Saved worker PID | Exit / reaped | Status | Cumulative RHS | Cumulative HEOS |
|---|---:|---|---|---:|---:|
| Parent | 10132 | 0 / true | completed | 32 | 4 |
| Pause | 10269 | 0 / true | paused | 40 | 8 |
| Resume | 10345 | 0 / true | completed | 54 | 12 |

These are saved exit/reap observations, not assertions about current PID liveness. There was no cancellation, timeout or worker failure publication in the three successful jobs. The parent numerical event is accepted. The driver took **125.067 s** and the outer supervisor **135.341 s**, retaining its original process-group containment limitation.

For every job, the driver request, supervised request and worker request are byte-identical; their SHA-256 matches `INPUT_BINDING.json` and `OUTCOME.json`. Operations, fixed isolated worker argv, source/output/cancel paths, canonical/raw case identities, runtime-before/runtime-after/admitted runtime and all qualification flags match. The actual parent → packet → resumed trajectory path chain is intact. Shared budgets remain null and external branch debits remain zero.

All **517** outer-supervisor input hashes still match their original recorded values and actual current bytes. All **169** source and installed package files match the frozen package. The current installed runtime includes iapws 1.5.5. The native01 failure's complete **203-file** set is byte-for-byte unchanged from the pre-native02 audit snapshot; its failed state was not overwritten or relabelled.

## Prefix, clock and cumulative ledger checks

The complete local packet passed the actual installed checkpoint reader with its private assets present; no admission seam or fabricated asset was used. Its original parent digest matches the genuine parent study. The installed operation readers validated the parent run correspondence, paused packet/request/journal correspondence, and completed terminal observation/balance/lineage scope.

The original **32 event files** remain byte-identical within the **76-event** resumed journal. The original first accepted step and eight callback observations match all saved fields. Resume adds exactly **14 RHS calls**. Independently counting original starts/returns from the resumed journal plus the original parent baseline reproduces **54 RHS, 12 HEOS constructions, 3 initial-energy evaluations and 8 wet requests**, without double-counting the copied prefix.

The result has **3 accepted steps, 22 observations and no rejected trial**. Its start is the fresh parent's exact candidate-1 endpoint; its end is exactly that Fraction plus **3/64 s**. Every step is exactly **1/64 s**. Full numerator/denominator strings are retained in `SAVED_REVIEW.json`.

Independent Fraction arithmetic checked all **9 per-cell step energy ledgers**, whole-step energy conservation, constant saved inventories, zero outer heat/work inputs and nonzero internal conduction. The installed terminal reader recomputed the complete original balance chain; the audit separately checked all **7 saved full-chain balance rows** and their per-cell residuals against the unchanged original tolerances.

| Quantity | Maximum absolute residual | Original tolerance |
|---|---:|---:|
| Per-cell step energy | 1.23005e-11 J | 0.001 J |
| Whole-step energy | 1.04592e-11 J | 0.001 J |
| Full-chain energy | 3.32378e-11 J | 0.001 J |
| Full-chain inventory | 3.11063e-17 mol | 1e-7 mol |

## One-use authority and retained time charge

The current packet is valid and **consumed**. Its claim contains only the original `started.json` and `restored.json`, both pointing to the actual resumed trajectory. Claim counters match the reconstructed suffix before any new RHS call. The saved capability observations retain available-before / unavailable-after; those are historical observations, while this audit independently confirms current unavailability.

The original saved segment charge **18.538305542 s** is retained below the restored charge **22.974842709 s** and terminal segment charge **42.509012917 s**. The terminal cumulative source charge is **90.192723917 s**, satisfying the original parent-plus-segment floor and source lifetime bound. This checks retained saved debit relationships; it does not reconstruct every unrecorded timer interval or charge offline time.

Repeated restore was refused with the exact reason `source_execution_service_restore_already_claimed`: no PID, return code or execution directory was created. No fourth physical worker was launched. No claim was cleared, copied for retry or rewritten.

The result remains the registered limited application/physics study with manufactured geometry/transport and sourced water/dry sensible heat. All material, training and full-firing-cycle qualification flags remain false. The completed trajectory reader's scope does not include a new controller arithmetic replay, a continuous-versus-resumed comparison, source authentication or full material validation.

## Review Summary

| Severity | Count | Status |
|----------|-------|--------|
| CRITICAL | 0 | pass |
| HIGH | 0 | pass |
| MEDIUM | 0 | pass |
| LOW | 0 | pass |

Verdict: APPROVE the native02 saved application acceptance within its stated scope. The original native01 failure remains preserved.
