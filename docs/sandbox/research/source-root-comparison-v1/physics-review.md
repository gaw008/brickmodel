# Independent clock/budget and joined-ledger checks

37 explicit checks passed in0.07520 s, source_root_comparison.py SHA67833a38bb5956bacb08ebfd2ce367ac5163db8ce2413cb53db5c3a19fad8486 unchanged. Actual code/log/result: check_clock_balance.py, CLOCK_BALANCE01.log, CLOCK_BALANCE_RESULT.json. No EOS, native run or repeated actual-source fixture execution.

Independent Fraction cases verify exact absolute roots with origin2^100+1/3 and distinct local starts, including zero distance for identical absolute point roots. A direct root distance twice the original tolerance correctly fails; there is no inappropriate1/3 factor. For budgets2/3/8/32, independent bisection reconstructs refined brackets and exact consumed remaining rounds. Prior ordering and positive-lower work are charged before comparison refinement. Exhausted small budgets retain wide bounds and false gates. The interval-distance formulas match exactly; tampered records are rejected, while an actually unchanged zero-round record is correctly not treated as tampered.

The original time_absolute_s/4 is a bracket-resolution target only, not a relaxed gate. The actual gate remains conservative absolute interval separation<=original time tolerance. Mathematically, exhaustion before the target need not force failure if the retained conservative distance already proves the gate; the tested wide counterexamples cannot prove it and do fail. This distinction avoids treating a computational target as a new physical tolerance.

Two synthetic trusted-input ledger chains isolate _joined_reference_residuals (they are explicitly not actual SourcePrefixTrial objects). Each individual segment passes its original local cumulative tolerance; joining them from the common origin correctly rejects cumulative energy and cumulative amount residuals. Energy example uses ignored increments2^-48 J atU100 with tolerance3*2^-49; amount example uses opposite liquid/vapor increments2^-54 mol at inventories1 with tolerance3*2^-55. Their summed residual exceeds the original tolerance, exposing any accidental budget reset between segments. This validates the helper arithmetic, not source-phase constitutive behavior.

Static path review confirms a shifted trial starts from the actual earlier reference endpoint, common endpoints lie before both refined lower bounds, original/source bindings remain checked, and common N/U/T/reported-P comparison uses final successful reference observations. Conditional full-pressure bounds remain separately labeled; positive common state and root-surrogate agreement do not authorize physical event time/state, dry writeback or material qualification.

The provider's38-call actual manufactured-host run was not repeated or counted as this independent test. Subsequent production changes need a narrow delta review before attributing these results to new bytes.
