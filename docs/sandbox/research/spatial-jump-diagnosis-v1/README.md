# 内部初始阶跃的保存数据诊断

证据包包含原六组记录的标准库算术审计、实际输出、输入hash、诊断及独立代码审查。输入仍是相邻 free-spatial-study-v1/raw-evidence.zip 的原始六组数据。脚本保留当时仓库绝对路径，搬迁后需显式修改输入位置，不改变原始数据。归档检查CRC及成员唯一性通过。

这是几何、初始通量和短时量级一致性检查，气体焓为保存数据依赖；不是空间收敛或独立物性验证。原空间不收敛结论保持。
