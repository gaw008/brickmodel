# Affine terminal event integration

Baseline e9db77b. The opt-in affine_midpoint method uses one conservative midpoint predictor and a fresh observation, then identical affine quadrature for all species/face/energy/mechanical-power fields. An immutable exact-rational quadratic certificate verifies every liquid ledger term, the greatest downward binary64 root endpoint and a bounded clock gap. Full interval inventory extrema, competing wet phases, signed evaporation positive-part integral and original correction budgets are checked. Source mutation/cancellation/failure cannot commit a speculative event. Default Euler stays available; source/numerical domains are explicit.

59 new checks comprise24clock,14integration/oracles and21guard tests. Together with40previous relevant checks,99passed34.213s. Initial test failures are preserved: absent API5fails; globalA error inherited from ordinary integration2fails (localterminalerror~4e-15); smaller .005 step exposed existing unresolved program-knot clock4fails; nonlinear independentoracle initially failed1e-9 time bound, then overstrict1e-16 amount policy rejected unrepresentable increments; valid tighter1e-14/1e-13 ordinary tolerances passed unchanged nonlinearoracle. These test-only settings do not change originalv5 native physical or numerical settings. Source/contract mutation at last switch/common comparison was found in candidate review and explicitly fixed/tested for cached/uncached/no-nested modes.

V5 retains v4 scripts except the single terminal_method flag. Freshcallback2.014814s passed. Both original-budget runs completed: inner87.845694/85.501777s,260/303evaluations,31/36trialpanels,14/16acceptedsteps. Each passes two terminal refinements plus independent half-control approach and commits one depletion at0.5002854010435919s, then continues with active A->B reaction to0.5078125s. Independent Fraction audit checks every prefix, all affine fields/components, root certificate, correction budgets,182input hashes perrun and actualinstalled/source identities. The precise comparison results and allraw traces are archived.

Both fullcap comparison runs share the same actual wet prefix because its inventory-fraction constraint dominates the maximumstep. The cap change refines dry continuation; zero event-time difference is not proof of event convergence order. Separate within-run independent wet approaches have different actualgrids and pass. No spatial convergence, raw-sludge material validation, free sintering, independent wet-temperature oracle or fullGoal completion is claimed.

The known .005 program-node failure remains open. The next spatial coupled task is scoped separately. Full installed regression outcome will be appended only after actual terminal and43installed-module before/after verification. Original raw failures remain byte-exact in ZIP; readable XML strips trailing whitespace perline and adds a finalnewline for Git hygiene.

Final full installed result: session8135 exit0,1279passed/zero failures/errors/skips,XML585.898s;43actualmodules matchsource before/after. All test/EOS handles terminal. Archive raw evidence and final audit alongside all retained failures.
