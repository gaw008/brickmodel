# Preregistered affine terminal integration
Baseline e9db77b; fullGoal unchanged. New opt-in affine_midpoint terminal method, default Euler unchanged. Midpoint Euler predictor uses actual representable midpoint time, one fresh source-qualified observation; identical affine integral weights for every face/reaction/energy/component. Exact Fraction quadratic clock and full interval inventory nonnegativity (strict positive competing wet liquid). Same phase writeback budgets, independent approach, successive comparison gates, cancellation and resource limits. Gross positive phase evaporation integrates positive part of affine signed rate exactly and rounds downward. Explicit failure on invalid midpoint/root/domain; no fallback or clipping.
Before native replay: independent analytic constant/linear sink roots and A->B solution; every accepted prefix ledger; control cost counted exactly with same gate versus Euler; midpoint source/native/cancel rollback; nonmonotone/competing inventory rejected; original legacy tests. Separate clock agent owns root certificate and its independent tests. Preserve baseline failures and intermediate artifacts. No native replay until reviewed controls pass. New wet experiment retains original physical settings and120/150s500panel limits, changing only declared terminal method.

Initial integration29tests:27pass2fail independent globalA1e-10 bound. Saved diagnosis shows startingordinary error1.40674e-10/1.28060e-10 while localterminalerror3.9968e-15. Preregister smaller ordinarycap .005 vs.01 for BOTH comparedmethods, sameinitialstep .005, to check fulltrajectory independentoracle at same1e-10 gate; no acceptance relaxation. Firsttestbytes/XML retained.

Smaller nominal .005 exposed existing ordinary-segment knot unresolvable_stage_time beforeaffineterminal for bothmethods (65pass4fail retained). It remains a separate unresolved clockcase. Independent-oracle control uses original .01 segment endpoints with stricter relative_tolerance1e-11 for BOTHmethods, unchanged1e-10 globaloracle and all eventgates. This addresses ordinary solutionerror without hiding the saved .005 failure; fullGoal stillrequiresresolve remainingknotcase.

Nonlinear time-quadratic sink independent cubic-root oracle reveals fulltrajectorytimeerror1.2141e-8 beyond1e-9 despite event comparisons pass; initial XML retained (5pass1fail). For nonlinearoracle only prereg stricter ordinary amountabs1e-16 and rel1e-13 toresolve liquid accumulation error; eventandexternaloracle gates unchanged. This does not alter v5 physical/integrationpolicy.

The1e-16 ordinaryamount setting isbelow representable2mol inventoryincrement and correctlyfailed unresolvable_amount_increment (5pass1fail retained). Use1e-14 absolute withsame1e-13relative for nextbounded nonlineartest; thisis tighterthan original1e-12, all external1e-9/eventgates remainfixed. No solverguard relaxed.
