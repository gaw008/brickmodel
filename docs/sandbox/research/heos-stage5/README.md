# Explicit optional HYBRID interface and consumer identity

The candidate introduces verified HEOS real-fluid conversion to existing WaterState/WaterResponse/SaturationPair public units and source-verified Python ideal-vapor methods. WaterReference remains physical reference only. Optional kw-only implementation field is added to caloric/fluid states; default None intentionally changes dataclass/asdict schema. Default provider canonical matches the archived bound-old record. HEOS descriptor/envelope includes all outer metadata, source IDs and definition in its digest, and hashes wrapper/descriptor/ideal-adapter/ideal-dispatch plus the pinned HEOS kernel and runtime.

Explicit factory accepts default python or heos with the reviewed exact manifest. The approved manifest bytes hash is fixed; kernel's actually read runtime manifest must equal the verified parsed bytes, preventing a caller-signed alternative or path-read swap from silently inheriting fixed labels. Missing runtime/assets raise typed source errors. Normal HEOS availability is still optional, not an automatic dependency or fallback.

Exact supported-type predicate is used at existing phase/rigid/canonical/chemical admission sites. State identity checks in chemical, ideal and default water responses include implementation. Alternate source assets include canonical implementation digest, which flows through existing source-assets comparisons and canonical target identities; source IDs aggregate into mechanical/storage/transport outputs. Transport provider name/version is actual selected implementation. Joined low ideal and chemical constructors explicitly forward selection. Python ideal coefficient access remains explicitly hybrid, not relabelled HEOS.

History/evidence:
- Initial patch script asserted on an omitted loader docstring before numerical execution; patch-initial.py and corrected patch.py retained. No baseline reset or scientific gate change.
- Initial212 default tests passed1.39s. Later298 related/default/phase/rigid/solid-storage tests passed14.68s, before final HEOS-only manifest admission pin.
- Initial descriptor collision across different outer metadata was independently reproduced; full canonical envelope fixed it. Original reviewer finding retained.
- smoke-attempt01 exit0/1.2176s: source/state/chemical identities, mixed backend and stripped implementation rejection, alternate/default canonical separation and default archived canonical equality.
- inverse-attempt01 exit0/3.7948s: original manufactured closed-storage fixture, independent Python EOS/brent pressure/energy reference; unchanged1e-5J/1e-4K/.2Pa gates, inventory and implementation source propagation. Not a whole wet trajectory or material experiment.
- final-attempt01 exit0/4.0238s repeats both after ideal-dispatch SHA and typed source error hardening; default response rejects nondefault state identity. Source/results before this revision retained.
- pinned-attempt01 exit0/3.9486s repeats both after fixed manifest and actual-kernel-runtime equality guard. Final smoke/inverse files bind final source. Reviewer independently tested untrusted manifest and swapped runtime refusal without EOS. Old source/results retained before-manifest-pin.

All actual processes used the reviewed supervisor, unique attempt directories,30s cap and unchanged-input records. Test scripts write their own result JSON; combined success requires both outputs and supervisor exit0. Some failure scripts only persist stderr if construction fails. Review does not claim independent reconstruction of unsaved inverse reference operands, full-host equivalence, full-domain coverage or speedup. Consumer source compatibility and software identity are distinct from scientific material applicability.

Production application uses exact reviewed candidate files. Applied-install validation is recorded separately under docs/sandbox/research/installed-sandbox-heos-interface-*. No claim of passing a still-running suite is made here.
