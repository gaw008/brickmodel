# Stage5 independent source review

Initial disposition: WARNING pending descriptor identity fix and actual alternate-provider/storage evidence. This reviews isolated package versus current repository source; no EOS, tests, production edits or installations were run. Parsed every candidate Python file with AST and performed a stdlib-only descriptor construction check.

[MEDIUM] Descriptor digest omits visible identity fields.
File: sludge_sandbox/water_implementation.py, WaterImplementation.sha256.
The SHA currently covers only canonical_descriptor, while schema/provider_id/provider_version/source_ids are independent public constructor fields. Reproduced two unequal descriptors with provider/source metadata p/1/x and q/2/y but identical '{}' payload yielding the same SHA. Current factory uses fixed values, so ordinary factory outputs do not yet collide, but this is insufficient for the advertised general descriptor identity. Include all identity fields in a canonical envelope digest or enforce exact consistency with its payload. Validate provider fields and each unique source ID as nonempty strings. This matters because the digest is inserted into source_asset_sha256 and propagated into existing canonical/compatibility contracts.

[LOW] Four added imports precede module docstrings in deforming_solid_storage, phase_storage, rigid_water_gas and water_chemical_potential, changing __doc__ to None. Move them into import blocks. No numerical effect.

Positive source findings:

- Copied _heos_kernel.py is byte-identical to final stage4 kernel. Wrapper maps native h/u/cp/cv/rho/s and response derivatives directly into exact WaterState/WaterResponse types; offset remains in original state properties. No numerical gate relaxation or hidden native energy translation found.
- HYBRID payload includes wrapper source SHA and verified kernel descriptor, pure scientific reference and limits, and explicitly declares Python ideal Helmholtz. _model intentionally points to original verified Python ideal internals for chemical entropy/Joined coefficient proofs. This preserves the real scientific source of the ideal branch.
- Optional implementation is kw-only on the caloric base and inherited by WaterState. Defaults remain None; old positional construction is preserved. Default asdict/field schema changes intentionally, even though WaterReference and all old default equations stay unchanged.
- Chemical liquid-state and IdealWaterVapor caloric checks now compare implementation with their provider. Source assets include a reserved descriptor-digest key. All identified source aggregation sites now use water.source_ids, including solid_fluid_storage and rigid_fluid_heat. Transport alternate provider ID/version are explicit. Existing reaction, Joined and phase-transfer asset comparisons carry the alternate digest.
- Canonical provider branch now uses exact-class helper and retains old default output shape. Exact old/new classes accepted; arbitrary duck providers and subclasses return false. Default loader remains Python; unknown selectors/missing HEOS manifest fail, Python+HEOS manifest is rejected. HEOS imports are lazy and CoolProp itself is imported only during kernel construction.
- Wrapper is a frozen dataclass with sealed kernel; _guard checks reference, limits, descriptor asset digest and kernel identity payload. Ordinary freezing is not a security boundary against object.__setattr__/native monkeypatches. Immutable descriptor construction should not itself be called source verification; actual file/config/fluid/anchor verification occurs in kernel construction.

Pending evidence: alternate constructor and reference/descriptor binding, default canonical fixture equality, old/new mismatch rejection, new coupled storage inverse, and actual installed/import-path test provenance. Root reports 212 default tests passing, but that summary alone is not independent review evidence here. Initial patch assertion correction was a construction-script issue, not a scientific run; preserve patch-initial.py as stated. No host admission follows from this review yet.

## Follow-up: corrected descriptor and completed isolated evidence

The digest collision and docstring issues above are fixed in the reviewed follow-up source. SHA now covers the full canonical envelope including schema/provider/version/source IDs; source IDs are validated as a nonempty tuple of unique nonempty strings. Repeated the original stdlib construction check and confirmed unequal outer identities now have different digests. Wrapper identity additionally binds its own source, water_properties adapter and descriptor code, plus Python and iapws/NumPy/SciPy versions; full source-asset equality is checked. Chemical construction explicitly forwards backend/manifest. One remaining source-closure recommendation has been sent: also bind `_water_python_backend.py`, which supplies the ideal adapter's actual native dispatch. Root is addressing final error normalization and this closure before final binding.

Read actual `default-tests.xml`: 212 tests, zero failures/errors/skips, 1.389 s. This verifies the retained XML count, not every import path independently.

`smoke-attempt01` completed exit0 in 1.217607917 s. All recorded before/after hashes and current files matched at review time. Reviewed assertions for repeated implementation equality, reference equality, exact response/state/descriptor object binding, differing default/HEOS canonical identities, source metadata, altered descriptor version digest inequality, stripped implementation rejection and cross-provider chemical mismatch rejection. Independently compared saved default canonical to archived `water-python-seam/bound-old.json` and confirmed exact equality; HEOS canonical differs. This is provider canonical equality, not universal default asdict compatibility.

`inverse-attempt01` completed exit0 in 3.794825875 s with all before/after/current input hashes agreeing. The script constructs a declared manufactured fixed-volume storage case, computes a separate Python-EOS pressure with brentq, forms the target from original water molar energy and cp30 fixture gas, then solves using HEOS storage. Original gates remain 1e-5 J energy, 1e-4 K and 0.2 Pa, with a bracket containing 300 K. Independently checked saved T=299.99999999999477 K, P=390657.9282261737 Pa against original P=390657.9282248903 Pa, energy residual -5.820766091346741e-10 J, temperature bound 1.2173689974029923e-7 K and bracket. Recomputed descriptor digest and matched the propagated mechanical source asset. Original EOS target operands and inventory outputs are not fully saved, so their construction/equality are verified script/runtime assertions, not an independent saved-operand reconstruction.

This is positive evidence for one manufactured closed-storage inverse. It does not establish a wet trajectory, phase depletion, all coupled consumers or arbitrary real material behavior. Final source binding must follow any remaining wrapper/helper identity edits; original completed attempts must retain their source snapshots.

## Final frozen candidate review

Disposition: **approve the reviewed source for an explicit optional HYBRID provider with the documented bounded qualification**; no remaining critical/high code issue identified. This does not assert general host/trajectory/material qualification. Root's currently running consumer regression suite and repository application verification remain separate requirements before delivery.

Reviewed final diff against `before-final/sludge_sandbox`: it adds the missing Python dispatch SHA, normalizes missing dependency/file/JSON-key failures during kernel construction to WaterSourceError, and makes default Python response reject any non-None state implementation. The numerical kernel and physical gates are unchanged. Unknown manifest structural types can still produce TypeError rather than this normalized error, but cannot yield a successful fallback. Explicit construction requires the supplied platform-specific verified manifest; missing central source registration or packaging must not be represented as an available bundled backend.

Actual `final-attempt01` completed exit0 in 4.023789041 s. Its driver sequentially runs final smoke and final inverse, so success requires both. All 64 recorded inputs agree before/after and independently with current bytes. Recomputed final descriptor digest from full envelope, matched its propagated asset key, and verified wrapper, ideal adapter, dispatch and descriptor source hashes against actual files. Final smoke and inverse descriptors agree. Saved default canonical still equals the historical bound-old fixture; final inverse passes the original T/P/energy/bracket gates. All candidate modules parse successfully. Reviewer ran no EOS/tests and made no source edits.

Concrete delivery boundaries: keep HEOS opt-in and Python default; archive the exact manifest and CoolProp source/license provenance that resolve its source ID; specify the optional runtime installation rather than suggesting the existing normal environment supplies it. A broad coupled-host admission would additionally require that host's actual identity/response/integration tests, beyond this one fixed-volume manufactured inverse. These are limits on claims and availability, not evidence of a numerical failure in this frozen candidate.

Final SHA-256:

- `sludge_sandbox/water_heos.py`: `3979bd4edd66def8c51b827d62c53fe5caae9541a1b48e605e48fa0aeb31ec99`
- `sludge_sandbox/water_properties.py`: `b374ea87873815a4d8959823a3323d29ce70d6c5ce0b706ba1a81f2b9437d8b6`
- `sludge_sandbox/water_implementation.py`: `b759a97c111aeb8588fc6a22b4088a83b7f766debfbf2a7761f7329b690b1a7c`
- `smoke-result-final.json`: `7916a7b5eb6a19960b94286672ffe04a011c99a70c2eb0c1bc9d431eaf192290`
- `inverse-result-final.json`: `ce73546435330432ec4e7b34dc83d96b4285037c6f22913d36578540c172b859`

## Fixed reviewed manifest admission

The subsequent manifest guard closes an additional admission gap: a caller may no longer self-sign another matching installation while retaining this wrapper's fixed provider label. The constructor now reads bytes once and requires SHA-256 `a2d309e79615b283477d8368fe03477704ed5ba468cc3bf11350773ac716783d` before any kernel construction. It parses those verified bytes, then compares the constructed kernel's actual recorded runtime manifest to that value. This post-construction comparison also rejects a path-content change between the wrapper read and kernel read. Neither check adjusts the numerical kernel or scientific tolerances.

Independently executed only the constructor's first try-block extracted by AST with a stub kernel (no EOS or provider imports): a fake `{}` file was rejected before the kernel stub was called; the reviewed real manifest combined with a stub returning a different runtime was rejected by `heos_manifest_changed_during_load`. Fake evidence is `review-untrusted-manifest.json`. The guard source reviewed has SHA `fa78d8d360890531405d63835600b7d30843c6056440c3c5c0d63b67ec8071da`. This admits exactly the reviewed Mac build/manifest, not portable arbitrary HEOS installations. Ordinary tamper-resistant filesystem assumptions still apply to loaded Python/native code, as in the existing source-verification model.

Also read final-default-tests.xml: 298 tests, zero failures/errors/skips, 14.682 s. This suite predates the last manifest-only guard, so final guard/source result binding is recorded separately when completed.

Final pinned-attempt01 completed exit0 in 3.948618084 s. Independently verified every recorded input-before/after/current hash, actual wrapper `fa78d8...`, unchanged kernel `20777eb...`, manifest, smoke/inverse descriptor agreement and full-envelope digest `cfb538c223ce8fb7f463f7337cd5183ca0a753318e966058986a0fd980f6b19e`. Original inverse T/P/energy/error-bound gates remain passing in the new saved result. `smoke-result-pinned.json` SHA is `ce0072ba8765a6d0fa922de8e7d229b94f836f2dc10fd5c822f2679496da11bc`; `inverse-result-pinned.json` SHA is `13185fe134f5dd4ea8332c4a27e0d459c5b4fa12ad7de1bbafcf4a8d98c49040`.

Final application recommendation: approve copying these exact reviewed sources as an explicit optional provider, together with the unchanged reviewed manifest and truthful source/license/runtime documentation. Python remains default. Applied-package regression checks remain necessary; neither this recommendation nor the 298 targeted tests is a completed full-suite or general coupled-model admission. No remaining blocking source issue was found in the final pinned delta.
