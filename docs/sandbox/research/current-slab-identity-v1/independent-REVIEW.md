# Independent exact CurrentSlab identity adapter review

Read next-IDENTITY_BLOCKER.md, actual CurrentSlab/ReferenceSlab definitions, complete candidate/source-before diff and13candidate tests. Source change is2imports plus an exacttype CurrentSlab branch before existing generic dataclass traversal. Source candidate SHA256794d4f0f6465335f7575e12a61268e513972519110491c4e1b4a635b1ccee598; source-beforec51278bae2c1f4043c804cf198c1180d81f64b147f149c5c86080004ada6a3fd. All frozen candidate files checked; repository still unchanged at review time.

Exact type gate and complete7field tuple match prevent unnoticed schema additions. Width array defines positiveN; all fields require exact ndarray, nativefloat64 dtype,1dimension, finite values and cellN/faceN+1 dimensions. Result binds module/type, each fieldname/order, explicit dtype tag, shape and every binary64 value as float.hex; signedzero and subnormal values are retained. No generic ndarray branch, repr fallback or omitted-cache rule added. Existing mappings/sequences/dataclasses still recurse through private fields, so motion._reference_state is included and runtime mutation detection remains. This is identity encoding, not a physical geometry validator or new material/codec admission.

Imported geometry module depends on NumPy/dataclasses/math only and has no storage import; no added import cycle. Previous canonical branches are byteunchanged. Source water-provider specialcase is unchanged. Copy/strided array layout is intentionally not physical identity when values/shape/dtype match; arbitrary arrays elsewhere remain rejected.

Actual reviewer execution:17passed0.38s, consisting13submitted candidate tests plus4independent tests. Independent struct.iter_unpack binary64 decoding reconstructs all7field encodings and JSON digest, checks subnormal/negativezero/maxfinite, rejects additional complex/bool/integer/bigendian/subclass arrays, hidden arbitraryarray cache, empty and wrongface shapes. Submitted tests demonstrate original movinghalfcell regression via isolated candidate canonical monkeypatch, perfield mutation, private referencecache mutation rejection, original noarray operator/data/water canonical and digest parity. Log retained. No repository patch, native liquid EOS, numerical model change or extra grid run performed.

No correctness/security issue found in the reviewed candidate. This extends the shared canonicalizer only for the precise supported CurrentSlab record, rather than the earlier suggestion of a wrapper-only adapter; all previously accepted noarray graphs tested remain identical and unsupported arbitraryarray rejection is preserved. Final applied-source/installed verification remains necessary: temporary monkeypatch tests are not evidence that the repository or installed package has already been repaired. Formal standalone repository test file still awaited for final review.

## Formal repository test review

Read standalone test_current_slab_identity.py SHA2565c9a826e8aea848d315824c3a7a8ab74510b84ea6c566310d9eb97742a8d977f and its freeze; ASTparse and hash match. All12 parametrized cases import real production _canonical/_digest with no candidate module, absolute tmp path, sys.path injection or canonical monkeypatch. Literal3oldgraph golden digests exactly match prechange saved captures. Privatecache mutation still checks real runtime refusal. Original actual movinghalfcell remains its existing separate regression (combined13case scope). No test coverage deletion or weakening found. Ready for parent application and installed-source tests; this reviewer has not claimed formal test execution before application.
