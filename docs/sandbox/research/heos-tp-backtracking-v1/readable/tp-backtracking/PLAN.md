# TP bounded backtracking implementation and checks

Numerical prerequisite is the actual six-fraction probe: five shortened steps pass the unchanged dynamic residual gate and full snapshots at the preserved295K/53692.54782795906Pa point. Candidate only; no production modification before review/control checks. Copy source9ab4b65, change only TP Newton control:8 accepted states,6 fixed trial fractions per update, at most43 actual density evaluations, original gate/branch/phase/slope/full-step bound/source transaction/final snapshot retained; no average thermodynamic values or target/bracket changes. Healthy full-step native work should remain unchanged. Track every actual trial separately.

Root independent no-EOS test scope is copied from ROOT_NEXT_TEST_SCOPE.md. Scripted responses are control tests, not an EOS, and retain all event/model acceptance gates. First test harness used targetpressure .01 against liquid saturationstub .001, inadvertently within original .01Pa ambiguity margin:14failed1pass, raw bytes/XML retained. Correct this artificial test pressure to .1Pa, leaving all residual sequences, thresholds and assertions unchanged, before rerunning baseline. This corrects fixture reachability, not a physical prediction or gate. The expected baseline lacks backtracking/diagnostic fields and must fail only new behavior tests; invalid source/native/branch and existing limits should pass.

## Review extensions preregistered before execution

The15new+11oldcontrols pass26; reviews identified two distinct coverage/diagnostic gaps. Native update/derivative exceptions must retain attempted density/fraction before failing (no change to fatal behavior). Add tests that a liquid fixture with r=1e16 has exp(step)==1 and all7 actual densities/merits identical, rejecting stagnation; a wrong phase on first trial is immediately fatal; a vapor control seed1e-320 with target1e-310 exercises original rho*1e-7 underflow tozero with residual0 (passes) and1e-320 (fails after6 trials), without treating this script as water physics. These are numerical branch tests only; original physical/gate inputs remain unchanged. Preserve candidate e2df before the diagnostic logging fix.

## Native 3x3 regression grid (before execution)

Use original source-qualified public HEOS provider, unchanged dynamic pressure gate/full Table-3 checks. Nine liquid cases are T=(294.999,295,295.001)K crossed with P=(53692.54782795906-.01,53692.54782795906,53692.54782795906+.01)Pa. These are numerical neighborhood probes, not material experiments or fitted new parameters. Public states must preserve exact requested T/P, density must be the last actually evaluated density, and abs(native_p-target_p)<=min(1e-4,rho*1e-7). One module-scoped original constructor; no second oracle EOS. First run oldsource under30s external cap and retain failures; then same tests/cases under reviewed rebound candidate. Do not adapt this grid based on output. Existing broader sandbox tests follow; the unchanged wet coupled replay still uses original120s internal/150s external/500panel and all earlier gates.
