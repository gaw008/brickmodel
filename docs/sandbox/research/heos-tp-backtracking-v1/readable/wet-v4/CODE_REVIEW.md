# V4 frozen experiment review

Read PLAN and supervisor, verified all five scripts against reviewed v3 bytes, and inspected callback/depletion/comparison ordering and identity checks. No provider imports, tests, EOS, install or source changes performed.

All five prepared_script_sha256 values match actual scripts, each byte-identical to v3. All 42 repository sandbox Python modules currently match installed counterparts. PLAN's pressure-bound/depletion-kernel/HEOS-kernel/manifest hashes match actual current files: TP kernel c1ecb59c702382c78891f4fcf6b6ac5eb8c68cc57869b1e261ec8a3a4a796b12 and approved manifest 5f9e39bf1d3376b931caaf8fbda478b482cafe4c8b57a490860ac6ed080bf6db. The five scripts retain prior reviewed fixture, active reaction/water transfer, nonzero interface and energy/source binding; physical parameters and integration/event/resource gates are unchanged.

No callback/depletion/comparison result file exists in v4 at this review. HERE resolves from each copied script's own directory, so fresh callback output and later reads target v4. PLAN requires callback first, terminal status/identity inspection, then depletion0; depletion1 and comparison require first-run acceptance. Depletion requires passed local callback and exact newly built initial state equality, including energy identity. Supervisor snapshots the local callback result for depletion. The script alone does not establish chronology or revalidate every old callback module identity; freshness is supplied by the reviewed empty output directory, fresh callback execution and root's mandated terminal/source inspection. This is adequate for the explicitly frozen single-executor experiment, and must not be generalized into an unattended stale-result-safe workflow.

The old callback attempt label and manufactured fixture labels remain as exact prior bytes and PLAN explicitly identifies them as such; v4 PLAN identity/attempt directory determines the new experiment. Original v3 failure evidence stays in its original directory. The numerical change is disclosed and source rebound; no claim of unchanged implementation identity is made.

Supervisor keeps callback 30 s, integration 150 s external/120 s internal, installed interpreter, before/after input hashes and complete-plus-zero-child success. No loop or automatic retry is present. Full returned ledgers are saved before completion assertions; final failure artifacts and compare-input hashes/error records remain intact. Known artifact write behavior is unchanged. This approval assumes exactly the prescribed ordered invocations with no live concurrent native process and no overwrite/retry of prior attempts.

## Review Summary

| Severity | Count | Status |
|----------|-------|--------|
| CRITICAL | 0 | pass |
| HIGH | 0 | pass |
| MEDIUM | 0 | pass |
| LOW | 0 | pass |

Verdict: APPROVE frozen v4 preparation and ordered execution contracts after root verifies the terminal native prerequisite. No v4 runtime outcome or wet-model acceptance is claimed.
