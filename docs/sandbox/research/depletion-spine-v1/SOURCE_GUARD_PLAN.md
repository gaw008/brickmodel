# Preregistered no-EOS spine source/contract fault checks

New bounded test file `test_spine_source_guards.py` imports the isolated candidate and existing independent fixture definitions. No production water provider is instantiated; no source hash framework or fake EOS is introduced. This plan precedes any execution. Root owns execution; this worker will only parse test syntax.

Fixture and unchanged numerical gates: existing `configured(reuse=True)` and `oracle(b=0)` from `test_independent_spine.py`, liquid1e-4mol, spectator A2mol, totalenergy600J, sink0.001mol/s, A->B k0.1/s, wet/dry work0.3/2W, interval0..0.2s. Existing integration/event/roundoff gates retained exactly. Counting actual calls of the existing `with_depleted_cells` marks trial-terminal transitions, not RK stages or guessed call counts. Fault injection on the third trial transition occurs after ordinary wet reuse in later terminal refinements. Returned actual reuse counters must confirm positive reuse; a missed injection or early failure cannot satisfy the test.

1. Independently change the original adapter's `source_ids` or `deterministic_contract` at the third trial switch, AFTER obtaining its copied depleted operator. This intentionally violates the frozen deterministic run contract. A stale copied trial must not be committed merely because its own old descriptor still matches. Require structured failed status, a binding-related reason, positive reuse, no events/corrections/roundoff totals, and only positive-liquid globally committed states. This checks the root binding again before final acceptance, not just before retrieving cached edges.
2. Trigger a `WaterSourceError('manufactured_fresh_terminal_source_guard_failed')` in the newly executed terminal observation after the third switch, independent of cached wet records. Require the exact structured reason, positive reuse, no committed speculative event or correction and unchanged globally active interface. The deliberate callback failure is a guard propagation test, not an assertion of native water validation.
3. Two identical closed-cell liquid inventories1e-4mol with equal sink0.001mol/s and zero faces produce simultaneous exact event time0.1s. With nested/reuse opt-in, require the existing `simultaneous_events_not_separated` unsupported result before any step/event, unchanged initial state and modes. No symmetry-breaking perturbation or relaxed separation gate.

All actual phase costs must still sum to recorded evaluation/panel/rejection totals. These tests do not claim that arbitrary mutation or stateful Python callbacks are valid deterministic model providers; they exercise fail-closed handling of explicit contract violations.

## Strengthened final-acceptance fault scheduling

The earlier third-switch checks passed but do not prove the final precommit boundary: the reacting spectator may require more terminal levels after the third transition. The descriptor tests now first execute an unchanged deterministic baseline solely to count actual trial terminal switches and confirm its final refinement is `independent_approach_pass`. They then independently rerun and inject each of the two descriptor faults at (a) baseline switch count minus one, the last terminal-verified candidate, and (b) baseline switch count, the independent approach terminal. Both faults occur after constructing the copied depleted operator. Four descriptor cases replace the earlier two; the fresh-terminal-failure and simultaneous-event checks remain, for six total cases.

The baseline count is scheduling metadata, not a numerical oracle or an observed gate adjustment. All state, event, correction, rollback and positive-reuse assertions are unchanged. The test source was AST-parsed only by this worker; root must explicitly execute after review. No candidate code changed.
