# Nested ordinary wet approach — bounded numerical change

Baseline production: a4f968f (1172 installed tests, 42 installed modules). The new optional strategy separates ordinary approach control from the existing Euler terminal threshold and reuses completed ordinary wet prefixes locally to one depletion search. Default legacy strategy remains available. No physical parameter, EOS, roundoff budget or positive validation gate is relaxed.

A selected event still requires two successive terminal comparisons plus a separate ordinary approach with both cap and actual inventory fraction halved. Both paths use the existing event/common-time amount, energy, temperature, pressure and time tests. The separately computed path must have a different actual ordinary grid. This guards local shared-approach bias; it does not validate the globally committed earlier history or replace whole-trajectory refinement.

The optional deterministic contract is a caller declaration for manufactured callbacks, not proof that arbitrary code is deterministic. Ordinary observations are detached into immutable value snapshots. No terminal correction, dry branch or global cumulative ledger is cached. Existing source/config guards execute on fresh native evaluations; event-local descriptor checks guard reuse and acceptance. Diagnostic phase costs measure actual work; reuse has separate counters.

## Evidence history (append exact final outcomes after execution)

- Original candidate: five initial independent tests passed (1.17s); 35 legacy no-EOS tests passed (7.85s).
- Code review caught mutable observation buffer aliases and duplicate failed-comparison diagnostics. Original candidate bytes retained, then fixed.
- Extended first: eight pass/six fail. Five failures were isolated test-class adaptation errors; one deliberately biased counterexample reached its14-refinement cap before comparing approaches. Test constructor binding was fixed; for that negative counterexample only,20 refinements were preregistered while all event gates stayed unchanged. First bytes and XML retained. Extended second:14 passed4.82s.
- Initial source-guard injection at third switch:4 passed0.37s. This did not establish last-acceptance correctness. Strengthened actual lastcandidate/independent-switch injection found4 failures/2pass1.74s: changed source declarations could be committed after the last cached traversal. This is a real candidate defect, not a test tolerance issue; the failing evidence is retained.

Manufactured A/B and imposed-deformation tests are numerical evidence, not qualified raw-sludge properties, free sintering, full-cycle completion or public experimental validation. Full Goal remains active.

Final candidate ab465ad7a1da9d39787afd7dd2c6dfc24e680936e9a6c99593b345ddbc49135a closes final source-binding acceptance;20 candidate checks passed5.828s,55 applied related checks passed13.260s. Reinstalled42 actual modules match source. v3 callback passed2.09734s; v3 depletion0 failed47.38958s (inner45.76055s) with liquid_property_solution:heos_tp_not_converged:147 evaluations,18 trialpanels,6 committedsteps,0events;12 observation/9 ordinarypanel reuses. This is a preserved numerical failure, not joint trajectory acceptance. No second whole-trajectory refinement was run. Full installed test is pending session11689.

Full installed terminal11689 exit0:1192 tests, zero failures/errors/skips, XML535.553s. Before/after42 actual imported modules match production source. Original v3 trajectory remains failed.

Exception-only original replay terminal43826 failed49.48452s, captured one matching HEOS exception with no logging error;176 immutable inputs audited and every non-time run field matches originalv3. Exact arguments295K/53692.54782795906Pa liquid;8 evaluated densities alternate across417ULP with residuals−0.00010403933993075043/+0.00010410800314275548Pa, both above dynamic min(1e-4,rho*1e-7) Pa (~9.97785749e-5). The295K evaluation is lower inverse bracket (295,310), at hoststage0.5002785505335737s, not converged bricktemperature. Captured localrho afterloop is nextunevaluatedrho, distinct from lastrecorded residual pairing.

A separate fresh source-qualified publicprovider call reconstructed from captured float.hex reproduced heos_tp_not_converged: supervisor92373 failedchild1/1.18991025s, inner0.947850625s, one requested state call and8iterations. Provider initialization native work is not separately counted; do not call this one nativeEOS evaluation. Source/gates unchanged; all test/EOS handles are terminal. Next work is a bounded numerically justified TP correction, preserving these failures, not relaxing the gate or calling fullcycle complete.

Readable XML copies strip trailing whitespace per line and add one final newline for Git hygiene; original raw bytes remain in original-evidence.zip and the manifest. The first staged whitespace check failed on raw traceback spaces; only readability copies were normalized, not test outcomes or archived originals.
