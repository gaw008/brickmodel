# 实验数据迁移及中断恢复记录

## 2026-09-26 22:51 UTC：base128最终审计增量备份完成

新增的`data/sandbox/research/sorptive-surface-tight-128-v1/base-full-audit.json`已单独上传到既有私人Drive备份目录，共10,976,520字节。上传前后本地大小与修改时间一致；云端读回的名称、大小、父目录与目标一致。本地原件保留，未重传既有分片。私有收据为`base128-audit-increment-20260926.json`；这里只确认上传与元数据，不声明远端逐字节恢复通过。refined最终审计尚未产生，待退出后另补备份。

最后一条原refined128审计PID99759仍在运行，启动时间为2026-09-25 19:19:46（本机进程显示时间）；完整命令和Desktop工作目录均匹配。遵守用户保留进度的选择，本次未移动目录、未暂停或重启进程。Research路径仍为临时链接。base审计已在此前退出0，完成标记与原数值预算通过；不重开该审计，也不因此授予材料资格。

最新容量实测仍为22:12 UTC的项目与私有备份合计144,524,140,544字节（约144.52GB），不是本次重测，20GB目标未达。云端下载的两次403阻塞没有变化，本次未重试，未删除唯一压缩原件。迁移记录和存储计划已将剩余受保护任务更新为单个PID99759，并关联本次增量收据。模型代码与验收未改变；本次仅补备份和更新收尾记录，耗时约5分钟。

## 2026-09-26 19:45 UTC：完整历史补备份完成

2026-09-26 19:45 UTC更新：第二批完整历史补备份已完成，1127个源文件分为44个包、260个云端分片，共21.218GB；另6个清单/恢复说明已上传。每个分片有大小及父目录读回，整批又按目标目录核对ID、名称与大小。两批补充合计362个数据分片、29.792GB及12个清单/说明文件，临时打包副本已清理。本地无损压缩源与独有Git历史继续保留，未将元数据核对写成远端下载恢复通过。

当前项目148.31GB，私有备份3.10GB，合计151.41GB；本地Git按需缓存现3.85GB。两条原低温审计仍在运行且尚无最终JSON，启动身份和Desktop工作目录一致，因此继续保留现场。迁出Desktop、总占用20GB及完整云端下载恢复仍未通过；后续只处理这些收尾，不重开已完成模型验收或旧计算。本轮归档/备份收尾到此约85.9分钟，未新增模型功能。

## 2026-09-26 18:46 UTC：两条续算结束，完整记录归档

refined256续算实际退出0，20000s、37737接受步、续算耗时46023.77s；终态摘要保存为`data/sandbox/research/carbon-calcium-temperature-column-v1/two-fifty-six-refined-completion.json`。两条恢复积分均已结束，不重开旧物理审计。仍保留原两条低温审计，项目未迁移。

38个闲置重复文件16.67GB已去重；18个不完整前缀和3个停止点的压缩数据及状态清单保留。另一个137.93MB文件经本地gzip字节对照后去重。新将开放64格base/refined、表面64格base三条完整记录43.96GB归档为13.68GB，全部无损字节对照通过后删除原始重复文件。恢复时按各`*-completed-archive.json`清单解压拼接；未增加科学资格声明。

交付后补备份已上传102个分片，共8.573GB，并完成名称/大小/目录读回核对；另有6个清单与恢复说明。新增完整64格归档及refined256续算将在下一批补传。项目和私有备份合计151.40GB；20GB尚未达到。原始数据只因有已核对的本地压缩副本而清理，没有依赖尚未通过的云端下载恢复。云端恢复入口仍403，不重复尝试或绕过限制。

## 2026-09-26 17:53 UTC：本地Git精简及一条续算完成

256格base续算已正常退出0，末条压缩记录为completed，终态物理时刻20000s、累计29078接受步，续算积分耗时40320.62s。结果摘要为`data/sandbox/research/carbon-calcium-temperature-column-v1/two-fifty-six-base-completion.json`。BDF历史重新初始化的限制保持；未开展新的全轨迹审计或更细网格。继续部分不在早先Drive冻结备份内，本地6.92GB合并压缩轨迹全部保留。refined续算及两条原审计仍运行，迁移继续等待。

本地Git从127.73GB降至3.10GB。先保留独立本地分支、reflog独有提交和两个Codex树引用，独立恢复后对独有对象作直接字节比较；再核对全部引用、HEAD、工作区/暂存区、未提交清单及关键交付文件。旧Git仅在这些核对通过后删除，原工作文件、计算进程和远端历史均未改动。私有目录另保留约3.08GB归档及恢复说明；不将移到备份目录当作减少磁盘占用。浅层/部分Git元数据可能按需从origin获取未缓存的历史，当前源码/参数/文本报告已本地缓存。

项目及私有备份共约197.60GB，磁盘可用约268.15GB，20GB目标未达到。Drive连接器返回新文件引用，但本机下载仍HTTP403；这是第二次同类失败，记录后不重复尝试，未完整恢复的云端副本不作为删除唯一原件的依据。

用户要求备份实验数据到 Google Drive 并迁出 iCloud 跟踪的 Desktop。首次容量盘点约394GiB，其中 data 约283GiB、Git目录约110GiB。完整项目曾同盘改名到 `~/Research/brickmodel-github`，原位置留下路径链接；当时 Git HEAD、未提交清单和目录inode均保持一致，没有复制或删除数据。

## 迁移失败与用户选择

首次操作被自动审批拒绝，未执行；用户随后明确允许 full access，才进行了移动。但是七个旧任务及另外两条256格任务是在原路径沙箱下启动，新授权不会追溯改变这些旧进程的路径权限。两条256格分片写入任务随后在删除已核对的临时raw块、创建清单时遇到 PermissionError。其轨迹分片已关闭并保留，不能声称任务正常完成或迁移无中断。

目录已同盘退回 Desktop，保护其余七条计算。用户随后明确选择“保留当前计算进度，结束后迁移”。因此当前正式迁移仍待执行，`~/Research/brickmodel-github` 暂为指向原目录的链接。线程已安排定时核对旧任务，结束后才迁移；本报告不声称iCloud已经释放任何容量。

## 两条中断轨迹的恢复

全部已关闭gzip分片已读到末尾，恢复清单分别为：

| 精度 | 分片 | 已保存接受步 | 最后保存物理时刻 |
|---|---:|---:|---:|
| base | 91 | 6682 | 44.99546102234567s |
| refined | 102 | 7473 | 34.7445698166656s |

清单保留 `recorded_prefix_without_summary` 和 `PermissionError`。旧writer对每块做过回读，但原始块大多已按旧流程删除；本轮只重新读取gzip流，不把它声明为重新完成原始字节对照，`byte_identical_roundtrip` 保持 null。原失败stderr和最后未删除raw块保留。

新增显式继续入口：

```sh
.venv/bin/python examples/sandbox/resume_carbon_calcium_temperature_column.py \
  --parameters parameters.carbon_calcium_resume_after_migration.json --case base
```

`refined` 使用同一参数文件的另一案例。继续入口从保存的积分坐标重新初始化BDF，记录 `integration_restart`；不声称恢复了原来未保存的完整BDF历史或相同的自适应步序。原参数、温域、容差、初始能量参考和全程累计熵保持原定义。

两个恢复点重新计算后，全部状态、界面通量、守恒坐标和初始能量参考与保存对象完全相同。实际续算首段分别从上述保存时刻连续开始，到44.99586699200006s和34.7446098166656s，没有跳过物理时间。两条续算均使用新 full-access 进程启动；全程完成、时间精化与完整物理审计仍未结束。

继续部分单独保存；只有续算正常结束后才生成原前缀与续算的合并清单。原始不完整记录不会被覆盖。没有新增或运行软件测试、SHA或材料资格判定。

## Google Drive 备份边界

私人备份的清单、云端ID及分片收据保存在项目外 `~/Research/brickmodel-backups/2026-09-26`，不发布到公共仓库。冻结清单含约95.22GB的已完成记录及复现资料；已有无损归档覆盖约187.01GB重复原始轨迹。约22.31GB当时仍活跃或需恢复的输出单列为待补备份，不把它们遗漏后宣称全备完成。

连接器实际拒绝超过100MiB的单次文件，因此大tar拆成不超过90,000,000字节的编号分片。逐项读回云端大小和父目录。首个33,733,140字节档案还取得完整远端文件引用，但该临时下载地址在本机返回403，未完成新的远端逐字节比对，也未绕过访问限制。源数据保留；只在上传和元数据核对后删除可重新生成的临时打包副本。

备份数量与是否全部完成以私人上传收据为准；本报告不把上传开始或元数据通过描述成全备、完整内容核验或物理模型通过。

恢复点另用既有 `SourceState.reconstruct` 与 `independent_exchange` 从原始来源记录独立重算，每档256格、255个界面。两档均通过原参数中的来源、元素、体积/压力、反应自由能、气流携能、全局内能与熵账预算。base/refined全局内能残差分别为2.7203e-8/7.0077e-9 J，熵账残差2.1396e-11/6.3306e-12 J/K；详见同名 `base/refined-restart-physical-review.json`。这一证据只覆盖两个恢复时刻，不替代完整积分审查。
