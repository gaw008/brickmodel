# Initial native-stage preparation review

Pre-execution timing clarification: root preserved CASE-before-session-deadline-clarification.json and replaced the misleading `math_wall_seconds=30` declaration with `session_elapsed_wall_seconds=90`. The session clock starts at construction and includes intervening host work, so it now matches the declared 90 s stage lifetime; this is not a separate 90 s allowance per query. Whole 120 s/external 130 s and all eight scientific/step-domain fields remain unchanged. This preparation revision occurred before any native stage result and is not a post-failure tolerance change. Future runner/session code must implement that exact shared lifetime and cumulative work counters.

Decision: PASS preparation only. CASE SHA `3f138997e43c720d52f2587ac3169ca2b255079c0d86051508f913d91fd992d6`; helper SHA `5821a5f6f9b0b2309b525a968c423613e779cca388faa39d36084bdf03f165a6`. No HIGH/CRITICAL preparation finding. No native/host/seed/proof/controller execution or repository/install change was made by the reviewer. This is not execution admission for the unfinished session/stage/runner.

Actual AST/hash/read-only evidence is `review_initial01.json`. It verifies the cited original test-source hash and extracts the original policy helper arguments directly: all six comparison thresholds **and both min/max step-domain values** match CASE exactly. The proposed exact interval 0 to 1/1000 s is within the unchanged step domain. Maximum host evaluations remains nine. Stage wall changes from the original default 30 s to explicitly declared 90 s; this is a computational resource policy change, not a scientific tolerance change.

The separately declared limits are initialization 30 s, mathematical session 30 s, stage 90 s, whole study 120 s and external 130 s, with at most two initial forward evaluations, nine stage host evaluations, 48 seed suggestions and 16 proof operations (four fresh pressure requests). These are predeclared limits, not a prediction or guarantee of successful completion. The completed single-query timings provide planning context but are not an upper-bound theorem for this stage workload. Future implementation must share cumulative counters/deadlines across failed and successful requests, rather than reset a local 30 s allowance for each query.

Actual diff against the independently reviewed fresh-query helper is limited to function/doc/result naming and removal of the pre-stage pair.evaluate block and its rates export. It still constructs current source water and the original manufactured reference/material/transport model, uses the same two declared virtual temperatures 300.125/305.25 K, saves prototypes before each forward call, and initializes each total U from that call's returned total U. Static analysis finds no pair.evaluate call in build_initial. The two forward evaluations occur in the strict two-input loop; returned pair/states are live current objects for subsequent stage work.

Current runtime/provider construction and reference-anchor evaluation inside build_storage are additional work beyond the two forward counters. Native internal count remains unknown. Caller initialization/whole deadlines must begin before construction. The original error envelopes, reaction, face, inventory and physical qualification remain unchanged. The inherited attempted-point.json remains an old builder parameter snapshot; case.json is the actual new temperature design.

The helper saves actual forward outputs and original states, checks original pair binding, and returns without committing a stage or generating a pressure PASS. Exceptions preserve current costs/status and re-raise. Hard-kill or setup errors still require the outer supervisor's terminal record. Future runner must freeze then-current installed modules/dependencies, actual assets and these exact preparation bytes; CASE currently supplies declarations, not executable enforcement.

On a normal complete step-doubling candidate, require all nine actual callbacks and four fresh endpoint pressure requests. If a prior native/math/domain/budget gate fails, retain exactly the reached calls and complete failed evidence; do not manufacture nine calls or continue merely to fill the target count. All full-step/two-half ledger paths and interval-polynomial positivity remain speculative and must pass their original gates independently. The forthcoming session, stage hooks and bounded runner still require separate independent review and a single recorded execution.
