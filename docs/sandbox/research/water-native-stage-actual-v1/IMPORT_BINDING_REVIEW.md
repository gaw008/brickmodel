# Import-binding correction review

APPROVE this bounded correction at run.py SHA 8ee22f3a98baf3e4fc27dcd3a561858851f3b342efc1187cc5ae5514fbc5d6e6 and prepare.py SHA 50fae43c6758c196f25c1d173b81e60b937b30c36a0903a048bf3f73c42525f8. No native execution, prepare invocation, stage/session query or installation was performed. This closes the previously reported HIGH; it does not substitute for final installed freeze or actual saved-data audit.

Original HIGH: prepare verified the installation but run only hashed that fixed directory. A different runtime import origin could supply actual computation while installed files remained unchanged. Actual import-only observation showed PYTHONPATH=src selected the checkout and default selected the venv. The original report metadata is preserved in runner-review01.json. Exact old run and prepare sources were reconstructed by reversing only the reviewed changes and verified byte-for-byte against those original SHA values: run.py.before-import-binding (77340c43...) and prepare.py.before-import-binding (74c0a813...). Current Ptolemy report was copied without modification into PTOLEMY_RUNNER_REVIEW.md to preserve separate authorship.

New guard checks find_spec package origin against the frozen resolved installation, exact distribution declaration/noneditable flag, and all loaded package/submodule paths against the frozen file members. It runs before construction and through caller guards. The installed root is explicitly saved by prepare. Hash read errors return None, allowing missing sources to follow the structured failure route instead of an unhandled file exception. A race or arbitrary malicious runtime object rewrite is outside this bounded local runner review; no such generalized guarantee is claimed.

The pure review_runner_fix.py executes the actual extracted guard from both old and new source using isolated import metadata and file fixtures. Actual results in runner-review-fix02.json: old guard admits shadow package; new guard rejects shadow package, shadow loaded submodule, changed distribution and missing source; normal control passes. The first pure fixture run failed because macOS /var is a symlink to /private/var; only the fixture root was normalized, with original harness and failure note retained. Production code was not changed by the reviewer.

The additional accepted-only assertion occurs after complete trial and session save. It requires two completed initial forwards/no initial pair callback, nine stage attempts/completions and samples, three steps/panels, four successful fresh queries, sixteen completed proof operations and known costs. Failed stages retain their actual reached work without executing extra calls to fill quotas. This enforces the already registered counts, not a new physical gate. The underlying six comparison thresholds, state interval and material data remain unchanged.

The exporter still saves every subtype dataclass field using the existing lossless numeric encoding. Source changes yield failure, while preserved trial/session records remain available. Final execution freeze must be generated only after the reviewed implementation is installed; matching104 count alone is not evidence that the future run succeeded. The independent auditor and supervisor remain separate review/evidence responsibilities.

## Final prepare membership delta

Prepare e63ba303dd7799c8fe18fea158683d5df0403fdd65f9103f927504221205b1ca adds only audit_stage.py to the frozen local script list. Reversing that single string insertion reproduces the previously approved prepare hash exactly. No computation, budget, import admission or runner behavior is broadened. The additional auditor bytes will be checked by the existing frozen-file guard. Actual execution freeze/preflight remains pending root execution.
