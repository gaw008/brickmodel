# 一次实际原生水 full/fine 时间步

当前104个非editable安装模块、252个来源/输入/脚本冻结。案例是原制造固体/反应/输运参数与真实水物性，初温300.125/305.25K；0到1/1000秒的一次完整步与两个半步。原六项科学门槛及原步长域保持；原严格全局/局部prefix守恒门槛另行使用，未用较宽步差门槛替换它们。

实际唯一attempt01完成：2次初始能量forward、9次host evaluate、4份当次压力证明；seed20/48、proof16/16，未增加原生观察来替代原sample。trial60.742858917/90s，session60.813020625/90s，whole62.257310250/120s，外部62.378195833/130s，exit0、无timeout且子进程已回收。session83328已终态。会话90秒从创建开始，包含host间隔，不按query重置。最初准备文件把该项误写为math30，已在任何原生执行前澄清为session elapsed90并保存原文件；科学门槛未变。

完整trial.json为6512839bytes。原始typed subtype、入口policy/input字节、九个观测、三套面板/积分账本、四query全过程及累计费用均保留；旧codec拒绝此新subtype。返回candidate只是试算，未作全局提交、事件切换或续算许可。

| 比较量 | 独立重算数值（显示舍入） | 原门槛 |
|---|---:|---:|
| 固体质量 kg | 2.5257015e-13 | 1e-8 |
| 库存 mol | 9.7987451e-12 | 4e-7 |
| 内能 J | 8.8550223e-8 | 1e-3 |
| 温度 K（含两端误差） | 8.7121490e-7 | 1e-3 |
| 压力 Pa（含两份区间半径） | 0.0058763665 | 1 |
| 时间 s | 0 | 1e-8 |

冻结audit_stage.py用标准库Fraction独立重算原始回调→predictor/精确积分→表示值舍入→写回、全部二次面板内极小值、三条路径和从共同初态累计的严格元素/质量/水/内能账本，六项比较全部通过。saved-audit01.json保留精确分数与原门槛。后续独立保存数学审查另核四query的Krawczyk、完整连接管、机械角点、native残差体积界与源绑定，未重复任何native运行。

本阶段修复并保留两个stage输入/门槛被改动的实际RED，以及runner只核冻结文件而未核实际导入路径的HIGH；现在在任何构造/物性调用前及guard中核实际安装路径/loaded modules与来源成员。源码/安装各110项测试通过。修前文件、独审、preflight、预算、超时回收与完整冻结原文件副本均保存。

每份压力证明保留原U/Cp/体积误差假设与source-supported普通液水分支选择，不能称为全域native误差或真实污泥参数准入。尚无空间收敛、完整湿干事件控制器、湿坯到烧冷全周期、真实材料三机制留出或多代结果；本次是一个普通时间步的条件性数值接受。
