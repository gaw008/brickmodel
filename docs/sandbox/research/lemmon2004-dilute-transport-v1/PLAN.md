# 氮气/氧气零密度关联式复现

2026-09-25 UTC，数值运行前冻结。以Lemmon/Jacobsen 2004原始PDF式2/5、表I/II/IV/V为依据，建立纯N2/O2的零密度黏度、导热率及温度导数。已视觉核对PDF页3/4/6/8：黏度为μPa·s、直径为nm，不能沿用文本提取错误的mPa。原始论文URL和页码在根参数中；本地缓存用于阅读，运行只依赖已冻结的离线数字。

阶段顺序：先实现根参数关联式；以80位独立表达式及自动数学微分审查100–1200K各点；核对表V的四个零密度计算点，误差不得超过半末位舍入。然后与前阶段保留的NIST历史表及GRI近似对照。算术相对预算1e-13，温度导数1e-12；NIST每物种/性质的不确定度和末位舍入预先冻结，任何失败保留。

表V是作者提供的计算复现值，额外位数不是实验精度。其零密度定义明确，但不意味着有限压力、狭窄孔隙或混合气体可直接采用同一数值。只取式2/5的零密度项；不包含式3/6的密度项、临界增强、真实气体EOS或多孔传输。本阶段也不把为黏度拟合的碰撞关联式替换到二元扩散公式。保持实砖与训练资格为false。
