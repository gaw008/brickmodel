# Native exact codec validation before service admission

Use the unchanged repository two-cell paired-event endpoint-root case, original wet initial state and .5→.50032 horizon, original physical/source/error budgets. Only the existing explicit ordered opt-in is selected. Freeze the reviewed numeric-type-compatible exact_record implementation and installation before starting. One native process, original512panel/800-second core budget and840-second external supervision. Prior two-cell legacy case took~68 seconds; four/eight exact complete runs took170/392 seconds. This is a bounded actual new codec validation, not a repetition claimed as new physical validation.

Save full original inputs and compact numerical core result before attempting the new strict record; a codec failure cannot erase the completed or partial core. Encode actual live observations and phase equilibrium, read strict record and consume the fresh record returned by live source validation. Verify final state and accepted ledger count against actual result, source unchanged and exact original horizon. Save structured failure and source/runtime-after even if encoding fails. Post-run independent prefix and closed-system audits remain separate; no checkpoint restoration or resumed trajectory is claimed. Raw material full-cycle and public heldout validation remain incomplete.
