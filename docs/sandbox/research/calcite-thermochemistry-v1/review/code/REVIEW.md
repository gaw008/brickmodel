# Calcite thermochemistry code review

**APPROVE for the reviewed code and pinned fact wiring.** No remaining CRITICAL or HIGH issue was found. Reviewed final module SHA-256: `04c798663f31e2cd5314f34d0ee7904a81643f0cc4c1164759fb6a829533bf33`. Full source/test/data hashes are recorded in `REVIEW.json`.

The implementation evaluates the stated five-term Cp polynomial and its analytic integral correctly. It retains the 298.15 K formation-enthalpy reference and sums product-minus-reactant enthalpy coordinates; it does not substitute the separately referenced high-temperature formation column. Mass and element bookkeeping use the prescribed extent and printed species formula weights. The calculation does not claim to predict reaction progress, kinetics, equilibrium, phase stability, high-temperature volume or kiln heat input.

The 80-digit local Decimal context isolates precision, rounding and arithmetic traps from the caller. Input limits reject nonfinite values and excessive representations/exponents; accepted extreme extents remain computational values rather than certified feasible inventories. Result uncertainty correctly remains separate from Decimal precision. All current trace dependencies are actual published-result paths, including explicit species molar masses, coefficient values and the reference temperature. Original source-page validity is handled by the separate scientific review and is not certified by this code review.

Two issues were identified in the initially inspected source and communicated to the author: ordinary file opening could block on a FIFO before reaching the byte bound; and mass/phase trace dependencies included unresolved paths. The author changed the reader to nonblocking descriptor opening with regular-file validation and made trace dependencies explicit while this review was running. The first independent execution already exercised those fixes and passed five tests. Although its files were pre-named `red.log` and `red.xml`, they contain a GREEN result; no executed pre-fix counterexample is claimed.

Final independent checks: **6 passed in 0.052 s**, covering FIFO rejection in a subprocess with a one-second termination bound; exact mass balance at a 50-digit extent with exponent 1000; hostile caller precision/rounding/Inexact-trap settings; a finite-difference derivative of reaction enthalpy against the reported ΔCp; concrete mass trace paths; and CLI failure handling plus exact CLI/Python success parity at zero extent. The CLI probe also checks that no CoolProp module was imported. These tests perform no native EOS calls or installation changes.

AST parsing passed for the new module, modified CLI and affected tests. Ruff, mypy, pylint, black and bandit are unavailable in the selected environment; no result from those tools is claimed. Existing author quadrature/boundary tests were read as coverage evidence and were not represented as new independent execution. This review changed only its owned evidence directory and scratch probes, not production code, tests or source data.
