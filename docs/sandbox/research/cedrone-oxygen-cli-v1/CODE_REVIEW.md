# 公开有限氧入口独立审查

**APPROVE，未发现阻断问题。** 本次仅审 candidate02 的三个新文件、既有监督器接线和预登记 NATIVE_PLAN；核心构相候选保持此前已审 `c7b59140…e184376`，未重做 TP 数学或执行 Element、EOS、安装、旧 native。

`derive_cedrone_oxygen_pool` 仅读取一次固定 SHA 的 printed-pool 字节；按实际传入 binary64 A 的精确 Fraction 从原 m 计算 b，再独立加 O₂/N₂，不读旧产物。D、21/79、二倍 O/N 原子增量、`.704+Madd` 与原 1 kg 报告样本基准一致。definition 为已有深只读值，初始绑定标记 false；RESULT 成功保存后，薄检查核同次初末相 A、原模型/policy/全部核心门和共享 19 项质量账才返回 true。既有源分类、未入池灰/水/矿物和材料 unknown/false 保持；允许新增序列化 provider 字段。

父入口从示例自身定位已发布监督代码，`--source-root` 仅供数据读取；独占新目录、固定同解释器 `-I` 的一次 worker，40 s + 5 s 清理，成功还要求退出0、leader回收及已声明输入前后相同。worker 的30 s边界与核心10 s不冒充能自行中断 in-flight native；继承原监督器的进程组/系统调度限制。worker记录两份实际模块的路径、SHA、字节数，结束重核；这不是全依赖或加载字节不可变证书，也未如此宣传。

导入/Element前缀保留、同次原 RESULT 先存后判、输出失败/超时分支已读。运行中计数明确 `attempt_count_final=false`，最终返回/异常处理后才为 true，因此尚未写入的“已调用”状态不被误当终态0次。未返回或保存失败不冒称完整结果。运行后源码变化可拒绝完成而保留真实 RESULT/OBS。

独立一次无 Cantera 导入的被动核对 **8组通过**：与旧同 λ=1/4 保存输入的12个精确加料字段、完整 pool、原 policy、精确 A 完全相同；definition 无重复 I/O 且嵌套不可变。只使用旧保存 A，不等同新 Element→同次实际相已验收。实读作者原9项通过0.10 s；两补项受影响3项真实 RED 后3项 GREEN（7 deselected，0.09 s），原失败保留，未复跑其套件。

预登记仅一次 λ=1/4 真入口，保持原门、152项独立NASA比较及原名义库存/G对照阈值；新计时/provider字段单列。旧点有profile、新点无profile，不能把耗时比称稳定加速；若失败仍保留，不补求 λ0 或重试。可按该原计划进入安装与唯一实际验收。

冻结 SHA：
- `src/sludge_sandbox/cedrone_oxygen.py`: `c85971c9e03351e86dbe84fde16daadceda106b49e6a6f32ae83a5cc220fc882`
- `examples/sandbox/run_cedrone_oxygen.py`: `b1713c05aa2cdfbabb772980ac69fa4df51b9ddbfdf44d183e39b2d4042eca4b`
- `tests/sandbox/test_cedrone_oxygen.py`: `680c05af854dd06f510fa1baae21da94009ce8ac1e971a7372b2a6b082e04440`
- `src/sludge_sandbox/tp_equilibrium.py`: `c7b5914070fa55cd0dbf0e0c18230b7b708ddcdbcd57ff80009c00798e184376`

## Review Summary

| Severity | Count | Status |
|----------|-------|--------|
| CRITICAL | 0 | pass |
| HIGH | 0 | pass |
| MEDIUM | 0 | pass |
| LOW | 0 | pass |

Verdict: APPROVE — 限此公开入口与原计划；真实物性绑定、性能和材料资格不由代码审查授予。
