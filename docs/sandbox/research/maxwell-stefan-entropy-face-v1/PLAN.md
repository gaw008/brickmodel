# 局部交叉扩散到共享网格通量

2026-09-25 UTC事前冻结。局部MS方程资格通过后，下一步定义明确的等温等压有限面离散，仍未接入原热反应模型。

令b=ln(xR)−ln(xL)，x(s)=exp(ln(xL)+s*b)/Σexp(ln(xL)+s*b)，0≤s≤1。该归一化是路径定义，不是把负浓度裁成正值。局部梯度为x'(s)/d=x(s)*(b−x(s)·b)/d。对相同D、c、T、P逐点解已资格局部MS方程，定义面通量Jface=∫J(s)ds。此路径积分是本项目离散选择，并不假定沿路径的实际稳态通量恒定。

因为ΣJi(s)=0，局部Π(s)=−RΣJi(s)*b_i/d。故面熵产生−RΣJface_i*b_i=d∫Π(s)ds≥0；Gauss正权重仍保留此结构。交换左右端点将路径反向，通量变号、熵产生不变。同一共享通量进入两格可使物种转移恰好抵消。二元常D下J(s)=−cD*x'(s)/d，精确积分等于Fick通量−cD*(xR−xL)/d，可独立量化有限求积误差。

根JSON固定3个四气体面和6个二元对，在600K、100kPa、1mm距离比较8/16/32阶；三档不更改物性和误差门槛。独立参照以60位原始非对称MS关系消去末种通量后求解，并用独立自适应积分；二元另用解析Fick结果。审查通量、左右反向、零跳跃、总摩尔通量和两种熵表达式。粗档失败如实保存，只有达到同一预算的阶数才获本组面资格。

有限面不是砖内真实孔道，也不包含压力/温度梯度或稀薄壁碰撞。全列时间与空间误差将是此阶段之后的独立工作。无软件测试、SHA或护栏新增。
