Research artifact for exact-temperature thermochemical memoization in the restricted Ca/C/O open-column model.

The complete two-cell refined path reaches 30000 s in 11353 accepted steps. Every physical header, accepted record, sample, boundary transition and dense polynomial matches the previously recorded trajectory exactly. The comparison excludes only declared cache/method-name metadata and wall time. This is numerical equivalence under the documented approximate material model, not real-brick validation.

The gzip asset restores the complete JSONL byte stream. The archive manifest and full-path comparison accompany it. Source code, root parameters, selected observations, profiles and the retained stale-method-label failure are on the corresponding branch. No checksum or material-qualification claim is made.

material_qualified=false; training_eligible=false.
