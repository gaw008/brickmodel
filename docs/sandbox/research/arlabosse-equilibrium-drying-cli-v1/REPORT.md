# 已验证两格排湿案例的中文命令行入口

新增 [run_equilibrium_drying.py](../../../../examples/sandbox/run_equilibrium_drying.py)，将既有两格物理案例变成显式来源目录、独占输出和中文提示的可运行示例。使用已安装的`sludge_sandbox`及原件缓存，脚本不导入临时研究目录，不含本机绝对路径。用户可以选择已实际验证的1/2/4步，总物理时长固定1秒。用法见[CLI说明](../../CLI.md#两格平衡排湿示例)。

## 已完成的实际运行

唯一公开入口试验使用默认1步，监督进程49.296135s后退出0，exec64431终态0并已回收。生成36,984,272 B完整JSON，`status=completed`，记录的驱动耗时43.269879s。该时钟采样在最终状态写入前，随后检查写入是否越过150s，外部监督另覆盖进程退出。没有重跑原三档。

末态两格温度330.003387471/332.822322277 K、压力93289.807781/105277.196294 Pa；累计排水5.85452937348e−5 mol。实际保存了输入、每个原初物性点、完整初始化、动态结果、每格每步账本、累计费用和源定义。数据来自实际安装包，未由旧结果复制生成。

物理构造沿用[原两格模型](../arlabosse-equilibrium-transport-v1/REPORT.md)，保留每项初始气量的原binary64常量，没有重新按总量分配而改变末位。相变动力学支路显式关闭，新主机执行已说明的瞬时平衡投影；k/D跨材料转移、低W延拓、平衡时间尺度与组成误差仍未知。完整固定温压/水/能量/化学势门保持，结果不是完整烧成或本厂预测。

## 入口检查和失败保存

输出在加载运行库和来源物性前独占创建，先保存输入。原初点、初始化和积分一旦返回，先保存已有对象再检查资源；运行库加载返回超时也先存阶段信息，然后停止来源构造。完整JSON在内存序列化成功后才写入，因此序列化失败保留上一次快照；I/O中断仍可能留下不完整文件，入口明确返回错误，不宣称原子事务或强杀恢复。

初始化/积分/驱动限额为40/100/150s；内部检查不能中断正在执行的底层物性调用。本次额外使用既有进程组监督165s加5s清理，登记原2792输入及入口/计划/启动器，共2795项。仅一个尝试，不自动重试。原进程组已回收，未声称包含主动逃逸的session。

作者9项非EOS检查通过0.08s；独立成功4步制造记录检查核全部步累计和序列化。独审实际发现的“运行库返回已超150s仍开始来源构造”反例保留，最小修复后受影响用例通过0.01s。代码及薄监督审查通过；没有因此重复旧物理测试。

最终代码SHA-256为`c1e567f6f1fd74d488d0e64c66b650d494711c3ed786f4f3aae7d79c430b1df8`。[代码审查](CODE_REVIEW.md)、[薄监督审查](LAUNCH_REVIEW.md)及[保存结果独审](SAVED_REVIEW.md)均完成。独审19组检查通过：八个完整对象逐字段、类型及binary64比对，仅3处执行耗时不同；物理时钟、全部状态、来源、物性试次和账本与原单步相同。全部步累计观察独立复算一致，预算与执行元数据另行列出。原2795输入前/后/当前字节及186包文件/179模块安装冻结相同。未运行新EOS或重复测试。

原证据31成员、未压缩38,271,968 B、压缩2,063,340 B，SHA-256 `d2ca15be41b0ec97f9784ffedf7044c9a50b2ea085e62c34d440a723ce376ca4`；全部成员已重开核对，见`EVIDENCE_MANIFEST.json`。完整Goal仍缺原泥高温及烧结冷却材料、全场/全周期与现实验证；本入口不改变这些资格。
