# Independent fixed-sphere surface contract

Read committed SPHERE_SURFACE_NEXT.md. Formula Rcond=(1/rc−1/R)/(4*pi*k) correctly integrates Fourier spherical half-shell resistance. Actual outer area As=4*pi*R² multiplies film and gray-radiation powers. Surface→cell Q=G*(Ts−Tc) is positive inward and therefore enters existing outward boundary ledger as −Q; gas enthalpy remains separately signed outward. No additional latent sink is justified merely by this surface balance.

Residual F=G*(Ts−Tc)−As*h*(Tg−Ts)−As*epsilon*sigma*(Trad⁴−Ts⁴) has nonnegative derivative for positive temperatures and coefficients. If any pathway is positive it has a unique root in the extrema of supplied temperatures; k=0 allows external film/radiation equilibrium with zero conducted heat. Allpaths-zero case has undetermined surface and preserved existing representative Tc/status. Radiation's environmental assumption must remain explicit and does not identify actual Nylen wall temperature.

For R=.02,rc=.01,k=.5,h=20,Tc300,Tg310, radiation0, G=4*pi*k*R, As*h=.032*pi. Independent algebra gives Ts=2740/9=304.444444444444...K and positive heat G*(40/9). This differs materially from the planar half-thickness result. Reusing base._conduction with reversed temperature arguments preserves half-shell geometry and surface sign. Static surface support must not remove existing sphere+motion/liquid restrictions.

Existing moving slab path explicitly supplies current.transport to _surface. Review will ensure area and conduction are both obtained from that argument, not cached reference transport; single base evaluation/inverse and unchanged knot handling remain essential.

Saved prior HEAD ProgrammedSolidFluidHeat source with Git commit and SHA in SLAB_GOLDEN.json before author freeze. Independently executed old class for4 dry slab configurations (film, nonlinear radiation, fully insulated, zero-k with gas flow),7 times across exact program knots and interiors:28 binary64 golden records including surface temperature/powers/residual/limit/status/iterations and complete face species/energy arrays. Actual log baseline.log. All liquid inventories zero; no native liquid EOS called. Candidate comparison awaits frozen author code. Numerical inputs are manufactured and never Nylen material admission.

## Independent Robin-mode derivation

For theta=A*sin(mu*r/R)/(mu*r/R)*exp(-alpha*mu²*t/R²), finite center limit and zero center derivative follow by Taylor expansion. At r=R, R*theta_r/theta=mu*cot(mu)-1. Thus outward Fourier film boundary -k*theta_r=h*theta requires mu*cot(mu)=1-hR/k=1-Bi. Bi1 gives mu=pi/2, the principal positive root; decay rate and outward sign in proposed test are correct. Cell volumes weight RMS, but initial/final reference is explicitly midpoint temperature, not volume average. Actual finite-volume film should converge to this continuum boundary. Current test chooses total storage capacity per volume=100*(30-Rgas)+10*50 and alpha=k/capacity, consistently accounting for gasCv and incompressible solidCp fixture storage. It independently separates timehalving from threegrid convergence. No duplicate multi-grid execution planned by reviewer.

Static candidate diff inspected before freeze: only _surface area and intoheat consumer change to selected transport helpers; evaluate branch continues passing current transport for both moving slabs. Root solver, residual/error gates, gas exchange, single base.evaluate and program breakpoint behavior remain unchanged. Removed constructor sphereboundary rejection is paired with positive sphere surface tests; motion/liquid rejection remains elsewhere. Await final author freeze before golden comparison.
