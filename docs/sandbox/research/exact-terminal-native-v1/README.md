# Fresh native terminal from original failed state

Baseline b4d48c7. Source/installed suites: exact_terminal_executor, exact_free_host, exact_root_order, exact_terminal_panel, exact_affine_depletion. Commands used venv Python pytest, PYTHONPATH=src:tests/sandbox for source; installed run used absolute tests from /private/tmp with no PYTHONPATH after frozen offline noneditable install. Results 57 passed in .66 s / 57 passed in .73 s. All 75 actual installed modules match source.

Original case and initial state identity are checked against retained four-cell evidence. The starting state is the full failed-panel initial state at .5002686443458968 s, already containing the prior UNCOMMITTED cell3 dry transition; input totals.events is 1. This is not a restart from the accepted prefix. Original roundoff policy, integration/depletion policy and common endpoint .50032 are preserved. The runner captures exact start/common times and full inputs before evaluation.

Actual native01/session30444: 45 s external supervisor terminal exit0 after 2.558452792 s, runner 2.227064916 s. Core speculative_completed, no reason, terminal .814022042 s. Three actual observations (initial/midpoint/endpoint), one predictor, one terminal panel, one root ordering, one writeback and one mode proposal completed. Initial Rates exactly match the original saved initial Rates; midpoint is newly sampled at the exact Euler-predicted state, not replayed.

Cell2 selected, 27 refinement levels, final proposed dry modes2/3. Candidate phase correction is 4.868073823251425e-21 mol, local gross evaporation1.5641070733517966e-12 mol; original 1e-8 fraction gate remains. Exact rational times and complete N/E/stretch and phase data are saved. Source/runtime identities, original operator, source case bytes and input state remain unchanged. Totals1→2 are both speculative branch totals, never committed event counts.

Result capture recursively retains all numerical dataclass fields and serializes native provider boundaries using explicit type/source/implementation descriptors. Native cache internals are not treated as scientific result fields. Runner SHA0906fc2a931c4261be64049a7d55eeee1e9335c8201eb958d1b41847a8eae8fb. No root process remains active after this completed supervised attempt.

This validates a source-bound actual single-terminal execution using real water EOS and manufactured solid data. It does not close the original full four-cell packet failure: mixed continuation, per-event/common comparison, successive/independent passes, cumulative original-prefix audits, atomic commit and versioned records/resume remain. Public raw-sludge evidence, full wet-firing-cooling and spatial convergence also remain required.

Independent stdlib Fraction audit recomputed all face/source/energy/stretch affine integrals, complete predictor/terminal states,27-level root evidence/samples/exclusions and original local/absolute/fraction/cumulative writeback gates. Actual ratio3.11236609e-9<1e-8; max represented ledger residuals N3.67683e-17mol/E6.89855e-12J/stretch3.74019e-17. RESULT_REVIEW and audit-result preserve scope and actual sourcebinding. The initial audit script misread the local allowance field as including the separately reported clock residual; that failed audit and corrected script are both retained. No scientific gate changed.

Final installed code d004b1b7 adds distinct DomainExit classification after the e324 native run. Source58/.66s and installed58/.75s pass with75 finalmodules matching. before-domain-fix-* files preserve exact prior logs/identity that bound the supervised native input paths. Native runtime files remain e324; do not claim a final-d004 native rerun. The success computation is unchanged by the reviewed two-line classification diff.
