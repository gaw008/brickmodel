# Actual accepted wet-state host profile

The bounded installed-runtime process completed (exit 0, no timeout) in 3.35708 s. Construction took 1.34028 s. Exactly two explicit FreeSolidSlab.evaluate calls used accepted state index 1 of the sealed completed replay. All physics modules match that run; only the previously committed run_service query change differs, explicitly bound to 87b5f78. Runtime identities and original manifest/result hashes remained unchanged.

Call 1: profiled wall 0.887659 s, CPU 0.884100 s; raw profiler export 0.014293 s and selected-output serialization 0.001358 s outside the measured call. Call 2: unprofiled wall 0.805864 s, CPU 0.802701 s; serialization 0.001486 s. Selected rates, geometry, free mechanics, thermal/skeleton states and inverse errors were exactly equal. This pair does not separate profiler overhead from warm/history effects and does not prove a speedup.

The call tree contains two total-energy inversions, eight thermal forward evaluations, 320 pressure trials and liquid-volume property calls, and 328 native state_tp calls. Both final cell pressure records report 38 bisection iterations and approximately 7.2032e-6 Pa final bracket widths; these are saved numerical-root diagnostics, not physical pressure uncertainty. _transaction is resumed 656 times across those calls: its recorded self time is approximately 0.711 s and cumulative time 0.751 s. Source inspection shows configuration/fluid checks around each native operation, but cProfile cannot resolve all time inside extension functions. Deforming-state _digest is not the dominant observed cost. Nested cumulative times must not be summed as independent percentages.

The measured function excludes phase-transfer chemical-potential work and paired-pressure comparison endpoint operations. It does not explain the complete event run or predict long-run/grid costs. No EOS tolerance, bracket, source guard, initial condition or physical coefficient changed.

The supported next candidate is reducing the number of pressure-root forward trials while retaining actual guarded property evaluation, sign-enclosing brackets, original bracket-width and residual gates. This changes the numerical path and requires an explicit policy, independent root tests and actual event/convergence revalidation; it is not a transparent optimization or evidence that the original spatial failure has passed. Original bisection and all failures remain available.
