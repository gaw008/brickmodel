# Ordinary RK phase-transfer candidate (not executed)

Scope: one current-geometry free solid cell, fixed solid composition, changing liquid and water vapor inventories. Explicitly manufactured mechanics/conductance; actual qualified water caloric/chemical bridge. No spatial model, nucleation, solid reaction or material admission.

Apply only the two accompanying source diffs after review. `WaterPhaseTransfer` explicitly admits exact `ClosedFreeSolidCell`; it does not impersonate a prior thermal host. The free host exposes reference templates only for immutable provider/source matching. Evaluation exposes actual current thermal states and corresponding thermal inverses (`inverse.state is storage_state`), plus complete dynamic inverses separately. The free host always declares manufactured classification, triggering the existing opt-in. Liquid index comes from its actual InventoryLayout. Water phase caloric/method/reference/asset/R matching is unchanged, including the existing disabled-coefficient exception. No new material-classification permissiveness.

Transfer source remains precisely liquid -= rate; vapor += rate. The final Rates forwards original mechanical rates and cell-power components. Stored total energy already contains both phase-reference energies and mechanical potential: no latent source and no additional dissipation heat. Current geometry and pressure are decoded once by the base evaluation at each ordinary RK stage before the phase driving pressure is evaluated. State identity remains the base energy-model identity (as existing wrappers); chemical/kinetic identities remain separately visible in wrapper/source diagnostics.

No changes to depletion admission: mechanical states must continue to raise `mechanical_depletion_integration_not_implemented` until the complete terminal implementation below is tested. Ordinary RK can approach an event but must reject negative inventory; it does not certify/switch depletion itself.

# Required depletion upgrade, deliberately not applied

1. Require explicit positive stretch_absolute_tolerance/stretch_scale for mechanical initial state. Existing exact operator admission can stay WaterPhaseTransfer/ManufacturedDepletionAdapter; new free host is already inside the former. Do NOT broaden arbitrary duck-typed callbacks or weaken water molar-mass/source/caloric guards. `observe` can consume the real state/inverse properties without inventing observations; validate paired mechanical Rates at every observation.
2. Frozen-rate terminal: integrate each mechanical rate with exact represented duration, round once, record signed `stretch_quadrature_roundoff = represented increment - exact integral`; advance actual stretches from the original state plus represented increments. Reject nonfinite/nonpositive values. Include stretches on constructed ConservedState and increment/roundoff on StepLedger.
3. Affine terminal: midpoint predictor must advance stretches by h_mid * start_rate. Evaluate the full coupled midpoint (N,E,n,t) once. Endpoint stretch integral uses h*r0+h*h*(r_mid-r0)/(2*h_mid), with actual represented midpoint delta. Preserve exact integral/roundoff per component. Certify positivity on the whole quadratic interval, including interior stationary minimum, as for inventory. A positive endpoint alone is insufficient. Both terminal strategies keep energy integration unchanged.
4. `depletion_writeback` already preserves mechanical_stretches; retain this exact copy and test it. Only liquid/vapor corrections are authorized, never stretch corrections. Interface switch must preserve energy and stretches exactly.
5. Event and common-time comparisons need explicit mechanical max differences, both independently bounded by the original stretch tolerance. Save detailed vector differences. Keep all existing N/E/T/P/time gates and two consecutive passes plus genuinely finer independent approach; never use mechanics in place of another gate. Add tail-default optional stretch difference fields to event/refinement evidence or immutable details without altering legacy positional values.
6. Global commit must locally accumulate represented stretch increments, exact increment-minus-roundoff, and absolute quadrature roundoff over ALL accepted ordinary and terminal panels. Check each prefix against original stretches under original stretch tolerance, not reset at each ordinary call/event. Require complete dimensions/binding before committing anything. Reuse the existing ordinary RK convention; no extra mechanical accumulator in public result is necessary. Failure leaves entire speculative event path uncommitted.
7. Cached ordinary states and ledgers already carry new immutable fields; verify no manual constructors/clones drop them. Existing source binding and final acceptance rechecks remain. Cancellation/domain/rejection/step and wall budgets unchanged.

# Required evidence before admission

Ordinary wrapper tests: independent positive transfer/source balance and unchanged base E/mechanical rates, one evolving wet interval with real N_liquid/N_vapor changes and current geometry; phase reference energy changes must feed temperature through inverse, not a latent source; disabled-source parity; invalid water binding and missing mechanical state rejected; no manufactured opt-in rejected. Preserve original rate/temperature domains.

Terminal no-EOS tests: analytic coupled phase sink + nonzero stretch dynamics for both terminal methods; midpoint depends on mechanical predictor; within-panel stretch turning point crosses zero despite positive endpoints rejected; frozen/affine ledger exact Fraction audits; event correction leaves stretches unchanged; rejected event branch/cancellation/source mutation leaves accepted prefix intact; event/common mechanical mismatch rejects despite N/E agreement; cumulative roundoff cannot reset across segments. Then separately preregister bounded source-qualified wet event and refinement comparison. This candidate does not claim those tests or wet terminal completion.
