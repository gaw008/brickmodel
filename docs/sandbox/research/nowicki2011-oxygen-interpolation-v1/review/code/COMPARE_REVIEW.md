# 锁定预测比较脚本补充审查

**Approve：未发现本次固定输入、单次比较流程的关键阻塞。** 审查对象 `compare.py` SHA256 为 `5eb73d0ecc8e1587aa01394c5d7b0159b0e426156149e0daa0ecf8eba9c2a697`。此前代码审查报告和 FREEZE 不变。

静态检查确认：先核预测摘要，再核固定 facts 摘要和预测已绑定的原表摘要；恰好配对预登记顺序的八个水平，禁止自动插值、重新拟合或改序。残差定义为预测达时减图示中心时间，MAE/RMSE/最大绝对差/偏差计算正确；原读取区间包含两端，越界距离按离最近区间端点计算。500°C 作者拟合常数单独列示，明确 `used_for_prediction=false`，没有将该对照纳入八点时间误差或材料合格门槛。原观察完整保留。

`test_compare_review.py` 仅用制造预测、制造读取区间及制造表行，**12 passed，0.06 s**。已验证含端点的区间比较和可手算指标、错误温度/错序/缺水平/反向或非有限区间/重复次要表行拒绝、三个绑定摘要改变后不输出、首次新建输出成功，以及第二次调用拒绝覆盖且已有字节不变。修改 FACTS_SHA256 仅发生在测试进程的制造 fixture 中，未修改脚本或真实 facts。

脚本直接执行标准库数据比较，不导入模型或求解器。我未解析真实 facts，也未执行真实实验比较。独立核对实际预测文件的不透明 SHA256 与已保存 `PREDICTION_LOCK.json` 一致，均为 `818b5a237d7958360f82730dc15baed0ec3a96092f51b6c9a4ffdc43457419bc`。

流程边界：脚本校验调用者传入的 `--prediction-sha256`，不会自行打开 LOCK 文件。因此正式执行必须传入上述已锁定摘要，不能重新声明一个新预测的摘要。当前实际锁定文件已经核对，代码审查不代替真正比较的结果审核，也不证明模型误差通过。

新增脚本和制造证据摘要见 `COMPARE_FREEZE.json`。原静态分析工具仍不可用，未安装或重复运行已有生产测试。
