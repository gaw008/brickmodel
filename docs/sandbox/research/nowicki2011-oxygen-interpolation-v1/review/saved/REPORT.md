# Nowicki 已锁定预测与实验对照的独立被动审核

结论：**保存结果与独立计算一致；比较完成，不代表材料验证通过。** 审核未运行或导入 CLI、模型接口、compare.py、优化器、求解器或原图提取器。

`audit_saved.py` 只用标准库读取原文件并计算，成功执行，用时 0.00406 s。原七个执行文件保持原样；原文件与来源摘要见 `AUDIT.json`，审核产物摘要见 `FREEZE.json`。

## 数值和逐点对照

以 70 位 Decimal 从两个端点直接解 `log(k)=a+b/T` 的系数，再用 Newton 迭代求立方根计算 Φ，与生产实现的浮点加权/log1p/expm1 路径不同。独立得到：

`k500 = 0.0002402786349834343237968056883374737071943078454184135949067367390528878 s^-1`。

保存值为 `0.00024027863498343442 s^-1`；八个达时的最大独立数值差 **6.0673e-13 s**，小于预登记的 1e-6 s。微分坐标率、Φ、逆温权重亦吻合，未用 500°C 作者打印常数修正插值。

| α_plot | 预测达时/s | 图示中心/s | 事前读界/s | 预测减中心/s | 在界内 |
|---:|---:|---:|---|---:|---|
| 0.1 | 143.627 | 216 | [107, 326] | -72.373 | 是 |
| 0.2 | 298.330 | 395 | [283, 506] | -96.670 | 是 |
| 0.3 | 466.525 | 575 | [464, 686] | -108.475 | 是 |
| 0.4 | 651.607 | 772 | [655, 870] | -120.393 | 否 |
| 0.5 | 858.584 | 991 | [875, 1105] | -132.416 | 否 |
| 0.6 | 1095.369 | 1231 | [1114, 1354] | -135.631 | 否 |
| 0.7 | 1375.765 | 1544 | [1400, 1660] | -168.235 | 否 |
| 0.8 | 1727.979 | 1963 | [1820, 2100] | -235.021 | 否 |

从原 facts 和保存预测重新配对、计算残差和区间关系，结果全部吻合：MAE **133.65160875873747 s**，RMSE **141.55899247376556 s**，最大绝对差 **235.0209294261565 s**，偏差 **-133.65160875873747 s**。八个中心残差均为负，3/8 落入原读取区间。

次要比较单独从原 Table 2 的 500°C/O2/0.1/shrinking_core 唯一行重算：作者拟合值 `0.0002221 s^-1`，保存预测高 **8.184887430632336%**。本审核在完成两端插值及八点时间对照后才读取此项，原结果也标记 `used_for_prediction=false`。它不与八点达时误差混合。

## 锁定和来源

- 实际 prediction SHA256、PREDICTION_LOCK、comparison 输入字段及正式比较命令的 `--prediction-sha256` 全部为 `818b5a237d7958360f82730dc15baed0ec3a96092f51b6c9a4ffdc43457419bc`。
- 原预测/比较执行记录分别保存退出 0，用时 0.043457500 s / 0.047262917 s，均在已记录的 10 s 硬限内；错误日志为空。命令路径与原输入/输出路径一致，本地 mtime 顺序为预测 → LOCK → comparison → 执行记录。
- facts、训练、metadata、原表、原 PDF、来源 registry、提取脚本与选择矢量文件的实际摘要均匹配来源绑定。生产模块身份、比较脚本及实施前代码冻结摘要也一致。facts 图身份为 Figure 2(a)、500°C、10 vol.% O2/Ar 实线，八个嵌入观察与原 facts 完整相同。
- 从保存的坐标与轴端点重新映射中心时间/α，核对中心秒取整以及区间 floor/ceil 外向取整。此处检查数值映射，不重复选择原图曲线或建立读取包络；后者由独立来源/数学审查负责。

## 结果资格

`comparison_completed` 只表示计算完成。原结果的 `material_acceptance_threshold=null`，没有创建“3/8 即通过”或合格百分比；也未宣称独立盲留出、独立重复件、实验置信区间或质量/能量验证。预测仍保留 Eq1 方向矛盾、未知反应热/耗氧为 null，以及 `experimental_validation_performed=false` 和 `full_material_package=false`。

读取包络是打印线宽、标定和输出分辨率的处理，不是实验散差。三个点在包络内不能升级成材料规律验证；五个点在界外同样不授权事后调大包络、重定参数或平移时间原点。

时间与退出码来自已保存运行记录，本地文件顺序只提供一致性证据，不是外部可信时间戳、独立运行计量或安装环境的再次认证。此审核确认当前被绑定的结果，不重新执行应用，也不将数值实现准确等同于图示实验吻合。
