# Independent provider and graphic reconstruction review

Reviewed current `src/sludge_sandbox/amadou_desorption.py`, its test file and `data/sandbox/research/amadou2006-desorption/digitize.py`. `git diff -- '*.py'` was empty because these new files were untracked, so read their full contents directly. Reviewed hashes are in RESULT.json. No source files changed by reviewer.

Provider: private Decimal80 context, shortest-decimal float policy, exact source temperatures, strict domain and returned-forward-endpoint inverse policy match documentation. Source metadata and private PDF integrity rechecked per operation. Output is source-specific with unquantified uncertainty and no material qualification. Metadata now explicitly preserves percent/fraction inconsistency and p9 nomenclature dry-basis locator recommended in SOURCE_REVIEW.md. No physics-validation assertion follows from numerical tests.

Independent execution: all28 submitted tests passed in0.25s. Three separately authored negative tests passed in0.02s: outside-repository PDF symlink rejected, ambient Decimal Inexact trap does not leak into numerical evaluation, changed metadata stops digitization before output creation. Test code and log retained. Ruff/mypy/pylint/black absent both PATH and isolated interpreter; static tool checks not performed, no dependency added.

Graphic review: independently viewed original rendered pp5–6, extracted SVG rectangles at paths250/268, eight experimental data paths253–260 and271–278, and excluded legend paths263/281. Magenta fit paths252/270 are not used as observations. Shared transform checked. Independent Fraction arithmetic reconstructs all16 marker bounding boxes, axis coordinates, full-stroke+registration interval endpoints and digitized ordinate centers. Independent100-digit mpmath exp/log computes table-coefficient curve; maximum difference from saved Decimal50 arithmetic is8.394774524733451e−51. All16 reported fitted ordinates lie in the deliberately conservative graphic envelopes; maximum central residual is0.0084769398682447641383895120709032556755783445008108668628670 kg water/kg dry solid.

[MEDIUM, resolved] Original digitizer pinned SVG only and duplicated coefficient constants without binding output to source metadata/PDF. Root added metadata SHA validation, source-asset path/hash checks, coefficients read from pinned metadata, output source assets/hash and parameter/equation node IDs. Re-read corrected code and independently traced all16 output IDs through the live provider EvidenceRegistry; bindings match. Root retained old observations before the binding change. No numerical changes resulted.

Verified completed typing cleanup for digitizer public functions; annotations do not affect arithmetic. Final digitizer SHA256 eb5b79eb27521480d510d7e629cffd9cd923456ae49966eebf04ca66aaabd1e3; independent readback rerun unchanged after annotations. No critical/high correctness or security issue identified. Graphical agreement is a reconstruction of an already fitted data set, never an independent holdout or full drying/material validation. Unknown experimental scatter and source's malformed domain remain unresolved source limitations.
