# Bounded sequential experiment evidence

Final installed implementation:53 modules match source;99 relevant installed tests pass2.93s (source2.97s). Two actual coupled/control manufactured candidates completed in31.86395s, two attempts, both child processes exited0 and were reaped. Independent unchanged original exact-arithmetic ledger audit passed both trajectories. CLI status/compare JSON equals Python API output. This is orchestration and manufactured numerical evidence, not a real-sludge firing validation or multi-generation search.

`original-evidence.zip` contains452 members; each was reopened and checked against `manifest.json` byte count/SHA. It includes preregistration, version identity, tests, original/final runs, frozen inputs, saved implementation, source assets, attempts, manifests, descriptive metrics, final verifier and review/deviation record. Canonical final run is `final-native-final/`; `saved-verification.json` records the explicit two-trajectory verification. The prior harness looked at the wrong directory and its empty loop falsely printed ledger success; that original harness, failed CLI results and the corrected `verify_saved.py` are retained. Never use the original empty-loop message as evidence.

Accepted-prefix resume dispatch is covered by synthetic orchestration test; experiment-level actual mid-candidate continuation remains a follow-up integration check. Existing checkpoint and child supervisor tests retain their own scope. Known no-launch supervisor_failed remains conservatively nonresumable. Time is admission-controlled and measured, with explicit overrun state; it is not a hard outer watchdog. No quality ranking, statistical success probability, real-material admission or full-cycle completion is claimed.
