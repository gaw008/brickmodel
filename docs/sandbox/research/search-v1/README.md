# 多代搜索实际验证

基线 c852a5e 后新增 search.py、CLI、样例和回归。当前研究范围仍为 manufactured_verification，不准入真实污泥预测；目标、约束和初温是声明的数值试验设计。

- 源码及非 editable 安装各 132 项相关测试通过，分别 5.37/5.36s；安装前后 55 个模块均与源码一致。
- 原生 CLI prepare/run/status 实际完成两代、五次求解，累计活动 wall 78.99539237499994s，小于原 220s 总预算；全部作业 completed、exit0、child_reaped。
- 父候选第一格温度 300 → 300.25 → 300.375K，第二格维持 301K。最终温差 0.6249899491094197K。每代全部候选和越界省略点、来源和父结果绑定均保存。
- 独立从原始温压快照重算目标及压力约束，核实本次所有候选均满足声明的 60000Pa 设计筛选值。该值不是材料破坏压力。本次实际改善超过冻结的 1e-8K 父候选保留门槛；不能将无条件取最小值解释为通用搜索规则。
- 五条各两步轨迹的原独立 Fraction 账本通过。CLI/Python 状态相同，逐代耗时与搜索调用累计关系通过。
- 入选案例仅将 refinement 从 0 改为 1，独立预算受监督复算四步；exit0/reaped，全部前缀账本通过。温差 0.6249899491123756K，差 2.9558577807620168e-12K，小于预登记 1e-5K；原代次下一候选差 0.12499801346228878K。此为所选案例时间离散检查，不是空间收敛、实验误差或现实材料验证。

代码审查发现并修复：浮点预算差的精确比较、损坏记录异常、负耗时抵消、最终审计及超预算覆盖原始失败；对应回归保留。验证审查指出不应无视最小改善策略，最终验证器明确检查本次实际差距超过该门槛。外层 subprocess 超时不能单独证明后代回收，本次回收结论来自六个实际终态 job 记录。

原始证据见 original-evidence.zip，manifest.json 为逐文件字节数/SHA-256，归档后重新打开逐项核验。PLAN.md 在原生运行前登记；verified.json、refinement-verification.json、安装身份、JUnit、完整运行输入/轨迹/账本和验证脚本均保留。来源文件仅沿用已允许归档的项目证据。

真实原污泥反应产物与热量闭合、自由烧结冷却、材料全周期、三个公开机制组及留出验证、全局敏感性和有依据的不确定性仍未完成。该增量不将 Goal 或 X04 的完整材料实验要求升级为 PASS。
