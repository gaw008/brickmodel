# Fixed-domain eight-cell exact event support: preregistration

Temporary research files only. No EOS, native execution, installation or repository changes by preparation agent. Root must review the runner and own the single external 840-second supervised process. Other agents own codec work; do not modify their files.

## Inputs and precisely authorized changes

Baseline is /private/tmp/brick-exact-packet-native-v1/attempt01/case.json, the actual completed four-cell exact run, not a repository default. Original bytes are retained as original-four-cell-case.json. case-grid-only-draft-not-run.json preserves the superseded only-grid/id draft; it was never executed. Final case-diff.json lists every difference: grid.cells 4→8, explicit case_id reacting-wet-free-slab-v1-exact-eight-cell-events-v1, and four integration extensive numerical values halved: Nabs 5e-13→2.5e-13 mol, Eabs 5e-9→2.5e-9 J, amount_scale 5e-5→2.5e-5 mol, energy_scale .5→.25 J. All other JSON values remain identical.

This follows the existing fixed-parent-domain contract in docs/sandbox/research/free-spatial-study-v1/PLAN.md, Physical and arithmetic scaling, and /private/tmp/brick-longer-spatial-plan-v1/PLAN.md, Transition to an actual spatial study. Actual build_case does NOT automatically scale policy by grid; it only scales nominal times by refinement. Therefore the four values are changed explicitly in the study case, not secretly overridden at runtime. These plans specify IntegrationPolicy scaling only: event N/E comparison gates, event time/T/P/stretch gates, roundoff policy and all inverse/source uncertainty budgets remain unchanged.

The original fixed global closed-energy and cumulative absolute constraint-cancellation targets remain 2e-8 J. Eight times the final per-cell Eabs equals the original four-cell aggregate 2e-8 J. An only-grid input would incorrectly make that sum 4e-8 J; that draft is not used. No gate may be widened if this experiment fails.

## Same physical initial-value problem and geometry

Keep H=.02 m, A=.01 m², two parent inventory/temperature rows, temperature step, material/source parameters, diffusivity/conductivity, phase-rate density, reaction, external pressure and free mechanics unchanged. Native builder distributes parent N/E by parent_cells/cells, so each eight-cell state has half the extensive quantities of its corresponding four-cell state. dx=H/8 and V0=A*dx. Normals clone the same parent intensive profile; the common tangent remains one scalar. Phase coefficient/interface reference area and parent bulk/additional errors scale with V0; composition weights beta/V0 change inversely, preserving q for the same parent composition. This is existing builder behavior, not new physical assumptions.

Runner requires complete source/runtime equality with the actual four-cell baseline before building, verifies the case's exact six-field diff, and saves original operator canonical inputs plus full exact source identity. After building it checks 8→4 restriction of every initial N/E entry with Fraction, normal cloning and tangent equality. Built initialization also retains its actual parent/child forward-enclosure checks. New case/source/point identities and eight comparison boxes are expected and explicitly saved; no artificial identity rebinding or copied four-cell pressure box is permitted.

## Run, costs and retained outputs

Use actual build_case and ExactFreeWaterTransfer, then integrate_exact_depletion from original .5 to .50032 seconds. Explicit ordered_affine_packet_v1 remains the same choice as the baseline. Original 512 charged panels, 100 rejections, 800-second cumulative core wall allowance, original steps/windows and nested controls remain. Exact driver explicitly records no spine reuse despite inherited requested policy; do not claim reuse happened.

The four-cell result used about170 seconds and717 host evaluations with four events. Eight cells roughly doubles per-evaluation work, but may require additional events/refinements or encounter exact shared roots. A rough ~340-second nominal estimate, with a twofold contingency ~680 seconds, fits within the unchanged800-second budget; it is not a completion guarantee. Only actual wall/panel guards and root's840-second external watchdog govern. No second native process, automatic rerun, inventory perturbation or budget increase is authorized here.

Save full research core result before success/identity assertions, including failures, all accepted states/ledgers, every speculative terminal/refinement/frame, observation vectors, paired certificates, correction totals, actual costs and source/time/policy metadata. Capture uses frozen operator identity to avoid losing source-failure records during serialization. A rejected event or exact coincidence is valid captured evidence; process success means capture completed, not that the scientific core completed. Runner stores structured exceptions and runtime-after in finally and verifies both source/output case copies remain unchanged.

## Acceptance and limits

After termination, run separately reviewed saved-data audits for every accepted prefix, every packet's two consecutive refinement passes plus independent pre-first-event grid/dual controls, each event/common six-gate comparison, source/domain-bound paired certificates, and all-prefix closed energy/constraint targets2e-8 J. Do not reuse a four-cell-hardcoded audit without adapting its input mapping and validating its source/domain assumptions. Retain every failed or partial output.

Success establishes actual eight-cell event-path support for this explicit manufactured model at this short fixed horizon. It does not establish spatial convergence, temporal convergence across grids, material qualification, service checkpoint/resume, full firing/cooling, or resolution of the original15-microsecond spatial FAIL. Same-parent children may remain exactly coincident; the current driver may correctly refuse rather than invent an ordering. No branch may force tie breaking, change physical initial data or waive original gates.
