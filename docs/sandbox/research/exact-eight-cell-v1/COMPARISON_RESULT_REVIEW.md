# Eight-cell saved comparison audit result

Executed after parent-confirmed native termination. Command:

```
PYTHONPATH=src /private/tmp/brick-water-backend-probe/venv/bin/python /private/tmp/brick-exact-eight-cell-v1/audit_comparisons.py > /private/tmp/brick-exact-eight-cell-v1/comparison-audit.log 2>&1
```

Actual exit 0, approximately 3.58 seconds; no EOS, model rebuilding or native rerun. Auditor SHA256 a6a331102323735ae4f29fbdbeaf5eefff0989a1b4cf849624fd832d305baa0a. Core-result SHA256 c031c849d9344ea912fc711d11489e246e98abf80c207db38cc8b3d800a9febb. Exact input hashes are saved in comparison-audit.json and all inputs remained unchanged.

The core completed. Both committed packets, containing eight events in total, match selected terminal ledgers/states and modes. All eight refinement records are retained individually. Six comparisons were independently recomputed from saved observation/state arrays and numerical root localization remainders. Each selected packet has two consecutive passing terminal refinements and the additional passing independent approach with both controls halved and a distinct grid before the first terminal event. Packet-wide maxima are checked against the individual event/common comparison rows, not treated as separate per-event measurements.

384 cell-level paired-pressure certificates were recomputed through the existing pure data auditor. Their 528 retained endpoint evaluations match the core's completed and attempted endpoint counters exactly. Every comparison uses all eight actual boxes bound to saved point/reference/domain/backend and canonical operator inputs. The case differs from the original four-cell input only in declared id/grid and four halved extensive integration numerical values; event gates are unchanged. Initial eight-to-four N/E restriction and normal/tangent inheritance are checked independently using exact Fraction sums.

This is comparison/source-domain and committed-association evidence. Parent separately audits every accepted prefix and global energy/constraint accounting. Omitted live inverse trees are not reconstructed. Eight-cell support over this short manufactured-model horizon does not establish spatial convergence, real-sludge qualification, full-cycle validity or resume support. Original spatial-failure evidence remains unchanged.
