# Full saved affine panel replay review

APPROVE the bounded replay and a separately supervised actual host point-evaluation probe, subject to explicit state/source/mode binding below. No evidence here establishes an accepted coupled event or new trajectory. Reviewer read replay.py/result.json and independently used standard-library Fraction arithmetic on saved data; no EOS or solver evaluation was run.

Verified result.source_sha256 against the complete original native failure JSON. The replay uses its complete failed-panel initial state, full start/midpoint Rates, original integration and roundoff policies and original speculative totals. Only the numerical root endpoint changes. Domain upper is the next original binary64 clock neighbor, already the old surrogate root enclosure; this is not an arbitrary larger physical interval. The chosen exact endpoint is after the actual midpoint. Root ordering excludes the two still-wet later cells through full-domain positive polynomial minima, and keeps the selected cell 2.

Independently recomputed every saved face-species, face-energy, species-source, total-cell-power, individual power-component and mechanical increment from h*r0+(rm-r0)*h²/(2*hm), checking array lengths and each once-rounded value. All match result.json. Original totals_before and correction policy match the failure record. Full represented-ledger residuals are 3.674656642841912e-17 mol and 6.898538942036453e-12 J, within original gates. These are represented-ledger/state residuals, not independent full-RHS or global truncation bounds.

Selected raw liquid is 1.4431058195930652e-20 mol; local gross evaporation is 1.5641070637888122e-12 mol; ratio 9.226387713494241e-9 passes the unchanged 1e-8 gate. Other inventories, E and stretches are preserved through paired writeback. All four full mechanical normal values plus the one common tangent survive. This is the actual full saved state, unlike the earlier selected-cell sentinel fixture.

Required next native point-probe conditions:
- Restore the original physical case and explicitly verify actual implementation/source bindings. Any deliberate energy-identity rebinding for changed implementation must be documented and preserve N/E/stretch bytes; do not silently treat an old digest as current.
- The input already contains a previous speculative dry cell 3. The point probe must reconstruct interface modes with cells 3 and 2 dry and cells 0 and 1 wet. A fresh all-wet operator with only cell 2 switched is not the matching model and will refuse zero liquid in cell 3.
- Evaluate at the exact semantic endpoint through the reviewed autonomous entry, without converting it to displayed binary64 time. No program schedule is involved in this direct host.
- Save source identities, actual modes, exact time, input/output and failure before assertions. Domain/inverse/kinetic rejection remains a legitimate outcome; current saved-data checks cannot predict source-admitted native evaluation.
- totals_before.events=1 and totals_after.events=2 describe speculative writebacks. Neither event was globally accepted by the failed original run. The new point evaluation must remain explicitly uncommitted and cannot advance a service checkpoint or assert packet completion.

Evidence limitations: replay.py saves its result only after assertions and uses live temporary candidate module files without recording their SHA in result.json. Its present successful output is reviewable against current files, but this script is not a general failure-preserving runner or immutable runtime manifest. Preserve script/candidate hashes when archiving, and use the existing bounded failure-recording native supervisor for the next probe. The original source-derived observation fields are reused, not regenerated; no two-refinement/common-time/global-prefix event acceptance has occurred.
