# Exact-time direct physical host

Baseline 1717de9. FreeSolidSlab and WaterPhaseTransfer now share checked state-only evaluation bodies with their unchanged float entry points. ExactFreeWaterTransfer accepts only the explicitly autonomous direct pair; it does not convert semantic time to a display float or admit programmed hosts. Original energy identity, interface precheck and source/physical equations remain.

Frozen source: free_solid_slab.py 45da0f0103514a88bda869fe2f2d54d0c2fd52fba4be6e9fc20d3a25baf359a0; water_phase_transfer.py 650dc159355b299f53d0c6a98a08a5d68cfc60a81f3d2379225335066c63b48e; exact_free_host.py 1153884bad187c94d156c351a85aacb11983d9bff346c02ceba4d64277bb3918.

Review found the initial adapter missed independent thermal water backend identities. Four actual failing binding tests reproduced this; final nine candidate tests pass. The corrected binding includes every actual provider slot, its concrete type and full implementation descriptor, alongside the full operator input digest. Both evaluation boundaries are checked. Original candidate and failed tests remain in the archive; earlier HANDOFF hashes describe that superseded candidate.

Root source: 38 passed in 2.94 s. Noneditable installed: 38 passed in 3.01 s. Suites: exact_free_host, free_solid_slab, programmed_free_solid_slab, exact_affine_depletion. All 72 installed modules match source. These tests include dry-host legacy/programmed regressions and instrumented binding refusal, not wet trajectory proof.

Actual native-attempt01: supervision terminal exit1 after 1.9112 s. First old-host evaluation succeeded but generic encode refused embedded HEOSCandidate; the exact call had not occurred. No physical mismatch was established. Complete failure report and old runner preserved.

Corrected research capture recursively preserves ALL evaluation dataclass numerical fields, and encodes actual provider objects with explicit concrete type, physical/source canonical descriptors and implementation descriptors. It does not serialize native cache internals. The original generic production codec is unchanged.

Actual native-attempt02: supervision terminal exit0 after 2.2378 s, runner 1.9258 s, external limit 45 s. Build used byte-identical original four-cell case and initial state. Old float call at .5 and exact autonomous call at 10^12+10^-10 returned byte-identical complete captures (1,079,815 bytes each). Runtime/source binding remained unchanged; the original input and final captured case bytes were checked. This far translated query is valid only because the admitted host is autonomous. Research runner SHA ab53d1b884c30e50588b6b6acfd0b3657304be4d99b1c9ddef90d2bc36926477.

This is point parity using an actual water backend and manufactured solid, not new ordered event success, spatial convergence, material admission or full firing validation. The original four-cell ordered event failure remains open. The next bridge must carry exact time through whole-state terminal panels, competing root ordering, mixed-mode evaluation, comparison, atomic commit and explicit records/resume. Programmed hosts require exact boundary forcing separately.
