# 同一 1 s 工况的预登记 1/2/4 时间加密比较

2026-09-12；仅新增 steps=2 和 steps=4 两次独立实际计算，当前文件为未执行计划。原 native01/1 步、原 40/40/80/100 s 预算及其 128 项接受结果只读保留；不得覆盖或修改为本计划预算。原实测初始化 19.11 s、积分 21.51 s、driver 42.80 s、监督 48.09 s 是本次资源估算依据。

## 固定物理与执行

逐档直接复用原 native_case.make_case：两格、总时长 1 s、同一原初库存/完整 U、温度/载气/几何/源定义/选择性蒸汽与热浴。每档都实际重新构造并完整执行零时刻初始化，不复用前一档的已投影状态。保留原 flash 参数和所有原 T325–338 K、P±误差90–110 kPa、全 U1e−5 J、μ1e−5 J/mol、peq−pv1e−5 Pa、库存与来源门；固定组成 inverse T≤1e−8 K、原累计算术费用 E≤1e−8 J/N≤1e−12 mol 不改。

仅步数改变为 2、4（步长 .5、.25 s）。每档显式新预算：初始化 40 s、积分 100 s、driver 150 s、既有原进程组监督 165 s、清理宽限 5 s。新预算属于基于已测成本的新加密试验，不追改旧失败/结果或物理门。串行最多两次；任一失败即保存原失败、退出且不开始下一档；不重试、不自调参数。原 supervisor 只约束原进程组，不能声称包含逃逸 session；整次底层 EOS 仅返回后检查内部时钟，外部监督保持原局限。

开始前核原保存 INPUTS/ACCEPTANCE/RUN/INITIALIZATION/INITIAL 及监督 metadata/status SHA；按原监督 inputs_before 核同一物理源码、安装包、数值栈、来源/原件和 native_case 字节。新 supervisor 再冻结这些输入与本目录计划/脚本，逐档结束继续核未变。任一差异停止，不用新物性替换旧基线进行公平比较。输出目录与 series 启动记录独占创建，已有结果拒绝覆盖。

## 每档完整接受与保存

复用原 native_driver 的实际逐格接受算法，步数相关 shape 扩为 n+1 个时刻/状态、n 个观察和 n 个动态账本，另有完整初始化账本。全部 n+1 个账本均验证共享面、精确 Nt/U、同 J、载气、两水投影、完整 μ/pv 来源对应、条件固定组成温度界、非负库存和域。累计费用从初始化继承一次，并逐步累加实际面/投影费。

保存原初构造前缀、来源 provenance、完整初始化和 RUN、全部接受检查或具名失败。另输出逐步 boundary 水/Q/H/Eout/分解残差，累计值从全部动态 ledger 求和；不能只取最后一步。Eout 为带符号外面 energy_j，Q 为入列热、H 为出列蒸气携焓，因此累计 ΔU = −Eout + ΣδU = Q−H−分解残差+ΣδU。总体水变化同理须含全部带符号投影。初始零时刻账本不计为 1 s 外排通量。

## 预指定比较；无新增事后门

主输出固定为累计出水 mol 与两格末态 T/K、Nc/mol、Nv/mol、P/Pa、U/J。辅助输出为累计 Q、H、带符号 Eout、分解残差及守恒和费用。由保存的 1/2/4 原始完整 RUN 求每项三值、带符号 d12=x1−x2 与 d24=x2−x4。

只有三档均完成并通过原接受、原物性/输入一致、初态与初始化一致、两相邻差均非零且同向、两差均大于下述已声明必要分辨诊断时，才输出描述性 r=|d12/d24| 和 p=log2(r)。r≤1 仍保留非收缩/非正名义 p，不将其当通过。差为零、异向或必要分辨不足则 p=null，保存明确原因与原始差。浮点投影层的差以 Fraction 精确求得，报告 p 仍是名义浮点诊断，不计算 Richardson 外推或“真值误差”。

每对的必要分辨诊断为 max(8 个各保存标量 ULP 之和、该项以下已存条件诊断之和)：T 同时列出固定组成 T 界与名义 flash T 括区宽；Nc/Nv 列出名义组成括区宽及实际库存投影；P 列出原点 pressure_error_pa；U 列出固定组成 |U残差|+点能量误差；累计出水仅列出累计边界水投影费。它们不组成全平衡误差充分界，也不授予相态误差证书。尤其温度固定组成界不是整体平衡误差，名义组成括区不是全组成误差，累计通量未量化物性/组成传播误差仍 unknown。诊断足够仅允许描述性趋势，不等于已证明时间收敛；空间、模型/材料误差与全周期仍未验证。material_qualified=false、training_eligible=false。

实施检查只用已保存 JSON / 制造数据、help、AST、拒绝覆盖/输入检查；不构造 EOS。实际两档仅由 ROOT 在计划/脚本审查冻结后启动。
