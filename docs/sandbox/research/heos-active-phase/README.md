# 安装 HEOS 的活动相变与规定变形验证

生产源码固定2b1d07f，本轮不修改物理内核。41个实际site-packages模块与源码逐字节相同，原callback与新的积分通过tests helper调用这些安装模块，加载路径和版本保留。材料、相变系数、机械参数仍明确制造测试，不能当污泥事实。

## 原回调

callback-attempt01 exit0/1.956049625秒。原fixture正确列序为[fixture_solid,H2O,H2O_liquid,fixture]，初值[2,1e-8,1e-4,.001]，t=.5、T300K。三物性构造器均显式HEOS，初蒸发速率.00035312221952942636mol/s；原peq1e-6Pa/rate1e-13mol/s门槛、Decimal/Fraction独立算术、一次逆解、当前几何、液汽精确±及无重复潜热全部通过。审核早期口头列序错误已纠正，实际脚本和检查一直使用正确layout。

## 活动与关闭相变的两条轨迹

在执行前另存INTEGRATION_PLAN.json，原PLAN保留callback绑定字节。两次独立运行同初始N/E/模型身份、材料、运动、环境、t=.5→.51，唯一物理差为K1e-7与K0。原相变测试精度rel1e-7、N1e-10mol、E1e-5J和逆解1e-5J/1e-4K保持；maxdt.005、max10steps/rejects、25秒积分/30秒监督。

active/disabled实际exit0，外部7.997904459/7.930239292秒，各15eval/2接受步。每prefix独立Fraction核对全N与能量边界/机械功、总水1e-11mol、固定固体/载气、液汽反应精确对称；K0每步反应全0。能量随压缩/界面/黏性功变化，未错误要求恒定；相变不额外加潜热。

同初态对照：活跃汽增3.4788077006184417e-6mol，活跃末T299.9986799403654K，关闭相变末T300.0001210171116K；相对降低.0014410767462322838K通过预登记1e-4K。末正汽压下亲和力8782.243610712829J/mol及局部熵产生.010031773299899526W/K独立重算通过。它只是局部相变耗散，不是全系统熵证书。K0比较验证机制反馈，不是独立轨迹真值；固定库存熵oracle不适用于这里。

## 时间细化与尚未通过项

REFINEMENT_PLAN在细化前登记两次减半及两最细N/E/T/P门槛。fine(.0025s)完成5实际接受步/36eval，积分14.562465375秒、外部16.49707725秒，全部prefix原门槛通过；多出的最后一步长2.220446049250313e-16秒，名义4步不能替代实际次数。

finer(.00125s)实际失败，积分wall_time_limit25.052022083秒，外部26.543135791秒，62eval/8接受，停在.5099999999999998，离终点尚差2.22e-16秒。原25/30限制未放宽，未覆盖状态/终点，也未执行三完整结果才允许的refinement_compare.py；三尺度检查未完成。

clock_reproduction.py在同安装内核用无EOS解析常库存复现：maxsteps8在57eval后因step上限停止；maxsteps10用64eval/9步完成，末步正好2ulp(.51)，所有N/E保持解析常量。这定位了累加舍入尾步的额外成本，不代表更细物理终点已通过。当前控制器的尾区间合并只覆盖更小的舍入情况，后续需有依据地改善时钟推进，完整积分剩余通量/功并验证breakpoints/maxstep语义，禁止直接snap状态。

独立审查、实际监督日志、原脚本/计划、细化失败、安装身份和零EOS复现全部保留。本证据不含耗尽、全程10%变形、空间输运收敛、真实污泥材料、完整干燥烧结冷却或外部实验验证。全Goal仍需这些必需项与CLI/UI/多代搜索。
