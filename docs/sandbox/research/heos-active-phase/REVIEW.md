# Stage9 acceptance prereview

Read tests/sandbox/test_water_phase_transfer.py, test_solid_fluid_heat.py and archived source-matched-wet-callback-02/child.py. No EOS/tests or source edits. This establishes review conditions, not approval of an unreviewed run.

Preserve the actual deforming fixture and explicit InventoryLayout: ('fixture_solid','H2O','H2O_liquid','fixture'), N=[2,1e-8,1e-4,.001]. Solid is column0, vapor1, liquid2, carrier3. The rigid phase-transfer test's different layout must not be copied. Same prescribed0..1 s motion, time.5 start, physical skeleton including nonzero viscosity3 Pa s, current geometry, original inverse1e-5 J/1e-4 K and exact backend/source matching. Public HEOS loader, IdealWaterVapor and WaterChemicalPotential must all carry the same verified HYBRID descriptor; record actual installed paths/versions/source hashes.

Callback acceptance should preserve original independent arithmetic: one total inverse; current storage/thermal inverse object reuse and exact current geometry; reconstruct equilibrium pressure from liquid mu and ideal-vapor standard-pressure mu with60-digit Decimal exponential, not callback peq/equilibrium helper output. Compute actual vapor pressure from exact Fraction(Nv)*R*T/Vg and rate from Fraction(K)*(peq-pv). Original gates: peq error<=1e-6 Pa, rate<=1e-13 mol/s and positive initial rate. Reaction difference is exactly -r on liquid,+r on vapor and zero on solid/carrier; exact pair sum zero. No duplicate latent heat: phase wrapper must retain base cell power and each of elastic/interface/dissipation/pore/body components unchanged. Preserve tag and current-pore/current-face assertions.

This callback reconstruction independently assembles the kinetic equation, but shares the chemistry/native-provider subcomponents. It is not a fully independent EOS or kinetic constitutive validation. If a separate Python chemical comparison is added, record it separately with preregistered bounds; do not call the tested equilibrium helper its own oracle. Save all mu/P0/R/T/Nv/Vg/K operands so Decimal/Fraction checks can be repeated offline. For positive vapor and nonzero rate, save chemical affinity and entropy production; recompute r*R*log(peq/pv) and check nonnegative finite result. This is local production, not a total trajectory entropy certificate.

Proposed .5->.51 s integration is a new moving-host active-transfer experiment, not the original sealed rigid test. Keep stated relative1e-7, amount1e-10 mol, energy1e-5 J and original inverse1e-5 J/1e-4 K; record .005 s step cap, max10 steps/rejects and25/30 s resource caps. They differ explicitly from the original rigid .01 step and90 s budget. Do not force exactly two accepted steps if adaptivity rejects/refines; require actual completion at .51 and report all counts/times.

For every accepted prefix reconstruct exact Fraction energy change against cumulative face energy and cell work, including all five components. This moving viscous system must not assert constant energy or zero work from the rigid test. Reconstruct all species from face/reaction ledgers; H2O liquid+vapor conserved (retain original1e-11 mol total-water gate), carrier and solid fixed, positive/nonnegative inventories and no clipping. Each phase reaction pair must be exact opposite and create no separate cell heat. Integrator energy absolute tolerance1e-5 is not by itself a physical model error bound. Record component quadrature roundoff and closure residuals rather than hiding cancellation.

The K=0 run should start from the same actual N/E/tag at the same time and same motion/host, changing only K. It is a mechanism-off comparison, not independent truth. Expect exactly zero phase source and unchanged phase inventories; verify the disabled path bypasses chemical evaluation where tested. Do not require active and disabled final energies or mechanical work to agree: phase changes alter T/P and hence work. Initial evaporation is expected from callback driving pressure; endpoint temperature decrease relative to the initial300 K is not guaranteed under imposed compression, so the rigid adiabatic sign assertion is not transferable. Any cooling-versus-K0 statement must use actual matched results, not assume it.

The fixed-inventory entropy oracle used in stages6-8 is invalid as an active-transfer truth model. Do not reuse it. Success of the nonzeroK and K0 runs with exact ledgers demonstrates bounded implementation consistency and an observed mechanism difference, not independent active-trajectory accuracy, physical calibration, full one-second wet motion, phase depletion or free sintering. Each run needs unique terminal/JSON evidence and unchanged source input hashes; retain every failure and do not loosen gates to complete.

## Actual installed callback audit

Callback-attempt01 complete/exit0 in1.956049625 s; all before/after/current input hashes agree. Recomputed60-digit Decimal equilibrium pressure from saved native chemical operands:3531.520077681101 Pa, equal to callback and independent fields. Recomputed Fraction vapor pressure and rate using Nv=1e-8 mol and K=1e-7 mol/s/Pa: rate0.00035312221952942636 mol/s equals recorded rate, positive and within original1e-13 gate. Saved reaction pair is exactly opposite, fixed columns zero. Descriptor full-envelope hash matches propagated source asset. Original inverse1e-5 J/1e-4 K remains. One-inverse/current-geometry/power/component assertions are retained in executed script; saved result supports only the declared single callback. Result SHA07bae2f610085d720c73559afe865746d9741bcb5b62f264274b207c63e7e0f1.

## Integration-script prereview

Active/disabled scripts share exact physical construction and differ only MODE; MODE selects K=0 for disabled. Same installed-module check and backend on all ingredients, same N/T/time/motion and source identity. Integration uses the preregistered original accuracy with .005 max step,25 s integration/30 s supervisor and10 steps/rejections. Exact Fraction prefix energy/species accounting, total-water1e-11 mol, zero boundary fluxes and fixed solid/carrier checks are appropriate for the moving host. Runner binds distinct integration plan without editing callback's original PLAN hash.

Minor evidence fixes requested before execution: derive result attempt_id from MODE rather than labeling disabled as active; explicitly assert disabled reaction ledgers and final rate are zero; compare final five component arrays individually to base as well as total power. The active-vs-disabled temperature difference gate is in INTEGRATION_PLAN but cannot run until both results exist; a subsequent comparison must require exact same initial N/E/tag and enforce that gate explicitly. It remains a mechanism counterfactual, not independent trajectory truth. No source physics issue found requiring a different integration setup.

Follow-up: inspected the frozen source after all requested fixes; mode-specific attempt IDs, disabled zero reactions/rate and per-component base equality are now present. Complete final CellWaterTransfer is saved. Active/disabled files remain byte-identical except MODE. **Approve this bounded execution setup**, not its pending outcome. Frozen SHA: active.py11950691cd0139b7d4eb5708056c7ff9a0402af8e68c2b35e04747be40538ca1; disabled.py4d8d7b26bdb2f45381483cc7839b6ec9b62509b3453d42c13bff85793124f103; INTEGRATION_PLAN.json99dadcd46938342559a05e5c9f602ca9de0db5f65aa14577e0234c2c47e11b51.

## Completed active/disabled integration audit

Approve bounded implementation/ledger and mechanism-comparison evidence. Active and disabled supervisor attempts both complete/exit0,7.997904459 and7.930239292 s,15 evaluations/two accepted steps each. All recorded before/after/current input hashes match. Reviewer ran only independent_audit.py, a stdlib saved-data audit, not EOS. Its full output is independent-audit.json.

Reconstructed every accepted-prefix energy/inventory/water-sum residual with exact Fraction arithmetic and matched the saved audits. Largest absolute energy residual is1.76307e-11 J, below1e-5 J; active water-sum residual magnitude<=6.02519e-21 mol, below1e-11 mol. Exact reaction pairs are opposite, solid/carrier sources zero, boundary species/energy zero; all inventories nonnegative and fixed solid/carrier unchanged. Disabled reaction ledgers and final rate are exactly zero. Five work component sets and summed-component roundoff magnitudes match recorded values and accumulated bound. Cell-power/component equality to base is preserved in the executed assertions; only resulting arrays are saved, so no separate independent saved-base-array comparison is claimed.

Compared both runs' exact initial objects, policy/inverse/layout/implementation/versions and common endpoint. K0 keeps all inventories unchanged. Active vapor increases3.4788077006184417e-6 mol with corresponding liquid loss. Saved disabled-minus-active final T is0.0014410767462322838 K, independently recomputed and exceeding the preregistered1e-4 K gate. comparison.json hashes match both result files. This is a mechanism-off counterfactual, not independent trajectory accuracy.

Final active affinity recomputed from R*T*log(peq/pv) is8782.243610712829 J/mol. Fraction reconstruction of K*(peq-pv) and rate*affinity/T exactly matches saved rate and positive entropy production0.010031773299899526 W/K. This checks the local constitutive diagnostic, not integrated total entropy. Disabled chemical fields are absent/None as expected. No real material, depletion or full wet-motion admission follows.

Refinement prereview: fine.py versus active.py and finer.py versus fine.py differ only initial/max step (.005->.0025->.00125), result path and attempt label. Physical setup, original accuracy and25/30 s caps remain. REFINEMENT_PLAN records adjacent terminal differences/ratios and requires the two finest N/E/T/P comparisons within1e-10 mol/1e-5 J/1e-4 K/.2 Pa before claiming step-size consistency. This is appropriate limited wording, not proof of precise convergence order or independent accuracy. Ensure each refinement supervisor binds this new plan explicitly; the original integration runner bound only PLAN and INTEGRATION_PLAN. Actual fine/finer results are not yet reviewed here.

## Actual refinement outcome: finest run incomplete

Reviewed run-refinement.py explicitly binding REFINEMENT_PLAN.json. Both refinement attempts' before/after/current input hashes match; no original audit script/result was modified. Fine completed/exit0 in16.497077250 s, integrator14.562465375 s,36 evaluations,five accepted steps,zero rejects. Nominal .0025 s stepping left an additional2.220446049250313e-16 s tail, so reporting four actual steps would be incorrect. Independently reconstructed all five saved energy/amount/water-prefix Fraction residuals and exact phase pairs; all original gates pass. Maximum absolute energy residual2.24349e-11 J. New saved-number audit is refinement-audit.json.

Finer failed/exit1 in26.543135791 s, integrator resource_limit/wall_time_limit after25.052022083 s,62 evaluations,eight accepted steps,zero rejects. Last accepted time.5099999999999998 differs from requested.51 by2.220446049250313e-16 s. The incomplete tail trial did not become an accepted endpoint; no endpoint comparison or successful third scale may be inferred. refinement_compare.py was not executed and this review does not certify three-scale consistency. Keep failure and resource limits unchanged.

Reviewed clock_reproduction.py and saved zero-EOS output: inert rates with analytically fixed N=1/E=100, same .5->.51 interval/.00125 nominal step. cap8 reports accepted_step_limit with57 evaluations/eight steps; cap10 completes with64 evaluations/nine steps. Independently reproduced all first eight timestamps by ordinary floating addition and verified the extra tail equals2*ulp(.51). The script asserts invariant states. This supports a clock-roundoff cause for the extra tail, not a license to extrapolate the failed scientific endpoint or a demonstrated physical accuracy result. Any time-controller fix requires a separate scoped review/evidence cycle; none was applied here.
