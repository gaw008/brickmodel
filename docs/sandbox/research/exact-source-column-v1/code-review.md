# Exact source column adapter independent code review

Reviewed production adapter and complete supplied tests. Fluid state contains exactly liquid/O2/N2/H2O mol slots; dry kg is bound into immutable source storage identities and total U. No dry mass is reinterpreted as mol. Pack/unpack require energy identity, shape and absent mechanics; wet/dry mode changes preserve stored energy identity but change operator binding. Actual face energy and fluid molar flux map directly; phase transfer occupies paired liquid/vapor local-source entries, not chemical heat. Original detailed source evaluation retained. Exact program time/breakpoints forward without float conversion. Old mechanical/free depletion drivers are untouched.

Found incomplete explicit chemical refusal: zero solid/gas rates alone allowed nonzero chemical_reference_power_w or phase_transfer_included=True to be silently ignored by new Rates cell_power=0. Actual code probe alters only original returned chemistry metadata, without bypassing real source-column evaluation. It proves both cases accepted. Production ReactionDisabled currently always returns supported zero metadata, so this is an adapter guard contract defect, not demonstrated current-path physical regression. Root asked to reject unsupported metadata/type/layout, not add chemical reference power as new heat.

Independent RED:2failed/2passed1.17s; two chemical cases fail expected rejection, changed fixed dry mass correctly rejects old packed energy identity, actual N3 shared face/phase derivative reconstruction passes. Saved probe and RED.log. Final approval pending guard fix/freeze; no EOS or broad suite execution.

Post-fix review: adapter now requires exact DisabledChemicalRates, exact tuple layouts matching fixed-solid and gas inventories, Fraction zero in every solid/gas/reference-power field and phase_transfer_included is False. This closes both demonstrated metadata drop cases without introducing a chemical heat source. Four reviewer tests actually pass; GREEN.log retains actual duration. Original RED remains intact. Both final Python files parse; no lint-clean claim or native/full-suite execution. No unresolved critical/high functional issue found. Reviewed hashes recorded, pending author final manifest match.

Author final manifest and both current file hashes match reviewed bytes. Final scoped approval confirmed. Reviewer actual rerun4passed1.13s, not the author12-test execution.

Callback boundary follow-up: initial error translation wrongly assumed DomainExit inherits IntegrationError; actual reviewer negative confirmed it was reclassified, and author corrected explicit except(IntegrationError,DomainExit). Reviewer first log also contains an incorrect reviewer expectation of failed rather than the actual numerical_failure status; fixed only reviewer test expectation. CALLBACK_FIRST.log retained. Final two independent checks2passed0.49s: direct evaluate preserves raw failure object, callback wraps ValueError with original cause and integrates as numerical_failure with initial prefix, DomainExit object is unchanged and integrates as domain_exit. No prior suite repeated. New final two-file manifest checked and FINAL_FREEZE.json refreshed.
