# Source run service: final independent code review

Verdict: **APPROVE** the 20 frozen implementation/test/case files listed in `CODE_REVIEW_FINAL.json`. No unresolved concrete HIGH or CRITICAL finding remains. Reviewer changed only this private review directory and performed no EOS call, native model run, installation or production edit.

Scope covers the observer and its four hooks, shared source failure-status admission, explicit configuration/builder, raw journal, execution service, and source CLI/read/export/replay/resume/supervisor routing. The paired pressure mathematics and old numerical integrators were not reimplemented or re-certified by this review. The unchanged source assumptions, budgets and event gates still apply.

The two real runtime statuses previously rejected by the passive codec now round-trip with their original `unsupported` and `numerical_failure` labels. The second wet-request cancellation no longer associates the previous request's returned state. Actual missing-manifest HEOS wrapper preflight writes `heos_started` and `heos_failed` with all six uninitialized fields explicit; this independent check made zero kernel constructors and zero EOS calls. Nonfinite raw values remain tagged diagnostic evidence rather than accepted numeric observations.

Occurrence counters and returned RHS values, initial points, candidates, transitions and returned stage roots are retained before journal writes. A subsequent journal failure retains an explicit undurable-return diagnostic, and failed observation slots remain failures. Initial-energy failure notification now preserves the original DomainExit and stores notification failure separately. The passive service preserves missing-input failures with an explicitly unavailable original case. RHS guards bind failure notifications to the active request rather than the preceding successful capture.

Three independently resealed-summary counterexamples were closed: failure records cannot gain numeric acceptance flags, changing/deleting the source kind cannot bypass validation of the fixed source artifact, and a failed execution cannot be relabeled completed. The only permitted final status differences are explicit failure downgrades; unchanged status also binds the original reason. Canonical publication failure clears unaudited acceptance flags. The final independent status probe rejected the same altered saved run with `source_run_terminal_status_changed`.

Configuration is closed and byte-bounded, all 17 assets have fixed path/size/hash bindings, each of the three storage/volume objects is distinct, and every declared scenario/policy field is consumed by the builder or controls. Source molar mass is read from the actual provider reference. Configuration validation and builder construction tests do not masquerade as initialized physical states. The other independent configuration review and its 44-test evidence were read; no redundant constructor/test suite was run here.

The shared reader invokes the source codec and correspondence verifier. Export identifies the passive record and omits private assets/events from its advertised bundle. Replay reconstructs a fresh run only under the frozen input/runtime contract; source resume is explicitly rejected. The supervisor routes a fixed source child, propagates its explicit asset root and existing cancellation/termination contract, and retains ordinary child reaping semantics.

Validation evidence is enumerated in JSON without summing overlapping suites. Saved author runs read here include service 12/16 tests, shared routes 56 tests, six targeted fixes, the last status fix, and configuration 44 tests. The observer's 61-test log was read and its final wet-cancellation line was checked independently. Reviewer probes preserve original RED runs and distinct GREEN outputs; the original first observer/service probe had only its pre-mutation observation summary preserved, as documented in its scope note. No full test suite or physical experiment was rerun by this reviewer.

The acceptance checker is a separate artifact: its first draft still needed an actual shared `read_run` call to fulfill its plan. Production approval does not claim that installed/native acceptance has already succeeded. The subsequent native run must use the registered default case, original science gates and fixed budgets, preserve failure, and perform no automatic retry. Material qualification, full firing cycles and the full Goal remain unfinished.

## Review Summary

| Severity | Count | Status |
|----------|-------|--------|
| CRITICAL | 0 | pass |
| HIGH | 0 | pass |
| MEDIUM | 0 | pass |
| LOW | 0 | pass |

Verdict: APPROVE — all concrete production findings identified in this review are closed for the recorded bytes.

Checker closure: `RUNNER_REVIEW_FINAL.md/json` approves the final checker/plan after adding actual shared `read_run` and failure output. All 20 production/test/case hashes remained unchanged. This does not assert future native success.
