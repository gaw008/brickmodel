# Installed native source service: final evidence review

**APPROVE** the saved execution and its stated software scope. No concrete unresolved discrepancy was found. Review used only standard-library file/JSON/hash checks of the existing run and event files; no model, EOS, native constructor, test suite or complete formal-record decoder was run. No private source text was copied into a public artifact.

The owned child completed once, returned exit code 0 and was reaped without hard termination. Service elapsed time is **64.29800774995238 s**; the supervisor's complete interval is **69.38390112499474 s**. These are distinct measurements. The actual record has 4 HEOS wrapper starts, 4 kernel returns and 4 complete wrapper returns; 3 initial-energy starts/returns; **32/32 RHS starts/returns**; and **8/8 extra wet-pressure starts/returns**. Kernel-return counters do not count every internal EOS operation or independently remeasure reference anchors.

All **111 events, 9,742,925 bytes** match their manifest hashes and consecutive ordinals. The independent event pass paired every source/wet return with its original request, checked N3 state shapes, exact source times/operator identities, all three cell observations and actual WaterState temperature/pressure/phase inputs. Sixteen RHS evaluations retain three wet modes; sixteen retain the original mixed mode with only cell 1 depleted. The original seed's numerical-failure status remains in the formal inspection; no failed callback event or missing actual return was silently relabeled successful. Two dry candidates and one transition return remain recorded.

All **17 private input assets** are exactly the case-declared paths, sizes and hashes. The original default case hash remains `323cdc6ebbd866c81e87ac1227155357253f0b05f2384c55fadead85218c2dc5`. The full run file inventory matches its manifest; all 20 previously reviewed source/test/case hashes remain unchanged. Frozen runtime modules agree with the saved implementation copy. All **143 Python modules / 150 installed package files** match their source files and the recorded installation identity. Saved installed-test XML reports **286 passed, zero failures/errors/skips, 202.949 s**; tests were not rerun here.

The saved passive checker is `verified`, took **9.560493750032037 s**, and retained no prohibited physical-entrypoint attempt. Its canonical record hash is `03900a0e9390dc3c06347ebc14185521784d29be8a640239e8457b4cf87ebf54`. Its original inspection reports 32 complete observations, no failed/unreturned capture slots, three completed saved-reference replays and seven cumulative transition balance rows. This review checked those saved results and hash bindings without repeating the complete decoder or arithmetic audit.

The actual final state is **conditional_numerical_event_accepted**, at reported end **1.0937596265580707e-6 s**. Both original reported endpoint P gates and both original conditional P gates remain false; all original per-cell conditional P gates remain false. All six pressure gates selected through the explicit shared wet/dry-volume strategy are true. N/U/T gates remain true. This is conditional numerical acceptance under the declared model and error assumptions; it is not a claim that the original independent pressure checks passed.

Material qualification, training eligibility, resume authorization and full-firing-cycle flags remain false. All 753 repeated raw `material_qualified` fields encountered in the saved DAGs are false. The formal passive audit still explicitly does not verify live storage identity, source-file authenticity, whole-domain EOS error hypotheses, the complete live pressure-producer decomposition, material validation or resume. Local asset hash agreement is an integrity/provenance check, not external scientific certification. The service's live association guards are not converted into a restored live object by this offline review.

`NATIVE_SAVED_AUDIT.json` records **1,937 structural/hash/correspondence assertions in 0.12815491697983816 s**. These counts include repeated material flags and per-file checks; they are not 1,937 physical experiments or new physics validations. Its script and all input artifact hashes are bound in `NATIVE_REVIEW_FINAL.json`. The full Goal remains active and incomplete beyond this source wet-to-dry execution-service slice.

## Review Summary

| Severity | Count | Status |
|----------|-------|--------|
| CRITICAL | 0 | pass |
| HIGH | 0 | pass |
| MEDIUM | 0 | pass |
| LOW | 0 | pass |

Verdict: APPROVE — saved native execution and claimed evidence scope agree.
