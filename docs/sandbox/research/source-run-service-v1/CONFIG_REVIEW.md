# Independent configuration/builder review

Verdict: APPROVE within the explicit N3 manufactured-domain profile; no actionable defect found. Four reviewed file hashes are in FREEZE.json. This reviewer did not author these files or change production. Actual read-only test run: 44 passed in0.81s, TEST01.log; no HEOS call.

Checked against RECONSTRUCTION_MAP and INTERFACE: builder assembles source Arlabosse dry caloric with exact anchor, fixed kg mass and ReactionDisabled; NIST O2/N2 and shared water reference; explicit fluid volume/error/envelope; actual nonuniform half-width faces; original saturation table/connection flags and all initial amounts/temperatures. Missing configuration fields do not become zeros: closed recursive shape validation rejects omissions and extras, bool is not float/int, nonfinite inputs reject, exact clock rationals must be reduced. Explicit user-supplied zero transport or declared error remains an explicit numerical assumption, not inferred evidence. The zero internal energies are named unset_energy_states and must be replaced by actual service initialization; this API does not claim initialized physical states.

The four HEOS constructions follow the real public constructor call graph: storage water (1), storage IdealWaterVapor's own water (1), WaterChemicalPotential water plus its IdealWaterVapor water (2). These are static verified paths, not a native execution count from this review. Test factory seams use real Python source providers while asserting the requested backend/manifest; they forbid liquid solve and initial U evaluation. No initial RHS or storage.evaluate appears in builder. Three distinct storage/volume objects share their source Cp/template as specified. Source M is taken from actual provider reference for roundoff controls, not zero-filled or fabricated.

Source bundle pins 17 path/size/hash records independently of JSON scenario data; config cannot choose factories/import paths. Root-resolved assets must stay within supplied bundle; regular-file/size checks and fresh content hashing reject missing/tampered/FIFO/path-escape cases. Validation downloads nothing. Water runtime retains its own manifest/module checks. Race-free immutable external storage across the entire future run is not proven solely by an initial hash; repeated checks and provider source checks retain their respective original scope.

Config canonical bytes/hash and recursive immutable mapping are rechecked; built content binding covers actual model/state associations. Fresh policy objects retain exact H separately and original float(H) initial/max step conversion, with actual source M. Default relative/N/U and event P/T tolerances remain unchanged. Numeric scenario edits are new virtual experiments, not a claim that all scenarios reproduce the prior native case or pass gates. Shared volume live declarations, constructor counters, persistence and cancellation belong to the service and were not re-audited here.

The 44 executed tests include malformed types/fields/limits, immutable content binding, independent 17-file bundle, escapes/FIFO/tampering, original constructor configuration/policies, and original saved constructor identity parity under the explicit Python provider seam. This is implementation validation, not physical material qualification; geometry/transport remain manufactured and chemistry remains disabled.
