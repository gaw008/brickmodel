# Native acceptance checker and plan review

**APPROVE** the exact script and plan hashes in `RUNNER_REVIEW_FINAL.json`. Read-only static review and compilation to this review directory succeeded. The reviewer ran neither the checker nor a native model and made zero EOS calls. All 20 files in the production review freeze were rehashed and remain unchanged.

The registered execution is a single owned `supervise source-run` child from the noneditable installed package and the fixed default case. It keeps the original 16 wet-trial / 24 dry-path / 97 total source callback limits, 16 additional wet requests, 510-second cooperative budget and explicit 570-second process limit with 10-second grace. No parameter/tolerance adjustment, native retry, executable research-script loading or test provider replacement is authorized by this plan. Kernel/anchor-return counters are not counts of every internal EOS operation.

The checker compares the actual terminal job/result and durable event counts, four wrapper/kernel completions, three initialization returns, returned stages/candidates/transition, and the original event flag. It does not require the event flag to be true. The revised code now invokes shared `read_run` inside the physical-entrypoint prohibition scope and compares its result/manifest with the saved inputs. It then calls the installed CLI on the canonical study, inspecting original capture 16, cell 1 and the complete selected-pressure gate matrix. Source-study replay/ledger checks remain passive. No new physical model or scientific oracle is introduced here.

The first draft omitted shared `read_run` and did not publish a checker failure report; both review findings are closed. The revised error branch writes a failed audit result with exception, stage, raw captured CLI output and attempted prohibited calls before rethrowing. Native job/run evidence remains separate and intact. Runtime/schema failure is an audit failure rather than a manufactured pass.

This approves running the registered acceptance once after the parent confirms final installed tests and byte identity. It does not claim the future native attempt has completed or passed, and grants no material, full-cycle or checkpoint-resume qualification.

## Review Summary

| Severity | Count | Status |
|----------|-------|--------|
| CRITICAL | 0 | pass |
| HIGH | 0 | pass |
| MEDIUM | 0 | pass |
| LOW | 0 | pass |

Verdict: APPROVE — no unresolved concrete checker/plan finding.
