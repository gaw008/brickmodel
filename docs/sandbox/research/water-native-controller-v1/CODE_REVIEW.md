# Independent frozen native controller review

Outcome: approve this opt-in wiring candidate; no reproduced CRITICAL or HIGH correctness finding. This is not native execution approval, physical-model validation, a new codec, or full Goal completion.

Reviewed controller51325b507a6a835a3c7230d02c423483bcb6a290fb252a1429913eb690c20ed8, helper1a7f13d201d627e277852d4895c15f700b2d861a6d31066c4e6ed735e1ab76d2, test22d0ee818f12edf154e34cee29db42211a63b550dba4341f11aa399a434d7ead. FREEZE and all listed evidence plus six actual source and installed dependencies matched before and after review (sha_verification.json). Repository Python diff was empty at entry. ruff, mypy, pylint and black were unavailable in shell and selected Python environment; no installation or source change was made.

## Actual checks

- Frozen 14 cases: 14 passed in126.59s; process62986 exit0, tests.log/tests.xml. Actual manufactured analytic storage, stage, exact root, writeback and controller execute. PressureSession.query and snapshot are explicitly instrumented, not real pressure certificates.
- Independent two controls: 2 passed in32.77s; process61827 exit0. Inflating only the independent comparison pressure radius beyond its original gate produces independent_approach_comparison_failed, retains all three rows and completed independent path, and leaves original external state/operator, no steps/packets, zero corrections. Mutating original initial U after a recorded query fails original-input guard with one retained attempt and distinct immutable original input bytes. These are controlled orchestration failures, not physical or native results.
- Independent copied default parity runner: process50050 exit0. Default wet refusal and fixture resource failure equal untouched baseline in complete nested non-wall values and host cost records. Only output location was redirected; original candidate evidence was not overwritten.

## Contract findings

1. No session construction or reset is added. The same exact object passes every ordinary trial, retry, refinement and event/common helper. Entry history survives injected ordinary rejection, reduced retry and independent approach. Session snapshot histories are not added to controller host costs. Original session origin, cumulative seed/proof accounting, budget exhaustion and unknown child-cost failclosed behavior remain unchanged source-bound dependencies; the instrumented tests do not independently retest their actual mathematical budgets.
2. Event/common helper binds exact actual Sample identity, states identity, time, role, current pair binding, interfaces, cell and WetCellRate before query. Event0 leaves cell1 wet and performs fresh reference/candidate queries in each of three comparisons. Dry zero-liquid cells require depleted_no_nucleation. Full state/rate and original controller bytes enter query context; actual numerical bound validation remains in reviewed BoundLiquidPressureRequest/session.
3. Ordinary stage receives native_cancel which executes native_guard, with stage wall capped by remaining controller wall. Event/common passes typed guard and remaining wall directly. InterruptedError/TimeoutError/DomainExit map by type, not reason substring. Session keeps attempted query evidence including failures. Controller host accounting occurs before its next guard.
4. Original Stop subclasses ValueError, so existing handler already retained current path. Actual old RED concerned completed comparison-row loss. Candidate stores only completed rows after each append; failure before row completion leaves it missing. Independent finally preserves current completed rows too. Existing RED evidence remains untouched.
5. All six original bounds, per-event order/modes, two consecutive passes, independent cap/safe halving and distinct pre-event grids remain. Prefix auditing and final guard still precede atomic accepted assignment. Failure publishes original external state/operator, empty steps/packets and correction totals; candidate paths and proofs remain diagnostic.
6. Entry bytes include original initial/times, stage/controller/roundoff policy, original fraction budget, context, source and session original configuration/physics. Guards re-encode and compare; independent initial-U and frozen controller-policy mutation tests demonstrate refusal with original bytes retained. Mode transitions update only existing context binding and retain original initial/correction basis.
7. Without session the original branch/result remains. Fixture plus session refuses before work. New result is explicit subtype; both legacy pack and encode_mixed_run reject it. Default parity cases are not a claim of exhaustive mathematical equivalence.

No native/EOS evaluation was requested or executed by this review. Real full-controller cost/resource preregistration, native execution, saved mathematical audit and material validity remain outstanding outside this wiring approval.
