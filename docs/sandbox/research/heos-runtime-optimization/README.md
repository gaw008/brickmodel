# HEOS 运行时检查优化与原湿态前缀

按 stage6 实际 profile 分两次隔离改动，保留每次失败，物理输入、EOS算术、数值门槛和25/30秒限制未改。

- raw-sha：构造仍双核验，runtime前后用原始流体字符串SHA。30点与7导数通过、9故障stub检查通过；湿积分27eval/3接受仍超时，不能算通过。
- shared-transaction：公开饱和操作仍自行事务；温压内部调用同算术私有饱和body，共享外层前后检查/锁/警告，无缓存。body AST等价、17故障/公开路径stub检查、30点/7导数通过；原四步湿前缀29eval/4接受完成，积分14.502s。共同前三步与前次库存/能量/账本精确相等。
- installed：已应用两个受审源码及真实manifest/source摘要，冻结非editable离线安装，NumPy2.5.2。41安装模块匹配；原四步积分14.4058s/外部18.5954s，独立熵/组件/Fraction账本原门槛通过；212相关回归通过。保留锁同步前普通离线解析失败记录，不冒称默认依赖已安装CoolProp。

每目录 original-evidence.zip 是实际临时目录原字节快照，含脚本、候选源码、结果、监督日志与审查；逐成员哈希见archive-manifest.json。关键记录另放可读副本。生产先前身份及数据可从c2f5942恢复；不要用新manifest覆盖历史解释。研究脚本里的绝对路径记录实际执行位置，重放需解压至对应隔离位置或明确记录路径迁移。

这里只验证固定相库存的0..1/64秒规定压缩。没有验证活动相变、整段10%湿压缩、自由烧结、真实污泥或全周期，也未做普遍性能基准。全1112 suite是先前dcabd8d，当前只新增212安装定向回归与该湿前缀，不能混为同一版本完整suite。
