# Stage7 independent review

Approve this isolated optimization's source and completed grid/derivative evidence. Wet-prefix result is not yet adjudicated here. No EOS or production changes by reviewer; executed only nine reviewer-owned stdlib/stub checks of the actual transaction AST.

Kernel diff has exactly three changes: stored runtime fluid digest switches from canonical to raw SHA, and both pre/post comparisons SHA the current UTF-8 raw fluid string. Constructor still independently verifies both raw and canonical forms before creating identity/state objects. Lock, nested transaction structure, warning handling, pre/post config checks and every numerical method/gate are unchanged. Thus formatting-only changes now reject more strictly; modified content cannot be hidden by keeping a previous parsed representation. JSON retrieval itself remains on each transaction boundary. This does not claim process-global mutation concurrency safety.

Expected manifest differs from production only in adapter_sha256, which matches actual candidate kernel `a5e727f2c1dd2697746d8fd720d6845b62cb42aed56d62d4ff9f223728f5c447`. Wrapper changes only its fixed admitted manifest hash to actual `a691956b6943cc68c3baf76a9a5672417e1fd202d3a8f5bf3d4a0f21a3072be4`. Wet child diff only changes manifest path and candidate source-recording directory; all previously reviewed scientific/initial comparison gates remain unchanged.

Actual reviewer-run `review_faults.py` extracts the current _transaction AST without importing providers or executing EOS. Nine checks passed: unchanged raw definition (two reads), changed content before/after, identical JSON with changed whitespace before/after, config change before/after, warning rejection, and nested transactions retaining four reads. Pre-check rejections occur before body execution. Recorded `review-fault-results.json` binds the tested kernel SHA. Stubs prove these Python guard paths, not native library mutation semantics or numerical accuracy. Reviewer owns only these test/report artifacts, not candidate sources.

Grid-attempt01 completed exit0 in1.300857292 s; derivative-attempt01 exit0 in1.185652875 s. All before/after input hashes match each other and actual current files. Independently recomputed all saved30 candidate/original differences and original grid tolerance comparisons. Recomputed all21 central differences from seven cases' five snapshots, exact mass/offset and steps; all also pass the original expected-value relative-scale comparator (not merely math.isclose's symmetric variant). These are unchanged bounded scientific checks, not a full-domain or speed certificate. Both runs use the newly bound kernel.

The unchanged four-step wet attempt must separately finish and satisfy its original ledger/entropy/component gates before qualifying that prefix. Prior stage6 resource failure remains retained. No timing-budget or accuracy change has been approved by this review.

Wet-attempt01 subsequently ended failed/exit1 (27.3785 s external elapsed), with integrator resource_limit/wall_time_limit,27 evaluations,three accepted steps and zero rejects. All before/after/current input hashes were independently checked. The original four-step endpoint was not reached; initial energy comparison remains the same passing1.7462298274040222e-10 J. Entropy/component endpoint stages remain unexecuted. This improves the amount of work observed within the resource budget versus stage6 but is still a failed/incomplete prefix; no exact speedup or scientific completion follows. Stage7 artifacts should remain frozen if a separate transaction-deduplication candidate is evaluated next.
