# Independent near-event initial observation review

APPROVE the repaired preparation for root's single registered2forward+1pair measurement, subject to its actual noneditable104-module execution freeze. No stage/controller/event or material admission.

Final run59deec10/build870d1f50 are unchanged; repaired preparef12ca941/CASE7271a293/HANDOFFade69049. All final CANDIDATE_FREEZE hashes independently match (review_final_freeze.json). No native call, full module import, prepare or runner execution occurred here.

MEDIUM resolved: original preparation omitted the referenced proposal and actual selection-rate trial from its source freeze, leaving the important small-inventory limitation outside frozen evidence. Author preserves before-provenance-fix. Exact inspected diff adds these two assets and explicit CASE/HANDOFF limitation only, changing no numbers, gates or callback logic.

Independent pure checks passed (review_pure01.json/log): actual old initial trial phase fractions exactly match both declared rates; binary64-nearest products match fixed inventory hex; both are strictly positive and below original4e-7mol gate. Isolated AST extraction executes only pure derived_case, with no old builder/module/native imports. It verifies only temperatures/liquid inventories differ, mismatched selection rule refuses, zero inventory refuses, and increased host cap refuses. Tests use temporary copies in this owned review directory, never modify author files. Code and pure input rules did not change in the narrow fix, so unchanged tests were not repeated.

Read full builder/runner/preparation/supervisor. Original constructor physical inputs stay unchanged; each returned total U freshly initializes that cell. Exactly2 forward attempts are guarded, completed values/checkpoints are saved before post-call guards, then exactly1 current pair evaluation saves input before and returned rates/counts before rejection checks. Partial exceptions remain saved, no retries/projection/clamping. Actual import root/distribution/submodule hashes and full installed/input membership are checked. Initialization30s, whole40s and watchdog50s remain separate original resource limits. External process group is killed on overrun and leader always reaped; original exit-race handling is narrow.

The positive inventory choice is a substantially different manufactured numerical seam. Its old-rate tangents are design inputs, not event root certificates. Both inventories lie below the existing amount gate; passing a future absolute amount comparison alone cannot demonstrate relative water resolution. This measurement performs no gate comparison, correction, stage or pressure proof. Native internal work may occur in constructors and is correctly reported unknown;2forward/1pair are high-level callback counts only. Original material/U/Cp/volume hypotheses remain conditional.
