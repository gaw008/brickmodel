# 低含水 full-U 反解：显式策略与实际性能

从 `cfd291c` 接入已分别经过代码、数学审查的 safeguarded secant/Newton/bisection。`LowMoistureSorptionColumn(inverse_strategy='LOW_MOISTURE_FULL_U_SAFEGUARDED_NEWTON_BISECTION_V1')` 显式选择；默认仍为原二分法，默认列身份和来源记录与原提交同对象比较完全一致。新选择将完整数值定义纳入列身份和 provenance，未改变储能/状态身份、物理来源、干态参考 U 或原误差门。

两个原温域端点仍实际计算并检查源/压力/支撑域；候选接受仍使用完整 U 残差、原能量误差及最小热容温度界。每第三个候选强制中点，其余候选采用守护 secant/Newton。未知误差、未决符号、不可表示温度及原最大迭代次数均明确失败。同一个最大迭代数不保证两种算法同样成功，独立二次函数反例已保留；此算法没有普遍更快或无条件收敛的声明。

## 实际验证

- 主机接口实现前 3 项真实 RED（缺少显式策略字段）保留；实现后 32 项相关测试通过，78.31 s。
- 独立主机代码审查通过，11 项被动检查；原反解模块与审查的草稿逐字一致。原制造曲线 16 项及独立数学 4 项证据随包保留，不算新原生实验。
- 非 editable 安装离线完成；182 包文件、175 Python 模块与源码冻结一致，安装前后 23 项有影响回归通过，51.87 s。与源码测试有重叠，不相加宣称唯一用例数。
- 唯一真实水物性算例保持原初态、来源、控制、`.05 s / 2 steps / 6 RHS`、全部 232 原验收条件；U `1e-5 J`、T `1e-6 K`、算术 `1e-8 J / 1e-12 mol` 及原资源限制均未改。
- 实际积分 **9.99039154 s**，对保存的原二分法 **25.09240875 s**，本算例耗时比 **2.51165**。driver 11.30565021 s、监督 16.53868054 s，退出 0、进程回收，2782 个登记输入 hash 未变；原算例没有重跑。
- 新最大 U 残差加误差 `6.72887073e-6 J`，温度界 `4.19162670e-7 K`。12 次反解记录中的候选总数 280→48；按成功返回的两端点加候选推算 evaluate 总数 304→72，**不是独立插桩计数**。
- 初态库存/U、初始物性点、原 m/q 节点和非策略 provenance 完全相同。末温新减旧约 `7.85e-7 / 2.92e-7 K`，末 U 差约 `-1.27e-10 / 1.16e-10 J`；完整差异保留，不声称轨迹逐字相同或已经时间收敛。

原生输入比较应比较 metadata 的 `inputs_before[*].sha256` 与 status 的 `inputs_after[*]`。一次被动诊断直接比较 dict 和字符串得出 False，随后识别字段形状并正确比较；原监督 `inputs_unchanged=true`，没有源文件变动或重跑。

本轮仅改数值候选选择并测量一个短段。材料/训练、时间空间收敛、完整烧成资格仍为 false。低 W、供体 D/k 和受控边界的条件范围与 unknown 误差沿用[上一阶段](../arlabosse-low-moisture-v1/REPORT.md)。

## 紧接的物理执行任务

保存状态的冻结代数估算显示外汽快尺度约 0.32 s、局部慢尺度约 1e5 s；快反解单独不能完成长程干燥。下一步实现冻结系数的正性 backward Euler 局部相变/排汽，再与现有内部 k/D 和热浴分步耦合，每段真实反解检查 T/P/U。完整共享 J/E/H 和投影账必须保留，禁止额外 latent 项或删水。原外格初始载气干极限不能保证 90 kPa，350 K 热浴也不能无限延长在原 338 K 解码上界内；下一工况须作为新的明确设计并实际核验，不能改写旧门或旧算例。

恢复目录：`/private/tmp/arlabosse-low-moisture-fast-inverse-v1`。新隐式叶子/适配准备：`/private/tmp/arlabosse-low-moisture-implicit-v1`。Goal 第11节仍未全部满足；本轮工具读到 Goal 面板 paused，本任务按用户继续指令执行，没有自行修改 Goal 状态。

保存结果独立核查通过：77项、0.058s，逐格/全域两阶段账和全部实际费用一致，0新EOS。实际算术费用U `2.25754847e-12 J`、库存 `8.19592631e-18 mol`。见[SAVED_REVIEW](SAVED_REVIEW.md)。
