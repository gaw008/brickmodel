# Explicit fast full-U comparison

Baseline cfd291c and saved /private/tmp/arlabosse-low-moisture-v1/native01 remain unchanged. The new case selects LOW_MOISTURE_FULL_U_SAFEGUARDED_NEWTON_BISECTION_V1 on the low-moisture column explicitly. No storage, energy identity, initial physical inputs, source definitions, tolerances, budgets or integration algorithm changes. Preserve original 232 native checks and .05s/2steps/6RHS, inverse U 1e-5J/T 1e-6K/100 candidates, arithmetic 1e-8J/1e-12mol, 60s integration/80s driver/100s supervisor/5s cleanup. Run exactly once after installed tests and host code review. Compare the saved baseline, do not rerun it. If numerical gates fail, preserve failure and diagnose before any new case. Native performance is measured on this one case only, no general speed or material/convergence qualification. Equal candidate budgets do not guarantee equal success for the two algorithms.

Compare actual initial inventories/U/source points and non-strategy provenance for equality; differences in returned final U/T/inventory are measured and retained, not claimed identical trajectories. The original acceptance errors remain unchanged. Installation is noneditable, source/installed files match the preregistered snapshot before and after tests/native. Source assets stay local per original licensing.
