# Actually evaluated source-prefix trial

Owned only source_prefix_trial.py and test_source_prefix_trial.py. No EOS/HEOS, installation or commit. All source evaluations use existing actual source-column fixtures with explicitly manufactured water and transport; these tests do not qualify a material.

API: evaluate_source_prefix_trial(adapter, initial, *, start, end, integration_policy, maximum_callbacks, cancel=None). Original policy is copied and retained with an exact field binding. A distinct original-bound reference policy records remaining wall time. A single total deadline, cancellation check and explicit callback-attempt cap govern initial, Euler midpoint, positive-prefix terminal and reference calls. Each attempted callback retains ordinal/role/exact time/state, an immutable input tuple, actual returned observation before validation, original binding where valid, and failure classification/text even for empty messages.

The midpoint uses the parent-owned advance_exact_euler helper. Prefix construction/auditing reuses source_net_panel/source_net_prefix. Initial and predictor require all fluids positive; full-prefix numerical boundaries are unsupported. The terminal actual source call performs source inversions and phase/transport/furnace evaluation; retained bounds are source temperature/inverse error and source pressure/total pressure error including available-volume uncertainty.

The unchanged integrate_exact supplies the independent same-interval reference. It must complete. Direct amount/U discrepancy uses original scales and <=1, without a 1/3 multiplier. Successful record.check passively replays saved reference outcomes through the same original integrator, matching every requested state/time and all output fields except elapsed time. This invokes no physics. Its computational wall allowance is not a certificate of original execution cost. Original recoverable DomainExit outcomes are replayed too, so successful ordinary reference retries remain valid. Original outcome and controls snapshots prevent relabel-only or cap-only alteration; policy snapshots protect both original and remaining policies. Failed midpoint/terminal attempts remain linked to actual predictor/prefix inputs.

Review corrections and real evidence:
- Initial RED: missing implementation/helper, initial-red.log.
- NaN comparison: max(0,nan) could conceal nonfinite energy. Actual two-case RED finite-red.log, pre-fix source before-finite-guard.py. Four input arrays now require exact ndarray/float64/finite before arithmetic.
- Failed attempt input: actual endpoint DomainExit record could have its failed input replaced. RED failed-input-red.log, source before-failed-input-binding.py. All attempts now retain input/failure bindings; known stage linkage is checked regardless outcome.
- Ordinary reference recovery: rejecting every failed reference attempt incorrectly rejected a completed adaptive reference. RED reference-recovery-red.log (1 failed,3.82s), source before-reference-recovery.py. Captures now distinguish DomainExit and replay it faithfully. The actual callback6 retry regression completes and passes discrepancy validation.
- Independent Python reviewer also retained original status relabel and reference ledger-tamper RED evidence; outcome binding and passive replay address them.

Final focused run recovery-final-tests.log: 31 passed in29.46s. Covers actual closed N1/N3 and programmed source hosts, first/mid/terminal and genuine reference captures, exact origin, input/control types, direct/no-factor gates, NaN, terminal-only failure, reference incomplete/wrong endpoint, callback cap, cancellation/wall after actual callback, caller policy mutation, source-free record checks, ledger/capture/outcome tampering, recoverable DomainExit and empty exception messages. No long trajectories or full suite were run by this worker. Final exact hashes are in FREEZE.json; prior freeze is retained.

Boundary: validated_positive_numerical_trial is a numerical consistency result only. No event-time certificate, wet/dry mode transition, drainage correction, vapor reassignment or physical material admission is implemented. Affine donor-direction diagnostics still do not prove physical donor consistency between samples.
