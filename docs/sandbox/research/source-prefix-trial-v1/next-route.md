# Next source-event approach/comparison step

Read-only route, 2026-09-09. No production edits, EOS, tests, commit, or inspection of a partial native artifact. The smallest next step is a checked comparison of *already actual* source observations plus a root-guided positive approach proposal. Keep ordinary evolution in integrate_exact; do not introduce another adaptive controller or loosen the old mechanical executor guards.

## First deliverable: same-endpoint source comparison, usually zero new callbacks

Add a narrow `compare_source_trial_endpoints(trial, *, event_policy: DepletionPolicy)` result. Reuse `SourcePrefixTrial.check` (including saved-rate reference replay); require validated_positive_numerical_trial and completed reference at the identical exact end. Compare the prefix-terminal capture and the final successful reference capture, both of which already contain actual source inverses. Failed reference attempts remain in their original sequence; do not reject a completed reference merely because it recovered from DomainExit.

Reuse the original `depletion_integration.DepletionPolicy` fields at lines107–144: time_absolute_s, amount_absolute_mol, energy_absolute_j, temperature_absolute_k, pressure_absolute_pa. Validate and retain the entire original policy plus a plain immutable binding; do not invent new tolerances or reuse only the looser D_NU scales. The roundoff_policy and mechanical packet controls are retained original configuration, not permission to invoke their correction machinery. If no source-event policy has been declared, report missing explicit tolerances instead of fabricating defaults.

Compute and retain per-cell absolute amount and U differences from the two packed endpoints, maximum differences, and each actual source inverse/error input. For cell i,

    dT_i = |Tprefix_i − Tref_i| + eTprefix_i + eTref_i
    dP_reported_i = |Pprefix_i − Pref_i| + ePprefix_i + ePref_i.

Use exact Fraction sums of represented values (or explicit outward rounding), not an ordinary float sum that can round a bound inward. Temperature errors are `SourceWetInverse.temperature_error_bound_k`; pressure errors are `SourceWetPoint.pressure_error_pa`, including the added available-volume error from `evaluate_wet_fluid`. Bare fluid.pressure_error_bound_pa omits that extra contribution. Enforce original amount/U/T gates separately and expose pressure's scope separately. Retain original D_NU as another diagnostic, not a substitute for event-policy gates. Same endpoint means zero comparison-time offset; it supplies **no event-time accuracy evidence**.

The arithmetic resembles `exact_depletion_integration.compare:252`, but its observation vectors and pressure comparison are mechanical-specific. A small pure componentwise difference-plus-errors helper could be extracted if both callers truly use it unchanged; otherwise the simple source vector extraction is the only new logic. Do not copy that function's entire packet/comparison loop or supply dummy stretches.

## Pressure boundary: current evidence cannot always certify the event gate

`SourceWetStorage.evaluate:203` calls prescribed-temperature fluid closure. Its pressure error includes mechanical/volume uncertainty at the decoded T. `SourceWetStorage.invert:229` separately bounds temperature error from total-U inversion; it does not propagate that temperature interval into pressure. Therefore dP_reported is a valid comparison of pressure intervals **at the reported decoded temperatures**, not automatically a bound on the true inverse-state pressure difference. `rigid_water_gas.py` explicitly solves closure at prescribed temperature. `pressure_comparison.py` likewise declares reported-temperature scope and `temperature_uncertainty_included=False`; its WaterPhaseTransfer/FreeSolidSlab binding cannot be imported as source coverage.

For nonzero inverse temperature errors, a physical full-state pressure gate needs an independently justified source-compatible pressure enclosure over each complete T interval, e.g. a validated bound on |dP/dT| over the source T/P/volume/inventory box or an interval closure that establishes the whole image. Do not assume monotonicity from two endpoint EOS samples or borrow a mechanical certificate. Until such a bound exists, report the actual reported-T pressure discrepancy, whether that limited gate passes, and `full_inverse_pressure_gate=unresolved`. If its reported-T bound already exceeds the original tolerance, reject it; lack of the missing propagation is never grounds to loosen that tolerance. Even a small reported-T bound does not complete event admission. This does not narrow the overall Goal; it identifies the next missing proof/interface precisely.

## Root-guided actual approach without an event controller clone

Reuse `source_net_roots.order_source_panel_roots` on the two genuine initial/Euler-midpoint captures already retained by SourcePrefixTrial. A failed full-prefix positivity check may leave prefix=None but still retains these actual captures: rebuild SourceAffinePanel from them with the original attempted end and bindings, rather than inventing a new observation. Do not require an accepted full prefix to inspect candidate roots; conversely, failed or missing midpoint evaluation provides no panel.

All4N liquid/gas polynomials compete, with exact original times and immutable source/energy/fixed-kg binding. Preserve explicit unsupported for every zero initial component, bounded unresolved brackets, exact ties and gas competitors. A tangent is not a depletion event. If no roots occur, continue using ordinary integrate_exact. For a unique liquid crossing, one bounded *proposal* may choose an exact positive end strictly below the root's proven lower bound using the original safe_inventory_fraction. Refine the existing bracket until its lower bound is positive if necessary; refuse if resource/minimum-step/program-knot constraints cannot be met. Then invoke the existing SourcePrefixTrial on that proposed interval to obtain fresh actual predictor/terminal/reference evaluations. No loop/controller, dry state or correction is introduced in this step.

For later coarse/fine root comparison, retain independently evaluated panels, identical first-event labels and common source/operator policy. The exact event-time discrepancy must include separation of candidate root times plus their individual enclosure widths, as the original compare function does. Recomputing two brackets from the same pair of observations is not independent time refinement. Different observation intervals or approach states require fresh physics and a common-endpoint continuation before applying original event amount/U/T/P tolerances. SourcePrefixTrial and integrate_exact supply those positive ordinary paths; a shared scalar error test cannot replace their actual evaluations.

No currently available result authorizes the final wet/dry transition. Net drainage residual must have an independently bounded paired liquid transfer across its actual face and the same donor-enthalpy transfer in U, with neighboring inventories and original ledger budgets checked. Evaporation-fraction/ULP allowances cannot pay for drainage. Source pressure/mobility uncertainty and donor consistency remain explicit limitations; preserve existing phase-correction gates and old ExactFreeWaterTransfer type checks.

## Focused validation and native gate

Use existing actual manufactured-water source fixtures for N1/N3 closed and programmed cases. Check saved same-endpoint observations without additional calls; D_NU passing while original event amount/U/T or reported-P tolerance fails; exact outward error summation; nonzero available-volume error; nonzero inverse-T error leaves full-pressure gate unresolved; missing/mismatched policy/time/source data; recovered reference DomainExit; and passive record tamper checks. For candidate roots, use actual first/midpoint source captures where available and explicit manufactured-rate cases for gas-first/tangent/tie/zero/unresolved rules. Verify a genuinely re-evaluated positive approach endpoint respects original step/knot limits and does not mutate interface modes.

First apply the pure comparison to the **completed and frozen** current native trial artifact. It already contains both actual same-endpoint source observations, so this deliverable needs no fresh native callback. A new native run is necessary only for genuinely new positive approach samples or a separately reviewed pressure-enclosure method. Parent subsequently reported the completed native cost: exact interval1/16384 s,11 actual calls (3+8), one accepted reference step/no rejection, D_NU=0.001086768806259706, trial81.125177 s and whole82.7321195 s (initial construction about1.48 s). The frozen artifact is /private/tmp/brick-source-prefix-trial-v1/root/native-result.json, SHA7c0907af104450ffc42cf142e3e3aebefaf21c597eb6806f09b7bdbe6d6d3505; independent physics audit is still pending. No new native run is needed for saved-endpoint comparison. If a genuinely new single11-call approach trial is later reviewed, the existing180 s inner/210 s outer resource ceilings have measured support; a different callback schedule needs its own estimate and preregistration. This note authorizes no automatic rerun. Keep original scientific policies unchanged and retain structured partial failures.
