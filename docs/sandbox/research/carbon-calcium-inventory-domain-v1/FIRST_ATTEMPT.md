# 初次静态原型：未获资格

2026-09-25 UTC。生产模型未变；旧域108个完整输出已保存为old-domain-baseline.json。

碳稀组36个状态完成全部静态标志，但原运行在下一组C=Ca=0.002 mol、O=0.006 mol、N2=0.004 mol、300 K、0.1 bar的独立高精度重建处终止。mpmath的数值Jacobian进入奇异LU路径，抛出TypeError；静态总报告未生成，不能把已完成的局部进度称为整个模型通过。

该状态原型给CO=4.716784439458993e-47 mol、CO2=6.877221670843837e-25 mol、O2=2.3583922197294965e-47 mol。C-Ca及O-3Ca在输入二进制数上均精确为零。直接以Ca-calcite恢复石灰时，calcite已舍入到Ca，导致石灰被相减成零；物理的石灰量应由Ca-C+CO+CO2直接恢复，约为CO2量。此项需要改正，不能只修改高精度参考来掩盖。

下一版：使用原独立元素/化学势方程的等价氧减二碳组合，并按各非零平衡项的总幅度缩放这一行，避免约1e-47 mol项被总氧约1e-3 mol掩盖；不改变物理方程或静态误差预算。并分别计算共存方解石与石灰库存，避免用舍入后的主相恢复微量副相。首轮stdout/stderr及此版本Git记录保留。
