# 精确记录的终端、比较与资源审计

在前缀审计基础上应用三个独立模块，仍不授予续算许可。

| 模块 | 已实现检查 | 源码 SHA 前缀 |
|---|---|---|
| `exact_terminal_proof_audit` | 所有已提交终端的候选根/无根排除、完整预测与终端积分、正库存、原累计写回、实际几何/来源/模式 | ff991e946c72953c |
| `exact_record_comparison_audit` | 逐事件与共同端点六项门槛、完整压力证书、原粗网格锚、连续两次通过及独立更细路径 | 1cef63c34c9cf77c |
| `exact_resource_audit` | 原资源政策、已保留尝试与路径关联、非重叠计算量、完整累计遥测及剩余额度 | c1bfaabb1a3325b2 |

源码相关测试分两组实际 31 passed / 35.71 s 和 13 passed / 11.00 s；最终冻结非 editable 安装的三个新模块测试 37 passed / 34.83 s。82 个实际导入模块逐字匹配源码。不是全仓库测试声明。

## 保存的真实水宿主记录复核

两次宿主读审计使用实际 79 模块，对原 78 模块严格逐项比较：所有旧模块和非模块字段相同，仅多出固定 SHA 的前缀审计器。分别保留两份身份，验证通过后才将原身份传入历史绑定；这不是跨版本续算许可。最终 82 模块安装发生在这些检查之后，没有冒称用最终包重新积分。

- 终端证明：唯一监督运行 exit 0 / 2.10263 s，审计函数 0.61268 s，2 个已提交终端、2 个候选根、1 个无根排除项通过，每个事件三组实际状态几何检查。未重复原 77 秒积分。
- 比较证明：唯一监督运行 **exit 1，保持失败**。审计函数已返回 2 个事件包、6 次比较、24 个压力证书及 24 次端点调用；外围脚本随后错误地断言只有一个事件包。另行只读核对原严格记录的 8 条细化记录与保存函数返回，并独立复审。没有覆盖失败记录、放宽科学门槛或重跑原生计算。
- 资源检查：对原严格记录和外部原政策做纯数据复核，0.25273 s，无宿主构建或 EOS。已用 79 个计费面板（普通 71＋重新规划 0＋终端尝试 8），原 512 上限剩余 433；拒绝额度剩余 100。累计运行时间保留原记录精确浮点值及其 Fraction 表示，没有改成审计耗时或重置为零。

## 已保留的失败与边界

比较审查实际复现并修复了更改粗解标签绕过原步长政策的问题。资源审查实际复现并修复三类低报：遗漏细化之外的已提交普通步、同步减少已保存终端面板的计费、删除仍被细化路径引用的顶层尝试。原 RED、修复前源码、全部测试和审查均在证据包。资源多重集比较使用完整数值记录及出现次数，已提交的路径副本不重复收费。

这些门禁检查保存的数值证明及实际来源关联，不重新计算全部 EOS/RHS。历史墙钟时间与未记录的丢弃普通调用不能从现有数值结果独立重建；续算必须使用权威父记录并保留累计遥测。尚未实现的完整门禁组合、实际状态恢复和取消后续算不能由三个 partial 报告自动推得。

真实原污泥材料包、公开三个机制组留出对照、全湿坯烧成冷却、空间收敛和多代搜索/最终交互验收仍未完成。

[逐文件哈希清单](manifest.json)与[95 成员完整证据包](evidence.tar.gz)保存候选、原生失败/成功结果、诊断、审查、测试和 82 模块安装核对。各模块 HANDOFF、CODE_REVIEW、实际 RESULT_REVIEW 在本目录独立保存，可直接核查。
