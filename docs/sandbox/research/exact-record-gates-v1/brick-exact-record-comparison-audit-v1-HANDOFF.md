# Exact record comparison audit candidate

`audit_exact_comparisons(record, actual_original_operator, *, original_initial, start, end, integration_policy, event_policy, case_sha256, runtime_identity)` first consumes fresh validate_binding output, not caller replaceable record fields. It binds full original inputs/policies/interval, projects typed evidence without rounding float/Fraction/time values, and recomputes the established research audit's six maxima for every saved event/common comparison associated with committed packets.

Original point-error temperature/pressure sums and explicit paired pressure selection are retained. Paired records bind every cell's saved T/P and original errors to actual comparison observations. Mode-specific expected pressure bindings are freshly computed from the externally matched actual operator: full source-label identities, implementation, reference, boxes, layouts and domains, rather than the research script's partial canonical reconstruction. Existing pure audit_pressure_comparison recomputes certificates; no EOS calls or provider reconstruction from record class names occur. Original-mode object is preserved instead of replacing implicit mode defaults with explicit modes (which would change content identity).

Every path's pre-first-event grid is reconstructed; controls start at original cap/safe values and halve appropriately. Two consecutive terminal passes and a separate independently finer branch are required for each committed packet. Actual frame/state/ledger and mode sequence are tied to comparisons and original metadata. Pressure endpoint counts are recomputed from records and compared with full actual counts (exact equality for completed result). Clock comparisons use exact event times plus nonnegative selected bracket widths and exact-polynomial-zero exception; full root proof belongs to the separate module. Guards use explicit require, not assertions disabled by python -O.

Returns immutable ExactComparisonAudit with scope partial_audit_original_comparison_gates and resume_authorized=false. Still missing complete prefix accounting, root/fullpanel proof, original resource accounting and resume semantics. This is not a native scientific validation or new material admission.

Tests use actual no-EOS integrator/codec fixtures, mutate stored pressure/clock/grid/safe/status/bool values, bind external policy, and ensure caller result-field replacement is ignored. A separate analytic real pure-pressure certificate tests the exact pressure auditor plus external observation/source-binding negatives. That test does not exercise a live full-host paired branch; a source-matched native saved-record check remains needed before claiming that coverage. Existing failure logs retained: implicit-versus-explicit original mode identity caused2initialfailures; exact decimal 0.01 versus binary64 observation error caused a fixture mismatch; original pure-pressure fixture's optional energy identity exposed a projection None handling error. These were fixed without tolerance changes.

Only candidate module/test/logs in this directory changed. Tests use production import name; temp conftest appends local package path and must not be applied to the repo. No original research scripts/results modified, no installation or EOS performed.

Coarse-anchor HIGH fix: original actual attack renamed the first coarse status and multiplied all approach caps by2 while retaining ratios. It passed previously; coarse-red.log preserves the expected-refusal failure. Every committed matched group now independently requires its original level0 terminal/coarse_reference anchor with no comparison, original coarse cap/safe, legal sequential terminal comparison statuses/levels within original refinement budget and a final independent passing comparison. Before-fix source/tests preserved. New negatives cover invented anchor, nonempty/empty-but-not-None coarse comparison, invalid terminal role and wrong integer coarse level. The first combined test used a bool level that the codec rejected before the numerical audit (13pass1fail test-exception expectation); final test uses integer2 to exercise this audit boundary. No scientific threshold changed.
