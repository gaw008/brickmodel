# Preregistered dry no-EOS test design (not executed)

Use existing dry `test_solid_fluid_storage.model(water)` fixture only as a source-qualified carrier/solid setup. Monkeypatch both native/provider liquid TP entrypoints to raise immediately. Set liquid amount zero. Increase only the explicitly manufactured test viscosity (for example 1e6 Pa s) so the actual ideal-gas pressure does not force rates outside the existing logarithmic-rate limit; do not raise that limit or change storage tolerances.

1. **Independent current volume/pressure/energy oracle.** At n=.97,t=.99,T=300K use exact Fraction reference dimensions and represented current geometry to calculate available volume and ideal-carrier pressure. Independently compute the fixed-solid and ideal gas caloric energy and logarithmic elastic/interface potential (high-precision Decimal logs); verify total energy within returned source/numeric bounds. Check zero-rate recoverable state and nonzero final free-rate state are distinct but share energies within combined bounds.
2. **Bounded total-energy inverse.** Make a forward target at 300K and invert in (295,310) with original InversePolicy. T discrepancy must fit actual returned inverse bound; addition/subtraction residuals must fit explicit outward bounds. No EOS may have been called.
3. **Ordering proof.** Instrument candidate `solve_free_rates` to assert it is called only after thermal storage has returned a pressure and exactly once per final point assembly; all recoverable-energy evaluations before inverse use zero rates. Reuse returned `thermal_inverse` directly in any later host test; no re-inversion.
4. **Stored mechanical energy affects decoded T.** Keep total E/N fixed and change current n/t within domain; independently solve the dry constant-Cv thermal energy equation plus mechanical potential. Verify decoded T changes and geometry drives ideal gas P consistently. Do not compare at fixed pressure or assume isotropic stretch.
5. **Error propagation parity.** At matching n/t and fixed Ns compare old prescribed point storage's recoverable/thermal errors with this candidate using identical declared additional bounds. The legacy motion belongs only to the comparison fixture, never to candidate implementation. Increasing reference-volume/additional-energy bounds must not shrink propagated bounds; unresolvable inverse precision must fail under original tolerances.
6. **Identity/domain guards.** Reject reacting wrapper, multicell reference, changed solid amount or missing solid species, wrong provider identity, mismatched original reference bulk, zero viscosity, false opt-in, negative pressure, stretch outside either domain, and exhausted current pore volume. Wrong-model or thermal-only target fails before inverse. Mutating a template mapping after construction must fail `_template_digest` check.
7. **Immutable state geometry and stable identity.** Output current arrays cannot have write access re-enabled; n/t/T changes do not change point model identity. Original template and skeleton potential/source modifications do change identity or fail admission. No dynamic state is substituted into reference identity.
8. **Free-rate boundary.** Deliberately choose a dry pressure/viscosity forcing rate beyond the original skeleton limit: propagate the original failure rather than clipping, changing viscosity, changing traction or pretending point evaluation is an admitted trajectory.

These tests establish an algebraic/point-storage interface only. They do not establish wet dynamic evolution, temperature uncertainty in mechanical rates, transport coupling, topology events or material calibration.
