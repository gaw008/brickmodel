# Open-boundary source-column native plan

Previous closed three-cell evaluation cost was 2.347864 s, and one full step took about 23 s. This extension adds one gas face and one scalar surface root without adding any liquid EOS query. First measure one actual open evaluation with the frozen example, under an outer 20 s hard cap. After code review and offline reinstall, run one full three-cell midpoint step of exactly 1/1024 s with the same example, 45 s integrator and 60 s external hard caps. Do not relax physical or inverse gates if it fails.

The example retains the previous actual source-mass storage, HEOS water and source gas functions, three-cell initial state, available-fluid-volume test geometry and inverse policies. The explicit virtual boundary varies gas temperature 340→346 K, effective radiation ambient 350→355 K, pressure 0.9→1.3 MPa and complete O2/N2/H2O composition. k=.5, h=10, emissivity=.8 and gas exchange coefficients are explicitly manufactured inputs; no material qualification is inferred.

Required observations: run.status completed, one ledger and endpoint, three successful evaluations, per-cell and global boundary-corrected exact Fraction balances, nonzero gas/thermal boundary exchange, actual midpoint boundary from exact time, and surface residual within its original policy. Preserve status/reason/prefix on failure; process exit0 alone is insufficient. This is not a full firing trajectory, spatial-convergence or real-material validation claim. Independent artificial-liquid time refinement and saved native ledger audit are reported separately.
