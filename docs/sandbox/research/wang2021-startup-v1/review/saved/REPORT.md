# execution01 保存证据独立审核

结论：**保存证据内部一致，S1 联合解按事前登记在参数边界处停止；不锁参，不授予模型资格。** 本审核不重跑训练、求解器或 EOS，也不导入应用模块。

`audit_saved.py` 使用标准库读取 15 个实际保存文件；执行成功，审核本身用时 0.01674 s。逐项结果、原保存文件 SHA256 和源输入摘要见 `AUDIT.json`；原输出没有被改写。

## 已独立核对

- 监督起点、终点的 9 个输入 SHA256 相同，并与当前实际文件及实施前 `review/code/REVIEW_FREEZE.json` 相符。内部绑定与监督绑定一致。实际终态保存为退出 0、未超时、子进程已回收；内部 163.240671833 s，监督 163.564390583 s，分别小于 600/630 s。
- 只解析训练 CSV 的 40/60°C 行，其余行仅检查温度字段后跳过。八条曲线共 117 个非初始点；八个训练条件的 t0 定义值排除。九份训练文件合计 1,053 个点，原来源定位/读图界/观察值均逐字段一致；没有解析 50°C 观察值。
- 从 CSV 观察值和已保存预测独立重算逐点残差、是否超界、每条件及汇总 RMSE/MAE/偏差/最大误差、八曲线等权 MSE，全部吻合。这里的曲线等权目标与所有点汇总 RMSE 明确分开。
- N=64/128/256 的初值加密检查失败记录完整，N=512 为预登记梯子第一个全部通过的网格。九次拟合的数值门槛均通过，保存的 672 条求解诊断满足既定质量/值域/非增条件。利用独立累计出流与已保存质量残差界核对三组网格/时间容差差值的一致性；九份细网格预测直接对累计 q 检查 `MR+q−1`，最大绝对值 8.89e-16。
- 七个固定 τ、第二起点、联合拟合顺序与 stdout 完全一致，九次均保存收敛终态。逐拟合记录满足 `residual_calls=nfev+2×维数×njev`，合计 391，等于整次 attempted/completed；每拟合 nfev≤60，共同调用上限 1,500 未重置。
- 固定剖面最优 τ=600 s，相邻目标差最小值 0.000132592692371914，第二起点目标差约 3.60e-12，均满足预登记筛查规则。

## 联合解没有通过资格门槛

| 项目 | 独立复算/核对结果 |
|---|---:|
| 联合 τ | 729.1683826313189 s |
| Ea | 149999.99999999997 J/mol |
| Ea/100000 距原上界 | 2.220446049250313e-16 |
| 八曲线等权 MSE | 0.006249292034772814 |
| 全部 117 点 RMSE | 0.0935260240556129 |
| 最大绝对 MR 误差 | 0.22529807668695506 |
| 超过原读图界 + 1e-6 的点 | **96/117** |
| 最终状态 | `joint_unresolved_no_freeze` |

联合 Ea 命中原搜索上界，`active_mask` 对应项为 1，距边界远小于 1e-6，因此停止符合登记。同时训练点仍有大量超界，不能称为图示分辨率内一致。

已保存的敏感度只属于选定的 **τ=600 s** 剖面。审核从三份 117×4 Jacobian 以独立的四阶 Gram 矩阵/Jacobi 旋转复算原与列归一化奇异谱、条件数和所有成对矩阵差：最严原条件数约 90.33043，归一化最小/最大奇异值比约 0.01356479，最小奇异值 0.002589454，观测差 εJ=0.00002303427，该点局部数值筛查确实通过。它不证明结构唯一性，也不代表联合解辨识通过。

`study.json` **没有** `joint_sensitivity`、`candidate_parameters` 或 `candidate_tau_s`；实际训练目录只含绑定、状态和九个训练结果文件，没有锁参或 50°C 评价产物。按已冻结执行路径，联合解在边界判断处返回，未进入联合敏感度计算。

## 证据范围

这是已保存结果的独立被动核查，不是第二次正向预测。原文件没有完整逐格状态和接受步数组，因此不能独立重放全时段值域/单调性、或重新计算 τ=0 模态参照；这些部分只核对保存诊断、门槛和累计出流的一致性。SVD 从完整保存矩阵重算，但没有重新生成扰动预测。

墙钟、运行时版本、子进程状态与调用计数来自原保存记录，并核对内部一致性；审核者没有在原运行中另设独立计时器或调用计量器。无锁参/无 50°C 解析结论还结合已冻结代码路径和保存目录，而不是操作系统级访问追踪。通过文件完整性和审核不等于通过材料模型、热湿耦合或完整 Goal。
