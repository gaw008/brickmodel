# S1 实施前代码审查

结论：**Approve，当前无剩余 CRITICAL/HIGH 阻塞**。仅准入本次事前登记规定的单次训练执行；没有审批拟合参数、Wang 材料资格、50°C 预测或完整砖模型。源码身份见 `REVIEW_FREEZE.json`。

本审查只读取旧 D1 实现/数据读取逻辑及本阶段代码和登记；没有解析真实 CSV、真实训练、调用 native EOS、重装环境或修改生产/作者测试文件。独立探针使用制造向量、替代优化器/子进程，以及直接检查被捕获的 FVM RHS/Jacobian；没有运行新的真实 PDE 拟合。作者报告的 38 项制造求解器测试不是本审查重复执行的结果。

## 修复与证据

| 审查发现 | 当前修复及独立验证 |
|---|---|
| HIGH：列归一化可隐藏原坐标极弱方向 | 原条件数 ≤ 1e6、三组 Jacobian 和最小奇异值/观测差门槛生效；弱方向反例在 `red02.xml` 原失败，当前通过拒绝测试。 |
| HIGH：仅网格一致允许质量缺口进入拟合 | `predictions` 强制守恒、值域和非增诊断；质量缺口 0.125 在 `mass-red.xml` 原失败，当前用实际 `mean_increase_max` 字段验证质量门槛拒绝。 |
| HIGH：三组 Jacobian 最大差漏掉首尾一对 | 当前包含全部三对；`pair-red.xml` 显示旧观测差 0.045，实际最大差 0.09，当前拒绝反例。 |
| HIGH：数值复核失败仍发布正式读图界比较 | `metrics` 要求显式布尔结果，失败时文件级 `numerical_check_passed=false`，数值 allowance、逐点比较及 outside_count 均 null，原预测/残差保留；实际 `Study.fit` 替代优化器路径已验证。 |
| HIGH：运行后输入无法读取会丢失监督终态 | 当前保留摘要错误、`inputs_unchanged=false` 和实际子进程终态；原反例 `driver-red.xml` 失败，当前保留制造退出码 7。 |
| MEDIUM：混合 bool/float 时间数组隐式转换后绕过输入检查 | 当前 object 转换保留单项类型，两类混合 bool 数组均在调用求解器前被拒绝。 |

并行修改导致 `probe-before-qualified-metrics.py` 和 `support-before-all-pair-error.py` 的实际归档字节已包含 metrics 修复。**文件名不代表 metrics 修复前状态**；`driver-red.xml` 是一项通过（metrics）与一项失败（监督终态）。该 metrics 发现有初始静态阅读证据及修复后执行测试，不能冒称已保留其修复前执行失败。所有原 RED 文件保留；初次 `red.xml` 的另一个失败来自审查测试自己的浮点精确相等断言，已在 `red02.xml` 分离。

## 当前独立回归

| 输出 | 结果 |
|---|---|
| `support-green.xml` / `support-green.log` | 7 passed，pytest 日志 0.33 s |
| `driver-solver-green.xml` / `driver-solver-green.log` | 11 passed，pytest 日志 0.35 s |

除上述反例，还验证原参数端点精确映射、曲线等权而非点等权、边界差分不越域、零谱可 JSON 编码并不通过、N=1/2/16 在 τ=0/600 下瞬时全层水量恒等式和稀疏解析 Jacobian 对有限差分、初始时刻无需求解且完整诊断。两个测试进程分开执行，support 探针的替代求解器模块没有污染实际求解器代数检查。

静态核对确认：半格表面阻力、通量符号及独立累计 q 正确；均值非增诊断按接受步与请求点的时间并集计算；没有回写守恒或裁剪状态。N/2、N、2N 和收紧容差的条件与当前登记一致。这些是观测收敛政策，不是全参数域严格误差界。

`probe.py` 仅加载 40/60°C；旧读取器在解析其余数值字段前排除 50°C，117 点要求保留。七 τ 各重新拟合原三参数，第二固定起点、局部检查及联合拟合路径与登记一致。600 s 共同时间和 1500 次实际优化残差调用不随起点重置；敏感度/数值检查是额外正向计算，仅共同时间计费，登记明确区分。边界、平剖面、未收敛和未分辨均不自动锁参。监督使用无 shell 的独立 Python 子进程，630 s timeout，当前后置摘要失败仍写终态；未执行真正超时耗时测试。

初始 `git diff -- '*.py'` 为空，因为新文件尚未追踪，因此按实际文件读取审查。当前解释器中 ruff、mypy、pylint、black 均不可用，未安装；静态审查不冒称这些工具通过。不为研究脚本增加无用途格式框架。
