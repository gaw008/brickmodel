# Initial support review

The original `study_support.py` was read before the solver and probe were complete. Endpoint mapping preserves the exact original Dref/kref search limits, Ea retains its declared 100000 scale, and residual weighting correctly gives equal total weight to each selected curve. Grid/tolerance refinement is explicitly described as observed convergence rather than a rigorous interval bound. Observation comparison remains pointwise against readout plus the stated numerical allowance, not an RMSE acceptance substitute.

**Pending numerical-rank gate finding:** `sensitivities` originally accepted a manufactured response whose weighted Jacobian is approximately `diag(0.5, 0.5, 0.5, 5e-13)`. Its raw condition number is approximately 1e12, but column normalization gives four unit columns and the gate returns `accepted=True`. This is mathematically full rank; the failure is admission as a numerically robust sensitivity screen despite the raw-conditioning criterion proposed in `MATH_PLAN.md`. The gate also originally compared only two perturbation steps on one grid/tolerance, rather than estimating a Jacobian discrepancy from independent grid/tolerance refinements. These are decision-policy deficiencies, not evidence that the manufactured response is a physical drying model.

The existing snapshot, manufactured test and actual outputs are retained. `red02.log/xml` isolate the remaining gate counterexample (two checks pass, one fails). The first `red.log/xml` also contains a reviewer-only exact-float-equality mistake in the weighting assertion; that assertion was changed to a 1e-15 relative arithmetic comparison without changing production code or the substantive rank counterexample.

The author was notified to retain normalized singular values as descriptive output while using the raw smallest singular value versus measured Jacobian-discrepancy scale and raw conditioning for admission, and to report a zero spectrum as unresolved without serializing NaN. Final review remains pending the author's changes and completion of the solver/probe. No CSV was read and no PDE solver was called by these support-only tests.
