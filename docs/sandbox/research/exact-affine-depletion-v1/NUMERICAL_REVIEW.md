# Selected-cell exact affine numerical review

APPROVE the bounded accounting helper, implementation 8448d3196084116b4b80c5348c48692665017490f969b2b94f505c7bb8601548. No blocker found within its explicit selected-cell numerical-surrogate scope. Reviewer independently ran all 14 pure tests: 14 passed in 0.07 s (reviewer-tests.xml/log), without EOS or repository edits.

Independently checked saved-cell2.json against the actual full native failure artifact: source SHA256, clock, original roundoff policy, three old signed terms, downward old evaporation, actual raw selected liquid and both evaporation observations all match. Thus the regression preserves the measured failure inputs and original correction gate, rather than testing a substituted physical case. Original binary64 clock still fails its fraction gate. The reported new 26-level enclosure changes numerical endpoint representation, not the sampled affine RHS or policy.

ExactAffineSamples forms each component acceleration using the represented exact midpoint interval. Strictly negative total rate at both ends establishes negativity throughout the entire affine domain; positive initial inventory and nonpositive upper inventory establish a unique decreasing numerical root. Every component integral is computed rationally and rounded independently once. Separate signed evaporation samples are integrated over their positive part, including either zero-crossing direction, and the denominator is rounded downward.

Evidence explicitly represents an adjacent dyadic bin of the original domain at its declared level. It checks lattice alignment, width, domain, signs and original time gate; it does not claim a nearest rational or exact irrational root. Refinement only proceeds within the fixed 256-level resource bound until existing local, absolute and evaporation-fraction correction budgets also pass. The local allowance is unchanged 4-ULP accounting plus the exact polynomial residual at the lower endpoint. A manually assembled evidence object is revalidated at writeback.

Writeback binds all three signed integrated terms to evidence individually, original initial liquid, exact gross diagnostic and original policy. It verifies reconstruction before zero-liquid bypass. Vapor paired correction, neighbor-rounding bound and local/cumulative storage/element/mass/correction budgets retain the original shared helpers and totals type. State/totals are immutable until successful construction; energy/mechanics and unrelated inventories are retained. It is not an event acceptance or mixed-mode transition.

Scope conditions remain essential: sampled source labels are not provider authentication; the saved fixture raw state is an accounting sentinel, not the full physical panel. Samples do not require root lower > midpoint, nor validate full other-species/other-cell positivity or inter-cell event order. These are explicitly required in the caller's coupled bridge, including a valid wet midpoint before the chosen terminal endpoint, exact full-panel quadrature, new source/clock records, all original full-state comparisons and once-only global commit. No EOS or true-RHS remainder has been recomputed. This approval does not declare the four-cell native failure repaired.
