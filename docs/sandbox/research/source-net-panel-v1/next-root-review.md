# Narrow independent review of ROUTE.md

The author route is sound. No redesign is needed. The numerical values in its eleven-row table check out: exact Fraction substitution verified the stated roots, exclusion minimum3/4, proportional tie and common-later-root example. Exact translation and invalid-input rows are contract tests rather than numerical substitutions. No EOS was run, and ROUTE.md was not modified.

Four concrete implementation clarifications are needed:

1. **The shared bisection step must preserve legacy zero endpoints.** Actual ExactAffineSamples accepts p(H)=0 but requires strictly negative endpoint derivatives. Actual locate/order moves the lower bound on p(mid)>=0 and constructs the old evidence only after each refinement. The pure helper must permit p(lo)=0 and p(hi)=0 as applicable; do not impose a newly strict sign condition or collapse a dyadic exact hit into a point in legacy wrappers. A new branch with derivative zero at a vertex is also valid (convex descending branch ends at its minimum; concave descending branch starts at its maximum). Do not transplant legacy strict global derivative validation into the new generic branch record. Keep legacy constructor/exception order, loop counts and exact evidence budgets unchanged.

2. **Define exact-point classification precedence explicitly.** The route's crossing root can be a rational point, including a dyadic hit. At p(H)=0 distinguish a first root at H from a later returning root at H; a global shortcut 'endpoint zero => first endpoint root' is wrong for a convex dip. Example p=3-8t+4t²,H=3/2 has first root1/2 despite p(H)=0. For a tangent at H (e.g. (1-t)²,H=1), choose and test whether root_kind is exact_tangent or exact_endpoint; favor exact_tangent to preserve the non-crossing meaning, with endpoint location separately implicit in its time. This only clarifies the new record; old behavior stays unchanged.

3. **Common-root membership must be exact and first-branch-specific.** In the given pair, polynomials are t²-t+3/16 (first root1/4, descending branch ending1/2) and t²-5t/4+3/8 (first root1/2, descending branch ending5/8). Their GCD root3/4 is beyond both first descending branches and must not produce a tie. For a linear GCD, evaluate the common rational root and require membership in both independently revalidated first-root branches/current enclosures, including point-root equality; testing only 0<root<=H is insufficient. Revalidation must recompute the branch from coefficients rather than trust stored branch endpoints. A proportional-quadratic shortcut is safe only after both positive-initial first-root records have independently passed validation; degree alone must not validate a forged record.

4. **n0=0 remains an explicit completeness boundary.** Rejecting zero-initial polynomials in this primitive is appropriate, but the source-level competition result must not say 'complete' if it silently omitted any declared inventory already at zero. At minimum it must carry an explicit unsupported/incomplete zero-initial label, unless a separately validated whole-panel nonnegative zero-state argument is supplied. This includes a zero gas slot even when liquid is positive. Do not conflate an identically-zero inventory with a positive inventory that has no root, or feed negative/zero energy coordinates into inventory competition. The route already states the physical limitation; enforce it in the result contract as well.

Additional focused regressions justified by these clarifications: root exactly at initial upper endpoint; p(mid)=0 with the legacy >= update; convex second root at H; tangent at H; proportional degree-two GCD with mismatched/forged branch records; n0=0 with zero derivative versus negative derivative (both unsupported here until separately admitted). These supplement, rather than replace, the eleven existing examples.

The author correctly separates generic root existence from positive-evaporation correction permission. Pure GCD/sign/minimum extraction can preserve the existing locate/order loops; extending old ExactAffineEvidence or writeback to drainage is not authorized by that extraction. No physical event, material qualification or completed depletion integration is established by this review.
