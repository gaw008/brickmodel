# Scoped repository adaptation follow-up

Reviewed only changes from previously reviewed temporary candidate and new actual-source integration tests. No repository modifications or EOS execution; parsed all three files. Prior math/derived-integrity review remains in REVIEW.md. Exact reviewed hashes in REPO_FOLLOWUP_FREEZE.json.

Production changes add ExactBoundaryState/exact time equality checks for programmed records, exact False material qualification for both enclosing SourceExactEvaluation and raw source observation, and exact DisabledChemicalRates tuple dimensions/Fraction-zero/reference-power/False phase flag. These close the reported record-consistency gaps without changing polynomial equations, interpretation or granting physical admission. Both material flags are validated before hashing, so omitted enclosing metadata cannot silently upgrade qualification.

New integration tests obtain original and explicitly constructed Euler interior states through actual ExactSourceColumn adapter and SourceWetStorage/column evaluation, under the existing artificial water seam. Packed interior state uses exact represented operands followed by float projection; it is accurately labelled an explicit Euler sample, not an accepted physical trajectory. After sample creation, additional inverse/storage/water/chemical/adapter calls are made fatal. Polynomial derivative checks directly reconstruct phase and shared source-face incidence at both sample times for all four fluid slots and U, with closed/open N1/N3 cases and global water/energy telescoping. Program-time corruption and chemistry type-substitution tests change metadata on real outputs; original physics evaluation is not bypassed. Raw source metadata mutation and both material flags are tested as rejected after build.

No new high/critical functional finding in this delta. Actual-source means actual implementation execution under artificial water, not native EOS or plant measurement, and the test docstring states that. This review did not repeat root's currently running20-test selection and makes no claim about its terminal result. Scoped approval applies to these hashes pending root verification completion; no need to reopen previous broad numerical review.
