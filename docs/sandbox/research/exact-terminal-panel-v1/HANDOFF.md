# Full-state exact affine panel candidate

Frozen exact_terminal_panel.py SHA256 2fdfb079e77c37c83745fe97cfd61ee4492c26814a80a762e0adc3ddf5490078. Own temporary directory only; no EOS, installation, production edits or mode transitions.

build_exact_affine_panel(state,first,midpoint_rates,*,start,midpoint,end,policy,liquid_index,selected_cell,wet_cells,source_binding) returns frozen ExactAffinePanel(raw_state,ledger,source_binding,qualification). Times are ExactEventTime, start<midpoint<end. No float projection. The caller must supply genuinely same-source actual rates and valid midpoint state; supplied labels are not an authentication mechanism. Midpoint rates receive shape/schema checks only, not derivatives(initial), because initially zero species may legitimately be produced then consumed. Initial rates retain original derivative validation.

Each face species/face energy/source/cell work/component/stretch integral uses exact h*r0+(rm-r0)*h²/(2*hm), independently once-rounded. Raw N/E directly sum initial plus represented signed ledger terms with Fraction, avoiding double-rounded net increments. All species polynomial minima are checked, including interior convex minima. Wet indices must exactly cover positive initial liquid inventory; every competing wet liquid remains strictly positive. All stretch minima and represented endpoints remain positive. Selected liquid may reach zero, but no event is certified or mode switched.

ExactStepLedger records shared faces once, power component integrals and exact quadrature rounding, stretch increments and rounding. Local raw representation, mechanical exact quadrature and component-sum errors use original policy absolute gates. Global cumulative audits remain caller responsibility. Positive nonzero integral underflow is explicitly refused rather than silently dropped.

Tests01:5 passed .06s. After completeness hardening of wet-cell declaration, tests02:5 passed .05s. Tests cover independent hand calculation of all fields/components/mechanics, origin 0/1e12 equality, read-only state, positive endpoints with negative interior species or stretch, strict competing-liquid zero refusal, selected zero allowance, shape/schema/time refusal. XML/logs retained. No native/provider fixture was invoked. The independent code reviewer has been requested; do not infer full ordered packet acceptance from these tests.
