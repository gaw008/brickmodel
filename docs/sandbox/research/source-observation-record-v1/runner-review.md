# Saved-input runner review

Approved `exercise_saved.py` SHA-256 `6be713ee0554b60c0cbcc445d7eba7ce32cfadf2b1b91be4f6549859e2719c6f` and preregistration `e06ca1f25c1417765c94f68a173b0d2f597a62dfee88278bd7492118433ddc67` for one registered passive exercise after the final codec/service defects are closed and the installed package identity is checked. Static read and AST parse only; this reviewer did not execute the runner or any model/EOS.

The three input byte counts/hashes and capture counts match the independently inventoried originals. Fixed iteration covers 32 N3, 32 N1, and 47 programmed observations. The independently written projection enumerates every dataclass field, including derived fields, and compares canonical legacy state/evaluation against the untouched input, preserving numeric types and original array/cell order. It does not use the codec's private projection as its oracle.

The programmed adapter explicitly uses recorded nested identities, assigns a declared import role, and leaves modes unknown. It preserves original ordinal/index and complete-file hash. The actual old capture has only ordinal, packed_input, time and evaluation; this does not silently broaden CLI import to guessed formats. Normal inputs retain their recorded context.

Provider/model constructors, native HEOS construction and the relevant evolution entry points are forbidden before the input loop. Importing the current `_heos_kernel` module itself performs no EOS work. No registry based on payload-selected import names is used. The four qualification/authentication/resume flags remain false; this exercise does not validate the surrounding transitions or source assets.

The runner creates a new output directory, writes each record exclusively, retains previously written outputs and accumulated checks, and writes the exception plus current attempt/partial completed count in the final result. A 60 s boundary check and 90 s signal cap limit the fixed 111-record task. Caught failures do not retry. Snapshot evidence remains available for a failed later record. The registered separate CLI observation import is passive I/O, not another simulation.

## Review Summary

| Severity | Count | Status |
|----------|-------|--------|
| CRITICAL | 0 | pass |
| HIGH | 0 | pass |
| MEDIUM | 0 | pass |
| LOW | 0 | pass |

Verdict: APPROVE — runner only, conditional on final code/installation checks; no execution was performed by this reviewer.
