# 下一次可执行工作

原三步非静止、连续/在线暂停两路径门槛已实际通过；不要重跑native01或旧native02。

1. 接入来源普通段的持久化恢复：以已有数值checkpoint和完整来源记录为基础，
   明确原输入、初态、时间/步长/误差、累计账本与真实费用的一次衔接；
   在新进程重新构造真实provider并核查来源，而不是反序列化或继续使用lease。
   新构造归入累计预算，损坏/域变/版本不符必须在新物理调用前拒绝。
   复用现有exact controller，不能只复制纯数值checkpoint就宣布来源恢复已完成。
2. 同时修正下一版worker的报告字段命名：原native01的ACCEPTANCE.case_sha256
   实际为canonical config SHA，raw case SHA已在父/监督记录正确保存。
   原文件保留；新出版协议分列raw_case_sha256与canonical_config_sha256，
   按新执行版本冻结，用纯成功/失败出版控制验证即可，不能为改标签重复旧物性实验。
3. 完成共享来源CLI/UI的运行、取消、恢复、空间曲线、原始证据跳转实际使用路径。
   然后扩展预登记的时间/空间验证与长时求解，不把此次0.046876s三格验证当作全烧成。

原Goal的原泥同材料反应热/动力学、有限供氧、烧结/冷却、三个机制公开留出、
完整循环、多代搜索和最终应用验收仍是必需项，不因数值门槛通过而删减。
材料阶段继续从原GNEST/El Salitre retort基准缺项出发寻找同材料能量/产物依据；
原基准审核已完成，不再反复做相同算术，不把缺热效应或缺相组成填成0。
