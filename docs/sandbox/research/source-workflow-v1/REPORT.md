# 完整来源执行范围与原三步续算对照

起点9544ec9；完整Goal继续，未达到全流程材料模型的完成条件。

这一阶段把上一阶段单RHS的执行优化接入实际父研究和普通段。
父研究与每次advance分别建立、关闭执行范围，暂停期间不保留lease；
初始能量、wet补查和逐RHS外的调用保持逐点核查。Native算法及常数未改。

新的workflow v3 profile与manifest明确绑定执行实现。旧v2清单、原案例和kernel保留；
旧v2的9544ec9三个执行源码快照见historical-v2，不能以变化后的scope代码继续冒用旧身份。
默认公开API仍走原逐点方式。新的受管理路径只由安装包的隔离模块进程进入。

共同预算是父一次加各普通段相对父计数的增量，暂停前缀不重复计数。
构造、读档、lease开关、在线暂停均计入原时间上限；未返回session的构造失败读取已保存的实际计数，
保存失败则标未知或下界。用户取消、资源耗尽、计算或发布失败分开。
晚关闭/审计/发布失败保留实际execution，清除可继续的checkpoint。

数值验证设定见[NATIVE_PLAN.md](NATIVE_PLAN.md)，结果前已冻结并独立审查。
原97RHS/16wet/510s、普通180s、外部570s、3步/路径与全部误差门槛保持。

实现与回归证据：
- lifecycle新增28项通过0.59s，另7项默认边界通过0.38s；原RED与并行import未完成失败保留。
- root第一轮35过/2失败：空cancel_file不满足共用非空字符串解析，以及开发中清单尚未更新。
  修为显式disabled与最终独立核查的版本清单，未改物理设定。
- 122项相关纯测试0.75s；新增4个worker实际控制流反例后，worker36项通过0.28s。
- 原服务、续算、配置79项通过109.55s；属于制造物性seam回归，不能计作真实HEOS验证。
- 非editable安装151模块/158包文件与源码逐字相同；安装292项通过48.01s。
- 独立审查5个末端取消/超时/发布失败控制0.24s通过，34项版本/配置/源码检查通过。
  159项冻结输入全匹配，无未闭合发现。子代理审核不是外部材料验证。

本阶段实际物性尝试：native01，由安装解释器以-I -m启动，由原POSIX supervisor监督。
父研究已实际completed且numerical_event_accepted=true，32RHS/4HEOS/3初U/8wet，
到父完成的worker累计48.38408975s。两路径已实际completed，各3接受步、22RHS、0拒步，全部状态/观察/账本一致。
worker共116.22751054s，监督117.36645858s、退出0、全部输入未变、子进程回收。
总76RHS/12HEOS/3初U/8wet。父、连续、暂停/继续的四个lease分别覆盖32/22/8/14 RHS，全部closed/verified。
三格ΔT为5.699694e-6、-1.103617e-6、-4.610047e-6K，
两端inverse界之和为6.342429e-10、3.583858e-9、2.810146e-9K，三格均分辨出真实数值变化。
逐步总U残差为8.640200e-12、-8.185452e-12、-1.045919e-11J，满足原门槛。
终点为0.04687599380522851s，不能把这段短时三格验证夸大为完整烧成。

[native01原始证据](native01/README.md)保存396个原文件的压缩归档及逐文件清单；
17个第三方来源asset不再分发，已有身份/本机原目录。原两路径记录及数值checkpoint均保留。
此阶段实际运行不依赖重跑旧父研究或改动误差/资源门槛。

明确的报告字段问题：ACCEPTANCE.case_sha256目前写入的是规范化配置SHA ead8e8db…，
而原始案例字节SHA为e57c51e4…；父result.json及监督metadata已正确保存二者。
这不更改输入或数值结果，但孤立使用该字段会误解。原文件不覆盖，独立派生报告分列两者；
下一版worker出版协议明确命名raw_case_sha256与canonical_config_sha256，见NEXT_STEP。

独立被动数值审查已终态通过：236个事件、两条完整结果、各22观察、7余额行、全部检查点字段及324个监督输入逐项核对通过。标准库独立算术0.246s，0应用导入/0EOS/0重建；不把984语义/证据断言与118520结构检查冒称119504个独立实验。七个全链余额行核验不等于重新计算历史全部求积，范围见[独立审查](review/native01/FINAL_REVIEW.md)。唯一已知MEDIUM为上述哈希字段误名，派生报告已分列，数值结论有效。

科学范围保持：原污泥来源比热与真实水物性参与的制造几何/输运数值验证。
不是材料预测资格、完整湿坯到冷却循环、真实产品性能或工厂控制。
数值checkpoint编解码不会授予历史source controller跨进程恢复。
