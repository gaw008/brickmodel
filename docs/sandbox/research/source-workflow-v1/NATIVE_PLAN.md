# 原三步两路径受管理执行预登记

状态：源码控制测试阶段，尚未启动本阶段实际物性运行。起点9544ec9。

目的：把已验证的单RHS重复读取优化接入真实父研究与每次普通段advance，
检验原native02失败的非静止三步门槛。不能把单RHS速度或制造算子测试当成本门槛通过。

执行：安装的Python以 `-I -m sludge_sandbox.source_workflow_worker REQUEST.json` 启动，
closed JSON仅规定新父研究、两条普通段、数值步长和可选取消文件，不装载用户Python。
进程监督原570s，清理1s；进程自身从入口计时，父研究/读档/构造/两路径/暂停/验证共510s。
总RHS最多97、额外wet请求最多16；普通段原180s、max_steps/max_rejections=4及全部容差保持。
当前合作取消在计数达到cap时保守停止，不能把它当作已允许越过cap；任何耗尽仍是resource_limit。
这些数值是既有数值验收/资源策略，不是材料常数。

案例是原source-nonstationary-heos-v2的完整物理/数值设定，仅换新workflow profile和清单。
新清单公开列出实际执行源码；旧v2清单及案例不改，9544ec9执行源码归档验证其历史身份。
本阶段不使用历史父研究进行live resume；新父记录必须在同一冻结运行时实际成功生成。

原比较量与门槛（结果前冻结）：
- 父研究completed且numerical_event_accepted=true；选择其候选1真实末态。
- 每条普通段从该末时前进3/64s，初始/最大步1/64s；3接受步、22RHS、0拒步。
- 连续路径与暂停1接受步再继续的路径，全部数值状态、步、观察与账本逐项相等，仅排除耗时。
- 数值检查点编码/解码相等；继续原live checkpoint对象，解码不授予跨进程来源恢复。
- 实际内部导热非零、U变化；库存N不变；至少一格ΔT超过两端原inverse条件误差界之和。
- 每步边界净能流为0，逐步总U变化在原energy_absolute_tolerance_j内。
- 父一次+两条各自增量只记一次；预期总76RHS、12HEOS构造、3初U、8wet，以实际记录为准。
- 父lease一次、连续lease一次、暂停路径每次advance各一次；全部关闭/逐RHS核查完成；暂停/发布无权限。
- 材料资格、完整烧成、训练资格、历史source resume均保持false。

若失败：保存原异常、真实接受前缀/未接受回调、counts、关闭/审计次级错误、输入前后身份和清理结果。
不得降低三步、放宽物性误差或时间限制、重写来源来重跑。修复必须基于实际原因另登记新尝试。
原单RHS成功和原native02 wall_time_limit失败证据都保留。
