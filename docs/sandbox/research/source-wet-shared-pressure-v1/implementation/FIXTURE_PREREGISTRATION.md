# New manufactured wet-pair test fixture

Before implementing the wet-pair module, select the existing test_source_wet_storage.setup liquid seam (constant v = 1.8e-5 m3/mol and constant Cp = 75 J/mol/K; this is explicitly artificial, never native water). Reuse its actual source Cp storage, fixed mass 0.2 kg and nominal fluid volume 0.001 m3. Declare original available-volume error 1e-12 m3, liquid molar-volume error 1e-15 m3/mol and |uP| bound 1e-6 J/mol/Pa for this new test scenario. These are manufactured numerical inputs, not measured material constants; original tests and thresholds remain unchanged.

Endpoints use Nl=0.2 mol, gas (0.2,0.2,0.001) and (0.2,0.200001,0.001) mol, target temperatures 330 and 330.000001 K. Actual SourceWetStorage.evaluate supplies target U and actual invert uses InversePolicy(1e-6,1e-6,100); all EOS calls are the explicitly patched manufactured seam. Tests do not require any event pressure threshold to pass. The fixture deliberately has independent nonzero original errors and distinct inventories/temperatures. Same-end comparisons separately exercise exact deduplication. Pure support/normal/error adverse tests may replace saved observations with explicitly labelled manufactured alternatives; they cannot claim new real EOS evidence.
