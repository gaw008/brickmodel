# Independent manufactured oracle

The preregistered standalone run completed with 1,414 assertions in 0.025839792 s (ORACLE01.log, ORACLE_RESULT.json). It imports only Python standard-library modules, does not import the candidate implementation, and performs no EOS/model construction or trajectory evaluation. Script SHA256: 5a82761cf0b32d211e980b108fc203af2932125753bc370e5613d6737949eb1b.

Four manufactured pairs cover changed temperature/gas/liquid inventories, reversed signed residual difference, equal-state cancellation, and unequal liquid inventories. Six hundred independently evaluated rounded closure observations validate the declared liquid-ratio error and full Gamma bound (1,200 assertions). Eight endpoint cases additionally check all-volume root signs, positive liquid volume support, widened continuation domain, retained old L, independent 100-digit quadratic roots in J, and temperature root movement. The 108 full paired comparisons evaluate independent roots at three shared volumes and nine temperature-corner/center combinations per pair. Two explicit negatives demonstrate an invalid upper bracket and noncancellation of equal states at different volumes.

Actual Gamma bounds range from 1.2651416377388227e-18 to 1.4165714407408313e-18 m3. All sampled machine closures are enclosed. The hypothetical machine volume rounding is separately included as abs(F(float(V))-V); it is not incorrectly assigned to EOS error or canceled as a shared parameter. The independent root bound includes retained/widened temperature propagation, and passes all 108 comparisons.

This verifies the contract's algebra on manufactured cases, not the candidate production module or its collector/identity/error-provenance gates. The joint intervals are deliberately broad (equal-state case still 2.12543 Pa); no acceptance at the original 1e-4 Pa threshold is claimed. No material qualification follows. Finite sampling is not a proof of a source provider's global epsilon or monotonicity. The analytic v, its slope, and its thermodynamically consistent uP = v - T vT were explicitly manufactured. The report's implementation count does not include all proposed future rejection tests: monotonicity-envelope and whole-box subnormal rejection still require candidate implementation tests. There was no failed run or overwritten failure in this oracle execution.

The independent Gamma expression and root/temperature bridge are consistent with CONTRACT.md. Production review must still establish that every live identity, original actual-fluid radius/projection, full-box normality, and all-domain declaration gate is enforced; this script does not certify those interfaces.
