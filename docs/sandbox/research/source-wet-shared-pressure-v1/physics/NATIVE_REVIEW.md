# Independent terminal native saved-data review

Input SHA256 6c55383e8d2063ed6c81f2126b40542ba44ee173fc1671b227573b908bfbfc53 was verified before analysis. Two standalone standard-library audits passed 66 + 215 = 281 assertions in 0.308364333 + 0.522750042 s. No EOS, storage construction, integration or production import was performed.

The four actual wet pairs give event/common bounds of 4.197615451054751e-5 Pa for cell 0 and 4.162917357817387e-5 Pa for cell 2. Exact Fraction reconstruction verifies their outward J, liquid v intervals, both all-volume root signs, GL/GG/GS and Gamma, actual fluid error, total projection, saved residual zeta, new whole-J temperature continuation, retained max slope and authoritative joint-only bound. The original 1e-4 Pa policy is unchanged. Dry cell 1 retains its existing separate joint selection. All three selected pressure gates pass at both comparison stages; the original reported and independent conditional pressure gates remain false. Numerical event acceptance is true, material qualification false.

Compared with original native SHA256 2660d33ec0e832e006d5adcccd8ddcf38cc314ac5e65a17830cdf2f73cbdc51d, all 32 actual source callback inputs, exact times, complete evaluations and interface modes are unchanged. Initial state, all three initial energy records, geometry/provenance, liquid transport, horizon and both policies match. Original per-cell endpoint differences/gates, conditional pressure bounds/gates and clock gate are identical. Complete terminal/projection/dry-result records compare identically wherever present after removing ONLY elapsed_seconds (runtime metadata). Thus the previous full-path inventory/energy/Darcy audit continues to refer to identical physical observations; this run did not numerically reintegrate those ledgers in the auditor.

Eight actual added liquid observations comprise four wet pair calls and four distinct provider/T/P/phase keys. Each saved WaterState matches the corresponding collector observation and recorded provider object identity; pair declarations match recorded storage/volume identities. Both wet endpoint views match their actual candidate cell pressure records. Saved identity evidence is checked for consistency; JSON cannot recreate live Python `is` identity, which remains a producer admission assertion. Pure comparison EOS attempts are recorded as zero.

Two auditor errors are preserved. SAVED01.log/audit_saved_wet01.py assumed WaterState.molar_mass was a stored field; it is a property derived from reference.molar_mass_kg_mol. SAVED02 uses that actual schema. SCOPE01.log/audit_saved_scope01.py compared run-dependent seed elapsed_seconds; the correction excludes only that explicitly named timing field, with all physical data retained. Both original model files remain untouched.

Artifacts: SAVED_WET_RESULT.json and SAVED_SCOPE_RESULT.json; audit_saved_wet.py and audit_saved_scope.py; SAVED02.log and SCOPE02.log. FINAL_MATH_DELTA.json verifies six mathematical function ASTs identical between the independently bridged 836a9f15 candidate and final 9a0b7af1742c91edab9a9999c106657136f491b3ef0ca5583494434fc6ce4e25. No oracle rerun was needed for the collector binding fix.

These are conditional numerical-model bounds. Four observations do not prove source-wide epsilon, stable-liquid monotonicity, or caloric derivative hypotheses, and no real material calibration or complete brick-cycle acceptance follows.
