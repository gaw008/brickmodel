# Production formula and manufactured bridge review

Read-only formula review found no mismatch between CONTRACT.md and the examined source_wet_shared_pressure.py _error_parts/enclose implementation. The source was 836a9f15256e73159cfa7e15de919d3a08ac4dd0a12933e2335090d58761e057 before and after the bounded bridge run; author final freeze is still separate.

The actual rigid closure computes (Nl*m)/rho, (fsum(n)*R*T)/p, and fsum(liquid,gas,-V). The new Gamma respects these operations instead of substituting rounded Nl*v. Its liquid product projection, exact-sum gas numerator projection, division spacing, and two-spacing aggregate margin follow the contract. It uses the entire outward J for v intervals, root-existence signs, compliance and machine margins. The full J-root radius feeds the new continuation; max(old L,new L) retains the original temperature contribution. Actual fluid error and total projection are retained independently; strict producer decomposition prevents silently assigning excess uncertainty to the shared parameter. The final selected bound is joint-only.

The fresh bridge completed 34 assertions in 0.936627875 s, with an actual source-host inverse using the explicitly manufactured constant-v water fixture and four new manufactured observations. Twenty independent exact pressure-root comparisons cover five common volumes and both full temperature endpoint choices per state. The other checks cover completed collection, actual four-query count, Gamma decomposition, residual-to-root propagation, actual-fluid/projection retention, temperature slope retention, authoritative joint sum, false qualifications, and before/after source hash equality. Observed joint bound was 2.757468438572496 Pa; this deliberately nonidentical fixture does not pass or claim the original 1e-4 Pa gate. No HEOS was run. No audit failures occurred.

Files: bridge_oracle.py, BRIDGE01.log, BRIDGE_RESULT.json. Independent roots use the known manufactured constant liquid molar volume and exact rational ideal-gas closure, not a candidate function as truth. This supplements the prior 1,414 independent linear-liquid checks without rerunning them. It does not prove source-wide EOS error, monotonicity, or caloric bounds; those remain explicit conditional hypotheses. It also does not certify material parameters or numerical-event admission. Full adversarial identity/record mutation coverage belongs to the separate code review and author tests.

Final-freeze correspondence: the author's subsequent WET_FREEZE notification reports exactly the same production SHA 836a9f15256e73159cfa7e15de919d3a08ac4dd0a12933e2335090d58761e057. Thus the formula review and 34 actual bridge assertions apply to that final candidate byte sequence; no repeat run was needed.
