Initial synthetic probe: success case passed; second case failed before any request because patch.dict(sys.modules) removed NumPy imported lazily by the fixed serializer, then Python rejected reloading it. This is a review-harness isolation error, not a production failure. Initial script and both output directories are preserved. Final probe imports the passive NumPy dependency before isolating synthetic sludge_sandbox modules. No EOS or physical provider was loaded.
