# Complete affine root-order review

APPROVE bounded numerical-surrogate ordering, frozen implementation 38a4f6b0bdbe6044e4a927e4c473ec295bbd1563e3dfcb05867287a879817142. Reviewer independently ran 11 pure tests: all passed in 0.10 s (reviewer-tests.xml/log). No EOS, installation or production modifications.

The supplied wet-cell tuple must exactly equal the ordered set of positive initial liquid cells; omission, duplication, reordered indices and bool indices are rejected. All shared state/rate arrays, exact times, complete evaporation vectors, explicit source labels and policies enter the input digest. verify_exact_root_order recomputes the entire operation and compares full records rather than trusting a public frozen instance or digest alone. This binds supplied data, not the authenticity of the provider that generated it. Midpoint shapes are checked without incorrectly applying its derivatives to the initial state.

For every wet cell the code derives the signed left-face, negative-right-face and source rate, and its acceleration from the actual exact midpoint separation. A no-root exclusion requires strictly positive minimum over the whole quadratic domain, including an interior convex minimum. A possible crossing is never discarded merely because its endpoint is positive: unsupported nonmonotone candidates reject explicitly. Remaining candidates have strictly decreasing inventory on the whole common domain and a unique bracketed root via ExactAffineSamples.

All candidate brackets undergo common dyadic refinement. Acceptance requires the earliest upper bound to be strictly less than every other lower bound; the time tolerance is not misused as a minimum gap. Each candidate also constructs validated ExactAffineEvidence with the original clock/local/absolute/evaporation-fraction budgets. Requiring all candidate budgets is conservative and explicit; it may refuse a selectable first root if a later candidate cannot meet its own budget. There is no hidden relaxation or suppression of a later cell.

Exact polynomial Euclidean division determines shared roots. A quadratic gcd means proportional candidate polynomials; both are strictly decreasing with a root in the same domain, so their unique domain root coincides. A linear gcd proves equality only when its root lies in (0,h]. Coincidence is reported as first-root unsupported only once that cluster lies strictly before all nonmembers. Equal later roots therefore do not block a separate earlier root. Raw failed brackets remain explicitly uncertified diagnostics.

Tests cover tangent ties separated by acceleration, strict gaps smaller than time tolerance, equal roots with different initial tangent estimates, whole-domain exclusions and nonmonotone counterexamples, missing cells and changed source/full-array inputs, large-origin invariance, finite budget/no-root outcomes, three simultaneous first roots, and later coincidence behind an earlier event.

Scope: this is complete liquid-candidate ordering for the supplied affine surrogate, not full-state positivity, a coupled true-RHS root enclosure or acceptance of an event packet. The caller must validate actual same-source midpoint observations and wet midpoint state, require selected terminal endpoint after midpoint, use the same endpoint for all N/E/mechanical quadrature, apply original writeback/global-prefix audits, and retain full refinement/common-time checks and resource accounting. Actual runtime source authentication remains outside the label/digest mechanism. No claim is made that the native four-cell run is now solved.
