Prepared only. Root launches after independent review and no native process active.

Create fresh attempt01 directory; write source/runtime/helper/test hashes and exact command/environment before child. Installed qualified interpreter /private/tmp/brick-water-backend-probe/venv/bin/python; import sludge_sandbox from installed package, PYTHONPATH tests/sandbox only (no src override). BRICK_FREE_SLAB_DEPLETION_ARTIFACT absolute fresh attempt01/result.json; PYTHONDONTWRITEBYTECODE=1. Child runs pytest temp test_free_slab_depletion.py with --junitxml=attempt01/tests.xml; redirect whole stdout/stderr to attempt01/child.log. Root verifies installed modules match intended source before launch and sameactualfiles after reap.

Hold actual Popen handle, outer deadline660s includes import/setup/initial/final callbacks; internal600s applies integrate only. Persist launch status running plus command/hash/start before spawning. Poll owned handle; on deadline terminate ownedprocessgroup then kill/reap within explicitly reported boundedgrace, preserve childresult/partial/log; never retry from a polling timeout. Status records exit code/signal/timed_out/reaped/start/end/elapsed/source_unchanged and existence/hash of result/XML/log. Parent kill may preclude child finally; then status must say incomplete result, not pass. Success requires childexit0 and result.status passed and completeexpectedXML with source unchanged. No savedPID signalling or arbitrary savedcommands.

Normal pytest uses tmp_path when the artifact environment variable is absent; supervised runs use the explicit fresh override. Actual no-EOS dry host diagnostic snapshot serialized successfully (21899 bytes), see dry-encoding-check.json. The test starts durable result before setup and stores initial and full integration payload before assertions. Test snapshots only actual numeric evaluation/state/error/source records, not live host/provider objects. A setup/assertion/finaldecode exception writes traceback plus alreadycomputed payload then reraises. A killedchild may leave last durable phase; supervisor retains this honestly. No completion asserted from stdout. Run is a one-case event numerical check, not spatial/time convergence or material validation.
