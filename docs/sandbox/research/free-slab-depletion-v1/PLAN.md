# Two-cell free slab: one depletion, one wet (design only)

Current read-only audit,2026-09-08. No repo edits, installation, test launches or native EOS. This proposes a fresh numerical manufactured experiment, not reuse of a previous singlecell result or material qualification.

## Existing compatibility: no new generic admission required

WaterPhaseTransfer explicitly admits exact FreeSolidSlab and its evaluation. _thermal_host returns the actual free host; storages supplies complete real reference templates solely for provider validation; _fluid_storages and _liquid_index explicitly recognize free slab. Each actual evaluate gets current thermal states/inverses from FreeSolidSlab; rates forwards cells+1 mechanical vector and all external_traction/mechanical_constraint/body components. Each finite-rate source is opposite liquid/vapor at fixed totalE, with phase reference energy carried through inverse. No latent/D source is needed.

with_depleted_cells validates host state, exact zero of selected cell liquid, valid unique indices; replaces only chosen mode, preserving mechanical state and spectator wet mode. Depletion observe already extracts per-cell actual T/P and source numerical bounds from real thermal inverses; Rates.derivatives validates mechanical shape. Candidate event times are based on net liquid rates including faces/reaction, with positive evaporation requirement; simultaneous/too-close events remain unsupported. After first event, continue_after_event replans wet spectator stages rather than pretending all cells dry. Frozen and affine terminal propagate all inventories, all energies and full mechanical vector. Quadratic positivity, exact ledger fields, original-prefix budgets, both event/common comparisons and independent finer approach cover full vectors. Existing drywriteback leaves mechanical state intact. No singlecell guard remains in this event chain.

No code blocker identified from these concrete APIs. Remaining validation gaps are joint two-cell mixed-interface behavior with3mechanical components, spectator liquid plus actual global constraint work, and native resource cost/temperature-domain behavior. A production change before observing evidence is unnecessary.

## Small no-EOS regression proposed before native run

Temporary ManufacturedDepletionAdapter with two cells, species(liquid,vapor), state N=[[1e-4,0],[1.,0]], E=[600,602], mechanical[1,1.1,1]. Wet sinks(.001,.0002), firstdry sink0 while spectator continues; no massfaces. Manufactured mechanical rates(.1,.2,.3), E rates(1,-1) with explicit opposite mechanical_constraint component. Exact liquid event t=.1, spectator remains1-.0002t, n0=1+.1t,n1=1.1+.2t,tangent=1+.3t and E exchanges analytical. Test both terminalmethods with full3-vector Fraction prefix audit, interface tuple('depleted_no_nucleation','existing_liquid'), exactly oneevent and no globalenergy creation. Also run nested independent approach, terminal cancellation rollback and equal event-time refusal. Label strictly adapter numerical coverage, not actualfree closure. Existing mechanical tests inspected mostly use onecell; this would add meaningful shape/mixed-mode semantics. Prepare/run only under root-coordinated no-EOS window.

## Preregistered native fixture proposal

Use actual test_free_slab_phase_transfer.slab_transfer source-qualified fixture, unchanged reference .02m/.014m²/2, manufactured solid/skeleton/transport, actual water+ideal-vapor chemical bridge; fixed2molsolid/cell. Conductivity .2, gasD/permeability0 as existing fixture. No liquidtransport first experiment. Initial rows [[1e-8,.01,1e-5,2],[.5,.008,.001,2]], T300/301K, normals(1,1),tangent1. Replace only first liquid amount relative to prior two-cell phase trajectory to create an intentionally tiny-inventory event. Keep both coefficients1e-7 mol/(s Pa), original fixture externalpressure and all domains. Cell1 has substantial liquid and initially condenses; it must remain wet, and its chemistry/heat/constraint work remain active. No assumptions that its liquid is constant or that each cell's constraint work vanishes.

Start0,end1e-4s; affine_midpoint, terminalwindow1e-6s, nested ordinary cap1e-4 with reuse, safe fraction.25, common horizon1e-4,14refinements. Ordinary initial/maxstep1e-4,min1e-14,rel1e-7,Nabs1e-12mol,Eabs1e-6J,stretchabs1e-9,stretchscale1,2000panels,100rejections. Event originalsinglecell gates time1e-8s,N1e-11mol,E2e-6J,T1e-5K,P.1Pa; stretch1e-9. Sourcewatermolar mass exact op.chemical.reference value. No tolerances changed in response to outcomes. Independently verify that initial callback actually evaporates cell0 and that estimated event lies strictly inside horizon; if not, save outcome and stop for explicit revised preregistration, not adjust coefficients silently.

## Resources and isolation

Root supplied measured singlecell178s/305evaluations ≈.584s/evaluation, and prior multicell13s/8 ≈1.625s/evaluation. Similar305calls estimate ≈496s, not178s. Plan600s integration wall and660s whole ownedchild watchdog for one run; call count and actual phase costs retained. Native initialization/diagnostics are outside integration wall but within660s. This is an explicit new bounded resource budget, not an accuracy relaxation; if project supervisor limits disallow it, stop with resource outcome rather than override. One EOS process at a time, actual source/installed identity before and after, source freeze and plan/script hashes before launch. No short observation timeout retry, no concurrentsecondrun. Report status at ordinary polling without altering child. If it exceeds600/660 retain partial acceptedprefix and resourcefailure; no automatic extension/retry. A separate half-cap experiment would roughly doublecost and is a later expressly budgeted follow-up, not hidden in this first gate.

## Save before assertions; exact independent audits

Persist complete encoded initial,policies,all states/times/steps/events/corrections/refinements,cost/reuse diagnostics,current operator interface modes,source/input/runtime hashes, and failure/traceback. Require nonempty trajectory/ledger and one actual eventcell0 before auditing; end1e-4, cell0liquid exact0, cell1liquid>0, interfaces dry/wet, both normal and common tangent finitepositive, terminal and midpoint complete3-component mechanics.

At every committed prefix: percell totalwater(liq+vap) residual<=1e-11mol since zero massfaces; inert/solids exactunchanged; independent Fraction E change minus actual face+component work<=original1e-6J/cell; component roundoff and mechanical represented/exact/absquad prefixes under original tolerances. Whole slab global E+pe*(sum currentV-sum initialV) <=4e-6J retained from two-cell phase plan; local E+pe*dV is NOT zero because real intercellheat and constraintwork. Audit local mechanical_constraint increments explicitly; sumconstraintpower residual bounded from actual free solve, not assumed exactly0. Save event/common T/P errors and all6comparison values, two consecutive terminalpasses, independentfiner distinctgrids. Decode final mixedstate once for actualT/P and currentgeometry/source labels.

Strict dry interface may legitimately reject condensation/nucleation demand, domain or unresolvedevent ordering. Retain these as structured failures; do not switch drypolicy, widen inversebrackets or declare event successful from an uncommitted branch. Success demonstrates software coupling under this manufactured fixed-solid reduced slab; reactions/evolving skeleton, liquidtransport, spatial mesh convergence and material-driven fullcycle remain required later Goal work.

Pre-run review addition: cumulative absolute represented sum of mechanical_constraint per step <= original 1e-6 J at every prefix; cumulative local absolute C each >1e-10 J to distinguish disabled constraint exchange, numerical detection threshold only, not material bound. Initial/final Csum within returned numerical bound. Final snapshot saved before new assertions.
