# Exact-time SSPRK2 numerical review

APPROVE the bounded standalone candidate c225da066745682080a06b9af06ee80ca25f13e7dd128e52fba0c0665b726f0c. No blocking numerical defect found in the examined stage, quadrature, error-control, knot and accepted-prefix paths. Reviewed actual candidate, baseline-integration.py and all 16 tests. Reviewer ran no tests/EOS, installed nothing and edited no production files. Author reports 16 tests passed in 4.73 s; this review is not an independent test execution.

Every actual callback time is ExactEventTime(at) from Fraction arithmetic. Each endpoint, midpoint, duration and quadrature weight is computed from these exact endpoints. A full SSPRK2 panel evaluates f(y,t), forms the Euler stage at t+h, evaluates there and validates the second Euler stage before accepting its convex combination. The actual accepted solution consists of two half panels, not the coarse estimator. Stage inventory and mechanical positivity checks are preserved; there is no clipping of negative inventory or strain.

The two half durations are exactly equal, so q=(h1^3+h2^3)/h^3=1/4 and q/(1-q)=1/3. The existing leading-local-error estimate and original amount/energy/mechanical scale tests are retained. This remains an asymptotic embedded numerical error estimate, not a rigorous global true-solution enclosure. Error norms and adaptive factors are binary64, intentionally separate from exact semantic time.

Each duration-rate product is rounded once by _scaled. Each half-panel field sums its two weighted stages; the accepted ledger combines the two half panels once. Shared faces remain single arrays entering neighboring cells with opposite signs. N/E local updates and cumulative ledgers are checked against the original initial state. Mechanical diagnostics additionally record represented increment minus exact represented-stage quadrature and check local/cumulative exact quadrature plus cumulative absolute rounding. Power-component quadrature errors and component-versus-total residuals retain baseline semantics and original budgets. These are representation/accounting checks, not truncation or EOS uncertainty bounds.

Future nominal durations are explicitly rounded downward to binary64 and then adopted as Fractions. This bounds controller denominator growth and does not round actual absolute stages or clipped quadrature intervals. It can alter meshes relative to a fully rational adaptive controller and must remain an explicit numerical strategy; no bit-identical general legacy-trajectory claim is warranted. Initial and maximum durations retain their represented binary64 meanings.

Exact knots clip panels before stage construction; strict interior ordering is checked. The tiny-tail branch takes half of the remaining exact interval only when that duration is at least the original minimum and no larger than the current permitted h. It does not discard a remainder or increase a maximum step. The next actual panel still undergoes the same error/domain checks. Refusing actual clipped intervals below minimum is explicitly more restrictive than the old nominal-only minimum check. The 0→3, nominal .3 test checks all 11 legal panels, exact endpoint and exact summed elapsed duration.

Tests cover translated autonomous states despite coincident display times, translated piecewise forcing and exact knot inclusion, independent analytic heat, exact callback sequence, nonlinear and mechanical-only refinement, positive-inventory rejection, full-prefix accounting, cancelled/domain/resource paths, and accepted-state validation. A future nonconstant component-power test with independent exact-stage quadrature would strengthen roundoff regression coverage; present formulas correctly retain baseline accounting with exact weights.

This review admits only the new independent exact-time solver interface. Existing physical hosts, native queries, depletion clocks/panels, event records, checkpoint/resume, service and provenance must receive their own explicit adaptation and validation. No claim is made that current wet ordered events or the failed four-cell case are solved by this candidate.
