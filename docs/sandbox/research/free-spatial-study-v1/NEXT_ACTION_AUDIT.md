# One next action: source-specific raw-feed/product closure feasibility audit

Recommend one bounded evidence action before another manufactured grid extension: complete the already identified Global NEST 10.30955/gnj.003738 Tables 2–5/model-definition closure audit, with an explicit go/no-go contract for a new source-specific lumped thermochemical model. The starting factual record is data/sandbox/research/raw-sludge-closure-audit-v1/{README.md,audit.json}; it already identifies the exact same-study candidate, inferred quantities and incomplete recovery. This recommendation does NOT claim the paper supplies a complete model. No new web fetching or paper facts were added in this audit.

Deliverable should be a source-bound, independently checked elemental/mass/energy constraint table, with all printed bases, uncertainty/rounding, unresolved residues and observation/derived distinctions retained; do not fit kinetics yet. It must answer whether there are enough independently identified product states to supply ReactionNetwork elemental counts and stoichiometry, and compatible IncompressibleSolidPhase/SolidShomateCaloric storage references and heat capacities. If not, the output is a concrete missing-observable list and rejected candidate, not a synthetic completed package. Root can assign only a new source research directory and audit tests; no runtime parameter admission or kernel modification belongs in this action.

Existing record says this feed is digested, dried/milled, with conditioning/lime discussion; it is not demonstrated untreated raw sludge or the current brick specimen. Tables 4/5 include inferred liquid yield/composition, borrowed water correlation, and incomplete high-temperature recovery. The 570°C balance used to construct parameters cannot also be an independent holdout. The six fitted mass-loss components do not automatically become molecular species. An endpoint LHV balance does not identify every evolving species Cp(T) or reference energy. Require actual lawful source access/visual table verification where the prior screenshots failed; if unavailable, preserve that limitation rather than marking verified. Do not combine this material with Areias, Ghodke, Arlabosse or Liu to fill missing entries.

Most relevant existing limits: docs/sandbox/KNOWN_GAPS.md (same-material domain; raw pyrolysis/char; sintering/viscosity; cooling mechanics), data/sandbox/research/raw-sludge-closure-audit-v1/audit.json, and data/sandbox/research/liu2023-energy/README.md. SSA is not raw sludge. Nitrogen TG does not establish air oxidation. Unknown product calorics, viscosity, pore connectivity, thermal expansion and failure laws remain unknown. A qualified source-specific surrogate would still not prove raw-sludge-to-fired/cooled-brick validity.

## Earliest obstacle in the actual short free case

Saved-only evidence: /private/tmp/brick-free-app-native/attempt01/result.json, SHA 2a0704e5346f723725bc5ace7a0c6bb8d0a72bb1a5b5106cc44bf93eb0e24313; actual case at that run, not an unrelated older dry fixture. Current case reaction bracket is 295–310 K (do not import the older 300 K lower bound). Start temperatures approximately300/301 K, stretches1, liquid1e-6 mol each. Horizon is2^-16 s=15.2587890625 microseconds.

From saved initial transfer rates, Nliquid/r is0.283173/0.267113 milliseconds, about18.6/17.5 times the short horizon. This makes liquid exhaustion the earliest plausible regime transition indicated by initial rates. It is NOT a located event or guarantee of order: vapor accumulation, pressure, temperature and rates change. The saved initial stretch rates yield0.5/|rate|≈8.37/8.39/7.98 seconds to the lower stretch bound under a frozen-rate extrapolation. Reaction initial A/r≈10 seconds. None of these scales is a trajectory prediction.

A saved-property cooling scale uses delta_u=(h_v-R*T)-u_l and initial closed heat capacity: latent-only cooling14.60/15.46 K/s, roughly0.34/0.39 seconds to295 K if rates were absurdly held constant beyond depletion. Transferring just the existing initial water corresponds to only0.00414/0.00413 K on that scale. This calculation omits heat-face transfer, reaction/composition recovery, pressure derivatives and mechanical work; it is only a comparison of local characteristic scales. Do not claim a rigorous source-domain safety certificate from it. Saved initial pressure is approximately41.6/41.7 kPa inside10–1000 kPa; no saved initial pressure derivative certifies its future trajectory. Full arithmetic and source hash are in initial-rate-scales.json. No EOS or forward integration was invoked.

## Exact event/application seams recorded for the subsequent software task

1. verification_case._validate/build_case/BuiltCase currently expose ordinary IntegrationPolicy and a WaterPhaseTransfer over FreeSolidSlab; no explicit case-level DepletionPolicy/RoundoffPolicy choice. Any event-enabled case must add a versioned explicit numerical schema, not infer or silently default event gates.
2. run_service execution currently calls integration.integrate at lines200–203. Existing depletion_integration.integrate_depletion already accepts actual WaterPhaseTransfer with mechanical ConservedState and supports affine_midpoint, nested approach, event comparison and roundoff ledgers. It can be reused; do not rewrite phase kinetics or treat mechanical stretches as species.
3. WaterPhaseTransfer.with_depleted_cells requires exact zero liquid and persists per-cell existing_liquid/depleted_no_nucleation modes. The final operator/modes must survive serialization and be used for snapshot, replay and checkpoint reconstruction. Calling snapshot with the untouched initial operator after depletion would be semantically wrong.
4. run_service resume/_resume and checkpoint.validate_cancelled/merge_integration currently bind ordinary IntegrationResult, original policy and accepted prefix. Event-aware service needs explicit event/correction/mode/clock evidence validation and cumulative original budgets across repeated resumes, not merely flattening the event result into ordinary steps. Preserve rejected/failing suffix evidence and accepted prefix.
5. source snapshots/catalog roots/trace, job supervisor and exports must retain event policy, corrections and resulting interface modes. The same underlying native wall/cancel/reap limits remain; extending a horizon is not authorization to remove resource bounds.
6. ProgrammedSolidFluidHeat can wrap FreeSolidSlab for actual furnace boundaries, and ProgrammedWaterPhaseTransfer exists separately, but integrate_depletion's exact top-level admission is WaterPhaseTransfer or ManufacturedDepletionAdapter. A furnace+event case must use an explicitly supported composition with program knots and source/operator identities, not blindly pass an unsupported wrapper. Case schema currently has no furnace program.

These seams explain why simply changing end_s cannot deliver wet-to-hot service behavior. The recommended immediate action remains the single source-closure audit above; the listed implementation work is the concrete subsequent application dependency, not a claim it is done. Even an event-enabled manufactured service would not provide material admission, oxidation/sintering/cooling laws, or a useful full-cycle prediction.
