# Nylen spatial integration audit (read only)

Audited actual current source September10; no repository edits/tests/native EOS runs. Source extraction belongs to root. The preceding Amadou provider is not a Nylen material property.

## Findings with authoritative source lines

- `mass_wet_transport.py:70-100`: WetPair requires exactly2 WetMixedStorage,2 inverse policies,2 coefficient entries; every storage must have solids('A','B') and the exact single reaction(-1,2,-1,0,0). `:131-174` computes that fixed finite-O2 reaction and just one shared face. `:202-225` ledger/writeback is intrinsically pair-indexed. Thus no arbitrary-N source material admission exists here. Setting rate=0 does not remove fictitious A/B identities or fabricated reaction-network admission.
- `mass_wet_storage.py:100-128`: actual RigidStorage must explicitly allow manufactured; mass solids exact typeMassSolid; reference network fully anchored/manufactured; gas orderO2,N2,H2O; reference298.15 must lie within interval. `:169-199` evaluates true liquid/gas mechanical closure but computes solid U via constant Cp*(T-Tref), and Cp lowerbound by the same constant. `mass_storage_bridge.py:64-77` only admits manufactured positive constant Cp/volume MassSolid. It cannot consume measured variable Cp or an uncertain caloric lower bound merely by changing a classification string.
- `rigid_fluid_heat.py:113-142,145-151,316-357`: arbitrary number of percell storages already exists. `:174-176` uses bulk=A*width; `_face`(:265-286) uses constant area and halfwidth serial resistance; evaluate independently computes planar heat conduction.
- `solid_fluid_heat.py:99-164`: arbitrary-N fixed-solid host exists, optional liquid transport/reactions. It requires exact SolidFluidStorage and RigidFluidHeat types; bulk geometry=A*width is checked. `:269-344` independently assembles gas/liquid/conduction faces, so changing RigidFluidHeat.evaluate alone DOES NOT fix solid-host geometry.
- `solid_fluid_storage.py:69-112`: solids exact IncompressibleSolidPhase, real molar species metadata, NIST formation convention and molar basis. `:127-180` inventories are molar. A bulk heterogeneous dry sludge Cp is not a species molar Cp; assigning dummy molar mass to enter this interface would reintroduce a solved scientific problem.
- `water_phase_transfer.py:79-145,220-267`: wrapper already handles arbitrary host cell count and calls the actual fluid/solid host, equimolar liquid/vapor transfer and shared total U. No extra latent-power source is needed. However this is pure-water chemical potential; multiplying it by an unrelated sludge activity function would omit consistent sorption thermodynamics.
- `geometry.py:51-90`: ReferenceSlab is already an Ncell compatible deformation geometry, common tangential facearea, percell widths. No radial reference/current class exists. Do not reinterpret CurrentSlab.volume_ratios as radial deformation.
- `deforming_solid_heat.py:62-97,142-161`: moving host checks slab reference geometry and replaces transport with a single current facearea. Sphere geometry must be explicitly rejected here until spherical mechanics is derived, preserving the final brick slab path.
- `integration.py:152-192`: Rates is already arbitrary-N and uses single shared face rates, `du=fe[:-1]-fe[1:]+power`; no geometry appears in conservation differencing. Existing integrator/ledger should be reused, not rewritten into a separate drying solver.
- `data/sandbox/research/nylen2024-drying/source.json`: two distinct MSJ/CB materials, spherical2/4cm, measured core/surface temperatures, distinct repeat pairing; constitutive_package=null. RoomRH50% is not heated-streamRH50%. Reported shrinkage conflict disallows inventing radial motion history.

## One bounded NON-duplicate implementation to execute next

**Extend the existing RigidFluidHeat/SolidFluidHeat shared spatial transport path to explicit static1D metrics, supporting current slab and source-observation sphere, without a new solver.** This fixes a real indispensable mismatch between acquired Nylen geometry and every existing coupled spatial host. It is not sufficient evidence of Nylen material validation; the scientific package gap below remains explicit.

Ownership (one implementer; independent reviewer):
1. `geometry.py`: add immutable static metric carrying face positions/areas, center positions, cell bulk volumes, and integrated halfcell area resistance. Existing ReferenceSlab API unchanged. Sphere volume is4pi/3*(rout^3-rin^3), facearea4pi*r^2. Center boundary is exactly noflow; never evaluate a center1/r singular resistance. Nonzero interior/surface paths use integral(dr/A(r)); constant k gives resistance integral/k. Slab metric reproduces current halfwidth/area exactly.
2. `rigid_fluid_heat.py`: one optional geometry input, default existing slab; validate widths/storage bulk volume against actual metric volumes. Expose a shared perface conduction helper and gas-face metric lookup, used in `_face` and `evaluate`. For existing `face_exchange(area,distance)` API choose physical interface area and equivalent halfdistances=Aface*integral(dr/A(r)); this produces radial series conductance without substituting Aface*dr for shellvolume. Check/interrogate temperature interpolation policy explicitly when mapping these equivalent distances; radial metric is a discretization choice, not observed interpolation.
3. `solid_fluid_heat.py`: consume SAME shared metric/helper at `:161`, `:257`, `:319-337`; do not create a second radial discretization. Liquid conductances must use same metric when enabled, or reject radial+liquidtransport explicitly for this first commit. Keep source/geometry binding and report that restriction.
4. `deforming_solid_heat.py`: reject static-sphere metric in prescribed slab motion, avoiding silently replacing sphere with constant area.
5. `tests/sandbox/test_rigid_fluid_heat.py` and `test_solid_fluid_heat.py` (or their existing nearest spatial test file; root chooses after actual file list): meaningful2/5/Ncell conservation tests, existing slab parity, shell geometry exact analytic volumes/conductance, zero-center face, outer surface halfcell, source/storage volume mismatch rejection. One manufactured spatial convergence check against analytic spherical heat equation validates geometry numerics only. Reuse existing low-cost artificial providers for these tests, then one bounded actual-water host smoke where established numerical policy permits. No native phase-event marathon necessary to verify geometry.

Outer boundary currently supports prescribed surface temperature or gas reservoir, not a source-derived convective h. Nylen observed surface T could become a prescribed measurement boundary for a separately declared core-temperature benchmark. That uses surface data as input, so surface temperature is not simultaneously an independent predicted validation output. Requires an accepted time-series boundary interpolation policy and actual same-condition boundary/core data; ProgrammedSolidFluidHeat is the existing boundary-program seam, not a reason to write a standalone ODE.

## Source-specific kg caloric integration still needed (not hidden by geometry work)

The eventual source mass caloric provider needs explicit methods `specific_sensible_h(T,anchor)` and `cp(T)` plus valid lower Cp bound/numerical error, domain, fixed material ID, reference anchor and original asset binding. Its pressure convention must say whether `u=h-p*v` uses reference or current p for incompressible solid and preserve h-u=pv. It needs actual dry-solid specific volume/density and (if reacting) compatible formation reference network. Fixed-inventory lowtemperature sensible offsets may be an explicitly chosen gauge; they do not authorize an unknown reactive absolute formation enthalpy.

Reuse `WetMixedState` kg/mol storage representation rather than force bulk sludge into `ConservedState.amounts_mol` with dummy molecular weights. A later mass-host integration must generalize the existing pair evaluator into shared N+1 face arrays/ledger and support an explicitly reaction-disabled model without fictional A/B chemical network. Keep original WetPair as regression adapter; existing exact controller is pair-specific and cannot be advertised as arbitrary-N until its indexing/event policies are generalized. This is a substantive multi-file follow-on, not a two-line provider swap.

For Nylen specifically still absent in registered source: matching MSJ/CB Cp(T,X), drydensity/porosity, conductivity(T,X), moisture mobility/activity or a preregistered effective fit, hotstream vapor boundary, heat/mass transfer, and justified radius history. Arlabosse Cp covers35..105degC of a DIFFERENT mixedfeed; Amadou activity only30/50degC of Rosheim. Neither can close MSJ/CB at88..138degC. A fit of effective diffusivity to selected Nylen conditions is allowed only as explicit calibration with new unseen holdout; it must not be relabeled measured constituent properties or used to validate fitted observations.
