# Independent final saved prefix audit

818 checks passed in 0.05087 s using stdlib-only passive JSON decoding and Fraction arithmetic. NATIVE_AUDIT03.log and NATIVE_AUDIT.json contain actual results. The audit independently used the original first/interior captures: hm=h/2, so each exact affine integral is h times the saved interior rate. It did not import or execute the prefix, helper, EOS or replay runner.

Verified seven independent singleton prefixes: 245 shared integral entries (35 each), all signed integral projections, 105 state values and their state/full residuals, represented and exact singleton cumulative arrays, 84 whole-prefix minima, and 28 face decomposition records. Raw Fraction liquid donor projection metadata was retained exactly. Gas-only outer face records were checked to have zero liquid flux, not misrepresented as liquid records. Each original policy equals the frozen native policy, including amount tolerance1e-7 mol and energy tolerance1e-3 J. Exact clocks, bindings, identity, fixed dry mass, minima and original trial status remain consistent with the pinned original native/panel artifacts.

Largest full residual: 2.3817631510377932e-17 mol and 6.874738982948538e-12 J. Every original rejected/accepted status is preserved in the sequence rejected, accepted, rejected, accepted, rejected, accepted, accepted. Strictly positive numerical prefixes do not upgrade the three rejected ODE trials. No physical or material qualification is granted.

Initial auditor execution failed with KeyError liquid_mol because it incorrectly assumed every diagnostic face was a liquid face. Original audit_native01.py and NATIVE_AUDIT01.log are preserved. The correction uses zero liquid only for saved gas-only boundary faces, then explicitly checks the schema and zero-flux boundary constraint. NATIVE_AUDIT02.log records the intermediate776 checks; final03 adds42 explicit schema/boundary checks. This was an auditor assumption failure, not a model failure or changed model output.

Input SHA8ca3d79c533801f70002b3ad6bace09e84446c87e62d9bd4233d8fce57fc95e0. Original native033dccd09ed268eeb2ce9570a37d52f66d4eb5da18b68f2f44a89bd01054c4f2 and auditc705d9410458690d0a8379da59e5a7c86a01fd4f59e2897699a5616e55c1914d are pinned; complete paths/hashes are in the JSON.

This is saved numerical affine-prefix arithmetic evidence only. The seven overlapping original trial panels were never concatenated into a physical trajectory or treated as seven accepted new ODE steps.
