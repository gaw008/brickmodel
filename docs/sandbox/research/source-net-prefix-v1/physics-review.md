# Independent prefix review, implementation comparison

Passed 79 standalone algebra assertions plus 144 comparisons/checks against the implemented prefix, in 0.03138 s excluding imports. PRODUCTION01.log and PRODUCTION.json are actual outputs; the latter binds all three source files before/after execution. source_net_prefix.py SHA 8bcaef378eef0c35a08bdcf39f01b06ded8bf9283981f251935e89504d51d5e0.

Independent expectations use Fraction only, with the previously registered three-cell/four-species affine rates, h=1/3, interior offset1/5, two contiguous prefixes. The test fixture is reused to construct required metadata/dataclasses; no tested integral, prefix, polynomial or update function supplies expected values. Every shared integral and its single binary64 projection, each liquid/vapor phase pair, final cell amount, signed state/full residual and exact cumulative exchange matches the independent oracle. Final amounts/U match the prior standalone arithmetic result exactly. Total-U, liquid molar transfer, liquid donor energy, decomposition discrepancy and separate donor projection diagnostics were checked at all four faces of both prefixes.

The earlier cumulative-full-residual defect is fixed: an independent large-integral case gives +1 mol full liquid residual per prefix with zero state projection; both prefixes separately pass tolerance1.5, while their cumulative full residual2 correctly raises source_prefix_cumulative_full_inventory_budget. It is not enough to check only cumulative represented-state residual. Both actual gates are signed per-cell/quantity comparisons to the original policy. The standalone oracle's absolute_resources field is a diagnostic sum, not a claim that production inherits a global sum-absolute budget.

Code review confirms all zero-initial inventories remain unsupported and prefix-wide minima are checked before projection. Zero minima produce only numerical_boundary status. The independent standalone negative-polynomial/tangent/root-order examples remain algebra checks, not additional production tests; parent/provider production tests cover those rejection paths. Liquid direction reversal remains a numerical sign indicator only and does not certify an intervening physical donor.

The initial standalone script's cumulative assertion was discovered to be tautological during self-review and replaced with an independently accumulated state-rounding sum; original algebra01.py and ALGEBRA01.log are retained, corrected ALGEBRA02.log records79 checks. There was no production failure in this comparison and no tolerance change. No EOS, physical trajectory or event/writeback admission occurred. Saved native singleton-prefix audit remains a separate pending artifact until parent supplies final output.
