# Saved numerical prefix replay static review

Read and AST-parsed script; no execution/import of runner or EOS. Reviewed SHA 3d835c98543c52e65ccbd042a3640a9fb55320319d0374eb5253e67df32f298e. Fixed helper path is hashed against previously reviewed35-class passive decoder before import; native/audit artifact hashes and audit native binding retained. No arbitrary saved class/module import. IntegrationPolicy uses normal explicit constructor then complete canonical record equality, preserving all original gates.

Seven original capture_start/+3 pairs retain full capture roundtrip, exact original sample/end times, source/operator/energy/fixed-mass bindings and original trial status. Source/rigid inverse/evaluation, water TP and equilibrium calls are patched to reject. Numerical prefix builder and checks use saved rates only. Each trial is audited as a singleton; overlapping/rejected/native-stage inputs are correctly not concatenated as a new contiguous path.

All prefix fields except redundant pinned panel, and all audit fields except redundant singleton prefixes, are saved. This includes original policy binding, exact and represented exchanges, integral and full residuals, minima and face diagnostics. Positive inventory is an explicit expected numerical gate, not a physical endpoint admission. Both rejected and accepted original trial statuses remain visible.

30s soft checks/45s alarm are bounded; errors save completed prefix and return1. Ordinary non-atomic writes retain same limitation as earlier fixed replay; no hard-kill recovery guarantee. No substantive blocker identified. Approve this hash after parent source/install verification; no replay success or actual physical trajectory claimed by static review.
