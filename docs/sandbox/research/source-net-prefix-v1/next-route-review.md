# Narrow next-route review

The proposed actually evaluated positive-prefix trial has the correct boundary: genuine first/Euler-midpoint/terminal observations, all inventories positive, original integrate_exact as independent numerical construction, no dry correction or event admission. Extracting only Euler advancement rather than copying the controller is appropriate. No conflict found with current physics restrictions.

Keep these points explicit in implementation: terminal prefix status must exclude any interior zero/tangent even if endpoint recovered; every initial zero remains unsupported. Reference output is usable only when the reference run completed the same exact interval. A resource-limited or rejected partial reference cannot supply a passing endpoint discrepancy. Register the exact discrepancy normalization and <= threshold before execution, without describing it as a truncation-error certificate. Preserve both the original policy binding and the distinct remaining wall-clock allowance; resource accounting must include prefix callbacks and reference callbacks. Callback counts11 and duration1/16384 s in the route are prepared estimates, not guaranteed outcomes.

Actual source terminal pressure bounds must include available-volume uncertainty. Affine liquid direction diagnostics remain insufficient to prove donor identity between observations. None of the trial or reference results authorizes source dry-mode transition or transport-to-evaporation projection.

Read-only review, no additional execution.
