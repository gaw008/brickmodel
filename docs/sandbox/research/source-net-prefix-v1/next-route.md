# Next bounded step: actually evaluated source-prefix trial

Read-only route, 2026-09-09. No production edits, tests, EOS or commit. The smallest next useful deliverable is a source-specific *speculative positive-prefix trial* that generates its own midpoint sample, evaluates its output in the real source operator, and is numerically compared with the existing adaptive integrator. It must not masquerade as the old mechanical terminal executor or commit a dry event.

## Reuse and concrete interface

Keep `exact_integration.integrate_exact:112` as the reference solver and ordinary source driver. It already works with `ExactSourceColumn`, exact program breakpoints and fixed-kg energy identity. Its full/two-half SSPRK2 check (285–312) and final physical evaluation (314) must remain intact, as must its accepted and rejected budgets, minimum-step rule, per-state and cumulative ledger gates. Do not copy its controller, RK2 routine or error estimator into another source solver.

Extract only its nested `advance` (199–212) as `advance_exact_euler(state, rates, duration: Fraction, policy) -> ConservedState`, preserving `_scaled`, `_updated`, positivity, optional mechanical branch and original exception classifications. The old closure calls this helper unchanged. This gives the new trial exactly the same Euler predictor arithmetic, without exporting the adaptive controller or casting an absolute exact time to float.

A small source wrapper can expose `evaluate_source_prefix_trial(adapter, initial, *, start, end, integration_policy, cancel=None)` and return the attempted inputs, actual ordered captures, predictor, panel/prefix, terminal observation, reference result, normalized state discrepancy and status/reason. The wrapper owns one outer deadline and counts attempted/completed callbacks; it retains partial evidence on every failure. It is not an integrator or event packet. Preserve a plain original policy binding as the prefix does.

Sequence:

1. Require exact `ExactSourceColumn`, valid packed state, fixed source/energy/mass bindings, all four inventories strictly positive in every cell, and start<end. Refuse a program knot strictly inside this single affine trial; the existing ordinary driver splits intervals at `adapter.breakpoints`.
2. Invoke `adapter.evaluate(initial,start)` once and retain the actual SourceExactEvaluation with state/time/ordinal before downstream checks. Classify exceptions exactly as `ExactSourceColumn.__call__`: preserve DomainExit and IntegrationError separately; wrap other ValueError as IntegrationError, never invent a domain classification.
3. Set midpoint=start+(end-start)/2 and form the Euler predictor with the extracted helper. Check its positive-fluid domain before invoking the actual adapter once at midpoint. These are genuinely evaluated source samples; the midpoint state must be the returned predictor, not a hand-edited sample or an accepted half-step mislabeled as Euler.
4. Build `SourceAffinePanel` from those two captures and call `build_source_prefix(panel,end,policy=...)`, retaining `audit_source_prefixes((prefix,))`. If the prefix is numerical_boundary, or roots identify an earlier gas boundary/tangent/ambiguous competitor, return unsupported-boundary evidence. Do not call a wet source operator on an exact-zero liquid inventory; do not cross a tangent just because its final inventory recovered. Initial zeros remain unsupported, even identically zero gas/liquid polynomials.
5. For a strictly positive prefix, evaluate `prefix.raw_state` at the exact end with the same adapter. `ExactSourceColumn.unpack` only checks structure and mode; it does not perform an energy inverse. The actual evaluate call is necessary: `SourceWetColumn.evaluate:184` invokes each `SourceWetStorage.invert:229`, enforces source T/U domains and inverse tolerances, and runs the phase, gas, liquid and furnace boundary calculations. Retain the complete returned per-cell inverse, T/P bounds and original source decomposition. Successful storage reconstruction alone is insufficient.
6. Run the existing `integrate_exact` from the same original state to the same endpoint with the unchanged original numerical policy (only remaining wall resource may be reduced) and exact breakpoints. Capture each reference callback once; do not merge repeated times or reuse an observation from a different packed state. Require its completed result, all original SSPRK2/ledger gates, then compare the proposed prefix amounts and U with that independently evolved fine endpoint using the same original amount/U scales. Require the direct normalized discrepancy<=1; report it separately from the integrator's own estimator. Do not multiply this cross-method discrepancy by 1/3: the affine midpoint prefix and the SSPRK2 fine result are different constructions. This is a consistency gate, not a rigorous truncation-error certificate or inherited event-clock accuracy.

## Thermodynamic and event boundary

Actual terminal source inverses expose `SourceWetInverse.temperature_error_bound_k` and `point.pressure_error_pa`; the latter includes available-volume uncertainty. Use those actual source values, not a bare fluid pressure bound. A later coarse/fine event comparison can reuse the mathematical structure in `exact_depletion_integration.compare:252`: compare event order, exact event times plus root bracket widths, amounts/U, and common-endpoint T/P differences plus each side's uncertainty. Source-specific extraction of the observation vectors is required; the existing function reads mechanical `base_evaluation` and stretches and cannot be made valid by dropping those fields or changing an isinstance guard. Existing paired mechanical pressure-comparison evidence is not automatically source evidence.

The ordinary SSPRK2 time-error gate controls its smooth numerical step; pure root ordering controls the affine surrogate. Neither establishes event time accuracy. A source event packet still needs independent approach/refinement trials with stable first-event order and explicit original event time/state/T/P tolerances. If the source pressure scope cannot support that gate, report unresolved; do not weaken the threshold. The proposed next trial does not call `with_depleted_cells`, `exact_depletion_writeback` or `execute_exact_terminal`.

`exact_terminal_executor.execute_exact_terminal:63` currently combines genuine observations with an evaporation-qualified correction, mode switch and final re-evaluation; `exact_depletion_integration.terminal:208` appends its speculative result only inside the old driver. Both are ExactFreeWaterTransfer/mechanical-specific. Their failure-retention/commit ordering is a useful pattern, not an adapter shortcut. For eventual drainage correction, residual liquid must move across its actual shared face and carry the same donor enthalpy, with paired neighboring inventory and U changes and independently justified error bounds. Gross evaporation-fraction/ULP allowances cannot pay for net drainage; no current bound authorizes that correction. Source wet/dry continuation remains blocked at that precise physics boundary, not at ordinary positive source integration.

## Bounded validation before one native run

First use the existing cheap `test_source_liquid_column.setup/config` and furnace fixtures (actual source host with explicitly artificial water/transport): verify exact predictor arithmetic and stage states, ordinal counts, identity/knot guards, N3 shared-water/U balance, positive endpoint actual inversion, domain failure and callback failure retention, gas/tangent/zero rejection, original numerical failure under strict tolerances, and an accepted positive trial with independently checked reference discrepancy. Inject an endpoint-only source-domain failure to prove that success is not inferred from the two samples. Freeze old Euler/integrator observations to show the extraction changes no legacy results. No HEOS is needed for these tests.

Only after independent code/physics review, run one installed HEOS positive-prefix probe using the existing source-liquid construct and its original numerical settings. A concrete small interval is the already declared minimum 1/16384 s; keep original initial/max/min settings and tolerances, letting exact endpoint clipping act normally. A successful one-step reference plus three source-trial observations costs 11 actual evaluations (8+3); do not add a redundant probe. Prior 47-call native work took about 448.6 s, so pre-register a measured resource-only budget around 180 s inner/210 s outer for this smaller trial, with a callback-count cap and honest timeout output. This estimate is not a success guarantee and must be finalized against the actual prepared runner. Save partial captures and structured results before audit; no automatic resource rerun or tolerance change. A pass establishes one evaluated positive numerical source trial, not a dry event or material qualification.
