# Installed passive checker final review

Verdict: APPROVE this checker and its plan for one bounded passive execution after the core/helper/service/test freeze, noneditable installation and source/installed byte matching. This is approval of the checking procedure, not a claim that the complete installed acceptance run has already passed.

Final checker SHA256: 3f70cb9e45718b6350b19f746d2c1d2df32faf277b241174bd439d7310c4eff3.
Plan SHA256: 618e5c67098d9d780f403f64da3b227ac02586a96d88cc049061879fe6b64354.

Both earlier MEDIUM review items are closed. invoke_cli persists original stdout in finally before checking status, and outer failures retain stage and partial counters. Two exact-helper control probes (nonzero CLI return and thrown TimeoutError) passed without invoking the full run, core or model. The final helper is AST-identical to that tested version. The later observation-coverage change was read directly: every original index now independently compares packed state, complete evaluation, phase/role, exact time, operator identity, energy identity and modes with its public observation record. The comparator supports either the exact archived SourceStudyNode kind or a real dataclass with matching fully qualified class name and complete field set; it does not call the core converter to construct expected values.

The full original document is recursively compared with restored roots/metadata/captures, preserving mappings, order, exact scalar types, float hex values, array shape/bytes/read-only state, Fraction numerators/denominators and exact event times. The only added fields permitted are the six explicitly enumerated old live omissions, each required to be an unavailable passive reference. The original SHA fixes this particular input. This independently checks retention and public-index correspondence; it does not replace the separate core numerical/association audit.

The installed CLI import and inspect paths are compared with Python; capture 16/cell 1 and the full 2×3 selected-pressure-gate matrix are explicit selections. The 180-second alarm, exclusive output directory/record, constructor/evaluation guards, retained failure stdout/stage/counts and original source SHA agree with the plan. The checker requires an installed package path, while exact source/installed identity remains a separate precondition. All old pressure failure fields and material=false are included in the complete retention check; the saved conditional event acceptance is not source authentication or resume authority.

No full checker, source core, original native dataset, numerical model or EOS was executed during this review. Review evidence is limited to static code/plan comparison and the two pure helper-control cases. The earlier failed reviewer harness selected a removed inline AST after the concurrent fix and stopped with StopIteration before executing any block; runner-failure-red/PROBE_SCOPE_NOTE.md and its log explicitly record that limitation. It is not an original application failure. The original finding report is preserved in PASSIVE_CHECKER_REVIEW_INITIAL.md.

## Review Summary

| Severity | Count | Status |
|----------|-------|--------|
| CRITICAL | 0 | pass |
| HIGH | 0 | pass |
| MEDIUM | 0 | pass |
| LOW | 0 | pass |

Verdict: APPROVE — one bounded passive installed acceptance run, conditional on the stated final freeze and installation checks.
