# Exact boundary program numerical review

APPROVE the bounded read-only adapter. No blocking numerical issue found. Reviewed exact_boundary_program.py SHA256 1904c9cb168555512df5855dbffad34161c49a63345edc89d7ba75ea83e33727 and test_exact_clock.py SHA256 87fc7bfb054277053bd19c5eea69e7faa6cb5d99049641cbf22592bf14136eeb against actual src/sludge_sandbox/boundary_program.py. No tests, EOS, install or production edits were executed by this reviewer.

The adapter admits exact existing validated BoundaryProgram/ScalarProgram types and an explicit Fraction translation. Each original binary64 knot is adopted as its exact represented rational, then the same translation is added to the entire schedule. A translated query has exactly the same interval position and interpolation weight. This is an explicit translation of forcing, not an implicit reset of a physical schedule when a solver resumes.

position uses exact ordered semantic times and bisect_left. Both end knots are admitted, exterior times rejected, an exact knot returns its original sample, and either exact neighbor selects its proper segment even when both display as the same binary64 time. Interior breakpoint selection excludes both requested endpoints, preserving the original boundary convention. Reversed or empty intervals are rejected.

Between knots the exact convex weight lies strictly between zero and one. Each output is the binary64 rounding of the exact rational convex combination of the original binary64 samples, identical to the existing program arithmetic for representable query times with zero translation. No normalization, extrapolation or new interpolation law is introduced. Finite scalar extrema bound the convex combination even for opposite-sign extreme samples; positive temperature/pressure samples remain positive. Fractions stay in [0,1] and retain the existing composition-sum guard. Exact time does not imply exact returned thermodynamic forcing values: those remain explicitly float-valued.

Tests meaningfully exercise two semantic neighbors collapsed to one displayed knot, the exact knot and interior breakpoints, a large non-binary64 schedule translation, independent multicolumn boundary values and legacy parity, read-only fractions, invalid query types and exterior/reversed intervals. Additional extreme scalar sample and translated outer-neighbor cases would be useful future regressions, but the direct exact-convex implementation establishes those arithmetic properties without a missing guard.

Scope limits: this adapter does not admit ExactEventTime into current integrators, callbacks, depletion records, native providers or checkpoints. The original input ProgramIdentity is forwarded; schedule translation remains a separate explicit view field. Future runtime source binding must include the translation/view content, not authenticate distinct translated schedules using only the forwarded ProgramIdentity. Deliberate object.__setattr__ mutation of frozen source objects is not newly guarded here; runtime admission must retain the existing operator content checks.
