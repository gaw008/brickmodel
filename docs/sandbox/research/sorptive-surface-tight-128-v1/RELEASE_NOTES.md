These attachments preserve the complete base and refined 1200 s research trajectories for the 128-cell sorptive surface model at tighter numerical precision. They are a research data archive, not a qualified material model or a production software release.

Both integrations and their time-precision comparison are complete. Full source, local water/energy, surface and entropy audits remain pending at preparation of these notes. Matching spatial qualification also remains pending. Previous failed calculations remain recorded in the repository.

Each tar contains original manifest-relative gzip parts. Unpack the tar files at the repository root and follow `complete-archive.json` to decompress and concatenate each record's parts in order. No hashes are generated or compared. Local compression round trips, tar-member byte comparisons and subsequent uploaded-file readback are recorded separately; none establishes physical qualification.

GitHub warned that the repository was approaching its size quota. These large files are stored as release attachments to keep future research data out of Git object history. Existing history and prior archives are retained. Model code, root parameters, source records, reports, manifests and the publication record continue to be committed to the project branch.

`material_qualified=false`, `training_eligible=false`.
