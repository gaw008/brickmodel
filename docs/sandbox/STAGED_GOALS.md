# 独立阶段 Goal 顺序

2026-09-12 用户要求：每个 Goal 只承担一个阶段，验证与开发交替。[项目总规范](../BRICK_PHYSICS_PROJECT_SPEC.md)保留完整范围，不再作为自动完成全部任务的 Goal。

**2026-09-20 UTC 当前交付：** [V0R-D](research/v0rd-silva2018-geometry-review-v1/REPORT.md)完成对`e78fb29`的限定复核：396项尺寸、99项作者计算字段读回一致，330项保存计算独立复算支持原结果；C/L与π推断、23孔/测点、主段终体积及公式冲突确认。总水守恒仅条件成立，能量/熵、材料输入与真实预测仍未验证。下一独立F0-E只整理同源本构/边界参数适用性和最小补资料草稿，不泛搜或重复33行；尚未创建。D1/材料/训练资格不变。本Goal从19:33:16 UTC起算20分钟，按授权推送后结束。

| 独立 Goal | 唯一任务 | 结束与准入 |
|---|---|---|
| **V0：验证已有成果** | 按[V0合同](../GOAL_BRICK_PHYSICS_SANDBOX.md)检查正误、证据与适用范围 | 已完成；完整审查报告和下一阶段准入；审查完成不等于全项通过。 |
| F0：必要修复/补证据 | 只处理 V0 一个明确阻塞或小组依赖阻塞 | 修复提交或新证据，不扩展新阶段功能，不自称验证通过。没有相应发现就不建立。 |
| V0R：再验证 | 检查 F0 是否解决原问题及影响旧结论 | 原反例保留，相关必需条件通过才开放下一开发；否则仍修复/验证。 |
| D1：下一阶段开发 | 在 V0/V0R 准入基础上只实现一个增量；连续干燥为候选，由审查决定范围 | 冻结目标、输入/输出与验收方案，交付代码和可复现案例。按用户 2026-09-19 约束不编写或新增测试、不生成或比对 SHA；实际执行的验证另行如实记录。实现完成与物理验证分开。 |
| V1：验证 D1 | 对 D1 冻结提交和受影响旧路径独立验证 | 四项检查、实际误差与准入结论；失败转具体修复及再验证。 |
| D2 / V2，依次推进 | 每次只开发一阶段，再验证该阶段 | 同一准入规则，不把全部剩余工作塞入单个 Goal。 |

验证不要求未采用且不被依赖的旧失败候选全部通过；也不能排除后续依赖的错误/未知项来制造准入。

上表是阶段定义，不表示所有应用 Goal 均已创建，不新建任务、定时器或后台链。每次只启用一个具体 Goal，记录起点、交付与预算，结束不隐式继承无限续期授权。V0 沿用 120 分钟上限，其余在相应 Goal 建立时明确，不自行设置 token 数值预算。

历史记录，2026-09-13 06:30:31 UTC：用户已取消旧完整 Goal，get_goal 核实为 null 后已通过 create_goal 创建独立 V0，当时状态 active，初始累计计数为 0。旧目标没有被假报完成。当时只执行 V0，其他阶段尚未创建；新 V0 成本不包含旧 Goal 累计值。用户随后授权阶段改动提交并推送 `origin/codex/physics-sandbox-v1`。
